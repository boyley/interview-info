# Java 并发编程与系统性能优化 — 面试备战手册

> 来源：极客时间
> - 《Java并发编程实战》（王宝令）— 并发理论基础/工具类/设计模式/案例分析
> - 《性能优化高手课》— 性能设计/实现/看护/调优方法论/案例
>
> 两课互补：并发是 Java 性能的核心维度，性能优化是系统级全链路方法论

---

## 📌 一、面试高频考点速查

| 核心考点 | 来源 | 重要度 |
|---------|------|-------|
| **并发 Bug 三大根源**（可见性/原子性/有序性） | 并发 01 | ⭐⭐⭐⭐⭐ |
| **Java 内存模型**（Happens-Before 规则/volatile） | 并发 02 | ⭐⭐⭐⭐⭐ |
| **synchronized 原理**（管程/MESA 模型） | 并发 03+08 | ⭐⭐⭐⭐⭐ |
| **死锁**（四个条件/避免策略） | 并发 05 | ⭐⭐⭐⭐⭐ |
| **线程池**（核心参数/拒绝策略/线程数设置） | 并发 22 | ⭐⭐⭐⭐⭐ |
| **CAS 与原子类**（ABA 问题/解决） | 并发 21 | ⭐⭐⭐⭐ |
| **AQS 原理**（CLH 队列/独占+共享模式） | 并发 14 | ⭐⭐⭐⭐ |
| **ConcurrentHashMap**（分段锁/CAS+sychronized） | 并发 20 | ⭐⭐⭐⭐ |
| **并发设计模式**（Immutability/Copy-on-Write/ThreadLocal） | 并发 28-36 | ⭐⭐⭐⭐ |
| **读写锁与 StampedLock** | 并发 17+18 | ⭐⭐⭐ |
| **CompletableFuture**（异步编程） | 并发 24 | ⭐⭐⭐ |
| **性能调优方法论**（三层分析法） | 性能 22 | ⭐⭐⭐⭐⭐ |
| **并行设计**（Amdahl 定律/串行资源瓶颈） | 性能 02+03 | ⭐⭐⭐⭐ |
| **缓存设计**（命中率/一致性/淘汰策略） | 性能 04 | ⭐⭐⭐⭐ |
| **Benchmark 基准测试**（JMH/微基准/宏基准） | 性能 17+18 | ⭐⭐⭐⭐ |
| **编译期优化**（循环展开/向量化/内联） | 性能 13 | ⭐⭐⭐ |
| **互斥锁优化案例**（性能提升10倍） | 性能 27 | ⭐⭐⭐ |
| **零代码修改性能提升20%案例** | 性能 28 | ⭐⭐⭐ |

---

## 📌 二、面试回答标准模板

> **面试官："Java 并发编程你熟悉吗？说说并发编程的三大问题"**
>
> **回答**（摘自课程原文）：
>
> "并发编程容易出现诡异 Bug，根源在于三个硬件层面的矛盾：**可见性、原子性、有序性**。
>
> **可见性**来自 CPU 缓存——多核时代每个 CPU 有自己的缓存，一个线程修改了变量，另一个线程不一定能看到。
> **原子性**来自线程切换——高级语言里一条语句（如 `count++`）需要多条 CPU 指令，线程切换可能发生在指令之间，导致看似原子的操作被拆分了。
> **有序性**来自编译优化——编译器和 CPU 可能为了性能改变指令执行顺序。
>
> 解决这三个问题靠的是 **JMM**（Java 内存模型），核心是 **Happens-Before 规则**——volatile 规则、锁规则、线程启动规则等，这些规则告诉我们什么情况下一个线程的写对另一个线程是可见的。"

---

## 📌 模块一：Java 并发编程

### 三、并发理论基础

#### 3.1 并发 Bug 的三个根源（课程第01讲）

```
根源                 硬件原因                    Java 解决方案
─────────           ──────────                ─────────────
可见性问题     ←   CPU 缓存                   ←  volatile / JMM
原子性问题     ←   线程切换/分时复用           ←  synchronized / Lock
有序性问题     ←   编译优化/指令重排序         ←  volatile / Happens-Before
```

**可见性实验**（课程原文代码）：
```java
// 两个线程各执行 10000 次 count++
// 你以为结果是 20000，实际是 10000~20000 的随机数
// 因为每个 CPU 缓存了 count，写回内存时互相覆盖
```

**原子性实验**：
```java
count += 1;  // 需要三条 CPU 指令：load → add → store
             // 线程切换可发生在任意两条指令之间
```

**有序性经典案例**（双重检查锁定的问题）：
```java
// 看似正确的单例，实际上 new 操作可能被重排序
instance = new Singleton();
// 实际可能变成：1.分配内存 2.将内存地址赋给 instance 3.初始化对象
// 导致其他线程拿到未初始化的对象
```

#### 3.2 Java 内存模型与 Happens-Before（课程第02讲）

**6 条 Happens-Before 规则**（课程原文总结）：

| 规则 | 说明 |
|------|------|
| **程序次序规则** | 同一线程内，前面的操作 Happens-Before 后面的 |
| **volatile 规则** | volatile 变量的写 Happens-Before 后续的读 |
| **锁规则** | 解锁 Happens-Before 后续对同一锁的加锁 |
| **传递性** | A HB B, B HB C ⇒ A HB C |
| **线程启动规则** | Thread.start() HB 该线程的第一个操作 |
| **线程终止规则** | 线程的所有操作 HB 其他线程检测到该线程终止 |

**volatile 的两层语义**：
1. **禁止指令重排序** — 内存屏障
2. **保证可见性** — 写后强制刷新到主内存，读前强制从主内存读

> ⚠️ **volatile 不保证原子性**（课程重点强调）：`volatile count++` 仍然不是线程安全的。

#### 3.3 管程模型（课程第08讲）

**管程（Monitor）是并发编程的万能钥匙**。synchronized 关键字和 JUC 包里的 Lock 都是管程的实现。

**MESA 管程模型**（课程使用的模型）：
```
┌──────────────────────┐
│        管程           │
│  ┌────────────┐      │
│  │ 入口等待队列  │      │
│  └────────────┘      │
│  ┌────────────────┐  │
│  │  条件变量等待队列  │  │
│  │  (可以有多个)    │  │
│  └────────────────┘  │
└──────────────────────┘
```

三个关键操作：`等待（wait）`、`通知（notify）`、`唤醒（notifyAll）`

#### 3.4 线程池详解（课程第22讲）

**七个核心参数**：

```java
new ThreadPoolExecutor(
    corePoolSize,          // 核心线程数
    maximumPoolSize,       // 最大线程数
    keepAliveTime,         // 空闲线程存活时间
    unit,                  // 时间单位
    workQueue,             // 工作队列（阻塞队列）
    threadFactory,         // 线程工厂
    handler                // 拒绝策略
);
```

**四种拒绝策略**：AbortPolicy（抛异常）、CallerRunsPolicy（调用者执行）、DiscardPolicy（丢弃）、DiscardOldestPolicy（丢弃最旧）

**创建多少线程合适？**（课程第10讲）：
- **CPU 密集型**：`线程数 = CPU 核数 + 1`
- **IO 密集型**：`线程数 = 2 × CPU 核数`（经验值），更精确的公式：`线程数 = CPU 核数 × (1 + 等待时间 / 计算时间)`

#### 3.5 CAS 与原子类（课程第21讲）

**CAS 原理**：Compare And Swap，Unsafe 类提供的 CPU 级别原子指令。

```java
// AtomicInteger 的 incrementAndGet 底层
do {
    oldValue = this.get();  // 读取旧值
    newValue = oldValue + 1;
} while (!this.compareAndSet(oldValue, newValue));
// CAS 失败就重试
```

**ABA 问题**：一个值从 A 变成 B 又变回 A，CAS 会认为没有变化。
**解决**：用 `AtomicStampedReference`（带版本号的原子引用）。

#### 3.6 AQS 原理（课程第14讲）

**AbstractQueuedSynchronizer** — JUC 锁的基石。

```
AQS 核心数据结构：
┌─────────────────────────┐
│  state（同步状态，volatile）│
└─────────────────────────┘
┌─────────────────────────┐
│  CLH 等待队列（FIFO）     │
│  ┌───┐  ┌───┐  ┌───┐  │
│  │   │→│   │→│   │  │
│  └───┘  └───┘  └───┘  │
└─────────────────────────┘
```

两种模式：
- **独占模式**（ReentrantLock）
- **共享模式**（Semaphore、CountDownLatch）

---

### 四、并发工具类

#### 4.1 锁的对比

| 锁 | 特点 | 适用场景 |
|----|------|---------|
| **synchronized** | JVM 内置，自动释放锁 | 简单同步，代码块/方法加锁 |
| **ReentrantLock** | 可中断、可超时、可尝试获取 | 需要公平锁、定时锁等高级功能 |
| **ReadWriteLock** | 读写分离，读不互斥 | 读多写少场景 |
| **StampedLock** | 读不阻塞写（乐观读） | 比 ReadWriteLock 更高吞吐 |

#### 4.2 并发容器

| 容器 | 原理 | 面试要点 |
|------|------|---------|
| **ConcurrentHashMap** | 数组+链表+红黑树，CAS+synchronized | JDK7 分段锁，JDK8 CAS+sync |
| **CopyOnWriteArrayList** | 写时复制 | 适合读多写极少，占用内存高 |
| **ConcurrentLinkedQueue** | CAS 无锁队列 | 高性能，无界 |
| **BlockingQueue** | 阻塞队列（Array/Linked/Delay/Synchronous） | 生产者-消费者模式 |

#### 4.3 线程协作工具

| 工具 | 作用 | 类比 |
|------|------|------|
| **CountDownLatch** | 等待 N 个线程完成后再执行 | "发令枪"，一次性的 |
| **CyclicBarrier** | N 个线程都到达屏障后再继续 | "集齐所有人再出发"，可循环 |
| **Semaphore** | 控制同时访问的线程数 | "限流器"，有许可证 |
| **CompletableFuture** | 异步编程，回调编排 | "烧水泡茶"并行任务编排 |

---

### 五、并发设计模式（课程第28-36讲）

| 模式 | 核心思想 | 典型实现 | 面试要点 |
|------|---------|---------|---------|
| **Immutability** | 对象不可变，天然线程安全 | String、Integer | 不变性 + 状态的"写时复制" |
| **Copy-on-Write** | 读时不加锁，写时复制新副本 | CopyOnWriteArrayList | 适合读多写极少 |
| **ThreadLocal** | 每个线程独享变量 | ThreadLocalMap | 注意内存泄漏（Entry key 是弱引用） |
| **Guarded Suspension** | 条件不满足时等待，满足时唤醒 | LinkedBlockingQueue | 标准的等待-通知模式 |
| **Balking** | 条件不满足时直接返回 | 单例模式（DCL） | 快速失败 |
| **Thread-Per-Message** | 每个请求新建一个线程处理 | Tomcat 早期模型 | 简单但线程开销大 |
| **Worker Thread** | 复用线程，避免重复创建 | 线程池 | 线程池的核心模式 |
| **两阶段终止** | 终止前先做清理工作 | Thread.interrupt() | 优雅终止线程 |
| **生产者-消费者** | 通过队列解耦生产消费 | BlockingQueue | 流水线思想 |

#### ThreadLocal 详解（课程第30讲，面试高频）

**核心思想**："没有共享，就没有伤害"——既然共享变量会引发并发问题，干脆每个线程持有自己的副本。

**使用方式**：
```java
private static final ThreadLocal<SimpleDateFormat> dateFormat =
        ThreadLocal.withInitial(() -> new SimpleDateFormat("yyyy-MM-dd"));

// 每个线程第一次 get 时调用 initialValue() 创建自己的副本
// 线程间互不干扰，无需加锁
```

**底层原理**：
```
每个 Thread 对象内部持有一个 ThreadLocalMap
ThreadLocal 对象本身作为 key，线程独享的值作为 value

Thread
  └── ThreadLocalMap
        ├── Entry(key=ThreadLocal弱引用, value=线程独享值)
        ├── Entry(...)
        └── ...
```

关键设计：**ThreadLocalMap 属于 Thread 而不是反过来**——因为如果 Map 属于 ThreadLocal（静态全局的），所有线程共享一个 Map 还得加锁；现在每个线程自己的 Map，天然线程隔离。

**内存泄漏（最常考）**：
```
泄漏原因：
  Entry 的 key 是 ThreadLocal 的弱引用（WeakReference）
  当外部强引用 ThreadLocal 被回收后，key 变为 null
  但 value 仍然被 Thread 强引用（线程存活 → ThreadLocalMap 存活 → value 存活）

危险场景：线程池！线程长期存活，value 永远无法回收 → 内存泄漏

解决：用完必须 remove()
  try {
      // 使用 ThreadLocal
  } finally {
      threadLocal.remove();   // ★ 一定要在 finally 里 remove
  }
```

**InheritableThreadLocal**：子线程继承父线程的值。
**注意**：不要和线程池混用——子线程的值是创建时从父线程复制的，线程池复用线程会拿到旧值。

**典型应用**（Spring 事务管理）：
```
Spring 把数据库连接放到 ThreadLocal 中
同一线程内的多个 DAO 操作共享同一个 Connection → 保证同一个事务
注意：@Async 异步方法会切换线程 → ThreadLocal 里的连接丢失 → 事务失效
```

**面试回答**：
> Q：ThreadLocal 的原理和内存泄漏问题？
>
> ThreadLocal 的核心是"没有共享就没有伤害"——每个线程在它的 Thread 对象里持有一个 ThreadLocalMap，ThreadLocal 作为 key，线程独享的值作为 value，所以天然线程安全。Spring 用它保存数据库连接实现事务。
>
> 内存泄漏是它的经典问题：ThreadLocalMap 的 Entry key 是**弱引用**，当外部不再持有 ThreadLocal 时 key 变 null，但 value 仍被线程强引用——尤其在**线程池**中线程长期存活，value 永远回收不了。所以使用后必须在 finally 里调用 `remove()` 清除。

---

### 六、案例分析

| 案例 | 核心优化点 | 面试关键词 |
|------|-----------|-----------|
| **Guava RateLimiter** | 令牌桶算法，SmoothBursty/SmoothWarmingUp | 限流、预热 |
| **Netty** | Reactor 模型，NIO 多路复用，零拷贝 | 高性能网络框架 |
| **Disruptor** | 无锁环形队列，CAS 替代锁，缓存行填充 | 单机最高性能队列 |
| **HikariCP** | 优化字节码，减少锁粒度，FastPathPool | 最快连接池 |

---

## 📌 模块二：系统性能优化

> 课程讲师尉刚强，核心观点：**从软件设计与架构维度出发的性能优化，不仅性能收益更大，还会让软件架构设计更加合理和简洁**。

### 七、性能设计与建模（课程第01讲）

**两种建模方法**：

| 模型 | 用途 | 方法 |
|------|------|------|
| **软件执行模型** | 静态分析单请求处理时延 | 执行图（节点+箭头，含并行/循环/分支） |
| **系统执行模型** | 动态分析多用户/资源竞争 | QNM排队网络模型 |

**案例分析**：智能对话引擎通过将语义计算模型拆分到多核并发执行，理论上性能提升近6倍。

### 八、并行设计（课程第02-03讲）

#### 8.1 六种并行架构模式

| 模式 | 原理 | 适用场景 |
|------|------|---------|
| **任务线性分解** | 按计算逻辑确定性拆分 | 业务逻辑确定 |
| **任务分治** | 动态创建子任务 | 递归类算法 |
| **数据几何分解** | 按数据分片 | SPMD 多核处理 |
| **递归数据** | 树状/图状数据动态拆分 | 搜索/遍历 |
| **数据流交互** | 并行单元间单向交互 | 生产者-消费者 |
| **异步交互** | 交互关系复杂的并行设计 | 微服务编排 |

#### 8.2 Amdahl 定律

```
加速比 = 1 / ((1 - P) + P / N)
P = 可并行化比例，N = 处理器数量
```

**核心启示**：串行部分是瓶颈。即使无限多 CPU，性能也受串行部分限制。

#### 8.3 案例：互斥锁优化 30 倍提升

> 在线抢货系统，使用 Redis 互斥锁导致吞吐仅 30TPS。改用**无锁化实现**（CAS+Redis Pipeline 保证原子性）后，吞吐提升至 1000TPS，**提升30倍**（课程第03讲案例）。

### 九、缓存设计（课程第04讲）

**缓存评估公式**：`AMAT = Thit + MR × MP`（平均访问时间 = 命中时间 + 失效率 × 失效代价）

**三种数据类型的缓存策略**：

| 数据类型 | 缓存方式 | 失效机制 |
|---------|---------|---------|
| **不变性数据** | 优先使用缓存 | 永久不失效或基于时间 |
| **弱一致性数据** | 推荐使用 | 基于时间失效 |
| **强一致性数据** | 一般不推荐 | 如需使用须用修改同步 |

### 十、IO 设计与通信设计

#### IO 三种交互方式

| 方式 | 原理 | 适用 |
|------|------|------|
| **同步阻塞** | 线程挂起等待 | 简单但线程多时性能受限 |
| **同步非阻塞** | NIO Selector 轮询 | 多通道场景 |
| **异步回调** | Node.js async/await | 减少 CPU 参与开销 |

#### 通信队列四层级（课程第06讲）

```
传输时延从低到高：
线程内队列 → 线程间队列 → 进程间队列 → 服务器间队列

消息队列选型：ZeroMQ（最低时延） > Kafka（最高吞吐） > RabbitMQ（最丰富功能）
```

### 十一、性能调优方法论（课程第22讲 — 课程核心方法论）

#### 11.1 三层定位分析法

```
性能问题定位的三种层次：
┌────────────────────────────────────┐
│ ① 业务模型分析                       │
│   └─ 找到触发性能问题的业务点           │
│      验证：打桩规避触发点，观察是否改善    │
├────────────────────────────────────┤
│ ② 软件架构分析                       │
│   └─ 分析组件/服务接口交互关系           │
│      找到具体哪个组件/服务出现瓶颈         │
├────────────────────────────────────┤
│ ③ 组件/服务实现分析                  │
│   └─ 深入代码和资源层                 │
│      识别串行资源/缓冲/缓存/Bug 等瓶颈    │
└────────────────────────────────────┘
```

**案例**：Web 页面查询数据下载慢，通过 Elasticsearch+Kibana 接口日志分析，定位到压缩数据处理与浏览统计逻辑共用同一任务队列，拆分为两个队列后解决。

#### 11.2 常见性能瓶颈点

| 瓶颈类型 | 例子 | 影响 |
|---------|------|------|
| **串行资源** | 互斥锁、单线程逻辑 | 吞吐饱和后不再增长，延迟急剧拉长 |
| **有限资源** | 线程池、连接池 | 到达上限后排队阻塞 |
| **缓冲区** | MQ 容量 | 过小丢请求，过大浪费内存 |
| **缓存命中率** | Redis、本地缓存 | 命中率低直接拉长响应时间 |
| **软件 Bug** | 批处理退化成循环 | 可能导致性能骤降 |

### 十二、Benchmark 与性能看护

#### 微基准测试（JMH — 课程第17讲）

```java
@Benchmark
@Warmup(iterations = 5)         // 预热，规避JIT影响
@Measurement(iterations = 5)
@BenchmarkMode(Mode.Throughput)
public void testMethod() {
    // 被测代码
}
```

**常见陷阱**：JIT 内联/OSR/逆优化、System.nanoTime() 误差、测量结果波动需基于置信区间。

**利特尔法则**：`并发用户数 = 平均响应时间 × TPS`

#### 宏基准测试（课程第18讲）

通过软件架构分析将系统级性能指标拆分为子系统/服务级测试。用 Glances 或 psutil 监控资源状态验证测试正确性。

#### 性能 CI（课程第21讲）

- **微基准测试**可像单元测试一样集成到 Pipeline
- **宏基准测试**：系统级用定时器触发，组件/服务级可合入触发
- 解决 N+1 性能问题——代码合入第一时间发现性能劣化

### 十三、编译期优化与 CPU 潜能（课程第13、16讲）

#### C/C++ 编译优化

| 优化 | 做法 |
|------|------|
| GCC 优化级别 | `-O1/-O2/-O3/-Os` |
| 分支预测 | `__builtin_expect(likely/unlikely)` |
| Cache 预取 | `__builtin_prefetch` |
| PGO 优化 | `-fprofile-arcs` + 运行 + `-fbranch-probabilities` |
| 编译器替换 | Clang 替换 GCC |

#### JVM 优化

| 参数 | 作用 | 推荐 |
|------|------|------|
| `-Xms/-Xmx` | 堆大小 | 高吞吐场景设相同值避免动态调整 |
| `-XX:NewRatio` | 新生代/老年代比例 | 根据对象特点调整 |
| GC 选择 | Parallel/CMS/G1 | 吞吐优先/响应优先/大内存 |

#### SIMD 向量化

- **C/C++**：`__m256d` + `_mm256_add_pd` 显式 SIMD，或 `gcc -mavx` 让编译器自动向量化
- **Java**（JDK 16+）：`jdk.incubator.vector` 模块，`FloatVector.SPECIES_512`
- **经典案例**（课程第28讲）：Web 服务只调整了数据访问模式让编译器成功向量化，**代码一行没改，性能提升20%**

### 十四、八大性能实现模式（课程第09-10讲）

| 模式 | 原理 | 类比 |
|------|------|------|
| **快速通道** | 二八效应，20%场景定制优化 | 快递 VIP 通道 |
| **并行分解** | 挖掘可并行执行部分 | 多核分工 |
| **批处理** | 多次操作合并一次处理 | 团购 |
| **弹性时间** | 离散化请求时间防拥塞 | 错峰出行 |
| **预计算** | 将计算提前到编译期/启动前 | 查表替代计算 |
| **耦合模式** | 合并相关功能减少调用次数 | 复合接口 |
| **搬移计算** | 计算从关键路径搬移到非关键路径 | 延迟计算 |
| **丢弃模式** | 极端场景丢弃非关键处理 | 保核心业务 |

### 十五、实战案例

#### 案例1：互斥锁优化，并发用户数提升10倍（课程第27讲）

**问题**：一个资源被全局锁保护，所有线程串行访问。
**分析**：串行资源导致 Amdahl 定律中的 P 值极小。
**解决**：将全局锁拆分为**分段锁**或**无锁数据结构**（如 ConcurrentHashMap、Disruptor 的思路）。

#### 案例2：Web 服务，代码一行不动，性能提升20%（课程第28讲）

**问题**：某 Web 服务响应慢，CPU 使用率高。
**分析**：发现是**编译器的自动向量化**没有生效——热循环的数据访问模式不利于 SIMD。
**解决**：只调整了数据结构和循环的访问模式（数据连续 + 无依赖），让编译器成功向量化，性能提升 20%，**代码一行没改**。

---

### 十三、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **并发Bug三大根源？** | 可见性（CPU缓存）、原子性（线程切换）、有序性（编译优化） |
| **volatile 能保证原子性吗？** | 只能保证可见性和禁止重排序，不保证原子性 |
| **synchronized 原理？** | 管程（MESA模型），JVM内置锁 |
| **死锁四个条件？** | 互斥/占有且等待/不可抢占/循环等待 |
| **创建多少线程合适？** | CPU密集型 = CPU核数+1；IO密集型 = 2×CPU核数（经验值） |
| **CAS 的ABA问题？** | 值从A→B→A，CAS认为没变；用 AtomicStampedReference 解决 |
| **AQS 是什么？** | AbstractQueuedSynchronizer，JUC锁的基石，CLH队列 |
| **ConcurrentHashMap原理？** | JDK7分段锁，JDK8 数组+链表+红黑树，CAS+synchronized |
| **性能调优怎么入手？** | 三层分析法：业务模型→软件架构→组件实现 |
| **系统性能瓶颈有哪些？** | 串行资源、有限资源、缓冲区、缓存命中率、软件Bug |
| **什么情况下代码不改也能提升性能？** | 编译期优化：改变循环结构让编译器向量化 |
| **Amdahl定律说什么？** | 加速比受串行部分限制，串行部分是关键 |

---

> 📝 **两课关系总结**：并发编程是 Java 专项，讲"怎么写对"；性能优化是全链路方法论，讲"怎么更快"。并发是性能的重要子集，两课合并让你既能从微观（Java 并发工具、锁、线程池）又能从宏观（设计、实现、调优方法）掌握性能。
