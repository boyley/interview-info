# Java 性能调优实战 — 面试备战手册（完整版）

> 来源：极客时间《Java 性能调优实战》（刘超，Java 技术专家，多年电商平台性能优化经验）
> 完整覆盖 7 大模块：概述 → Java 编程调优 → 多线程调优 → JVM 调优 → 设计模式调优 → 数据库调优 → 实战案例

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| **性能调优标准**（响应时间/吞吐量/资源利用率） | 01 | ⭐⭐⭐⭐⭐ |
| **性能调优策略**（微基准/宏基准/预热） | 02 | ⭐⭐⭐⭐ |
| **String 优化**（StringBuilder vs + / intern） | 03 | ⭐⭐⭐⭐⭐ |
| **ArrayList vs LinkedList**（真实性能差千倍） | 05 | ⭐⭐⭐⭐⭐ |
| **HashMap 底层优化**（JDK7→8 红黑树/扰动/尾插法） | 07 | ⭐⭐⭐⭐⭐ |
| **I/O 模型**（BIO→NIO→AIO/零拷贝） | 08 | ⭐⭐⭐⭐⭐ |
| **序列化优化**（Protobuf vs Java 序列化） | 09 | ⭐⭐⭐⭐⭐ |
| **RPC 通信协议优化**（长连接/Netty） | 10 | ⭐⭐⭐⭐ |
| **性能测试工具**（ab/JMeter/LoadRunner） | 加餐 | ⭐⭐⭐ |
| **synchronized 锁优化**（偏向→轻量→重量） | 12 | ⭐⭐⭐⭐⭐ |
| **Lock 优化**（ReentrantLock/ReadWriteLock/StampedLock） | 13 | ⭐⭐⭐⭐ |
| **乐观锁（CAS/ABA）** | 14 | ⭐⭐⭐⭐ |
| **上下文切换优化**（vmstat 排查） | 15+16 | ⭐⭐⭐⭐ |
| **并发容器**（ConcurrentHashMap/COW/BlockingQueue） | 17 | ⭐⭐⭐⭐ |
| **线程池大小**（CPU/IO 密集公式） | 18 | ⭐⭐⭐⭐⭐ |
| **协程**（用户态调度/Quasar） | 19 | ⭐⭐⭐ |
| **JIT 即时编译**（分层编译/逃逸分析/锁消除） | 21 | ⭐⭐⭐⭐ |
| **GC 优化与内存分配**（堆比例/大对象/双11案例） | 22+23 | ⭐⭐⭐⭐⭐ |
| **内存持续上升排查**（jstat/jmap/MAT） | 24 | ⭐⭐⭐⭐⭐ |
| **设计模式调优**（单例/享元/生产者消费者/装饰器） | 26-30 | ⭐⭐⭐⭐ |
| **SQL 优化/索引失效/死锁** | 32-35 | ⭐⭐⭐⭐⭐ |
| **分库分表/电商表设计** | 36+37 | ⭐⭐⭐⭐ |
| **InnoDB 参数调优** | 38+39 | ⭐⭐⭐⭐ |
| **分布式锁/事务/缓存/双11实战** | 41-44 | ⭐⭐⭐⭐ |

---

## 📌 二、性能调优概述（模块一）

### 1. 如何制定性能调优标准（第01讲）

**为什么要调优**：未调优的系统是"定时炸弹"——问题随访问峰值（大促/用户增长）暴露。好的调优还能省硬件成本（案例：资深工程师让服务器减半还提升了性能）。

**什么时候开始调优**：**不是开发早期**。早期只需保证编码干净（减少磁盘IO、降低锁竞争、用高效算法、用设计模式）。调优在**编码完成后的性能测试阶段**进行。

**性能瓶颈资源**：

| 资源 | 常见问题 |
|------|---------|
| **CPU** | 死循环、正则回溯、Full GC、上下文切换过多 |
| **内存** | 堆溢出、内存泄漏 |
| **磁盘 I/O** | 比内存慢几个数量级（SSD 也慢） |
| **网络** | 高并发下带宽成为瓶颈 |
| **异常处理** | 构建异常栈跟踪开销大，热路径避免 |
| **数据库** | 最常见的瓶颈 |
| **锁** | 引起上下文切换（JDK6+ 已优化） |

**性能指标**：
- **响应时间**：数据库响应、服务器响应、网络响应、客户端响应
- **吞吐量（TPS/QPS）**：TPS=每秒事务数（一个用户操作），QPS=每秒查询数
- **磁盘**：IOPS（随机读写，OLTP）/ 吞吐量（顺序读写，视频）
- **网络吞吐量**：取决于网卡、CPU、带宽
- **资源利用率**：CPU%、内存%、磁盘 I/O、网络 I/O
- **负载能力**：观察负载增加时的响应时间曲线

### 2. 如何制定性能调优策略（第02讲）

**两种基准测试**：
- **微基准测试**：单个模块/方法级（如 synchronized vs 非 synchronized）
- **宏基准测试**：模拟真实环境、场景、目标的全链路测试

**预热效应**：JVM 先解释执行，HotSpot JIT 编译热点代码。首次测试慢，预热后结果稳定。

**多 JVM 实例**：单 JVM 测试好不代表多 JVM 好——会竞争资源。

**分析/优化方向**：
```
分析：自底向上（OS → 数据库 → JVM → 应用代码）
优化：自顶向下（应用 → 系统 → OS）
```

**兜底策略**：限流（按压测最大TPS）+ 智能水平扩展（K8s）+ 秒杀预扩容。

**性能测试工具（加餐）**：
| 工具 | 特点 |
|------|------|
| **ab（Apache Bench）** | 命令行简单压测：`ab -n 1000 -c 100 http://test.com/`，看 Requests per second |
| **JMeter** | GUI，支持复杂场景、CSV 参数化、断言 |
| **LoadRunner** | 商业，稳定，支持 IP 欺骗模拟真实用户 |

---

## 📌 三、Java 编程性能调优

### 1. String 优化（第03讲）

**String 不可变性**：
```java
String s = "Hello" + "World";   // 编译期优化，常量池
String s = a + b;                // 运行时 new StringBuilder().append(a).append(b).toString()
```

**关键优化**：
- 循环拼接用 **StringBuilder**（每次 `+` 都 new StringBuilder，循环中性能差百倍）
- `String.intern()` 复用常量池，减少内存（JDK6 存永久代 OOM，JDK7+ 存堆）

### 2. 正则表达式优化（第04讲）

**回溯陷阱**：NFA 引擎贪婪匹配导致大量回溯，CPU 飙升。

| 匹配模式 | 写法 | 回溯 | 适用 |
|---------|------|------|------|
| 贪婪（默认） | `\w+` | 多 | 一般场景 |
| 懒惰 | `\w+?` | 少 | 避免过度匹配 |
| **占有** | `\w++` | **无** | 确定不需要回溯，性能最好 |

**优化建议**：能不用正则就不用，用 `indexOf`/`startsWith` 替代简单匹配。

### 3. ArrayList vs LinkedList（第05讲）

| 操作 | ArrayList | LinkedList |
|------|-----------|-----------|
| 尾部插入 | O(1)，偶尔扩容 | O(1) |
| 中间插入 | O(n) | O(n)查找+O(1)插入 |
| **随机访问** | **O(1)** 直接下标 | O(n) 遍历 |
| 遍历 | **for 循环 O(n)** | for-each 用迭代器 O(n)；**for 循环 get(i) 是 O(n²)** |

**关键结论**：99% 场景用 ArrayList。错误使用性能差异**千倍**。LinkedList 只在频繁头尾操作且不随机访问时有优势。

### 4. Stream 性能（第06讲）

| 方式 | 性能 | 适用 |
|------|------|------|
| for 循环 | 最快 | 简单遍历 |
| 增强 for | 略慢（迭代器） | 代码简洁 |
| Stream 串行 | 稍慢 | 可读性好 |
| **Stream 并行** | 数据量大时更快 | CPU 密集型，注意线程安全 |

**注意**：并行流用 ForkJoinPool，I/O 密集任务会饿死其他并行流。

### 5. HashMap 优化（第07讲）

**JDK 7 → 8 的优化**：
- 扰动函数：JDK7 4次位运算+5次异或 → JDK8 1次（`h ^ (h >>> 16)`）
- 扩容重哈希：JDK7 重新计算 → JDK8 通过 `hash & oldCap` 判断（低位0→原位，1→原+旧容量）
- 引入红黑树：链表 >8 转红黑树，<6 转回链表
- 头插法→尾插法：**避免多线程扩容死循环**（JDK7 并发 put 导致 CPU 100%）

**初始化容量**：`new HashMap(capacity / 0.75F + 1)` 避免扩容。

### 6. I/O 模型（第08讲）

| 模型 | 阻塞点 | 连接模型 | 适用 |
|------|--------|---------|------|
| **BIO** | 读/写/连接阻塞 | 一线程一连接 | 连接少 |
| **NIO** | Selector 非阻塞 | 多路复用 | 高并发短连接 |
| **AIO** | 异步回调 | 回调通知 | 文件IO/长连接 |

**零拷贝**：`FileChannel.transferTo()` 数据直接内核态到网卡，跳过用户态拷贝。

### 7. 序列化优化（第09讲）

**为什么避免 Java 序列化**：
1. 跨语言差（只有 Java 能反序列化）
2. **安全问题**（readObject 可实例化任意 Serializable 类 → 反序列化漏洞）
3. 体积大（比 Protobuf 大几倍）
4. 速度慢（慢 2000 倍）

| 序列化 | 体积 | 速度 | 跨语言 |
|--------|------|------|--------|
| Java 原生 | ❌ 大 | ❌ 慢 | ❌ |
| **Protobuf** | ✅ 小 | ✅ 快 | ✅ |
| Kryo | ✅ 小 | ✅ 快 | ⚠️ 仅Java |

**原则**：远程通信避免 Java 序列化，优先 Protobuf。

### 8. RPC 通信协议优化（第10讲）

**RMI 的三大瓶颈**：Java 序列化慢、短连接每次 TLS、BIO 阻塞。

**优化方向**：
- **长连接**：内部服务间避免重复 TCP 握手（性能大幅提升）
- **NIO + Netty**：处理线程模型和内存拷贝优化
- TCP（可靠流式）vs UDP（无连接更快，容忍丢包用）

---

## 📌 四、多线程性能调优

### 1. synchronized 锁优化（第12讲）

**JDK 1.6 后的锁升级**：无锁 → 偏向锁 → 轻量级锁 → 重量级锁（单向不可逆）

| 锁状态 | 开销 | 适用 |
|--------|------|------|
| **偏向锁** | 极低（CAS记录线程ID） | 只有一线程访问 |
| **轻量级锁** | 低（自旋CAS） | 多线程交替访问 |
| **重量级锁** | 高（OS互斥量） | 多线程同时竞争 |

**JIT 额外优化**：
- **锁消除**：逃逸分析发现锁对象只被当前线程访问 → 去掉锁
- **锁粗化**：连续对同一对象加锁解锁 → 合并成一次

**调优参数**：`-XX:BiasedLockingStartupDelay=0`（关闭偏向锁延迟）、`-XX:-UseBiasedLocking`（高并发场景关闭偏向锁）。

### 2. Lock 选型（第13-14讲）

| 锁 | 读并发 | 写并发 | 特点 |
|----|--------|--------|------|
| **ReentrantLock** | 互斥 | 互斥 | 可中断、可超时、公平可选 |
| **ReadWriteLock** | 共享 | 互斥 | 读多写少 |
| **StampedLock** | 乐观读 | 互斥 | 更高读吞吐（乐观读不阻塞写） |
| **CAS（无锁）** | 无锁 | 无锁 | 简单竞争 |

**CAS 的 ABA 问题**：值 A→B→A，CAS 认为没变 → 用 `AtomicStampedReference`（带版本号）。

### 3. 上下文切换优化（第15-16讲）

**上下文切换的五大原因**：锁竞争、IO阻塞、CPU时间片耗尽、主动让出（yield/sleep/wait）、GC停顿。

**监控工具**：`vmstat` 看 `cs`（上下文切换）、`r`（运行线程）、`b`（阻塞线程）。

**优化手段**：
| 手段 | 做法 |
|------|------|
| 减少竞争 | CAS 替代锁、无锁数据结构 |
| 减少线程数 | 找到最优线程数，不是越多越好 |
| 协程 | 用户态调度，切换成本低一个数量级 |
| 减少锁粒度 | 分段锁（ConcurrentHashMap）、读写分离 |

### 4. 并发容器（第17讲）

| 容器 | 原理 | 适用 |
|------|------|------|
| **ConcurrentHashMap** | JDK7分段锁，JDK8 CAS+sync+红黑树 | 并发Map首选 |
| **CopyOnWriteArrayList** | 写时复制数组 | 读多写极少 |
| **ConcurrentLinkedQueue** | CAS无锁队列 | 高性能无界 |
| **BlockingQueue** | 阻塞队列 | 生产者-消费者 |

### 5. 线程池大小（第18讲）

```java
// CPU 密集型
线程数 = CPU 核数 + 1

// IO 密集型
线程数 = CPU 核数 × (1 + IO等待时间 / CPU计算时间)
```

**实践**：先公式估算，再压测调优。用有界队列，避免 OOM。

### 6. 协程（第19讲）

```
协程 = 用户态调度的轻量级线程（fibers）
  - 切换不经过内核，成本远低于线程
  - 每个线程可承载成千上万个协程
  - Java 支持：Project Loom（虚拟线程）、Quasar 库
  - 适用：高并发 + 每个请求有阻塞 IO（DB调用/网络调用）
```

---

## 📌 五、JVM 性能调优

### 1. 内存模型（第20讲）

```
JVM 内存模型五区：堆、程序计数器、方法区、虚拟机栈、本地方法栈
堆：Eden + Survivor(From/To) + 老年代
方法区：Java8 用元空间（Metaspace）取代永久代（PermGen）
  - PermGen 固定大小易 OOM；Metaspace 用本地内存动态扩展
```

### 2. JIT 优化（第21讲）

**编译链路**：源码 → javac → 字节码 → 解释执行/JIT → 机器码

**分层编译（C1 + C2）**：
```
0层 解释执行 → 1-3层 C1（快，简单优化）→ 4层 C2（慢，极致优化）
```

**JIT 优化技术**：方法内联、逃逸分析（栈上分配/锁消除/标量替换）、死代码消除、循环展开。

**调优参数**：`-XX:MaxInlineSize=325`（内联阈值）、`-XX:CompileThreshold=10000`（热点编译阈值）。

### 3. GC 优化（第22讲）

**GC 选型**：

| 场景 | 推荐 GC | 核心参数 |
|------|---------|---------|
| 吞吐量优先 | Parallel Scavenge + Parallel Old | `-XX:+UseParallelGC` |
| 响应时间优先 | G1 | `-XX:MaxGCPauseMillis=200` |
| 大内存低延迟 | ZGC（JDK11+） | `-XX:+UseZGC` |

**GC 日志分析**：
```bash
-XX:+PrintGCDetails -Xloggc:gc.log -XX:+PrintGCDateStamps
```
**关键指标**：Young GC 频率（太频繁→Eden 过小）、Full GC 次数（太多→老年代小或泄漏）、GC 停顿时间（>1s 需关注）。

**调优思路**：
```
年轻代高位频繁 Minor GC → 调大年轻代
年老代高位 Full GC → 看 Full GC 后是否降下来
  → 降了 → 调大年老代
  → 不降 → 怀疑内存泄漏
```

### 4. 内存分配优化（第23讲）

**默认比例**：新生代:老年代 = 1:2；Eden:S0:S1 = 8:1:1。

**分配策略**：大对象直接进老年代（`-XX:PretenureSizeThreshold`，避免 Minor GC 复制开销）。

**双11案例**：API 创建 20MB 对象，默认堆导致 Full GC 频繁。
```
Fix1: 调大堆 → Full GC 频率降，吞吐+50%，响应-30%
Fix2: 调大新生代 → Minor GC 频率降，吞吐再升
Fix3: 固定 SurvivorRatio → GC 总时间降
```

### 5. 内存持续上升排查（第24讲）

```
症状：CPU 飙升 + Full GC 频繁 + 老年代持续增长

排查步骤：
① top → 找进程 PID
② jstat -gc <pid> 1000 → 看各代 GC 频率
③ jmap -dump:live,format=b,file=heap.bin <pid> → 堆转储
④ MAT 分析 → 定位大对象/泄漏点
⑤ jstack → 看线程栈

案例：ThreadLocal 泄漏——ThreadLocalMap 的 key 是弱引用，
  外部 ThreadLocal 被回收后 value 仍被线程强引用（线程池场景最危险）
  → 必须 remove()
```

---

## 📌 六、设计模式调优（模块五）

### 1. 单例模式（第26讲）

| 实现 | 特点 |
|------|------|
| 饿汉 | 简单，不用也占内存 |
| **懒汉（DCL）** | volatile + synchronized 双重检查 |
| **静态内部类** | 线程安全、懒加载、无同步开销 |
| **枚举** | 天然防序列化/反射破坏，最佳实践 |

**readResolve()**：Serializable 单例反序列化返回同一实例。

### 2. 原型模式与享元模式（第27讲）

**原型模式**：`clone()` 复制已有对象，避免昂贵构造/初始化。
**享元模式**：共享细粒度对象减少内存。经典：`Integer.valueOf()` 缓存 -128~127、String 常量池。

### 3. 并发设计模式（第28讲）

| 模式 | 作用 |
|------|------|
| **Future/Promise** | 先提交任务，后取结果，避免阻塞 |
| **线程池** | 复用线程，控制并发 |
| **生产者-消费者** | BlockingQueue 解耦生产消费 |
| **Master-Worker** | 主分任务，工人处理，聚合结果 |

### 4. 生产者-消费者模式：电商库存优化（第29讲）

```
库存检查与实际扣减分离：
  库存更新请求进入缓冲队列
  消费者批量处理扣减（乐观锁/分布式锁）
  允许"激进超卖"（略超分配购买名额）最大化销量

结果：降低数据库压力，避免库存表竞争
```

### 5. 装饰器模式：商品价格策略（第30讲）

```java
new CouponDecorator(
    new MemberDiscountDecorator(
        new BasePrice()))   // 基价 + 会员折扣 + 优惠券
```
**优点**：方便增删促销，符合开闭原则。

---

## 📌 七、数据库性能调优

### 1. SQL 优化（第32讲）

**性能杀手**：
1. 大表无索引或索引失效
2. 行锁升级表锁（InnoDB 更新条件无索引）
3. 大结果集不分页（LIMIT M,N 随 M 增大变慢）

**EXPLAIN 关键列**：`type`（const > eq_ref > ref > range > index > ALL）、`rows`、`Extra`（Using filesort/Using temporary 是坏的）。

**分页优化**：`LIMIT 100000,20` 慢 → 用索引分页 `WHERE id > last_seen_id LIMIT 20`。

### 2. 事务调优（第33讲）

**隔离级别**：Read Uncommitted（脏读）→ Read Committed → Repeatable Read（InnoDB默认）→ Serializable。

**InnoDB 锁**：
- 共享锁(S)/排他锁(X)；行锁基于索引，无索引条件→表锁
- **间隙锁（Next-Key Lock）**：只在 Repeatable Read 级别，防幻读

**调优**：
- 用**最低隔离级别**满足正确性
- **减少锁时间**：事务内锁操作尽量靠后（如订单→库存扣减，扣库存放最后）
- 避免表锁：UPDATE/DELETE 的 WHERE 用索引

### 3. 索引失效与优化（第34讲）

**联合索引最左前缀**：
```sql
KEY idx_name_age_sex (name, age, sex)
-- ✅ 走索引：name / name+age / name+age+sex
-- ❌ 不走：age（跳过最左列）
```

**索引失效场景**：LIKE '%xx'、OR 条件、隐式类型转换、函数操作、`!=` 或 `<>`。

**自增主键 vs UUID**：UUID 随机插入导致**页分裂**和内存碎片，插入性能差。推荐自增整数主键。

### 4. 死锁分析（第35讲）

**真实死锁案例**：幂等检查表在 Repeatable Read 下，两个事务同时 SELECT（间隙锁）+ INSERT（插入意向锁）互相等待。

**避免策略**：
1. 唯一索引（防幻读不产生间隙锁）
2. `innodb_lock_wait_timeout` 调短
3. 所有事务按相同顺序访问资源
4. 用更低隔离级别（Read Committed 禁用间隙锁）
5. UPDATE 用主键避免辅助索引+聚簇索引锁冲突
6. 避免长事务

### 5. 分库分表（第36讲）

**原则**：能不拆就不拆。先尝试分区、NoSQL、读写分离。

**何时拆**：单表超千万/索引层级>4、高并发超连接/磁盘/网络上限、日增数据量大。

**水平切分**：`user_id % N` 或一致性哈希；按时间范围（有热点）。
**分库问题**：跨库事务（XA/TCC）、跨库 Join（冗余字段）、跨库分页（用 ES）、全局主键（雪花算法）、动态扩容（初始 2^N 份，×2 扩）。

### 6. 电商表设计优化（第37讲）

| 模块 | 优化 |
|------|------|
| 商品表 | 复杂属性用 KV 存储（MongoDB） |
| 购物车 | 热冷分离：近1月放 Redis，老数据 DB |
| 订单表 | 一开始就用雪花 ID；按 user_id 分库；管理端查询异步同步到 ES |
| 库存表 | 预热到 Redis + 分布式锁扣减；无限库存跳过检查 |
| 优惠券 | 预加载计数到 Redis，异步更新 DB |

**设计原则**：复杂字段用 KV、热点用缓存、尽早规划分库、冗余字段避免 Join、**不用外键**（加锁伤性能）。

### 7. InnoDB 参数调优（第38+39讲）

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| `innodb_buffer_pool_size` | 物理内存 60-80% | 最重要的参数，缓存索引和数据页 |
| `innodb_log_file_size` | 足够大 | Redo log 太小→频繁 fsync→IO 瓶颈 |
| `innodb_flush_log_at_trx_commit` | 1（安全）/2（性能） | 1=每次fsync，2=每秒fsync |
| `innodb_io_capacity` | 匹配磁盘IOPS | 脏页刷新IO上限 |
| `innodb_buffer_pool_instances` | >1GB时启用 | 减少竞争 |

**InnoDB 逻辑存储**：表空间 → 段 → 区(1MB/64页) → 页(16KB) → 行。
**redo log（WAL）**：先写日志再写内存再写磁盘，保证持久性。
**undo log**：MVCC 读一致性 + 回滚。
**buffer pool LRU 优化**：分 young/old 两个子列表，防止大扫描冲掉热数据。

---

## 📌 八、实战案例

### 1. 分布式锁设计（第41讲）

| 方案 | 可靠性 | 性能 |
|------|--------|------|
| **Redis SET NX EX** | ⚠️ 主从切换可能丢 | ✅ 高 |
| **Redisson（RedLock）** | ⚠️ 争议 | ✅ 高 |
| **ZK 临时顺序节点** | ✅ 可靠 | ⚠️ 中 |
| **数据库 FOR UPDATE** | ⚠️ 阻塞死锁 | ❌ 低 |

**注意**：Redis 锁 TTL 要大于业务执行时间，否则锁提前过期；或看门狗续期。

### 2. 分布式事务（第42讲）

**电商游戏案例**：买了道具但币没扣（跨数据库不一致）。
- **XA（2PC）**：同服务多数据库；阻塞，吞吐低
- **TCC（Seata）**：业务补偿，性能好，业务侵入大
- **Seata AT**：自动解析 SQL 生成 undo log，业务侵入小
- **MQ 最终一致**：非关键路径（积分/通知）

### 3. 缓存优化（第43讲）

**缓存更新策略**：先更新 DB 再删缓存（Cache Aside）。删除失败用**延迟双删**或 MQ 重试。

**缓存三大问题**：
| 问题 | 解决 |
|------|------|
| **穿透**（查不存在） | 缓存空值 + 布隆过滤器 |
| **击穿**（热点key过期） | 互斥锁/永不过期+异步刷新 |
| **雪崩**（大量key同时过期） | 过期时间加随机偏移 |

### 4. 双十一抢购瓶颈调优（第44讲）

**案例流程**：
```
发现瓶颈：下单接口 TP99 过高
排查：数据库 CPU 100%，慢查询集中在库存扣减
优化：
  ① 静态商品页上 CDN
  ② 倒计时从服务端取 + 客户端随机错峰同步
  ③ 购买资格系统（名额 > 库存）分离抢购与下单
  ④ 库存预热到 Redis + Lua 扣减
  ⑤ MQ 异步写订单（支付回调→MQ→减库存/积分/短信）
  ⑥ 订单表按 user_id 分库，管理端查询走 ES
结果：TP99 从 2s 降到 200ms
```

**核心原则**：峰值期间减少数据库操作、缩短下单路径、削峰限流、非关键路径异步化、数据丢失用重试机制兜底。

---

## 📌 九、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **性能调优指标有哪些？** | 响应时间、吞吐量(TPS/QPS)、磁盘IOPS/吞吐、网络吞吐、资源利用率 |
| **什么时候开始调优？** | 不是开发早期，是编码完成后的性能测试阶段 |
| **String 拼接为什么慢？** | `+` 循环中反复 new StringBuilder，用 StringBuilder |
| **ArrayList 和 LinkedList 怎么选？** | 99% 用 ArrayList（随机访问快）；频繁头尾操作才用 LinkedList |
| **HashMap JDK7和8区别？** | 红黑树、扰动简化、尾插法防死循环、扩容重哈希优化 |
| **synchronized 锁升级过程？** | 偏向→轻量→重量，单向不可逆，JIT 还有锁消除/锁粗化 |
| **线程池大小怎么设？** | CPU密集=核数+1；IO密集=核数×(1+等待/计算) |
| **上下文切换怎么优化？** | 减少竞争(CAS)、减线程数、协程、减锁粒度 |
| **内存泄漏怎么排查？** | jstat看GC → jmap dump → MAT分析 → jstack看线程 |
| **大对象为什么进老年代？** | 避免 Minor GC 反复复制（PretenureSizeThreshold） |
| **为什么避免 Java 序列化？** | 跨语言差/安全性差/体积大/速度慢2000倍 |
| **单例最佳实现？** | 静态内部类或枚举（防反射/序列化破坏） |
| **联合索引最左前缀？** | 查询必须从最左列开始，跳过则索引失效 |
| **死锁怎么避免？** | 统一访问顺序、低隔离级别、唯一索引、短事务 |
| **双十一抢购如何调优？** | 预热库存到Redis+Lua、资格系统分离、MQ异步、订单分库 |
| **缓存三大问题？** | 穿透(空值+布隆)、击穿(互斥锁)、雪崩(随机过期时间) |

---

> 📝 **课程结构**：这份完整版覆盖了全部 7 大模块（44讲）。面试最常考：性能指标与策略、String/集合优化、锁优化、线程池、内存泄漏排查、设计模式、索引与死锁、双11实战。每个考点都有"为什么"层面的解释——这是和普通候选人拉开差距的关键。
