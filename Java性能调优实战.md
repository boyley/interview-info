# Java 性能调优实战 — 面试备战手册

> 来源：极客时间《Java 性能调优实战》（刘超）
> 刘超，Java 技术专家，多年电商平台性能优化经验
> 涵盖 7 大模块：编程调优 → 多线程 → JVM → 设计模式 → 数据库 → 实战案例

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| **String 优化**（StringBuilder vs + / intern / 字符串常量池） | 03 | ⭐⭐⭐⭐⭐ |
| **ArrayList vs LinkedList**（真实性能差千倍的原因） | 05 | ⭐⭐⭐⭐⭐ |
| **HashMap 底层优化**（JDK7→8 红黑树/扩容/扰动函数） | 07 | ⭐⭐⭐⭐⭐ |
| **synchronized 锁优化**（偏向锁→轻量锁→重量锁） | 12 | ⭐⭐⭐⭐⭐ |
| **Lock 优化**（ReentrantLock/ReadWriteLock/StampedLock 选型） | 13 | ⭐⭐⭐⭐ |
| **线程池大小设置**（CPU/IO 密集公式） | 18 | ⭐⭐⭐⭐⭐ |
| **上下文切换优化**（CAS/协程/线程数控制） | 15+16 | ⭐⭐⭐⭐ |
| **JIT 即时编译优化**（内联/逃逸分析/锁消除/栈上分配） | 21 | ⭐⭐⭐⭐ |
| **GC 优化**（GC 选型/日志分析/调优参数） | 22 | ⭐⭐⭐⭐⭐ |
| **SQL 索引优化**（联合索引最左前缀/索引失效场景） | 32+34 | ⭐⭐⭐⭐⭐ |
| **MySQL 死锁分析**（行锁/间隙锁/Next-Key Lock） | 33+35 | ⭐⭐⭐⭐ |
| **分布式锁/事务/缓存实战** | 41-44 | ⭐⭐⭐⭐ |
| **Stream 性能**（串行 vs 并行 vs for 循环） | 06 | ⭐⭐⭐ |
| **序列化优化**（Protobuf vs Java 序列化） | 09 | ⭐⭐⭐ |

---

## 📌 二、Java 编程性能调优

### 1. String 优化（课程第03讲）

**String 的不可变性**：
```java
String s = "Hello" + "World";  // 编译期优化，直接引用常量池
String s = a + b;               // 运行时等价于 new StringBuilder().append(a).append(b).toString()
```

| 场景 | 推荐做法 | 原因 |
|------|---------|------|
| 循环拼接 | **StringBuilder** | 每次 `+` 都会 new StringBuilder，循环中性能差百倍 |
| 字符串比较 | `s1.intern()` 后 `==` | 常量池比较比 equals 快（引用比较 vs 字符遍历） |
| 大量重复字符串 | `String.intern()` | 复用常量池，减少内存占用 |

**注意**：`intern()` 在 JDK6 中存永久代（OOM 风险），JDK7+ 存堆。

### 2. ArrayList vs LinkedList（课程第05讲）

| 操作 | ArrayList | LinkedList |
|------|-----------|-----------|
| **尾部插入** | O(1)，偶尔扩容 | O(1) |
| **中间插入** | O(n)，移动元素 | O(n) 查找 + O(1) 插入 |
| **随机访问** | **O(1)**，直接下标 | O(n)，需遍历 |
| **内存** | 连续空间 | 离散节点，每个节点多两个指针 |

**关键结论**：绝大多数场景用 ArrayList。LinkedList 只在频繁头部插入/删除且不随机访问时才有优势。课程中提到，错误使用性能差异可差**千倍**。

### 3. Stream 性能（课程第06讲）

| 循环方式 | 性能 | 适用场景 |
|---------|------|---------|
| **for 循环** | 最快（无额外开销） | 简单遍历 |
| **增强 for 循环** | 略慢（迭代器） | 代码简洁 |
| **Stream（串行）** | 稍慢（Lambda + Spliterator 开销） | 可读性好 |
| **Stream（并行）** | 数据量大时更快 | 利用多核，注意线程安全 |

**结论**：数据量小时 for 循环最快；数据量大且 CPU 密集型时 parallelStream 更快，但要注意线程安全。

### 4. 正则表达式优化（课程第04讲）

**回溯陷阱**：NFA 引擎的贪婪匹配可能导致大量回溯，CPU 飙升。

| 匹配模式 | 写法 | 回溯 | 适用 |
|---------|------|------|------|
| **贪婪**（默认） | `\w+` | 多 | 一般场景 |
| **懒惰** | `\w+?` | 少 | 避免过度匹配 |
| **占有** | `\w++` | **无** | 确定不需要回溯的场景，性能最好 |

**优化建议**：能不用正则就不用，用 String 的 `indexOf`/`startsWith` 替代简单匹配。

### 4. HashMap 优化（课程第07讲）

**JDK 7 → 8 的优化**：
- 扰动函数：JDK7 4次位运算+5次异或 → JDK8 1次（`h ^ (h >>> 16)`）
- 扩容后重哈希：JDK7 重新计算 → JDK8 通过 `hash & oldCap` 判断（低位0→原位，1→原+旧容量）
- 引入红黑树：链表 >8 转红黑树，<6 转回链表
- 头插法→尾插法：避免多线程扩容死循环

**初始化容量**：`new HashMap(capacity / 0.75F + 1)` 避免扩容。

### 5. I/O 模型：BIO → NIO → AIO（课程第08讲）

| 模型 | 阻塞点 | 连接模型 | 适用场景 |
|------|--------|---------|---------|
| **BIO** | 读/写/连接都阻塞 | 一线程一连接 | 连接数少、固定架构 |
| **NIO** | Selector 非阻塞 | 多路复用 | 高并发、短连接 |
| **AIO** | 异步回调 | 回调通知 | 文件 I/O 或长连接 |

**零拷贝**：`FileChannel.transferTo()` / `transferFrom()` 直接在内核空间传输，跳过用户空间拷贝。

### 6. 序列化优化（课程第09讲）

| 序列化方式 | 编码大小 | 速度 | 跨语言 |
|-----------|---------|------|--------|
| **Java 原生** | ❌ 大 | ❌ 慢 | ❌ 仅 Java |
| **Protobuf** | ✅ 小 | ✅ 快 | ✅ |
| **Kryo** | ✅ 小 | ✅ 快 | ⚠️ 仅 Java 生态 |
| **Hessian** | ⚠️ 中 | ⚠️ 中 | ✅ |

**原则**：远程通信避免使用 Java 序列化，优先 Protobuf。

---

## 📌 三、多线程性能调优

### 1. sychronized 锁优化（课程第12讲）

**JDK 1.6 后的锁升级**：无锁 → 偏向锁 → 轻量级锁 → 重量级锁

| 锁状态 | 开销 | 适用场景 |
|--------|------|---------|
| **偏向锁** | 极低（仅CAS记录线程ID） | 只有一个线程访问 |
| **轻量级锁** | 低（自旋CAS） | 多线程交替访问 |
| **重量级锁** | 高（OS互斥量） | 多线程同时竞争 |

**调优参数**：
- `-XX:BiasedLockingStartupDelay=0`：关闭偏向锁延迟（JDK8默认4秒）
- `-XX:-UseBiasedLocking`：关闭偏向锁（高并发场景下撤销开销大）

### 2. Lock 选型（课程第13-14讲）

| 锁 | 读并发 | 写并发 | 特点 |
|----|--------|--------|------|
| **ReentrantLock** | 互斥 | 互斥 | 可中断、可超时、公平可选 |
| **ReadWriteLock** | 共享 | 互斥 | 读多写少 |
| **StampedLock** | 乐观读 | 互斥 | 更高读吞吐（乐观读不阻塞写） |
| **CAS（无锁）** | 无锁 | 无锁 | 适合简单竞争 |

### 3. 线程池大小设置（课程第18讲）

```java
// CPU 密集型
线程数 = CPU 核数 + 1

// IO 密集型
线程数 = CPU 核数 × (1 + IO等待时间 / CPU计算时间)
```

**实践经验**：先按公式初估，再压测调优。

### 4. 上下文切换优化（课程第15-16讲）

**上下文切换的五大原因**：
1. 锁竞争（线程阻塞等待锁）
2. IO 阻塞（等磁盘/网络数据）
3. CPU 时间片耗尽（抢占式调度）
4. `yield()` / `sleep()` / `wait()` 主动让出
5. GC 的 Stop-the-world 停顿

**监控工具**：`vmstat` 看 `cs`（上下文切换次数）、`r`（运行线程）、`b`（阻塞线程）

| 优化手段 | 做法 |
|---------|------|
| **减少竞争** | 使用 CAS 替代锁、无锁数据结构 |
| **减少线程数** | 线程过多→频繁切换，找到最优线程数 |
| **协程** | 用户态调度，切换成本比线程低一个数量级 |
| **减少锁粒度** | 分段锁（ConcurrentHashMap）、读写分离 |

---

## 📌 四、JVM 性能调优

### 1. GC 选型（课程第22讲）

| 场景 | 推荐 GC | 核心参数 |
|------|---------|---------|
| **吞吐量优先** | Parallel Scavenge + Parallel Old | `-XX:+UseParallelGC` |
| **响应时间优先** | G1 | `-XX:+UseG1GC -XX:MaxGCPauseMillis=200` |
| **大内存低延迟** | ZGC（JDK11+） | `-XX:+UseZGC` |

### 2. GC 日志分析

```bash
-XX:+PrintGC -XX:+PrintGCDetails -XX:+PrintGCTimeStamps
-Xloggc:gc.log
```

**关键指标**：
- Young GC 频率（太频繁→Eden 过小）
- Full GC 次数（太多→老年代过小或内存泄漏）
- GC 停顿时间（超过 1s 需关注）

### 3. 内存泄漏排查（课程第24讲）

```
症状：CPU 飙升 + Full GC 频繁 + 老年代持续增长

排查步骤：
① jps → 找到进程 PID
② jstat -gcutil PID 1000 → 看 GC 频率
③ jmap -dump:file=heap.hprof PID → 堆转储
④ MAT / JProfiler 分析 → 定位大对象或泄漏点
⑤ jstack PID → 查看线程栈
```

### 4. JIT 优化（课程第21讲）

| 优化 | 效果 | JVM 参数 |
|------|------|---------|
| **方法内联** | 消除调用开销 | `-XX:MaxInlineSize=325` |
| **逃逸分析** | 栈上分配、锁消除 | `-XX:+DoEscapeAnalysis` |
| **锁消除** | 去掉不必要的锁 | JIT 自动判断 |
| **标量替换** | 拆对象为局部变量 | JIT 自动判断 |

---

## 📌 五、数据库性能调优

### 1. SQL 索引优化（课程第32+34讲）

**联合索引最左前缀原则**：
```sql
KEY idx_name_age_sex (name, age, sex)

-- ✅ 走索引
WHERE name = 'xx' AND age = 20 AND sex = 1
WHERE name = 'xx' AND age = 20
WHERE name = 'xx'

-- ❌ 不走索引（跳过了最左列）
WHERE age = 20 AND sex = 1
```

**索引失效场景**：LIKE '%xx'、OR 条件、隐式类型转换、函数操作、`!=` 或 `<>`

### 2. 行锁与间隙锁（课程第33+35讲）

**行锁**：InnoDB 在索引上加锁，不走索引则锁全表。
**间隙锁**：REPEATABLE READ 级别下，锁定一个范围（防止幻读）。
**Next-Key Lock**：行锁 + 间隙锁的组合。

**死锁案例**（课程第35讲）：
```sql
-- 事务 A                        事务 B
UPDATE t SET x=1 WHERE id=100;   UPDATE t SET x=2 WHERE id=200;
UPDATE t SET x=1 WHERE id=200;   UPDATE t SET x=2 WHERE id=100;
-- 互相等待 → 死锁
```
**避免**：所有事务按相同顺序访问资源。

### 3. 分库分表（课程第36讲）

**何时需要**：单表数据超千万、索引层级过深、磁盘 IO 成为瓶颈。

**拆分方式**：垂直（按业务拆表）→ 水平（按 ID 哈希拆表）

### 4. InnoDB 核心参数调优（课程第38讲）

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| `innodb_buffer_pool_size` | 物理内存的 60-80% | 最重要的参数，缓存索引和数据页 |
| `innodb_log_file_size` | 足够大（避免频繁刷盘） | Redo log 太小→频繁 fsync→IO 瓶颈 |
| `innodb_flush_log_at_trx_commit` | 1（最安全）/ 2（性能好） | 1=每次提交 fsync，2=每秒 fsync |
| `innodb_io_capacity` | 匹配磁盘 IOPS | 脏页刷新的 IO 上限 |
| `innodb_buffer_pool_instances` | >1GB 时启用 | 减少 buffer pool 竞争 |

---

## 📌 六、实战案例

### 1. 分布式锁设计（课程第41讲）

| 方案 | 可靠性 | 性能 |
|------|--------|------|
| **Redis SETNX + Lua** | ⚠️ 主从切换可能丢 | ✅ 高 |
| **ZK 临时顺序节点** | ✅ 可靠 | ⚠️ 中 |
| **Redlock** | ⚠️ 争议 | ✅ 高 |

### 2. 分布式事务（课程第42讲）

**TCC + MQ 最终一致性**：电商订单场景的常用方案。

### 3. 缓存优化（课程第43讲）

**缓存刷新策略**：先更新 DB，再删除缓存（Cache Aside）。删除失败时用延迟双删或 MQ 重试。

### 4. 双十一抢购瓶颈调优（课程第44讲）

**案例流程**：
- 发现瓶颈：下单接口 TP99 过高
- 排查：数据库 CPU 100%，慢查询集中在库存扣减
- 优化：库存预热到 Redis，用 Lua 脚本扣减；MQ 异步写订单
- 结果：TP99 从 2s 降到 200ms

---

## 📌 七、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **String 拼接为什么慢？** | `+` 在循环中会反复 new StringBuilder，用 StringBuilder 或 StringBuffer |
| **ArrayList 和 LinkedList 怎么选？** | 99% 场景用 ArrayList（随机访问快），频繁头部操作才考虑 LinkedList |
| **HashMap JDK7 和 8 区别？** | 8引入红黑树、扰动函数简化、尾插法防死循环、扩容后重哈希优化 |
| **synchronized 锁升级过程？** | 偏向锁→轻量级锁→重量级锁，单向不可逆 |
| **线程池大小怎么设？** | CPU密集=CPU核数+1；IO密集=CPU核数×(1+等待时间/计算时间) |
| **上下文切换怎么优化？** | 减少竞争（CAS）、减少线程数、协程、减少锁粒度 |
| **内存泄漏怎么排查？** | jstat看GC → jmap dump → MAT分析 → jstack看线程 |
| **联合索引最左前缀？** | 查询必须从索引最左列开始，跳过左边列则索引失效 |
| **死锁怎么避免？** | 所有事务按相同顺序访问资源，超时机制 |
| **双十一抢购如何调优？** | 预热库存到Redis+Lua扣减，MQ异步写订单，TP99从2s降到200ms |
