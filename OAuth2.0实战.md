# OAuth 2.0 实战 — 面试备战手册

> 来源：极客时间《OAuth 2.0 实战课》（王新栋）
> 王新栋，某知名电商平台技术架构师，开放平台负责人
> 涵盖 15 讲：基础篇 → 进阶篇 → 安全防范 → Spring Security 集成

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| **OAuth 2.0 四大角色**（资源拥有者/第三方应用/授权服务/受保护资源） | 01 | ⭐⭐⭐⭐⭐ |
| **授权码许可流程（Authorization Code）** — 最核心流程 | 02+03 | ⭐⭐⭐⭐⭐ |
| **为什么要有授权码？**（直接返回 access_token 不行吗） | 02 | ⭐⭐⭐⭐⭐ |
| **OAuth 四种授权类型对比**（授权码/隐式/密码/客户端凭证） | 06 | ⭐⭐⭐⭐⭐ |
| **JWT 令牌结构**（Header/Payload/Signature）+ HS256 vs RS256 | 04 | ⭐⭐⭐⭐ |
| **Refresh Token**（为什么需要/如何安全使用） | 05 | ⭐⭐⭐⭐ |
| **PKCE 协议**（移动端无 Server 场景/授权码防窃听） | 07 | ⭐⭐⭐⭐ |
| **OAuth 2.0 安全漏洞**（CSRF/XSS/水平越权/授权码失窃/重定向 URI 篡改） | 08 | ⭐⭐⭐⭐ |
| **OIDC（OpenID Connect）** — OAuth 2.0 + 身份认证 | 09 | ⭐⭐⭐ |
| **Spring Security 整合 OAuth 2.0 + JWT** | 11 | ⭐⭐⭐ |
| **OAuth 2.0 在微服务中的应用** | 12 | ⭐⭐⭐ |
| **开放平台网关设计** | 14 | ⭐⭐⭐ |

---

## 📌 二、面试回答标准模板

> **面试官："OAuth 2.0 的授权码流程是怎么工作的？"**
>
> **回答**（摘自课程原文）：
>
> "OAuth 2.0 授权码流程涉及四个角色：**资源拥有者**（用户）、**第三方应用**、**授权服务**、**受保护资源服务**。
>
> 核心是两步走的策略：
> **第一步**，用户访问第三方应用，第三方应用引导用户跳转到授权服务页面，用户同意授权后，授权服务返回一个**授权码 code**（通过浏览器重定向）。
>
> **第二步**，第三方应用用这个 code，再加上自己的 app_id 和 app_secret，在**后端通信**中向授权服务换取 access_token。
>
> 为什么要多一步授权码？因为 access_token 是访问用户数据的核心凭据，必须确保传输安全。在第一步的前端跳转中，如果直接返回 access_token，容易通过浏览器历史记录或 Referer 头泄露。而授权码 code 是一次性的、与 app_id 绑定的临时凭据，即使被截获也无法换取 access_token（因为没有 app_secret）。关键是第二步是**后端到后端**的通信，安全得多。"

---

## 📌 三、OAuth 2.0 基础

### 1. 四大角色

| 角色 | 英文 | 类比 |
|------|------|------|
| **资源拥有者** | Resource Owner | 用户，拥有数据 |
| **第三方应用** | Client / 3rd-party App | 想访问用户数据的应用 |
| **授权服务** | Authorization Server | 发放授权码和令牌 |
| **受保护资源服务** | Resource Server | 存储用户数据的 API |

### 2. 授权码许可流程（最核心、最安全）

```
用户(资源拥有者)        第三方应用            授权服务          受保护资源
    │                    │                   │                  │
    │  ① 访问            │                   │                  │
    │───────────────────▶│                   │                  │
    │                    │  ② 引导跳转授权页   │                  │
    │                    │──────────────────▶│                  │
    │  ③ 用户同意授权     │                   │                  │
    │◀──────────────────▶│                   │                  │
    │                    │                   │  ④ 返回授权码code  │
    │  ⑤ 授权码code      │◀──────────────────│                  │
    │◀───────────────────│                   │                  │
    │                    │  ⑥ code + secret   │                  │
    │                    │  后端通信换令牌      │                  │
    │                    │──────────────────▶│                  │
    │                    │ ⑦ 返回access_token  │                  │
    │                    │◀──────────────────│                  │
    │                    │  ⑧ 携带token访问API │                  │
    │                    │────────────────────────────────────▶│
```

### 3. 面试回答：为什么一定要有授权码？

> **Q：OAuth 2.0 为什么不直接返回 access_token，非要中间加一个授权码？**
>
> 核心原因有两点：
> 1. **安全隔离**：第一步通过浏览器前端跳转，请求路径会被记录在浏览器历史、Referer 头中。access_token 是最终访问数据的凭据，不能暴露在前端通道。授权码 code 是一次性的临时凭据，泄露了也无法直接使用。
> 2. **身份验证**：第二步是**后端到后端**的通信，第三方应用需要带 app_secret 才能用 code 换 token。这样就验证了第三方应用的身份，确保只有合法的应用才能拿到 access_token。

---

## 📌 四、四种授权类型（Grant Type）

| 类型 | 适用场景 | 安全性 | 特点 |
|------|---------|--------|------|
| **授权码（Authorization Code）** | Web 应用有 Server 端 | ⭐⭐⭐⭐⭐ | 两步走，最安全，推荐首选 |
| **隐式（Implicit）** | 纯前端/无 Server 端 | ⭐⭐⭐ | 直接返回 access_token，已不推荐 |
| **密码（Resource Owner Password）** | 自家应用（高度信任） | ⭐⭐⭐ | 用户直接提供用户名密码 |
| **客户端凭证（Client Credentials）** | 服务器间通信（无用户） | ⭐⭐⭐⭐ | 仅用 app_secret 换取 token |

**课程建议**：授权码许可类型是最完备、最安全的，应优先使用。隐式许可已不推荐，OAuth 2.1 中已移除。

---

## 📌 五、JWT 令牌（课程第04讲）

### 结构

```
JWT = Header.Payload.Signature
```

| 部分 | 内容 | 示例 |
|------|------|------|
| **Header** | 令牌类型 + 签名算法 | `{"typ":"JWT","alg":"HS256"}` |
| **Payload** | 声明（Claims），可读不可改 | `{"sub":"user123","exp":1584105790}` |
| **Signature** | 防篡改签名 | `HMACSHA256(base64UrlEncode(header) + "." + base64UrlEncode(payload), secret)` |

### HS256 vs RS256

| 算法 | 类型 | 密钥 | 适用场景 |
|------|------|------|---------|
| **HS256** | 对称 | 单一密钥（签名+验证用同一个） | 内部系统，授权服务和受保护资源共有一个密钥 |
| **RS256** | 非对称 | 私钥签名 + 公钥验证 | 开放平台，授权服务私钥签名，资源服务公钥验证 |

---

## 📌 六、PKCE 协议（课程第07讲）

### 为什么需要 PKCE？

移动 App 没有 Server 端时，无法安全保存 app_secret。没有 app_secret 的授权码流程容易被截获授权码。

### PKCE 工作原理

```
第一步（换授权码）：附带 code_challenge
第二步（换令牌）：附带 code_verifier，授权服务比较两个值

code_challenge = BASE64URL-ENCODE(SHA256(ASCII(code_verifier)))
```

即使授权码被截获，攻击者也拿不到 code_verifier（无法从 S256 哈希逆推），因此无法换取访问令牌。

---

## 📌 七、OAuth 2.0 安全漏洞（课程第08讲）

### 五大安全漏洞

| 安全漏洞 | 原理 | 防范方法 |
|---------|------|---------|
| **CSRF 攻击** | 攻击者用自己的授权码替换受害者的授权码 | 使用 state 参数（随机值） |
| **XSS 攻击** | 注入恶意脚本到请求输入中 | 对 `<script>` 等标签转义过滤 |
| **水平越权** | 用户 A 获取用户 B 的数据 | 数据归属校验（检查订单 ID 是否属于该用户） |
| **授权码失窃** | 授权码被截获后换取 access_token | 校验 app_id + 授权码一次性使用 |
| **重定向 URI 篡改** | 修改回调地址导致授权码泄露 | 回调 URI 精确匹配校验 |

---

## 📌 八、OpenID Connect（OIDC，课程第09讲）

### OAuth 2.0 vs OIDC

| | OAuth 2.0 | OIDC |
|--|-----------|------|
| **目的** | 授权（让第三方访问数据） | 身份认证（验证你是谁） |
| **关系** | 面粉 | 面包（OAuth 2.0 + 身份认证） |
| **核心产物** | access_token | access_token + **ID_TOKEN** |
| **ID_TOKEN 格式** | — | JWT（包含 iss/sub/aud/exp/iat） |

**ID_TOKEN 的 5 个必要声明**：`iss`（颁发者）、`sub`（用户唯一标识）、`aud`（受众）、`exp`（过期时间）、`iat`（签发时间）

### 联合登录与单点登录
- **联合登录**：用一个微信账号登录多个不同的 App
- **单点登录（SSO）**：在多个子应用间只需登录一次
- 两者底层都是 OIDC

---

## 📌 九、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **OAuth 2.0 四大角色？** | 资源拥有者（用户）、第三方应用、授权服务、受保护资源服务 |
| **为什么要有授权码？** | 前端通道不安全，授权码一次性+app_secret 后端验证，隔离 access_token |
| **四种授权类型？** | 授权码（Web推荐）、隐式（纯前端/不推荐）、密码（自家应用）、客户端凭证（服务间） |
| **JWT 是什么？** | JSON Web Token，Header.Payload.Signature 三段式自包含令牌 |
| **HS256 vs RS256？** | HS256 对称（同一密钥）、RS256 非对称（私钥签名+公钥验证），开放平台用 RS256 |
| **PKCE 是什么？** | 移动端无 Server 时防授权码截获，code_challenge + code_verifier 校验 |
| **OAuth 2.0 有哪些安全漏洞？** | CSRF（state防范）、XSS（转义过滤）、水平越权（数据归属校验）、授权码失窃、重定向URI篡改 |
| **OIDC 和 OAuth 关系？** | OIDC = OAuth 2.0 + ID_TOKEN（JWT格式），在授权基础上实现身份认证 |
| **Refresh Token 的作用？** | access_token 过期后无需用户重新授权，使用 Refresh Token 获取新的 access_token |
