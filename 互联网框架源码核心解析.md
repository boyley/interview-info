# 互联网常用框架源码核心解析 — 面试备战手册

> 内容来源：Spring 官方文档、Apache 官方文档、MyBatis 官方文档、Netty 官方文档、源码分析博客及技术社区
> 涵盖：Spring IoC/AOP → Spring Boot 自动配置 → MyBatis → Netty → Dubbo

---

## 📌 一、Spring Framework — IoC 容器源码核心

### 1. 容器启动入口：`refresh()`

`AbstractApplicationContext.refresh()` 是 IoC 容器的启动模板方法，定义了完整流程：

```java
public void refresh() {
    prepareRefresh();                              // 准备刷新
    ConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory(); // 获取BeanFactory
    prepareBeanFactory(beanFactory);               // 准备BeanFactory
    postProcessBeanFactory(beanFactory);            // 子类扩展
    invokeBeanFactoryPostProcessors(beanFactory);   // 执行BeanFactoryPostProcessor
    registerBeanPostProcessors(beanFactory);        // 注册BeanPostProcessor
    initMessageSource();                            // 初始化消息源
    initApplicationEventMulticaster();              // 初始化事件多播器
    onRefresh();                                    // 子类初始化特殊Bean
    registerListeners();                            // 注册监听器
    finishBeanFactoryInitialization(beanFactory);   // ★实例化所有非懒加载单例Bean
    finishRefresh();                                // 发布事件
}
```

### 2. Bean 完整生命周期（11个步骤）

```
① 实例化（createBeanInstance → 反射创建裸对象）
② 属性填充（populateBean → @Autowired/@Value注入）
③ Aware回调（BeanNameAware → BeanFactoryAware → ApplicationContextAware）
④ BeanPostProcessor前置处理（postProcessBeforeInitialization）
⑤ @PostConstruct（CommonAnnotationBeanPostProcessor触发）
⑥ InitializingBean.afterPropertiesSet()
⑦ @Bean(initMethod) / XML init-method
⑧ BeanPostProcessor后置处理（postProcessAfterInitialization）→ ★AOP代理在此生成
⑨ 存入单例池（singletonObjects），Bean就绪
⑩ 使用中...
⑪ 销毁：@PreDestroy → DisposableBean.destroy() → destroy-method
```

**核心类**：`AbstractAutowireCapableBeanFactory` 中的 `doCreateBean()` 方法。

### 3. 三级缓存解决循环依赖

```java
// DefaultSingletonBeanRegistry
一级缓存: Map<String, Object> singletonObjects         // 完整Bean（已完全初始化）
二级缓存: Map<String, Object> earlySingletonObjects    // 早期引用（已实例化未填充属性）
三级缓存: Map<String, ObjectFactory<?>> singletonFactories // Bean工厂（可生成代理对象）
```

**工作原理**（A依赖B，B依赖A）：
```
① A实例化 → 将ObjectFactory放入三级缓存
② A填充属性发现依赖B → 触发B的创建
③ B实例化 → 放入三级缓存 → B填充属性时从三级缓存获取A的ObjectFactory
   → 调用getEarlyBeanReference()获取A的早期引用（若有AOP则返回代理）→ 放入二级缓存
④ B完成初始化 → A继续填充属性
⑤ A完成初始化 → 从二级缓存移到一级缓存
```

### 4. BeanPostProcessor — AOP 的核心扩展点

| 扩展点 | 执行时机 | AOP相关 |
|--------|---------|---------|
| `postProcessBeforeInstantiation` | 实例化前 | 可返回代理替代默认实例化 |
| `postProcessAfterInstantiation` | 实例化后/属性注入前 | 控制是否进行属性注入 |
| `getEarlyBeanReference` | 循环依赖时 | 提前暴露AOP代理对象 |
| `postProcessBeforeInitialization` | 初始化前 | `@PostConstruct`处理 |
| `postProcessAfterInitialization` | 初始化后 | **★ AOP代理在此生成** |

AOP 发生在 `postProcessAfterInitialization` 阶段，`AbstractAutoProxyCreator` 通过拦截器链创建 JDK 动态代理或 CGLIB 代理。

---

## 📌 二、Spring Boot — 自动配置源码核心

### 1. `@SpringBootApplication` 的三层复合

```java
@SpringBootConfiguration     // 声明为配置类
@EnableAutoConfiguration      // 开启自动配置（核心）
@ComponentScan                // 组件扫描
```

### 2. 自动配置执行流程

```
@EnableAutoConfiguration
  → @Import(AutoConfigurationImportSelector.class)
    → selectImports() → getAutoConfigurationEntry()
      → ① 加载候选配置：
           Spring Boot 3.x: META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
           Spring Boot 2.x: META-INF/spring.factories（key=EnableAutoConfiguration）
      → ② 排除 exclude 指定的配置
      → ③ filter() → 应用 @Conditional 条件过滤
           OnClassCondition（类路径检查）
           OnBeanCondition（Bean容器检查）
           OnWebApplicationCondition（Web环境检查）
      → ④ 返回符合条件的配置类列表
```

### 3. `@Conditional` 条件注解体系

| 注解 | 判断条件 | 失败后果 |
|------|---------|---------|
| `@ConditionalOnClass` | 类路径存在某类 | 整个自动配置类不生效 |
| `@ConditionalOnMissingBean` | 容器中不存在某Bean | 用户自定义的Bean优先级高 |
| `@ConditionalOnProperty` | 配置项满足条件 | 可通过配置文件开关 |
| `@ConditionalOnWebApplication` | 当前为Web环境 | 非Web应用跳过Web相关配置 |

**核心设计思想**：用户自定义配置 > 自动配置（通过 `@ConditionalOnMissingBean` 保证）。

### 4. 自定义 Starter 的完整步骤（面试必问）

理解了自动配置原理，就能自己写 Starter。以自定义一个 `hello-spring-boot-starter` 为例：

#### ① 创建工程，依赖核心

```xml
<!-- 核心依赖：自动配置 + 注解 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-autoconfigure</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter</artifactId>
</dependency>
```

#### ② 编写配置属性类（读取 application.yml 前缀配置）

```java
@ConfigurationProperties(prefix = "hello")
public class HelloProperties {
    private String name = "world";   // 默认值，对应 hello.name
    // getter/setter...
}
```

#### ③ 编写自动配置类

```java
@Configuration
@EnableConfigurationProperties(HelloProperties.class)  // 注册属性类
@ConditionalOnClass(HelloService.class)                 // 类路径有才生效
public class HelloAutoConfiguration {

    @Bean
    @ConditionalOnMissingBean   // ★ 用户自定义了就覆盖自动配置
    public HelloService helloService(HelloProperties properties) {
        return new HelloService(properties.getName());
    }
}
```

#### ④ 注册自动配置类（关键一步）

> **Spring Boot 2.x**：在 `META-INF/spring.factories` 添加：
>
> ```
> org.springframework.boot.autoconfigure.EnableAutoConfiguration=\
> com.example.hello.HelloAutoConfiguration
> ```
>
> **Spring Boot 3.x**：新建 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`：
>
> ```
> com.example.hello.HelloAutoConfiguration
> ```

这就是 `AutoConfigurationImportSelector` 扫描的文件——**Spring Boot 就是靠它才知道有这个自动配置类的**。

#### ⑤ 可选：加 `spring-boot-configuration-processor` 依赖

生成配置元数据（`spring-configuration-metadata.json`），让 IDE 编写 `hello.name` 时有提示。

#### ⑥ 使用方引用

```yaml
# 用户只需配置：
hello:
  name: "Claude"
```

引入依赖后 `HelloService` 自动装配完成，无需任何代码。

### 自定义 Starter 面试回答

> **Q：怎么自定义一个 Spring Boot Starter？**
>
> 六个步骤：
> 1. 建工程，引入 `spring-boot-autoconfigure` 依赖
> 2. 写配置属性类，用 `@ConfigurationProperties(prefix="xxx")` 绑定 `application.yml` 里的配置
> 3. 写自动配置类，用 `@ConditionalOnMissingBean` + `@ConditionalOnClass` 控制生效条件
> 4. **关键**：注册自动配置类——Spring Boot 2.x 写进 `META-INF/spring.factories`，3.x 写进 `AutoConfiguration.imports`
> 5. 加 `spring-boot-configuration-processor` 生成配置提示
> 6. 使用时只需引入依赖 + 配 yml
>
> 核心机制就是 `@EnableAutoConfiguration` → `AutoConfigurationImportSelector` 扫描配置文件 → `@Conditional` 过滤哪些生效。

---

## 📌 三、Spring Cloud Eureka — 核心源码

Eureka 是 Spring Cloud Netflix 体系中的服务注册中心，遵循 **AP 原则**（可用性优先），核心设计理念是"**最终一致性**"。

### 1. 核心数据结构

```java
// AbstractInstanceRegistry
ConcurrentHashMap<String, Map<String, Lease<InstanceInfo>>> registry
//   外层 key = 服务名（appName）
//    内层 key = 实例ID（instanceId）
//      value = Lease（租约，包装实例信息 + 最后心跳时间 + 过期时间）
```

每个注册的实例都包装在 `Lease` 对象中，记录 `lastUpdateTimestamp`（最后心跳时间）和 `duration`（90秒过期），剔除时判断 `now - lastUpdateTimestamp > duration`。

### 2. 服务注册

```
客户端调用链：
DiscoveryClient.register()
  → AbstractJerseyEurekaHttpClient.register()    ← POST /v2/apps/{appName}
  
服务端调用链：
ApplicationResource.addInstance()                ← Jersey REST 入口
  → PeerAwareInstanceRegistryImpl.register()
    → AbstractInstanceRegistry.register()         ← 写入 registry MAP
    → recentlyChangedQueue.add()                  ← 加入最近变更队列
    → invalidateCache()                           ← 使读写缓存失效
    → replicateToPeers()                          ← 同步到集群其他节点
```

**注册数据结构变更**：
```
registry = {
  "ORDER-SERVICE" → {
    "192.168.1.1:order-service:8080" → Lease{info, lastUpdateTimestamp, duration}
    "192.168.1.2:order-service:8081" → Lease{info, lastUpdateTimestamp, duration}
  },
  "USER-SERVICE" → {
    "192.168.1.3:user-service:8082" → Lease{info, lastUpdateTimestamp, duration}
  }
}
```

### 3. 心跳续约（Renew）

```
客户端每 30 秒：DiscoveryClient.renew()
  → PUT /v2/apps/{appName}/{instanceId}

服务端：
InstanceResource.renewLease()
  → AbstractInstanceRegistry.renew()
    → 找到对应的 Lease
    → 更新 lastUpdateTimestamp = 当前时间（+90s，源码 bug 导致实际 180s）
    → 记录心跳计数到 MeasuredRate（用于自我保护判断）
    
若服务端返回 404（实例不存在）：
  → 客户端自动重新注册（register()）
```

### 4. 服务下线（Cancel）

```
客户端 shutdown() → DiscoveryClient.unregister()
  → DELETE /v2/apps/{appName}/{instanceId}

服务端：
InstanceResource.cancelLease()
  → AbstractInstanceRegistry.internalCancel()
    → ① 从 registry Map 中移除该实例
    → ② 放入 recentlyChangedQueue（保留 3 分钟）
    → ③ 清除 readWriteCacheMap 缓存
    → ④ 发布 EurekaInstanceCanceledEvent 事件
```

### 5. 服务剔除（Eviction — 定时任务）

```
EvictionTask（每 60 秒执行）：
  → 遍历所有 Lease
  → 检查 (now - lastUpdateTimestamp) > 90s？
  → 若已触发自我保护 → 跳过剔除
  → 计算本次可剔除数量：
      可剔除数 = 实例总数 × (1 - 0.85)    ← 默认阈值 0.85
      若过期实例 > 可剔除数 → 随机选取剔除
  → 调用 internalCancel() 剔除
```

### 6. 自我保护机制（Self-Preservation）

**触发条件**：上一分钟实际收到的心跳数 < 期望心跳数 × 阈值

```
期望心跳数 = 服务实例数 × (60s / 30s) × 0.85
  例如有 2 个实例：2 × 2 × 0.85 = 3.4 ≈ 3 次/分钟

上一分钟实际心跳数 = MeasuredRate.getCount()

若实际 < 期望 → 触发自我保护 → 不再剔除任何实例
若实际 >= 期望 → 退出自我保护
```

**设计目的**：防止网络分区时 Eureka Server 误判所有服务不可用。即使部分实例心跳超时，也保留其注册信息，保证**可用性优先**。

### 7. 三级缓存架构

```
写操作 → registry (内存，ConcurrentHashMap)
              │
              ▼ 立即失效
         readWriteCacheMap (Guava LoadingCache，180s 过期)
              │
              ▼ 每 30s 同步
         readOnlyCacheMap (ConcurrentMap，缓存最终数据)
              │
              ▼ 客户端读取
```

**读取流程**：`readOnlyCacheMap` → 未命中 → `readWriteCacheMap` → 未命中 → `registry` 中加载并回填缓存

**写入流程**：写 `registry` → 使 `readWriteCacheMap` 失效 → 定时任务（30s）将 `readWriteCacheMap` 同步到 `readOnlyCacheMap`

**为什么用三级缓存**：读写分离。注册表被频繁查询，用只读缓存和读写缓存两层隔离写操作，减少锁竞争。

### 8. Peer 节点复制（集群同步）

```
注册/续约/下线操作
  → replicateToPeers()
    → acceptorQueue ← 放入批量队列
      → AcceptorRunner（每 10ms 处理一次）← 打包成 batch
        → 向其他 Peer 节点发送复制请求
```

**重要**：服务剔除（Eviction）是本地行为，**不会**同步到其他 Peer 节点。每个节点独立执行 EvictionTask。

### 9. 客户端核心流程

```
DiscoveryClient 初始化：
  → ① 首次全量拉取注册表（fetchRegistry）
  → ② 启动 InstanceInfoReplicator（延迟40s后调用 register()）
  → ③ 启动 HeartbeatThread（每30s心跳续约）
  → ④ 启动 CacheRefreshThread（每30s拉取增量注册表）
  → ⑤ 监听状态变更，触发重新注册
```

### 10. Eureka 源码面试问答（高频）

> **Q1：客户端启动后，服务是怎么注册的？**
>
> 四步：客户端 `DiscoveryClient.register()` POST 到 `/v2/apps/{appName}`；服务端 `ApplicationResource.addInstance()` → ① 写入 registry 双层 Map → ② 加入 recentlyChangedQueue → ③ 使 readWriteCacheMap 失效 → ④ replicateToPeers 同步给集群其他节点。

> **Q2：Eureka 为什么用三级缓存？**
>
> 为了**读写分离**。① registry（内存，写操作直接改这层）→ ② readWriteCacheMap（Guava 缓存，180s 过期，写入后立即失效）→ ③ readOnlyCacheMap（30s 从②同步）。客户端读主要走③，减少对注册表的锁竞争，提高读并发。代价是最坏 30s 的缓存延迟。

> **Q3：客户端心跳怎么续约？**
>
> 每 30s `DiscoveryClient.renew()` PUT 请求 → 服务端找到 Lease 更新 `lastUpdateTimestamp`，并计数到 MeasuredRate（供自我保护判断）。若返回 404（实例不存在），客户端自动重新注册。

> **Q4：自我保护机制是什么？什么时候触发？**
>
> 触发条件：上一分钟实际心跳数 < 期望心跳数 × 0.85。期望心跳数 = 实例数 × 2（每个实例 60s/30s）。触发后**不再剔除任何过期实例**，心跳恢复后自动退出。这是 AP 原则的体现——网络分区时宁可保留旧数据，也不让客户端拿不到服务列表。

> **Q5：过期实例怎么被剔除？**
>
> EvictionTask 每 60s 执行：检查 `now - lastUpdateTimestamp > 90s` → 自我保护触发则跳过 → 按公式 `可剔除数 = 实例总数 × 0.15` 限制本次数量 → 随机选取剔除。

> **Q6：集群节点之间怎么同步？**
>
> 注册/续约/下线 → `replicateToPeers()` → 放入 acceptorQueue → AcceptorRunner 每 10ms 打包成 batch 发送。注意：**剔除（Eviction）是本地行为，不传播**到其他节点。

> **Q7：为什么 Eureka 选 AP？和 ZooKeeper 有什么区别？**
>
> 注册发现场景，服务列表的**可用性** > **一致性**。Eureka 用最终一致性（三级缓存 + 心跳），网络抖动时宁可保留过期数据也不让客户端拿不到列表；ZooKeeper 是 CP，选举期间可能整个集群不可用。所以服务注册发现用 Eureka/Nacos（AP），配置中心用 ZK/Etcd（CP）。

> **Q8：Nacos 相比 Eureka 强在哪？**
>
> ① 注册中心 + 配置中心二合一 ② AP/CP 模式可切换 ③ 有 Web 控制台 ④ 配置实时推送（长轮询）。Eureka 已停止更新，新项目建议 Nacos。

---

## 📌 四、MyBatis — 核心源码

### 1. 完整执行链路

```
SqlSessionFactoryBuilder.build(inputStream)
  → 解析XML，构建Configuration
  → SqlSessionFactory.openSession()
    → 创建 DefaultSqlSession（含 CachingExecutor + SimpleExecutor）
    → sqlSession.getMapper(XxxMapper.class)
      → MapperProxyFactory 创建 JDK 动态代理（InvocationHandler=MapperProxy）
      → mapper.selectXxx(params)
        → MapperProxy.invoke()
          → MapperMethod.execute()
            → sqlSession.selectOne()/selectList()
              → CachingExecutor.query()  ← 二级缓存
                → BaseExecutor.query()   ← 一级缓存
                  → SimpleExecutor.doQuery()
                    → 创建 StatementHandler（RoutingStatementHandler→PreparedStatementHandler）
                    → ParameterHandler 设置参数
                    → JDBC PreparedStatement.execute()
                    → ResultSetHandler 处理结果集
```

### 2. Executor 执行器

| 类型 | 特点 | 场景 |
|------|------|------|
| **SimpleExecutor** | 每次执行创建新 Statement，执行后关闭 | 默认，常规单条SQL |
| **ReuseExecutor** | 缓存 Statement，相同SQL复用 | 高频重复SQL |
| **BatchExecutor** | 批量执行，统一提交 | 批量插入/更新 |
| **CachingExecutor** | 装饰器，包裹基础执行器添加二级缓存 | 二级缓存场景 |

### 3. 二级缓存与一级缓存

**一级缓存**（`BaseExecutor.localCache`）：会话级别的 `PerpetualCache`，相同 SQL 和参数在同一 SqlSession 中复用。

**二级缓存**（`CachingExecutor`）：Mapper 级别的全局缓存，通过装饰器模式实现。`TransactionalCacheManager` 管理事务提交后才生效。

### 4. Mapper 代理机制

`MapperProxyFactory` 通过 `Proxy.newProxyInstance()` 创建 JDK 动态代理，每个方法调用进入 `MapperProxy.invoke()`：

```java
// 若方法继承自Object → 直接反射调用
// 否则 → 创建 MapperMethod 对象
  // MapperMethod.SqlCommand：封装方法名 + SQL类型(SELECT/INSERT/UPDATE/DELETE)
  // MapperMethod.MethodSignature：封装返回值类型 + 参数解析器
// → MapperMethod.execute() → 调用SqlSession对应方法
```

---

## 📌 四、Netty — 核心源码

### 1. Reactor 线程模型

```
EventLoopGroup bossGroup（1个/多个EventLoop）
  └── 每个EventLoop绑定一个Selector + 一个专用Thread
      └── 循环：select → processSelectedKeys → runAllTasks

NioEventLoop.run() 工作循环：
  ① select()：检测就绪的IO事件
  ② processSelectedKeys()：处理OP_ACCEPT/OP_READ/OP_WRITE
  ③ runAllTasks()：执行队列中的定时任务/普通任务
```

**核心设计**：一个 `EventLoop` 绑定一个 `Thread`，一个 `EventLoop` 服务于多个 `Channel`，但一个 `Channel` 只注册到一个 `EventLoop`——无需加锁。

### 2. ByteBuf 缓冲区

| 特性 | ByteBuf | NIO ByteBuffer |
|------|---------|---------------|
| **指针** | 读指针 + 写指针分离 | 一个 position，需 flip() |
| **扩容** | 自动扩容 | 固定容量 |
| **内存池** | PooledByteBuf 池化管理 | 无 |
| **零拷贝** | CompositeByteBuf 组合视图 | 无 |

**核心实现类**：
- `PooledDirectByteBuf`：堆外直接内存（默认IO读写最佳方式）
- `PooledHeapByteBuf`：堆内存
- `CompositeByteBuf`：组合多个 ByteBuf 为统一视图，不拷贝数据（零拷贝）

### 3. ChannelPipeline 与 ChannelHandler

```
入站事件（read）：Head → Handler1 → Handler2 → ... → Tail
出站事件（write）：Head ← Handler1 ← Handler2 ← ... ← Tail
```

- **双向链表**结构，节点为 `AbstractChannelHandlerContext`
- `HeadContext`：入站+出站，委托 `Unsafe` 执行底层 IO
- `TailContext`：仅入站，终止事件传播，未处理异常打印警告

**Handler 生命周期**：
```
handlerAdded → channelRegistered → channelActive
  → channelRead → channelReadComplete
  → channelInactive → channelUnRegistered → handlerRemoved
```

`ChannelInitializer` 是一种特殊的 handler，在 `handlerAdded` 中调用 `initChannel()` 添加自定义 handler，之后将自己从 pipeline 中移除。

---

## 📌 五、Dubbo — 核心源码

### 1. Dubbo 增强 SPI 机制

Dubbo 没有用 Java 标准 SPI，而是自己实现了一套更强大的增强 SPI。

| 特性 | Java SPI | Dubbo SPI |
|------|---------|-----------|
| 按需加载 | 一次性加载全部 | 按名称加载 |
| 默认实现 | 无 | `@SPI("dubbo")` |
| 自适应扩展 | 无 | `@Adaptive` 运行时动态选择 |
| IOC/AOP | 无 | 自动注入依赖 + Wrapper装饰链 |

**核心类 `ExtensionLoader`**：
```java
ExtensionLoader.getExtensionLoader(Protocol.class)    // 获取扩展加载器
    .getExtension("dubbo")                            // 按名称获取具体实现
    .getAdaptiveExtension()                            // 获取自适应扩展（运行时从URL取参数选实现）
```

**自适应扩展**：`Protocol` 接口的 `export()` 方法被 `@Adaptive` 标记 → 动态生成 `Protocol$Adaptive` 代理 → 从 URL 中取 `protocol` 参数决定使用哪个协议实现（dubbo/tri/injvm）。

**Wrapper（装饰器链）**：若扩展类有以接口为唯一参数的拷贝构造 → 识别为 Wrapper → 层层包裹：

```
Protocol$Adaptive → QosProtocolWrapper → ProtocolListenerWrapper
  → ProtocolFilterWrapper → DubboProtocol（实际实现）
```

### 2. 服务暴露与引用

**`Protocol` 接口**（`@SPI("dubbo")`）：
- `export(Invoker invoker)`：服务提供方暴露服务
- `refer(Class<T> type, URL url)`：服务消费方引用服务

`AbstractProtocol` 模板方法定义公共骨架，`DubboProtocol` 实现具体细节。

### 3. 负载均衡

内置策略（通过 SPI 可插拔）：
- `RandomLoadBalance`（默认）：加权随机
- `RoundRobinLoadBalance`：加权轮询
- `LeastActiveLoadBalance`：最少活跃连接
- `ConsistentHashLoadBalance`：一致性哈希

---

## 📌 六、面试核心速查表

| 框架 | 最常考的核心点 | 源码关键类 |
|------|--------------|-----------|
| **Spring IoC** | `refresh()` 流程、Bean生命周期11步、三级缓存循环依赖、AOP代理时机 | `AbstractApplicationContext`, `AbstractAutowireCapableBeanFactory`, `DefaultSingletonBeanRegistry` |
| **Spring Boot** | 自动配置加载流程、`@ConditionalOnMissingBean`、`AutoConfiguration.imports` | `AutoConfigurationImportSelector`, `OnClassCondition`, `ConfigurationClassParser` |
| **MyBatis** | 完整执行链路、Mapper代理机制、Executor三种类型、二级缓存 | `MapperProxy`, `MapperMethod`, `SimpleExecutor`, `CachingExecutor`, `StatementHandler` |
| **Netty** | Reactor模型、EventLoop线程模型、Pipeline双向链表、ByteBuf读写指针 | `NioEventLoop`, `DefaultChannelPipeline`, `AbstractByteBuf`, `PooledByteBuf` |
| **Dubbo** | 增强SPI(IOC+AOP)、自适应扩展、Wrapper装饰器链、Protocol + Invoker | `ExtensionLoader`, `Protocol$Adaptive`, `DubboProtocol`, `AbstractProxyInvoker` |

---

> 📝 **学习建议**：源码面试不用全背，重点掌握每个框架的**核心类 + 关键方法 + 设计思想**。比如 Spring 面试问"Bean生命周期"答出 11 步 + AOP 在 `postProcessAfterInitialization` 阶段生成就行；问"循环依赖"答出三级缓存 + `getEarlyBeanReference` 即可。

> 声明：本文档基于 Spring/Spring Boot/MyBatis/Netty/Dubbo 官方文档及技术社区源码分析整理，具体源码细节以对应框架实际版本为准。
