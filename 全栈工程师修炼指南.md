# 全栈工程师修炼指南 — 面试备战手册

> 来源：极客时间《全栈工程师修炼指南》
> 涵盖 40+ 讲：网络协议 → MVC → 前端 → 数据持久化 → 运维 → 安全 → 性能

---

## 📌 一、面试高频考点速查

| 核心考点 | 重要度 |
|---------|-------|
| **HTTP 协议演进**（HTTP/1.0 → 1.1 → 2.0 → 3.0） | ⭐⭐⭐⭐⭐ |
| **HTTPS 与 TLS 握手** | ⭐⭐⭐⭐⭐ |
| **WebSocket 服务端推送** | ⭐⭐⭐⭐ |
| **RESTful API 设计原则** | ⭐⭐⭐⭐ |
| **MVC 架构思想**（模型/视图/控制器） | ⭐⭐⭐⭐ |
| **缓存策略**（浏览器缓存/CDN/服务端缓存） | ⭐⭐⭐⭐⭐ |
| **CAP 理论与数据存储选型** | ⭐⭐⭐⭐⭐ |
| **集群部署与负载均衡** | ⭐⭐⭐⭐ |
| **网站安全**（XSS/CSRF/SQL注入） | ⭐⭐⭐⭐ |
| **前端性能优化** | ⭐⭐⭐⭐ |
| **JavaScript 异步编程**（Promise/async-await） | ⭐⭐⭐ |

---

## 📌 二、HTTP 协议演进（课程第01讲）

| 版本 | 特点 | 关键改进 |
|------|------|---------|
| **HTTP/1.0** | 每个请求新建 TCP 连接 | 无状态，短连接 |
| **HTTP/1.1** | 持久连接 + 管道化 + Host 头 | 连接复用，支持虚拟主机 |
| **HTTP/2.0** | 多路复用 + 二进制分帧 + 头部压缩 + 服务端推送 | 解决队头阻塞，性能大幅提升 |
| **HTTP/3.0** | QUIC 协议（基于 UDP） | 0-RTT 建连，更好弱网表现 |

**HTTP/2 多路复用**：在同一个 TCP 连接上并发传输多个流（Stream），解决了 HTTP/1.1 的队头阻塞问题。但 **TCP 层仍有队头阻塞**（丢包时所有流都等重传），HTTP/3 用 QUIC 解决。

## 📌 三、HTTPS 与 TLS（课程第02讲）

```
HTTPS = HTTP + TLS

TLS 握手过程（简化）：
1. Client Hello（支持的加密套件）
2. Server Hello + 证书（含公钥）
3. 客户端验证证书，生成预主密钥，用公钥加密后发送
4. 双方用预主密钥推导出对称加密密钥
5. 开始加密通信
```

**TLS 1.3 改进**：1-RTT 握手（甚至 0-RTT 恢复），移除不安全加密套件。

## 📌 四、服务端推送（课程第03讲）

| 技术 | 原理 | 适用 |
|------|------|------|
| **轮询** | 客户端定时请求 | 简单实时性低 |
| **长轮询** | 请求挂起直到有数据返回 | 兼容性好 |
| **WebSocket** | 全双工，单个 TCP 连接 | 实时双向通信 |
| **SSE**（Server-Sent Events） | 单向推送 | 服务端到客户端的实时通知 |

## 📌 五、RESTful API 设计（课程第04-05讲）

| 原则 | 做法 |
|------|------|
| **资源导向** | URL 表示资源（名词），不用动词 |
| **HTTP 动词** | GET（查）/ POST（增）/ PUT（全量改）/ PATCH（部分改）/ DELETE（删） |
| **状态码** | 200 OK / 201 Created / 400 Bad Request / 404 Not Found / 500 服务端错误 |
| **无状态** | 每个请求携带所有必要信息 |
| **版本控制** | `/api/v1/orders` |

## 📌 六、MVC 模式（课程第07-10讲）

```
模型（Model）    — 数据和业务逻辑
视图（View）     — 用户界面展示
控制器（Controller）— 接收请求、调用模型、选择视图
```

**两种 Model 风格对比**：

| 风格 | 做法 | 特点 |
|------|------|------|
| **贫血模型（Anemic）** | 模型仅含数据（POJO），业务逻辑在 Service 层 | 简单，适合 ORM（Hibernate） |
| **充血模型（Rich）** | 模型包含数据和业务逻辑 | 更 OOP，领域驱动设计 DDD |

**MVC 的变体**：MVP（Model-View-Presenter）、MVVM（Model-View-ViewModel，如 Vue.js）

**AOP（面向切面编程，第11讲）**：在不修改业务代码的情况下，横切关注点（日志、事务、权限）统一处理。关键概念：Aspect（切面）、Join Point（连接点）、Advice（通知）、Pointcut（切点）。

## 📌 七、JavaScript 面向对象与异步编程（课程第15+19讲）

### JavaScript 继承机制

JavaScript 使用**原型链**（Prototype Chain）而非类的继承。ES6 的 `class` 只是原型的语法糖。

| 概念 | 说明 |
|------|------|
| **原型链** | 对象通过 `__proto__` 链接到构造函数的 `prototype` |
| **`this` 绑定** | 隐式绑定、显式绑定（call/apply/bind）、new 绑定、箭头函数（词法 this） |
| **闭包** | 函数 + 对周围状态的引用，实现私有变量 |

### 异步编程演进

| 方式 | 特点 | 问题 |
|------|------|------|
| **Callback** | 简单直接 | 回调地狱、信任问题（可能不调或调多次） |
| **Promise** | 三种状态（pending/fulfilled/rejected），链式调用 | 需要手动 return |
| **async/await** | Promise 语法糖，同步方式写异步 | 注意 await 是串行的 |

### 事件循环（Event Loop）
```
微任务队列（Promise）→ 宏任务队列（setTimeout/setInterval）→ 渲染
```

## 📌 八、前端框架对比（课程第16讲）

| 框架 | 特点 | 适用 |
|------|------|------|
| **React** | 虚拟 DOM、单向数据流、JSX、函数组件+Hooks | 灵活、生态丰富 |
| **Vue** | 模板语法、响应式数据绑定、渐进式 | 上手简单 |
| **Angular** | 全功能、TypeScript、依赖注入、RxJS | 大型企业应用 |

## 📌 七、缓存（课程第21-22讲）

| 缓存层级 | 位置 | 特点 |
|---------|------|------|
| **浏览器缓存** | 浏览器本地 | 强制缓存（Cache-Control）和协商缓存（ETag/Last-Modified） |
| **CDN 缓存** | 边缘节点 | 就近访问，加速静态资源 |
| **服务端缓存** | Redis/Memcached | 扛热点数据库请求 |

**缓存淘汰策略**：LRU（最近最少使用）、LFU（最不经常使用）、FIFO（先进先出）

## 📌 八、CAP 与存储选型（课程第24讲）

| 存储类型 | 典型代表 | 场景 |
|---------|---------|------|
| **关系型数据库** | MySQL、PostgreSQL | 强一致、事务性场景 |
| **KV 缓存** | Redis、Memcached | 高并发读写、缓存 |
| **文档型** | MongoDB | 灵活 Schema |
| **搜索** | Elasticsearch | 全文搜索、日志分析 |

## 📌 十一、网站安全（课程第31讲）

| 攻击类型 | 原理 | 防范 |
|---------|------|------|
| **XSS（跨站脚本）** | 注入恶意脚本到页面 | 输入输出转义过滤，HttpOnly Cookie |
| **CSRF（跨站请求伪造）** | 利用用户已登录状态发起恶意请求 | Token 校验、Referer 校验 |
| **SQL 注入** | 拼接恶意 SQL 语句 | 预编译 + 参数化查询 |
| **HTTP 劫持** | ISP 在响应中注入内容（如广告） | HTTPS |
| **DNS 劫持** | 篡改 DNS 解析 | HTTPS 不能防；用 DoH（DNS over HTTPS） |
| **DDoS** | 流量洪水 | 多层防护：防火墙、流量清洗、CDN |

## 📌 十二、浏览器缓存策略（课程第21讲）

```
第一次请求：浏览器 → 服务器（200 + Cache-Control: max-age=3600 + ETag）
第二次请求：浏览器 → 检查缓存未过期 → 直接使用本地缓存（强制缓存，200 from cache）
第三次请求：缓存过期 → 浏览器 → 服务器（If-None-Match + ETag）
            服务器返回 304（协商缓存，Not Modified）→ 浏览器使用本地缓存
```

| 缓存类型 | 控制头 | 流程 |
|---------|--------|------|
| **强制缓存** | Cache-Control / Expires | 未过期直接使用，不发请求 |
| **协商缓存** | ETag（文件指纹）/ Last-Modified（时间） | 每次请求验证，若未修改返回 304 |

## 📌 十三、集群部署策略（课程第29讲）

```
负载均衡（Nginx/LVS）→ 应用集群 → 缓存集群 → 数据库集群
                ↑ 水平扩展
```

**六种部署策略**：

| 策略 | 做法 | 适用 |
|------|------|------|
| **滚动部署** | 逐步替换旧实例 | 最常用，无需额外资源 |
| **蓝绿部署** | 两套完整环境，切换流量 | 快速回滚 |
| **灰度/金丝雀** | 小比例先试，逐步放量 | 降低上线风险 |
| **重建** | 停旧启新 | 有停机时间 |
| **A/B 测试** | 特定用户分流 | 功能验证对比 |
| **影子部署** | 镜像流量副本 | 不影响线上 |

## 📌 十四、CI/CD 与 DevOps（课程第28-30讲）

## 📌 十二、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **HTTP/1.1 到 HTTP/2 最大改进？** | 多路复用 + 二进制分帧 + 头部压缩 + 服务端推送 |
| **HTTPS 握手过程？** | Client Hello → Server Hello+证书 → 客户端验证+加密预主密钥 → 双方推导对称密钥 |
| **WebSocket 解决了什么问题？** | 实现真正的服务端推送，全双工通信，替代轮询/长轮询 |
| **RESTful 设计要点？** | 资源URL + HTTP动词 + 状态码 + 无状态 + 版本控制 |
| **浏览器缓存策略？** | 强制缓存（Cache-Control）和协商缓存（ETag/Last-Modified），先强后协 |
| **XSS/CSRF 区别？** | XSS 注入恶意脚本，CSRF 伪造请求；XSS 用转义过滤，CSRF 用 Token |
| **MVC 架构的好处？** | 解耦——模型管数据、视图管展示、控制器管调度，利于分工和测试 |
| **全栈工程师的意义？** | 能理解前后端全链路，从全局视角做技术决策，减少沟通成本 |
