# 分布式金融架构 — 面试备战手册（完整版）

> 来源：极客时间《分布式金融架构》（任杰，前 Morgan Stanley / 蚂蚁金服 / eBay）
> 本版基于完结版（27讲）重建，覆盖：金融业务全景 → 系统正确性 → 分布式正确性 → 高可用容灾
> 核心主题：**正确性**（业务正确 → 架构正确 → 基础正确）。金融软件被称为"架构的珠穆朗玛峰"——钱处理错了就是真金白银的损失

---

## 📌 一、面试高频考点速查

| 核心考点 | 重要度 |
|---------|-------|
| **金融正确性思维**（事前/事中/事后） | ⭐⭐⭐⭐⭐ |
| **事件溯源（Event Sourcing）** | ⭐⭐⭐⭐⭐ |
| **信息流与资金流分离** | ⭐⭐⭐⭐⭐ |
| **TCC 分布式事务**（含空回滚/防悬挂） | ⭐⭐⭐⭐⭐ |
| **2PC 原理**（准备成功状态） | ⭐⭐⭐⭐⭐ |
| **线性一致性 / 会话一致性** | ⭐⭐⭐⭐⭐ |
| **FLP 不可能原理** | ⭐⭐⭐⭐⭐ |
| **Raft 共识（三问题等价）** | ⭐⭐⭐⭐⭐ |
| **分布式数据方案**（Redis/RocksDB/Spanner/TiDB） | ⭐⭐⭐⭐ |
| **动态分库** | ⭐⭐⭐⭐ |
| **容灾**（两地三中心/三地五中心） | ⭐⭐⭐⭐ |
| **混沌工程 / SRE/SLO/SLI** | ⭐⭐⭐⭐ |
| **双时序数据库** | ⭐⭐⭐⭐ |
| **隔离级别与 MVCC** | ⭐⭐⭐⭐ |
| **领域驱动设计（DDD）** | ⭐⭐⭐ |
| **对账的区块链思想** | ⭐⭐⭐ |

---

## 📌 二、金融系统核心思维

### 1. 正确性 = 事前 + 事中 + 事后

```
┌─────────────────────────────────────────────┐
│ ① 事前（输入正确）                            │
│   - 内容正确：双时序数据库、校验和(HMAC)、版本号 │
│   - 顺序正确：不乱序/不丢/不重复              │
├─────────────────────────────────────────────┤
│ ② 事中（处理正确）                            │
│   - 函数式编程（不可变数据，可组合性）          │
│   - 计算精度（知道业务规模上限）               │
├─────────────────────────────────────────────┤
│ ③ 事后（验证正确）                            │
│   - 用不同环境验证（不同语言/实现）去相关        │
│   - 实时校验（防损害，加延迟）                 │
│   - 异步对账（日切/轧差，需业务补偿）           │
└─────────────────────────────────────────────┘
```

### 2. 信息流与资金流分离（第02讲核心）

```
信息流 = 资金"使用权"的转移（想象中钱的流转）
资金流 = 资金"所有权"的转移（钱的实际流转）

白天写纸条来回转账（信息流）→ 晚上对账 → 银行清算（资金流）

架构影响：
① 非银行参与者只能产生信息流，不能产生资金流
② 二者不同速度流转 → 支付系统本质是异步处理系统 → 最终一致性
③ 最终靠"核算系统"（对账）确认同步准确
```

### 3. 轧差与异步——金融峰值流量的两大法宝

```
央行：银行存存款准备金，跨行转账 = 准备金记账变动
清算机构（银联/网联）：白天只记账不转钱，晚上轧差（算净额只转一笔）
异步消息处理：请求缓存慢慢消费（削峰填谷）→ 需"推拉结合"查状态
```

---

## 📌 三、金融业务全景

### 1. 不同金融业务的技术要点（第03讲）

| 业务 | 赚钱原理 | 系统要求 |
|------|---------|---------|
| **传统信贷** | 利息差价 | 信用评级靠信贷员，IT 非核心 |
| **互联网信贷** | 大数据风控 | 批处理/大数据框架，不要求实时 |
| **资产证券化** | 打包房贷拆分卖出 | 计算复杂 + 数据量巨大（暴力穷举需千万台机器） |
| **场内交易** | 撮合（低延时+高吞吐） | **FPGA 硬件实现**（互联网软件方案不适用） |
| **场外交易** | 主动定价（信息不对称） | 领域驱动设计降低复杂度 |

**核心洞察**：交易所是"每时每刻都在秒杀"，延时要求毫秒~微秒级 → C/汇编，**单进程架构**（不用微服务，调用链长），甚至把机器搬进交易所机房。

### 2. 领域驱动设计 DDD（第04-05讲）

**三领域分类**：
| 领域 | 做法 |
|------|------|
| **核心领域** | 竞争优势来源（定价/风控），自研+最好的人 |
| **通用领域** | 跨行业共性（短信/安全/存储），买第三方 |
| **支持型领域** | 支撑核心（账务/行情），尽量买，低耦合自研 |

**建模逻辑**（第05讲）：
| 概念 | 特点 |
|------|------|
| **实体（Entity）** | 有唯一标识符、有生命周期 |
| **值对象（Value Object）** | 无标识符、按属性比较、**不可修改**（修改返回新对象） |
| **领域服务** | 跨多个实体的业务逻辑，**无状态**、无随机性 |
| **聚合（Aggregate）** | 有向图子图，唯一入口=聚合根，**聚合内一个事务** |
| **仓库（Repository）** | 防腐化层，隔离存储与使用 |

**关键**：金融领域服务设计原则是**无状态 + 无随机性**——这是正确性要求，也是金融系统用不可变架构的原因。

---

## 📌 四、系统正确性保障

### 1. 事件溯源（第07讲，面试最高频）

**三个关键术语**：
```
命令（command）：外部指令（转账请求），收到先检查合理性
事件（event）：命令检查通过后"一定要执行的事"，命名用过去式
状态（state）：事件执行的结果（账户余额）
一个命令可生成多个事件（转账=扣款+入账）；命令不合法可生成空事件(NOP)
```

**四个核心机制**：
| 机制 | 说明 |
|------|------|
| **FIFO 队列存储** | 命令/事件各一个队列存文件，只追加（顺序写充分利用硬盘吞吐） |
| **自动机执行** | 事件队列当头到尾执行的磁带；**执行不能有任何随机性**（伪随机可以，种子记入事件） |
| **时光机（Time Machine）** | 重置状态机重新执行到指定时间点 → 回到过去任意状态 → **审计能力** |
| **快照（Snapshot）** | 存当前状态，恢复时加载快照再执行后续事件；金融由**日切**决定打快照频率 |

**CQRS 读写分离**：写入组件只写，查询组件只读，可分别优化（写用 LSM 树、读用 B+ 树）。

**数学本质**：事件溯源 = 不可变架构；任何时间点状态 = 之前所有事件效果的累积（事件 = 状态对时间的导数）。

### 2. 计算输入的正确性：双时序数据库（第06讲）

**核心问题**：30 年保险合同，CPI 数据后期多次修改，如何证明当年用的数据是对的？

```
两个时间轴：
  发生时间（valid time，VT）= 业务发生时间
  记录时间（transaction time，TT）= 数据修改时间

数据可见范围 = "既存在且有意义"的时间范围
查询 = 找离当前查询时间点"最近"的"合理"数据
```

**价值**：
- 数据不变性（不可覆盖/篡改，金融硬要求）
- 用合同签订时间查询 → 可复现 30 年前所有数据
- **情景计算**：保持发生时间不变、调整记录时间 → 监管 What-if 场景

### 3. 计算结果正确性：顺序与一次性（第08讲）

**顺序正确性三要求**：不乱序、不丢、不重复

| 问题 | 解决 |
|------|------|
| **不乱序** | 自增 ID + 接收缓冲（应用层复现 TCP 方案） |
| **至少一次** | 消息中间件自己的 DB 存消息+ack标记，依赖数据库事务+幂等 |
| **至多一次** | 去重：业务属性（订单号）/DB主键/上游自增ID |

**函数式编程**：不可变数据+无随机 → 函数可组合（Goldman 的 Slang/SecDB、Morgan Stanley 的 A+、Jane Street 的 Ocaml）。

### 4. 数据传输质量（第09讲）

**交易数据（事务型）**：需顺序正确性 + 一次性。主要异常是**限流**：
- **漏桶算法**：固定容量桶+匀速消费，适合限制"对外发送速度"
- **令牌桶算法**：固定速度放令牌+峰值能力，适合总流量微调

**市场数据（非事务型）**：允许掉数据
- 选**订阅发布**（Pub/Sub）而非消息——每个消费者独立处理所有消息且顺序一致（Kafka 在金融用得多）
- 时效性：越接近现在的数据价值越高 → 允许丢 → 可把 Kafka **ISR 设为 0** 牺牲容灾换速度
- **实时市场数据**：层级分发（交易所→局域网广播→VIP推送），优化**延时不是吞吐量**
- **FIX 协议**：利用"连续两个数据大部分内容相同"，只传变动部分（类似视频关键帧+变化帧）

### 5. 数据存储选型（第10讲）

| 数据 | 存储 | 说明 |
|------|------|------|
| 图数据 | 图数据库 | 金融少用，KYC/反洗钱 |
| 时序数据 | **KDB/Q**、ClickHouse | **列存储**（只读需要的列），CPU 向量指令，单线程无锁 |
| 树状数据 | NewSql | 解决对象关系阻抗不匹配，但二级索引最终一致，金融谨慎 |
| 普通业务 | 关系型数据库 | **事务简化架构**（ROI 最高） |

**对象关系阻抗不匹配**：OOP 对象是图结构，关系库是集合论——转换不自然，是 NewSql 诞生的原因。

---

## 📌 五、分布式事务：TCC 与 2PC

### 1. TCC（业务层，国内最常用）

```
第一阶段：Try（预操作，本地事务真实提交）
第二阶段：全部成功 → Confirm（确认）
          有失败   → Cancel（业务级反向补偿，不是回滚）
```

**TCC 三个硬性要求**（消息乱序处理）：
| 要求 | 说明 |
|------|------|
| **空回滚** | 允许 Cancel 一个不存在的交易（Cancel 可能先于 Try） |
| **记录空回滚** | 记录防止重复取消 |
| **防悬挂** | Try 发现已有空回滚标记 → 必须失败 |

**宏观视角**：整个金融业务 = 大型 TCC，**对账系统就是阶段二协调者的宏观代表**。

### 2. 2PC（数据库层）

```
阶段1：协调者问所有库"能提交吗" → 每个库预处理（锁+改）→ 回"准备成功/失败"
阶段2：全部成功 → 命令提交；否则 → 全部回滚
```

**关键——"准备成功"新状态**（答疑三）：
```
2PC 需要数据库有第三种状态"准备成功"
恢复时：有该标记的交易不回滚（像提交），但**不释放锁**，等协调者阶段2指令

2PC 与重做日志：事务基于 Redo Log（本质和事件溯源日志一样）
崩溃恢复：begin后→什么都不做；修改后→用前镜像(undo)；commit后→用后镜像(redo)
2PC 就是在重做日志上扩展"准备成功"标记
```

### 3. 面试回答

> Q：TCC 和 2PC 区别？TCC 怎么处理消息乱序？
>
> 2PC 是数据库层强一致但阻塞（协调者单点+锁持有）。TCC 是业务层补偿（Try/Confirm/Cancel），性能好但业务侵入大。TCC 处理乱序靠三个硬性要求：**空回滚**（允许取消还没 Try 的交易）、**记录空回滚**、**防悬挂**（Try 发现空回滚标记就失败）。宏观上整个金融业务就是大型 TCC，对账系统是阶段二协调者的代表。

---

## 📌 六、一致性：从单机到分布式

### 1. 隔离级别（第12讲）

| 级别 | 解决的冲突 |
|------|-----------|
| **Read Uncommitted** | 脏写（写写覆盖） |
| **Read Committed** | + 脏读 |
| **Repeatable Read** | + 模糊读 |
| **可串行化** | 所有冲突 |

**快照隔离（Snapshot Isolation）**：最常用默认级别——事务开始拍所有正确数据的快照（排除进行中和未开始的事务）。用 **MVCC** 实现：数据只追加带版本、删除=墓碑+GC（像 Java GC）、修改=删除+新增。

### 2. 会话一致性（第14讲，4种无层级）

```
① 单调写一致 — 各节点看到的写顺序一致
② 单调读一致 — 后来的读不能返回更旧的值
③ 自读自写 — 能读到自己以前所有的写
④ 先读后写 — 你读到别人的写后，你的写必须排在他之后
```

### 3. 线性一致性（Linearizability）

```
所有操作（各带开始/结束时间）可重排成一个全序
且满足非重叠操作的顺序约束 + 结果业务逻辑上有效
效果：让每个操作看起来像在一个时间点原子完成
```

### 4. CAP 的正确理解（课程批判）

```
CAP 不是"三选二"——它说的是网络分区时只能选 CP 或 AP
CAP 的 C 定义不严谨（实际指线性一致性，理论没说）
把丰富的一致性谱系压扁成二元选择，只适合给初学者讲
```

**选型决策流**：单机→快照隔离；多机低要求→会话一致性；多机高要求→线性一致性；分库容灾→每分片先线性一致，再 TCC/2PL。

---

## 📌 七、共识与分布式数据方案

### 1. FLP 不可能原理（第15讲）

**结论**：完全异步系统 + 至少一个机器可能故障 → 不存在非随机共识算法。

**为什么实践中能用 Paxos/Raft**：加一个**时钟判断消息超时**打破"完全异步"（1996 论文证明不可靠但最终准确的故障检测器足以达成共识，且是必要条件）。

### 2. Raft（第16讲）

**共识解决的三个问题（等价）**：
```
共识 ⇔ 全序广播 ⇔ 线性化存储

① 文件同步：多数派工作文件就一致（配置/路由表）
② 全序广播：所有机器记录消息顺序完全一致（记账顺序不能错）
③ 线性化存储：分布式锁、分布式事务
```

**客户端使用**：只和 Leader 通信；结果未知 → 假设失败重试 → 服务端必须去重。

### 3. 分布式数据方案（第18讲）

| 方案 | 核心 | 取舍 |
|------|------|------|
| **Redis** | 单线程+epoll，异步主从 | 牺牲一致性换速度；不能满足会话一致性 |
| **RocksDB** | LSM 树+SSTable | **写快读慢**，延时锯齿状；B+树延时平稳 |
| **Spanner** | Paxos 同步日志=分布式状态机，原子钟 TrueTime | 分布式关系型数据库开山鼻祖 |
| **TiDB** | Raft 同步；**写用行存储(TiKV)+读用列存储(TiFlash)** = CQRS | 国产，Spanner 加强版 |

**通用取舍（重要）**：
- 稳定性、正确性、速度不能三者同时兼顾
- 读、写、存储空间不能三者同时优化
- 分布式下没有"正确架构"，只有"满足业务需求的合适架构"

---

## 📌 八、正确性案例

### 1. 分布式事件溯源（第17讲）

```
复制状态机（Replicated State Machine）三要素：
  ① 线性一致性（单 Leader 提供服务）
  ② Leader 选举
  ③ 全序广播（替换节点完全一致）

关键：复制"事件队列"而非"命令队列"——命令→事件可能随机，两个Leader会推演出不同事件

只有 Leader 对外通信；Follower 只复制事件更新状态；反向代理隐藏多机集群
一致性读：把读请求作为 no-op 命令通过共识排序
```

### 2. 运行时动态分库（第19讲）

```
核心：快照处理存量 + 命令队列处理增量 + 状态机重算实时一致

① 分库前：新集群B（共识集群）复制A的快照，成为A的读模式节点尾随命令
② 分库中：协调者发"分库命令"给A → A写进命令队列（复制到容灾节点）
   → A的自动机执行改配置（A永不宕机，属于B的消息返回重定向错误）
   → B执行同一分库命令 → 更新配置
③ 分库后：上游靠重定向错误学路由；下游被动（协调者推送）或主动（解析分库命令）

分库命令几乎与业务无关（只声明归属），重复执行幂等
```

### 3. 账务系统优化（第11讲答疑）

**合并存储**：每个事件和变动前后余额存一起（按用户时间线）→ 历史查询=找最近事件，无需快照。

**对账的区块链思想**：
```
两个不变量：
  ① 前一个事件的最终余额 == 后一个事件的初始余额
  ② 最终余额 == 初始余额 + 变动
把用户相邻余额变动连成链条 → 走一遍链条就能验证任意状态
区块链 = 用去中心化共识构建了这个串联
```

---

## 📌 九、容灾与混沌工程

### 1. 容灾分级（第20讲）

**正确容灾两个要求**：SLA（时限内恢复）+ 事务正确性（尤其原子性——金额风险不可控）。

```
数据容灾（Raft 共识）：
  3 节点：① 2节点+1节点 → 只能单节点级容灾
          ② 3节点放3个数据中心（两地三中心）→ 数据中心级容灾
  5 节点：三地五中心 → 城市级容灾
  结论：两地三中心=数据中心级容灾；三地五中心=城市级容灾

单节点容灾：fsync 只到硬件控制器缓存，断电仍可能丢
  → 企业级硬件（带电池）或用钱换正确性
  → 或者不管不问（既然用集群容灾，单节点丢了当故障处理）
```

### 2. 混沌工程（第21讲）

```
原理：通过改变环境提高出问题概率，若仍未发现问题 → 平常正确性更高

核心概念：
  SLA（服务质量协议）含 SLO 和 SLI
  SLI（指标）= 监控对象（吞吐/延时）
  SLO（目标）= 指标要达到的目标（如99.999%）
  混沌工程中：SLI 是因变量，SLO 是实验假设

过程：因变量监控 + 伤害半径控制（测试→回归→预生产→生产灰度）

Jepsen 测试：专门证明分布式系统正确性，很多知名数据系统并没那么正确
```

**为什么说"正确性是信心"**：Raft 涉及无限状态无法形式化证明；正确性 = "系统不会出大问题"的置信区间。作者自述：基于 Raft 的支付系统研发 3 个月、**混沌测试一整年**（最后一个 bug 半年才发现）。

---

## 📌 十、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **金融正确性怎么保证？** | 事前（输入校验/版本号）+ 事中（函数式/精度）+ 事后（不同环境验证/对账） |
| **信息流和资金流分离是什么？** | 信息流=使用权转移（白天记账），资金流=所有权转移（晚上清算），分离使支付系统天然异步+最终一致 |
| **事件溯源是什么？** | 命令→事件→状态，复制事件队列而非命令队列；自动机无随机性，支持时光机审计 |
| **TCC 怎么处理消息乱序？** | 空回滚 + 记录空回滚 + 防悬挂 |
| **双时序数据库解决什么？** | 记录时间+发生时间双轴，可复现历史数据、做情景计算，华尔街核心能力 |
| **CAP 正确理解？** | 不是三选二，是分区时二选一；C 实指线性一致性 |
| **FLP 不可能原理？** | 完全异步+一个故障→无共识算法；靠超时打破 |
| **共识解决的三个问题？** | 文件同步、全序广播、线性化存储，三者等价 |
| **动态分库怎么不停机？** | 快照存存量+命令队列存增量+状态机重算，A 永不宕机 |
| **两地三中心 vs 三地五中心？** | 数据中心级 vs 城市级容灾 |
| **混沌工程和测试区别？** | 测试已知答案断言正确；混沌工程用假设理解系统 |
| **对账怎么做？** | 相邻余额变动连成链条（前事件终余额==后事件初余额），走链验证=区块链思想 |
| **Redis/RocksDB/Spanner 取舍？** | 稳定正确速度不能全要；读写存储空间不能全优，选合适的 |

---

> 📝 **课程精华**：这门课把"正确性"讲透了——从事件溯源、双时序数据库到混沌工程。面试交易所/金融岗时，能讲出"信息流资金流分离""复制事件而非命令""对账是宏观 TCC 协调者""正确性是置信区间"这些概念，就能和普通候选人拉开巨大差距。
