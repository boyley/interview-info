# 分布式金融架构 — 面试备战手册

> 来源：极客时间《分布式金融架构》（任杰，前 Morgan Stanley / 蚂蚁金服 / eBay）
> 核心主题：**正确性**（业务正确 → 架构正确 → 基础正确）
> 金融软件被称为"架构的珠穆朗玛峰"——钱处理错了就是真金白银的损失

---

## 📌 一、面试高频考点速查

| 核心考点 | 重要度 |
|---------|-------|
| **金融系统正确性思维**（事前/事中/事后） | ⭐⭐⭐⭐⭐ |
| **TCC 分布式事务**（含空回滚/防悬挂） | ⭐⭐⭐⭐⭐ |
| **2PC 原理**（准备成功状态/重做日志） | ⭐⭐⭐⭐⭐ |
| **线性一致性（Linearizability）** | ⭐⭐⭐⭐⭐ |
| **CAP 理论的正确理解**（不是三选二） | ⭐⭐⭐⭐⭐ |
| **Raft 在金融中的应用**（全序广播/线性化存储） | ⭐⭐⭐⭐ |
| **FLP 不可能原理** | ⭐⭐⭐⭐ |
| **隔离级别**（脏写/脏读/模糊读/快照隔离） | ⭐⭐⭐⭐ |
| **事件溯源（Event Sourcing）** | ⭐⭐⭐⭐ |
| **运行时动态分库** | ⭐⭐⭐⭐ |
| **领域驱动设计（DDD）** | ⭐⭐⭐ |
| **轧差与清算**（金融峰值流量应对） | ⭐⭐⭐ |
| **对账系统设计**（相邻余额串联/区块链思想） | ⭐⭐⭐ |

---

## 📌 二、金融系统的核心思维

### 1. 金融正确性 = 事前 + 事中 + 事后

```
┌─────────────────────────────────────────────┐
│ ① 事前（输入正确）                            │
│   - 内容正确：双时序数据库、校验和(HMAC)、版本号 │
│   - 顺序正确：不乱序/不丢/不重复（id+缓冲去重）  │
├─────────────────────────────────────────────┤
│ ② 事中（处理正确）                            │
│   - 函数式编程（不可变数据，可组合性）          │
│   - 计算精度（知道业务规模上限）               │
├─────────────────────────────────────────────┤
│ ③ 事后（验证正确）                            │
│   - 用不同环境验证（不同语言/不同实现）去相关    │
│   - 实时校验（防损害，加延迟）                 │
│   - 异步对账（日切/轧差，需业务补偿）           │
└─────────────────────────────────────────────┘
```

**面试加分点**：金融对正确性的验证不是只在开发时做，而是事后用**对账**来兜底——这和普通业务"开发完就完事"的思路完全不同。

### 2. 轧差与异步——金融峰值流量的两大法宝

```
央行：银行把存款准备金存在央行，跨行转账 = 准备金的记账变动
清算机构（银联/网联）：白天只记账不转钱，晚上轧差（算两家银行净额）
     → 只转净额一笔 → 大幅减少真实资金移动

异步消息处理架构：金融机构把请求缓存下来慢慢处理（削峰填谷）
     → 结果不是同步的 → 需要"推拉结合"查状态
```

**面试回答**：
> Q：金融系统怎么扛住高峰流量？
> 两个关键机制：**轧差**——银行间白天只记账，晚上算净额只转一笔，大幅减少真实转账；**异步处理**——把请求缓存慢慢消费，配合轮询/推送查询状态。这本质上是把实时消息转成批量处理的异步框架。

---

## 📌 三、分布式事务：TCC 与 2PC

### 1. TCC（Try-Confirm-Cancel）—— 业务层分布式事务

```
第一阶段：Try（预操作，本地事务真实提交）
第二阶段：全部成功 → Confirm（确认）
          有失败   → Cancel（业务级反向补偿，不是回滚）
```

**示例**（跨机转账 x→y）：
- x 扣款在 Try 阶段做
- y 入账在 Confirm 阶段做
- 第一阶段期间钱暂时"消失"（x=0, y=0），Confirm 后恢复
- 宏观原子 ≠ 微观原子，中间状态业务可见

**TCC 的三个硬性要求**（异常处理）：

| 要求 | 说明 |
|------|------|
| **空回滚** | 允许 Cancel 一个不存在的交易（可能 Cancel 先于 Try 到达） |
| **记录空回滚** | 要把空回滚记录下来，防止重复 |
| **防悬挂** | Try 发现已有空回滚标记 → 必须失败，否则交易悬挂 |

**面试回答**：
> Q：TCC 怎么处理消息乱序（Cancel 先到）？
> 三个硬性要求：**空回滚**——允许取消一个还没 Try 的交易；**记录空回滚**——记录防止重复取消；**防悬挂**——Try 如果发现已有空回滚标记就失败，避免交易悬挂在半路。

### 2. 2PC（两阶段提交）—— 数据库层

```
阶段1：协调者问所有库"能提交吗" → 每个库预处理（锁+改）→ 回"准备成功/失败"（未提交）
阶段2：全部准备成功 → 协调者命令提交；否则 → 全部回滚
```

**2PC 的关键——"准备成功"新状态**：
```
2PC 需要数据库有第三种状态"准备成功"（区别于开始/提交）
恢复时：有"准备成功"标记的交易不回滚（像提交），但**不释放锁**
      要等协调者的阶段2提交指令才真正提交
```

**2PC 与重做日志**：
```
事务基于重做日志（Redo Log），本质和事件溯源日志一样
崩溃恢复分析：
  在"begin"后崩溃 → 什么都不做
  在修改记录后崩溃 → 用前镜像覆盖（undo）
  在"commit"后崩溃 → 用后镜像覆盖（redo）

2PC 就是在重做日志上扩展一个"准备成功"标记
```

---

## 📌 四、一致性：CAP / 会话一致性 / 线性一致性

### 1. CAP 的正确理解（课程观点）

**CAP 不是"三选二"**：
- 它说的是在**网络分区（脑裂）**时，只能选 CP 或 AP
- CAP 的 C 定义不严谨——实际指的是线性一致性，但理论没明说
- CAP 把丰富的一致性谱系压扁成一个二元选择，**只适合给初学者讲**

### 2. 会话一致性（4 种，无层级关系）

```
写主库，读主库或副本：
① 单调写一致 — 各节点看到的写顺序一致
② 单调读一致 — 后来的读不能返回更旧的值
③ 自读自写 — 能读到自己以前所有的写
④ 先读后写 — 你读到别人的写后，你的写必须排在他之后
```

### 3. 线性一致性（Linearizability）—— 最重要的分布式一致性

```
所有操作（各带开始/结束时间）可以重排成一个全序
且满足：非重叠操作的顺序约束 + 结果业务逻辑上有效

效果：让每个操作看起来像在一个时间点原子完成
```

**选型决策流**：
```
单机 → 快照隔离（很少用可串行化）
多机低要求 → 会话一致性（单调读写 + 自读自写）
多机高要求 → 线性一致性
分库容灾时：每个分片集群先保证线性一致（集群像单机），再用 TCC/2PL 做分布式事务
```

---

## 📌 五、FLP 不可能原理（面试深度加分）

**结论**：在**完全异步**的分布式系统中，只要有**一个机器可能故障**，就不存在非随机的共识算法。

**为什么实践中能用 Paxos/Raft**：
```
共识需要"可能故障的机器"+"完全异步"两个条件
故障无法避免，但可以打破"完全异步"——加一个时钟判断消息超时
所以 Paxos/Raft 都用超时机制
（1996年论文证明：不可靠但最终准确的故障检测器足以达成共识，且是必要条件）
```

---

## 📌 六、Raft 在金融中的应用

### 共识解决的三个问题

| 问题 | 说明 | 金融应用 |
|------|------|---------|
| **文件同步** | 多数派工作，文件就一致 | 配置、路由表 |
| **全序广播** | 所有机器记录消息顺序完全一致 | 复制 DB 日志、**记账顺序不能错**、区块链账本 |
| **线性化存储** | 实现线性一致性的存储 | 分布式锁、分布式事务 |

**关键等价**：共识 ⇔ 全序广播 ⇔ 线性化存储（都可用分布式状态机证明）

### 客户端使用 Raft 的要点

```
客户端只和 Leader 通信
结果未知（既不成功也不失败）→ 假设失败重试 → 服务端必须去重
```

---

## 📌 七、事件溯源（Event Sourcing）

**核心思想**：复制事件队列，而不是命令队列。

```
命令 → 事件（可能是随机的）→ 两个 Leader 可能推演出不同事件
所以复制事件能保证全序

架构：
  只有 Leader 对外通信（返回结果给用户）
  Follower 只复制事件、更新状态
  反向代理隐藏多机集群，持有用户 TCP 连接，路由到当前 Leader
```

**一致性读**：把读请求作为 no-op 命令发到写节点，通过共识排序 → 满足自读自写和单调读。

**分库后的协调者**：跨节点事务（x→y转账）需要 2PC/TCC，协调者是单点 → 用共识复制协调者的操作日志（WAL）到多机实现容灾——这就是分布式数据库的构建方式。

---

## 📌 八、运行时动态分库（课程第19讲）

**核心洞察**：快照处理存量数据 + 命令队列处理增量 + 状态机重算实时一致（事件溯源思路）。

```
分库前：
  新增集群B（本身是共识集群）
  协调者记录分库阶段、发命令
  B 先复制 A 的最新快照，学习快照位置，成为 A 的读模式节点尾随新命令

分库中：
  协调者发"开始分库"命令给 A
  A 把"分库命令"写进命令队列 → 复制到容灾节点 → 自动机执行 → 改内部配置
  A 永不宕机；属于 B 的消息返回重定向错误
  B（读模式节点）也执行同一个分库命令 → 更新配置

分库后：
  上游收到重定向错误 → 学习新路由
  下游被动（协调者推送）或主动（解析分库命令）获知
```

**面试要点**：分库命令**几乎与业务无关**（只声明归属，不含数据格式），重复执行幂等（副作用只是改配置）。

---

## 📌 九、账务系统优化与对账

### 1. 账务系统 = 余额状态机

```
账务系统的自动机状态 = 余额的 K/V 表

合并存储：每个事件和变动前后的余额存一起（按用户时间线）
  → 历史查询 = 找查询时间最近的事件，无需快照
```

### 2. 对账优化——相邻余额串联（区块链思想）

**两个不变量**：
1. 前一个事件的最终余额 == 后一个事件的初始余额
2. 最终余额 == 初始余额 + 变动

把用户相邻余额变动 + 每个事件的前后余额连成**链条** → 走一遍链条就能验证任意状态。

**本质**：这就是区块链的思路——区块链只是用去中心化共识构建了这个串联。

---

## 📌 十、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **金融系统正确性怎么保证？** | 事前（输入校验/版本号）+ 事中（函数式/精度）+ 事后（不同环境验证/对账） |
| **TCC 和 2PC 区别？** | 2PC 数据库层强一致但阻塞；TCC 业务层补偿（Try/Confirm/Cancel），性能好但业务侵入大 |
| **TCC 怎么处理消息乱序？** | 空回滚 + 记录空回滚 + 防悬挂三个硬性要求 |
| **CAP 正确理解？** | 不是三选二，是网络分区时只能选 CP 或 AP；C 实指线性一致性 |
| **线性一致性是什么？** | 所有操作可重排成全序，看起来像原子完成 |
| **FLP 不可能原理？** | 完全异步 + 一个机器可能故障 → 无共识算法；靠超时打破完全异步 |
| **Raft 在金融里干嘛？** | 全序广播（记账顺序）、线性化存储（分布式锁）、复制状态机 |
| **事件溯源为什么复制事件不复制命令？** | 命令→事件可能随机，两个Leader会推演出不同事件；复制事件保证全序 |
| **动态分库怎么不停机？** | 快照存存量 + 命令队列存增量 + 状态机重算，A 永不宕机 |
| **对账怎么做？** | 相邻余额变动连成链条（前事件终余额==后事件初余额），走链验证 |
| **轧差是什么？** | 银行白天只记账，晚上算净额只转一笔，减少真实资金移动 |

---

> 📝 **课程精华**：这门课教的不只是技术，更是一种"正确性思维"。面试时如果你能讲出"金融系统事后要用对账兜底"、"CAP 不是三选二而是分区时二选一"、"事件溯源复制事件而非命令"，就能和普通候选人拉开差距。核心知识可迁移到任何高要求分布式系统设计。
