# 即时消息（IM）技术剖析与实战 — 面试备战手册

> 讲师：袁武林（新浪微博） | 来源：极客时间
> 本文档整理自课程全部 22 讲 + 开篇 + 结束语，涵盖：基础原理 → 场景实战 → 进阶架构

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| IM 系统整体架构（接入层、业务层、存储层） | 01 | ⭐⭐⭐⭐⭐ |
| 消息实时性：短轮询 vs 长轮询 vs WebSocket vs TCP长连接 | 03 | ⭐⭐⭐⭐⭐ |
| 消息可靠投递：ACK + 超时重传 + 去重 + 完整性检查 | 04 | ⭐⭐⭐⭐⭐ |
| 消息序号生成器与消息有序性（全局序号 vs 分布式ID） | 05 | ⭐⭐⭐⭐ |
| 心跳机制：TCP Keepalive vs 应用层心跳 vs 智能心跳 | 08 | ⭐⭐⭐⭐ |
| HttpDNS 与 TLS 传输安全 | 06 | ⭐⭐⭐⭐ |
| 消息未读数：分布式锁 / Redis事务 / Lua原子化 | 07 | ⭐⭐⭐⭐ |
| 多终端漫游：版本号同步机制 | 09 | ⭐⭐⭐⭐ |
| 直播高并发：扇出优化、微服务拆分、自动扩缩容 | 10 | ⭐⭐⭐⭐⭐ |
| 流控与熔断：漏桶 vs 令牌桶、Hystrix | 12 | ⭐⭐⭐⭐ |
| 万人群聊：读扩散、合并变更、离线优化 | 20 | ⭐⭐⭐⭐⭐ |
| 多媒体消息：分片上传、CDN加速、秒传、边下边播 | 14/15 | ⭐⭐⭐⭐ |
| APNs 与 Android 厂商通道 | 16 | ⭐⭐⭐ |
| HTTP Tunnel 与通道高可用 | 13 | ⭐⭐⭐ |
| 多级缓存架构（本地+L1+主从） | 17 | ⭐⭐⭐⭐ |
| Docker 容器化与水平扩展 | 18 | ⭐⭐⭐⭐ |
| 端到端 Trace（Dapper/Zipkin） | 19 | ⭐⭐⭐⭐ |

---

## 📌 二、IM 系统基础架构

### 1. 完整 IM 系统的构成

#### 👤 使用者视角
- **用户账号**：唯一标识 + 头像/昵称
- **账号关系**：好友/互粉等关系链
- **联系人列表**：好友列表或聊天对象列表
- **消息**：聊天互动产生的核心数据
- **聊天会话**：双方消息记录的聚合展示

#### 🏗️ 开发者视角

```
┌─────────────────────────────────────────────────────────┐
│                    客户端 (Client)                        │
│        用户收发消息的终端设备，与服务端网络通信              │
└────────────────────┬────────────────────────────────────┘
                     │  (TCP长连接/WebSocket)
┌────────────────────▼────────────────────────────────────┐
│                   接入服务 (Access Layer)                  │
│  ● 连接保持  ● 协议编解码 (Protobuf等)                     │
│  ● Session维护 (UID ↔ TCP连接映射)                      │
│  ● 消息推送                                              │
└────────────────────┬────────────────────────────────────┘
                     │  (RPC / 消息队列)
┌────────────────────▼────────────────────────────────────┐
│                 业务处理服务 (Business Layer)               │
│  ● 消息存储  ● 未读数变更  ● 最近联系人更新               │
│  ● 内容过滤/安全识别  ● 消息路由                           │
└─────────┬──────────────────────────────┬────────────────┘
          │                              │
┌─────────▼──────────┐     ┌─────────────▼──────────────┐
│     存储服务         │     │     外部接口服务             │
│  ● 账号/关系链/消息   │     │  ● APNs (iOS)              │
│  ● 消息内容/索引表    │     │  ● GCM/厂商通道 (Android)   │
│  ● 联系人列表        │     │  ● 统一推送                 │
└────────────────────┘     └────────────────────────────┘
```

#### 🔑 **关键面试题：为什么接入服务和业务处理服务要独立拆分？**

| 原因 | 说明 |
|------|------|
| **稳定性隔离** | 接入层保持长连接稳定，不随业务频繁变更重启；业务层迭代频繁，避免导致连接断连重置 |
| **职责分离** | 接入层负责网络通信（稳定性、编解码），业务层专注业务逻辑，降低开发门槛 |

---

### 2. IM 系统四大核心特性

| 特性 | 要求 | 实现手段 |
|------|------|---------|
| **实时性** | 消息毫秒级到达 | 轮询→长轮询→WebSocket/TCP长连接 |
| **可靠性** | 不丢消息、不重复 | ACK机制 + 超时重传 + 去重 + 完整性检查 |
| **一致性** | 多用户/多终端顺序一致 | 全局序号生成器 + 本地整流 |
| **安全性** | 传输/存储/内容安全 | HttpDNS + TLS + 端到端加密 + 敏感词识别 |

---

## 📌 三、消息收发架构详解

### 1. 消息存储设计

#### 三张核心表

```
消息内容表 (IM_MSG_CONTENT)
┌──────┬──────────┬───────────┬─────────┬─────────────┐
│ mid  │ content  │ sender_id │ msg_type│ create_time  │
├──────┼──────────┼───────────┼─────────┼─────────────┤
│ 1001 │  你好    │  张三      │  文本   │ 2024-01-01  │
└──────┴──────────┴───────────┴─────────┴─────────────┘

消息索引表 (IM_MSG_RELATION)
┌──────────┬──────────┬──────┬──────┬─────────────┐
│ owner_uid│ other_uid│ mid  │ type │ create_time  │
├──────────┼──────────┼──────┼──────┼─────────────┤
│  张三    │  李四    │ 1001 │ 发件 │ ...         │  ← 发件箱索引
│  李四    │  张三    │ 1001 │ 收件 │ ...         │  ← 收件箱索引
└──────────┴──────────┴──────┴──────┴─────────────┘

最近联系人表 (IM_MSG_CONTACT)
┌──────────┬──────────┬──────┬──────┬─────────────┐
│ owner_uid│ other_uid│ mid  │ type │ create_time  │
├──────────┼──────────┼──────┼──────┼─────────────┤
│  张三    │  李四    │ 1001 │ ...  │ ...         │  ← 只存最新一条消息ID
└──────────┴──────────┴──────┴──────┴─────────────┘
```

**关键点**：收发双方各自有自己的索引记录（发件箱/收件箱），删除不影响对方。

#### 🔑 **面试题：消息内容表和索引表为什么要分离？**
- 减少冗余存储，内容只存一份
- 索引表支持按用户维度查询，内容表按消息ID查询
- 分库时可分别按不同维度分片

#### 🔑 **面试题：为什么需要单独的联系人表？**
- 消息索引表用于查询历史聊天记录（会话维度）
- 联系人表用于查询某人的所有最近联系人（用户全局维度）
- 联系人表是更新操作，消息索引表主要是插入操作

### 2. 消息收发通道

```
发送方 ──[发送通道]──▶ IM服务端 ──[接收通道]──▶ 接收方
                           │
                    ┌──────┴──────┐
                    ▼             ▼
                 消息存储       未读数变更
                           │
                    ┌──────┘
                    ▼
              第三方系统通道 (APNs/厂商通道)
```

**发送通道**：HTTP API 或 TCP 长连接
**接收通道**：TCP/WebSocket 长连接（服务端推送）

---

## 📌 四、消息实时性（高频面试点）

### 三种技术方案的演进

```
                   实时性                                 资源消耗
    ┌──────┐  ┌──────┐  ┌──────────┐  ┌───────────┐
    │短轮询│→│长轮询│→│WebSocket│→│TCP私有协议│
    └──────┘  └──────┘  └──────────┘  └───────────┘
     低      中         高              高
     高      中         低              低
```

| 方案 | 工作原理 | 优点 | 缺点 |
|------|---------|------|------|
| **短轮询** | 客户端定期高频请求服务端 | 实现简单，迁移成本低 | 大量无用请求，费电费流量，服务端QPS压力大 |
| **长轮询** | 无新消息时服务端挂起请求，等待超时或有消息才返回 | 降低无效轮询，减少网络开销 | 服务端资源仍高，有1000个请求等待就有1000个线程在轮询 |
| **WebSocket** | 单个TCP连接全双工通信，服务端可主动推送 | 真正边缘触发，控制开销低，Web原生支持 | 需要浏览器支持HTML5 |
| **TCP长连接协议** | 维护UID↔TCP连接映射，服务端通过映射推送 | 实时性最好，私有协议高效安全 | 开发成本高，心跳维护复杂 |

#### 🔑 **面试题：TCP长连接如何保证能准确找到用户对应的连接？**
- 用户上线时建立TCP连接，服务端维护 `UID → (网关机IP, TCP连接句柄)` 的映射关系
- 消息推送时通过中央在线状态服务（如Redis）查询接收方所在的网关机
- 有状态连接可以通过 Session 机制追踪

---

## 📌 五、消息可靠投递

### 1. ACK 机制

```
IM服务端 ──推送消息(SID)──▶ 客户端B
    │                          │
    │◀─── 返回ACK包(SID) ─────│
    │                          │
    ▼
从"待ACK列表"删除此消息
```

**核心流程**：
1. 服务端推送消息时携带唯一 SID（Sequence ID）
2. 消息加入"待 ACK 消息列表"
3. 客户端接收成功后回 ACK 包（携带 SID）
4. 服务端收到 ACK 后从待 ACK 列表删除

### 2. 超时重传与去重

```
服务端推送 msg1 (SID=1) ──▶ 客户端B
    │  ┌─ ACK包丢失 ─┐      │ (已收到)
    │  │              │      │
    ▼  └── 超时 ──────┘      │
服务端重传 msg1 (SID=1) ──▶ 客户端B
                               │ (去重：SID=1已存在)
                               ▼
                           去重后不展示
```

### 3. 消息完整性检查（兜底机制）

```
用户上线 ──(携带本地最新时间戳 timestamp1)──▶ IM服务端
    │                                            │
    │                                            ├─ 比对服务端最新时间戳
    │                                            ├─ 查出 timestamp1~timestamp2 的消息
    │◀────────── 补推丢失的消息 ──────────────────│
    │                                            │
    更新本地时间戳为 timestamp2
```

**注意**：实际用全局自增序列号替代时间戳，避免多机器时钟不同步问题。

#### 🔑 **面试题：有了 TCP 的 ACK 为什么还需要业务层 ACK？**
- TCP ACK 只保证数据到达内核态缓冲区，不保证应用层处理成功（如写入DB失败）
- TCP ACK 只保证单个包的确认，业务层需要确认整条消息语义的成功接收
- 业务层 ACK 能实现更灵活的重试和去重策略

---

## 📌 六、消息有序性（时序一致性）

### 1. 时序基准的选择

| 候选方案 | 问题 |
|---------|------|
| 发送方本地时钟 | 用户可随意调整时钟，多发送方时钟不同步 |
| 发送方本地序号 | 重装应用序号清零 |
| IM服务器时钟 | 集群化部署，NTP同步仍有毫秒级误差 |
| **全局序号生成器** | **推荐方案** |

### 2. 全局序号生成器

**实现方式**：
- **Redis incr**：原子自增，但单点瓶颈，可用性风险
- **DB自增ID**：单点部署，性能受限
- **Snowflake算法**：分布式时间相关ID生成，秒级精度（如微博）

**优化策略**：
- 群聊场景下按群维度独立 ID 生成器，避免跨群全局有序的硬要求
- 业务上可接受秒级内的细微乱序（用户无感知）
- 微信：每个用户独立空间的递增版本号（保证递增，不保证连续）

### 3. 服务端包内整流（严格有序场景）

```
生产者：packageID + seqID(自增)
消费者：按 packageID + seqID 整流 → 超时内有序收到才执行
```

### 4. 客户端本地整流

```
下推消息时携带序号 → 接收方比较序号
  → 序号正常：追加到会话
  → 序号跳跃：往前查找恰当位置插入
```

#### 🔑 **面试题：消息序号生成器为什么可以不是全局递增的？**
- 群聊不需要跨多个群的绝对时序性，只需要保证某个群内有序
- 业务可接受秒级内的细微误差
- 按群/用户维度独立序列空间，可大幅降低并发压力

---

## 📌 七、安全性

### 1. 传输安全

#### HttpDNS（防 DNS 劫持）

| 问题 | 解决方案 |
|------|---------|
| 路由器DNS被篡改 | 重置路由、改默认密码 |
| 运营商 LocalDNS 劫持 | **HttpDNS**：绕过LocalDNS，HTTP协议直连DNS服务器 |
| DNS 跨网调度错误 | HttpDNS 获取真实出口IP，精准调度 |

#### TLS 传输层加密

```
非对称加密 + 密钥交换算法 → 保护对称加密密钥
对称加密 → 加密消息内容
数字签名 + CA认证 → 防止身份伪造
```

**缺点**：TLS 握手比 TCP 多 1-2 个 RTT，有性能开销。

### 2. 存储安全

- **密码存储**：高强度单向散列算法（SHA/MD5）+ 每个账号独立"盐"值
- **消息内容**：**端到端加密（E2EE）**—— 收发双方各自生成密钥对，私钥不交给服务端
  - 典型案例：WhatsApp、Telegram
  - 国内 QQ/微信 因监管要求未采用

### 3. 内容安全

| 手段 | 用途 |
|------|------|
| 敏感词库 | 文字内容安全识别 |
| 图片识别 | 色情/广告/涉政图片识别 |
| 语音转文字 + OCR | 语音和图片文本挖掘 |
| 外链爬虫 | 分析风险链接 |

---

## 📌 八、消息未读数

### 1. 两个核心概念

| 概念 | 说明 | 存储方式 |
|------|------|---------|
| **会话未读** | 当前用户和某一聊天方的未读数 | 按会话维度存储 |
| **总未读** | 当前用户所有未读消息总和 | 独立存储（高频使用） |

### 2. 一致性问题与解决方案

#### 问题场景
```
场景1：加会话未读成功 + 加总未读失败 → 用户不知有新消息
场景2：清未读操作被加未读操作覆盖 → 有未读提示但找不到会话
```

#### 三种解决方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **分布式锁** | 修改前加锁，变更后解锁 | 普适性好 | 吞吐降低，锁管理复杂 |
| **Redis事务** | WATCH+MULTI+EXEC | 实现简单 | 乐观锁高并发下失败率高 |
| **Redis+Lua** | Lua脚本原子化执行多条语句 | 性能好，复杂逻辑支持 | 需学习Lua，注意不能有耗时操作 |

```lua
-- 原子化变更未读示例
redis.call('HINCRBY', KEYS[1], KEYS[2], 1)  -- 会话未读+1
redis.call('INCR', KEYS[3])                   -- 总未读+1
```

---

## 📌 九、心跳机制

### 1. 为什么需要心跳？

| 目的 | 说明 |
|------|------|
| **降低服务端无效连接** | 及时清理已断开的连接、映射关系、设备信息 |
| **客户端断线重连** | 连续心跳超时 → 客户端主动重连 |
| **防NAT超时** | 运营商定期清除无数据收发的NAT映射表，心跳保活 |

### 2. 三种实现方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **TCP Keepalive** | 系统协议栈实现，默认2h+重试9次+75s超时 | 无需开发，无额外数据 | 灵活性差，不能判断应用层可用 |
| **应用层心跳** | 客户端定时发业务层数据包 | 灵活设置间隔，反映应用可用性 | 少量额外数据开销 |
| **智能心跳** | 根据网络环境动态调整心跳间隔 | 最大限度节约资源 | 实现复杂，确认阶段可能降低可用性 |

**业界实践**：WhatsApp 30s/1min，微信约 4.5min，微博 2min

#### 🔑 **面试题：心跳机制可以结合 TCP Keepalive 和应用层心跳一起使用吗？**
可以。TCP Keepalive 作为连接层的存活探测（防 NAT 超时），应用层心跳用于更精确地反映应用可用性，两者互补。

---

## 📌 十、多终端消息漫游

### 1. 两种实现方式

| 方式 | 原理 | 适用场景 |
|------|------|---------|
| **设备维度在线状态** | 多端同时在线，推送给所有登录设备 | 实时多端同步 |
| **离线消息存储** | 消息在服务端存储，设备上线时同步 | 离线设备上线拉取历史 |

### 2. 版本号同步机制

```
服务端维护每个用户的最新版本号
客户端维护本地最新版本号

用户上线 → 提交本地版本号
服务端比对 → 不一致则拉取增量消息/信令
客户端更新版本号

版本号特性：
- 每个用户独立序列空间
- 64位整数，唯一且递增
- 消息和信令都分配版本号
```

### 3. 关键问题与解决

| 问题 | 解决方案 |
|------|---------|
| 离线存储超限 | 降级到从联系人列表和索引表获取最近消息 |
| 离线存储写入失败 | 存储上一条版本号，通过版本号串联性检测 |
| 消息打包下推 | 多条消息合并 + 压缩 |
| 发送方设备版本号同步 | 下推一条仅携带版本号的单独消息 |

---

## 📌 十一、直播互动高并发

### 1. 核心挑战

```
点对点：  2人 × 每10秒一句 = 0.1 消息/秒
500人群： 500人 × (500/10)  = 25000 消息/秒
10万直播间：10万 × (10万/10) = 10亿 消息/秒（理论值）
```

### 2. 一条消息如何扩散给百万人？（两级扇出）

#### ❓ 直觉误区：百万连接 ≠ 百万次写

很多人以为"百万人收到一条消息 = 每条消息写一百万次"。实际上，核心设计是**一条消息只写入一次**，靠"两级扇出"扩散出去：

```
用户A发送弹幕
     │  （上行：业务处理层，压力小，每秒互动一般 <1000 条）
     ▼
业务逻辑层 ──只写一次──▶ 全局消息队列 (Pub/Sub)
                               │ 队列广播给所有网关机（几十台，量很小）
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
         网关机1            网关机2           网关机3
       本机维护：          本机维护：         本机维护：
       房间→连接映射       房间→连接映射      房间→连接映射
      （挂约2000连接）    （挂约2000连接）   （挂约2000连接）
              │                │                │
              ▼                ▼                ▼
         各自推给本机上该房间的用户连接
```

**扇出发生在两个位置**：
- **第一级** = 全局 MQ 把 1 条消息复制给 N 台网关机（N 只有几十台，很便宜）
- **第二级** = 每台网关机在进程内把自己挂的几千条连接逐一写出去
- 全程不查询任何外部状态服务

#### 为什么不用"精准投递"？

| 场景 | 做法 | 问题 |
|------|------|------|
| 小群（500人） | 查中央在线状态 → 找到每个接收人所在网关 → 只投给那几台 | 精准、省资源 |
| 直播间（10万+人） | 每条消息查一次在线状态 | **10万次查询/条消息**，中央在线状态直接成为瓶颈 ❌ |

所以直播场景**放弃精准投递**，改**全量广播给所有网关机**（网关机只有几十台，全量也便宜），由每台网关机本地判断"这个房间的用户有没有挂在我这"。

#### 在线状态本地化

- 用户进直播间时，每台网关机**本机**记录"这个房间有哪些连接在我这"
- 推送不再依赖中央在线状态 → **扇出从业务处理层推迟到网关层**
- 这份数据是**连接生命周期**维度的：连接断开即自动消亡，无需维护一致性

#### 数字感受（10万人直播间，50台网关机）

一条消息 = **1 次写 MQ + 50 次网关广播 + 每台网关 2000 次 socket 写**
发送（上行）压力很小，真正的瓶颈全在"扇出后的下推"，所以优化重点全在下推链路。

#### ⚠️ 现实约束：并不是"每条弹幕都推给百万人"

- 理论值：10万人每人每10秒一句 = **10亿条/秒**下推，任何系统都扛不住
- 服务端必然**限流 + 选择性丢弃**；且客户端每秒能处理几十条消息就是极限
- "全员可见"一般只给**主播公告、系统消息、礼物打赏**这类低频高价值消息
- 普通弹幕靠**抽样、合并、限流**后展示，观众看到的是服务端精选后的子集

#### 💡 从 10 万到百万，再往上扩

- 网关机水平扩容（无状态 + 本地状态，扩缩容容易），MQ 按房间 Hash 分片
- 入口调度服务按机器负载下发接入 IP，避免新旧机器负载失衡（见下文④）
- 存储侧仍是**读扩散**：一条消息按房间只存一份，用户靠"游标/进房时间"增量拉取（详见「万人群聊」一节）

### 3. 优化方案

#### ① 在线状态本地化

```
❌ 传统方案：每条消息查中央在线状态 → 10万次查询/条
✅ 优化方案：网关机本地维护房间在线状态
   1. 消息扇出到全局消息队列（所有网关机订阅）
   2. 每台网关机根据本地状态推送给本机上的用户
   3. 不再依赖中央在线状态
```

> 💡 完整的两级扇出过程、为什么不用精准投递、百万级扩展方案，见上文「**一条消息如何扩散给百万人？**」

#### ② 微服务拆分与隔离

```
核心服务（发弹幕、打赏、送礼、消息下推）
非核心服务（直播回放、第三方同步）

上行通道（发消息）↔ 下行通道（消息推送）独立隔离
```

#### ③ 自动智能扩缩容

```
收集业务指标（在线人数、QPS） + 机器指标（带宽、PPS）
→ 压测找瓶颈临界点
→ 设定阈值触发自动扩缩容
```

#### ④ 接入层全局调度

```
❌ 普通Round Robin → 新旧机器负载不均衡
✅ 入口调度服务 → 根据机器负载实时计算权重 → 负载低的优先接入
```

---

## 📌 十二、流控与熔断（服务高可用）

### 1. 流控算法对比

| 算法 | 原理 | 特点 | 适用场景 |
|------|------|------|---------|
| **漏桶算法** | 请求先入桶，桶以固定速率漏水 | 精确控制速率，平滑流量 | 需要严格限制访问速度 |
| **令牌桶算法** | 匀速往桶里放令牌，请求消耗令牌 | 允许突发流量，限制平均速率 | 有波动但需控制总量的场景 |

### 2. 全局流控实现

**Redis + Lua**：
```lua
local current = redis.call('get', KEYS[1])
if current and tonumber(current) >= tonumber(ARGV[1]) then
    return 0  -- 限流
end
redis.call('incr', KEYS[1])
redis.call('expire', KEYS[1], 1)
return 1
```

**优化技巧**：
- 滑动窗口 + 细粒度分桶 → 解决边界不平滑
- 本地批量预取 → 降低中央资源压力（如每次预取10个token，压力降到1/10）

### 3. 熔断机制

**雪崩效应**：一个依赖服务变慢 → 拖慢调用方 → 级联影响上层服务

**解决方案**：
- **手动降级**：通过开关控制
- **自动熔断**：Hystrix / Resilience4j（设置阈值，快速失败）

---

## 📌 十三、万人群聊系统设计

### 1. 存储优化：读扩散

```
点对点（写扩散）：
  一条消息 → 为收发双方各存一条索引

群聊（读扩散）：
  一条消息 → 只在群维度存一份
  用户查询时从群维度的消息索引中获取
```

| 要点 | 解决方案 |
|------|---------|
| 新用户只看加群后消息 | 加群时记录时间/最新消息ID，查询时过滤 |
| 单个用户删除消息 | 维护用户+群维度的"删除索引"，查询时聚合剔除 |

### 2. 未读数合并变更

```
加未读请求 → 按群ID归入暂存队列
  → Timer（定时1s）或 Flusher（队列满100）
  → Aggregator合并 → 针对每个用户将多次incr合并为一次
  → 写入底层资源
```

**注意**：存在一定延迟 + 掉电可能丢失部分操作

### 3. 离线优化

| 优化点 | 方案 |
|--------|------|
| 重复暂存 | 离线Buffer只存消息ID，下推时再获取内容 |
| 高并发ACK | 批量ACK（类似TCP Delay ACK），合并多个ACK包 |
| 在线下推 | 不维护全局在线状态，网关机本地自治 |

### 4. 网关机本地自治

```
群消息 → 业务层 → 全局消息队列
  → 每台网关机订阅
  → 查询本地在线用户映射
  → 下推给本机用户
```

---

## 📌 十四、多媒体消息优化

### 1. 上传优化

| 手段 | 说明 |
|------|------|
| **多上传接入点** | 不同运营商提供不同接入点，避免跨网 |
| **独立上传通道** | 与文本消息通道隔离，避免大文件阻塞 |
| **分片上传** | 文件分块并行上传，失败时只重传失败分片 |
| **分片大小自适应** | WiFi下2M，4G下1M，3G/2G下256K |
| **断点续传** | 分配唯一操作标识，服务端暂存已上传分片 |
| **秒传** | 先计算文件Hash（MD5/SHA-1），服务端已存在则免上传 |

### 2. 下载/播放优化

| 手段 | 说明 |
|------|------|
| **CDN加速** | 就近访问，降低延迟，缓解源站压力 |
| **CDN预热** | 提前强制CDN回源，避免高并发时命中率低 |
| **流加密（HLS）** | AES加密TS分片，鉴权通过才返回密钥 |
| **分辨率自适应** | 根据不同设备提供多种分辨率缩略图 |
| **WebP格式** | 比JPEG/PNG小30%，iOS需内置解析库 |
| **渐进式JPEG** | 从模糊到清晰加载，用户体验好，>10KB时压缩比更高 |
| **H.265转码** | 同画质比H.264节省50%空间，编码复杂度高10倍 |
| **边下边播** | 下载关键信息后即可开始播放，无需等全部完成 |
| **预加载** | 打开会话页自动预加载3s或512KB的视频流 |
| **长连接推送** | 语音流/缩略图通过长连接实时推送 |

---

## 📌 十五、第三方系统推送

### 1. APNs（iOS）

```
IM服务端 (Provider) ──[长连接+证书+DeviceToken]──▶ APNs
                                                      │
                                                      ▼
                                                  用户设备
```

**Payload 格式**（JSON，最大4KB）：
```json
{
  "aps": {
    "alert": {"title": "新消息", "subtitle": "来自：张三", "body": "你好"},
    "badge": 1,
    "sound": "default"
  }
}
```

**APNs 缺陷**：
- ❌ 不保证到达率，可能丢消息
- ❌ 可能出现分钟级延迟
- ❌ 设备离线时只保留最新一条消息（QoS机制）
- ❌ 角标不支持累计+1操作

### 2. Android 厂商通道

| 厂商 | 说明 |
|------|------|
| 小米/华为/vivo/OPPO/魅族 | 各自维护系统级推送通道 |
| 第三方Push服务 | 个推、信鸽、极光、友盟（统一接入多厂商） |
| **统一推送联盟（推必达）** | 工信部主导，支持运营商信令通道推送，到达率99.999% |

#### 🔑 **面试题：为什么有 APNs 还要自建长连接？**
APNs 可靠性无保障（可能丢消息、延迟高），自建长连接可以控制消息到达率，
APNs 一般作为自建连接不可用时的辅助通道。

---

## 📌 十六、HTTP Tunnel 与通道高可用

### 1. 通道连得上

| 问题 | 解决方案 |
|------|---------|
| 端口被限制 | 使用80/443/8080/14000等安全端口 |
| 协议不通（仅HTTP代理） | HTTP Tunnel：用HTTP封装私有协议 |
| DNS劫持/接入点故障 | HttpDNS + 预埋接入点IP列表 |

### 2. 通道连得快

| 手段 | 说明 |
|------|------|
| 多运营商机房 | 各运营商专线接入，避免跨网 |
| HttpDNS精准调度 | 获取真实出口IP，精准分配接入点 |
| **跑马竞速** | 客户端并发测速多个VIP，选最快的接入 |

### 3. 通道保持稳定

| 手段 | 说明 |
|------|------|
| 通道层与业务解耦 | 通道只负责连接管理，业务逻辑下沉 |
| 上下行通道隔离 | 上行短连 + 下行长连，避免互相影响 |
| 多媒体独立通道 | 图片/视频上传下载与核心消息通道分离 |

---

## 📌 十七、多级缓存架构

### 1. 缓存分布式算法对比

| 算法 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **取模求余** | Hash(key) % N | 实现简单 | 增删节点后命中率骤降（震荡） |
| **一致性哈希** | 环形空间，顺时针找最近节点 | 增删节点只影响局部 | 节点少时数据倾斜 |
| **一致性哈希+虚拟节点** | 物理节点虚拟出多个虚拟节点 | 解决数据倾斜 | 实现稍复杂 |

### 2. 多级缓存架构

```
用户请求 → 本地缓存（应用服务器内存）
            ↓ 未命中
         L1缓存（多组小容量，存极热数据）
            ↓ 未命中
         主从缓存（全量缓存，主写从读）
            ↓ 未命中
         DB存储
```

| 层级 | 容量 | 特点 | 问题解决 |
|------|------|------|---------|
| **本地缓存** | 极小 | 离应用最近，性能最好 | 带宽问题、成本问题 |
| **L1缓存** | 小 | 多组并行，热点数据 | 带宽问题、穿透问题 |
| **主从缓存** | 大 | 全量数据，主写从读 | 基础缓存能力 |

**本地缓存一致性方案**：短过期时间（如5s），平衡命中率和一致性。

---

## 📌 十八、水平扩展（Docker容器化）

### 1. 各层水平扩展方案

| 层级 | 瓶颈 | 解决方案 |
|------|------|---------|
| **接入层VIP入口** | 单LVS扛不住 | DNS轮询多VIP |
| **接入层服务** | 长连接量大 | 无状态化 + 中央在线状态解耦 |
| **业务层** | 处理能力 | 服务化改造 + 注册中心 + 服务发现 |
| **资源层** | 读写压力 | 分片(Sharding) + 多从库 |

### 2. Docker 容器化的价值

- 将运行环境打包到镜像，消除部署机器差异
- 快速分发部署，实现弹性扩缩容
- 典型场景：图片/视频转码服务的扩容（依赖工具多，传统部署困难）

---

## 📌 十九、端到端 Trace 监控

### 1. 核心概念（Dapper论文）

| 概念 | 说明 |
|------|------|
| **Trace** | 一次请求的完整调用链，全局唯一 TraceID |
| **Span** | 两个服务间的请求/响应过程，有父子关系 |
| **Annotation** | 自定义事件，可携带KV键值对（如房间ID、UID） |

**四个时间戳**：
- Client Send → Server Receive → Server Send → Client Receive

### 2. 常见 Trace 系统

| 系统 | 公司 |
|------|------|
| Zipkin | Twitter |
| Jaeger | Uber |
| 鹰眼 | 阿里 |
| Mtrace | 美团 |

### 3. 主动探测监控（回环探测）

```
机房1 探测程序(UID1) ──发消息 N──▶ 机房2 探测程序(UID2)
                                   ↓
                ←──回消息 N+1───
```
- 检测本机房API可用性
- 通过连续数字检测对方机房可用性
- 弥补被动监控在服务不可用时数据上报失败的问题

---

## 📌 二十、期中/期末实战要点

### 期中实战（简易IM系统）
- **技术栈**：Java + HTTP短轮询 + MySQL + Redis
- **核心功能**：文本聊天、未读数（总未读+会话未读）、联系人自动更新
- **核心表**：IM_USER / IM_MSG_CONTENT / IM_MSG_RELATION / IM_MSG_CONTACT
- **未读存储**：Redis（K-V存总未读，Hash存会话未读）

### 期末实战（升级IM系统）
- **升级点**：HTTP短轮询 → WebSocket长连接
- **新增功能**：ACK机制、应用层心跳、idle超时断连、断线自动重连
- **服务端框架**：Netty（Java NIO）
- **WebSocket 客户端**：HTML5原生API

---

## 📌 二十一、面试必问核心问题汇总

### 架构设计类
1. **画一下 IM 系统的整体架构图？**
2. **为什么要拆分接入层和业务层？**
3. **消息存储为什么分内容表和索引表？**
4. **一条消息从发送到接收的完整生命周期是怎样的？**

### 实时性与可靠性类
5. **短轮询、长轮询、WebSocket、TCP长连接的区别和演进？**
6. **ACK 机制如何保证消息不丢？超时重传如何解决重复问题？**
7. **有了 TCP ACK 为什么还需要业务层 ACK？**
8. **心跳机制的原理和实现方案有哪些？**

### 一致性类
9. **如何保证消息的时序一致性？**
10. **全局序号生成器有哪些实现方式？各有什么优缺点？**
11. **消息未读数为什么不一致？如何解决？（分布式锁/Redis事务/Lua）**

### 高并发类
12. **百万人在线的直播间，一条消息如何扩散给所有人？为什么不能逐人精准推送？**（两级扇出 + 在线状态本地化）
13. **直播互动场景如何应对高并发？**
14. **万人群聊消息存储为什么要用读扩散？**
15. **未读数合并变更的原理是什么？**
16. **流控的漏桶和令牌桶有什么区别？**

### 缓存与扩展类
16. **一致性哈希如何解决缓存节点增减的命中率问题？**
17. **多级缓存架构（本地+L1+主从）是如何设计的？**
18. **IM 系统各层如何实现水平扩展？**

### 安全与推送类
19. **HttpDNS 解决了什么问题？**
20. **端到端加密（E2EE）的原理是什么？**
21. **APNs 有哪些缺陷？为什么还要自建长连接？**
22. **Android 统一推送联盟是什么？**

---

> 📝 **最后建议**：面试时不仅要会回答原理，更要能结合场景讲出"为什么这样设计"，比如"微博/微信/Telegram在XX场景下是怎么做的"。课程中大量来自微博的真实踩坑和优化经验（通道拆分、notify-pull、智能心跳等）是让你脱颖而出的亮点。
