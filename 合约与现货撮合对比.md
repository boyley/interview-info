# 合约与现货撮合对比 — 合约撮合的"加了什么"

> 面试高频题："你做过合约，合约撮合和现货撮合有什么区别？"
> 一句话主线：**撮合核心(订单簿、价格时间优先)一样，合约在"撮合之前"和"撮合之后"多了整套保证金/方向/强平/结算体系**。
> 说明：本仓库的撮合引擎是独立外部服务(消费 RocketMQ)，本文既讲"本仓库里合约特有的下单校验/账户逻辑"，也补全"订单簿撮合引擎的标准设计"，让你能完整讲。

---

## 📌 一、本质区别一句话

> **现货撮合：用余额买资产，撮合完就结束了。**
> **合约撮合：用保证金撬动名义价值，能开多开空，撮合只是开始——之后还有持仓、保证金率、爆仓、资金费、结算。**

| 维度 | 现货(币币) | 合约(期货) |
|------|-----------|-----------|
| 交易对象 | 币(买 BTC 卖 USDT) | **合约张数**(面值 × 张数，如 1 张=1 USD) |
| 能不能做空 | 不能(只能先买后卖) | **能开多也能开空** |
| 资金占用 | 全额：买 100U 的币要 100U | **保证金**：100U 名义价值只要 10U(10 倍杠杆) |
| 仓位状态 | 无"持仓"概念，余额即仓位 | 有 CoPosition(方向/均价/保证金/盈亏) |
| 下单字段 | 数量 + 价格 | 数量 + 价格 + **开/平 + 全/逐 + 杠杆 + 方向** |
| 撮合完 | 币+钱互转，结束 | 开仓→挂持仓；平仓→配对已有持仓 |
| 后续事件 | 无 | **资金费、盯市结算、强平、穿仓分摊** |
| 风险 | 最多亏完本金 | **可能亏穿(负债)，需要爆仓机制兜底** |

---

## 📌 二、订单模型多了什么(CoOrder vs 现货订单)

```
现货订单:  symbol + price + volume + side(buy/sell) + type
合约订单:  symbol + price + volume + side + type
        + open(开仓/平仓)          ← 开平方向
        + positionType(全仓/逐仓)   ← 保证金模式
        + leverageLevel(杠杆倍数)   ← 杠杆
        + 4个费率(open/close×taker/maker) ← 开平仓手续费分开
        + source(6=爆仓单)          ← 强平单特殊标记
```

**订单类型**：合约除了 LIMIT/MARKET，还有 **IOC(立即成交否则取消)、FOK(全成或全撤)、POST_ONLY(只挂不吃)**、爆仓单——现货一般只有限价/市价。

---

## 📌 三、开平仓 × 买卖 = 四种组合

现货只有 `BUY`(买币)和 `SELL`(卖币)，且 SELL 前必须有币。
合约是 **open/close × buy/sell** 四种：

| 组合 | 含义 | 仓位影响 |
|------|------|---------|
| OPEN + BUY | 开多(做多) | 新增/增加 BUY 仓位 |
| OPEN + SELL | 开空(做空) | 新增/增加 SELL 仓位 |
| CLOSE + SELL | 平多 | 减少 BUY 仓位 |
| CLOSE + BUY | 平空 | 减少 SELL 仓位 |

仓位按 `uid + contractId + side` 分多/空两条；**净持仓模式**禁止同时持有多和空。

**平仓校验(现货没有的)**：平仓量 ≤ 可平量(`volume − pendingCloseVolume`)，防止超卖/重复平仓——用 `updatePendingClose` 把平仓占用挂在仓位上。

---

## 📌 四、撮合引擎本身的设计(标准订单簿撮合)

虽然引擎不在本仓库，但架构设计是明确的，面试照这个讲：

```
撮合引擎(独立服务, 单线程无锁确定性核心)
  输入: FUTURES_ORDER_{symbol}(RocketMQ, 每合约单队列 → 串行)
  内部: 订单簿 = 价格优先 + 时间优先
        买盘降序(最高价优先), 卖盘升序(最低价优先), 同价先到先得
  输出: 成交回报 FUTURES_MATCHING_TRADE_{symbol} + 写共享库(订单/成交/持仓)
```

**为什么用 MQ 单队列保证同合约串行？**
- 撮合必须**确定性可重放**(价格/时间优先, 顺序即公平)，多线程引入锁和不确定性
- 每合约一个 topic + `setDefaultTopicQueueNums(1)` → 同合约消息天然保序、串行消费
- 不同合约不同 topic → **按合约横向扩展**(每个合约一个撮合线程/集群)
- 业务服务(下单/校验/落库)可以多实例水平扩展，和撮合解耦互不拖累

> 现货撮合引擎如果做过，架构完全一样：**订单簿 + 价格时间优先 + 单线程/单交易对**。合约和现货的撮合"引擎本身"没区别，区别全在**撮合前(校验/保证金)和撮合后(持仓/爆仓/结算)**。

---

## 📌 五、撮合前：合约特有的校验(本仓库核心逻辑)

现货下单基本只验余额/精度；合约下单要过一整层风控校验(`ValidateServiceImpl`):

| 校验 | 说明 |
|------|------|
| 杠杆区间 | 是否在合约配置允许的杠杆档位内 |
| 数量/金额精度 | 精度、最小/最大下单限制 |
| 持仓上限 | 不能超过该合约最大持仓 |
| **可用保证金** | 全仓可用 = 余额 − 全仓占用；不足拒单 |
| **杠杆×价值上限** | 挂单总价值 ≤ 当前杠杆允许最大价值(`maxHoldAmount`) |
| **强平价防线** | **平仓限价单不得越过强平价格**——平多价低于强平价、平空价高于强平价直接拒单 |
| 市价单金额范围 | 市价开仓 volume=金额，校验金额能扫到的盘口档位不偏离最新价(防插针) |

> 第 6 条是合约最经典的差异：现货想多少钱卖都行，合约**不允许你挂"肯定强平价以下"的平仓单**——否则用户先爆仓、平台要用更高价买回，把亏损转嫁给平台。

### 保证金冻结(撮合前)

```
限价开仓: 冻结额 = volume × price × multiplier × marginRate / leverage
市价开仓: 冻结额 = volume(金额) × marginRate / leverage
流程: U_MARGIN_BALANCE(可用) → U_MARGIN_LOCK(冻结)
成交后: U_MARGIN_LOCK → U_POSITION_LOCK(仓位保证金)
平仓后: 解冻回 U_MARGIN_BALANCE
```

---

## 📌 六、撮合后：合约的"后续事件"(现货没有)

| 事件 | 触发 | 做什么 |
|------|------|--------|
| **盯市结算** | 按 settlementFrequency(分钟)定时 | 用标记价把未实现盈亏落账为已实现 |
| **资金费** | 按 capitalFrequency(小时)定时 | 多付空收，平衡多空持仓(永续合约替代交割的机制) |
| **爆仓强平** | 轮询线程扫描保证金率 | 低于维持线 → 冻结 → 阶梯减仓 → 系统接管 |
| **穿仓分摊** | 定期结算 | 系统接管仓亏损由风险准备金 + 盈利用户分摊 |
| **条件单** | 成交回报驱动 | 止损/止盈/追涨杀跌自动下单 |

这些决定了**合约撮合系统的复杂度不在撮合，而在撮合两侧**——这是最值得在面试里强调的观点。

---

## 📌 七、几个"现货人"最容易说错的点

1. ❌ "合约就是带杠杆的现货" → ✅ **合约是多/空 + 保证金 + 爆仓 + 资金费/结算的完整体系，杠杆只是其中一环**
2. ❌ "市价开仓数量是张数" → ✅ **市价开仓的 volume 是金额**，由撮合按盘口深度换算张数
3. ❌ "强平就是直接市价卖" → ✅ **强平先按接管价记账转给系统，再阶梯减仓、系统接管仓位市价平掉**
4. ❌ "撮合引擎是业务服务的一部分" → ✅ **撮合是独立确定性核心，业务用 MQ 异步投递、按合约单队列串行**
5. ❌ "维持保证金率是固定的" → ✅ **梯度档位，仓位越大维持保证金率越高**

---

## ✅ 使用检查清单

- [ ] 能一句话说出合约 vs 现货的本质区别(撮合同、两侧异)
- [ ] 能画出四种 开/平×买/卖 组合和仓位映射
- [ ] 能讲清合约下单比现货多了哪些校验(尤其强平价防线、可用保证金)
- [ ] 能讲清 MQ 单队列保证同合约串行撮合的设计
- [ ] 能列出撮合后合约特有的"后续事件"(结算/资金费/爆仓/分摊)
- [ ] 能纠正上面 5 个常见误区
