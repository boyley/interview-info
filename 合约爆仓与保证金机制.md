# 合约爆仓与保证金机制 — 从触发到分摊的全流程拆解

> 模块：`futures-liquidation`(爆仓) + `futures-settlement`(穿仓分摊)
> 面试最值钱的环节，能完整讲清"爆仓怎么设计"的候选人很少。
> 一句话主线：**轮询扫描 → 算保证金率 → 低于维持线 → 冻结撤单 → 阶梯减仓 → 系统接管 → 接管仓位市价平掉 → 亏损由风险准备金+盈利用户分摊**。

---

## 📌 一、保证金体系(爆仓的地基)

### 1. 全仓(Cross) vs 逐仓(Isolated)

| 维度 | 全仓(Cross) | 逐仓(Isolated) |
|------|-------------|----------------|
| 保证金范围 | 整个账户(同保证金币种所有全仓仓位共担) | 单笔仓位独立保证金 |
| 保证金率 | (总权益 − 其他合约维持保证金) / 全部仓位价值 / 汇率 | (本仓 holdAmount + 盈亏) / 本仓价值 / 汇率 |
| 爆仓影响 | 全部全仓仓位一起处理 | 只处理该仓 |
| 用户感受 | 高风险高杠杆，资金利用率高 | 单仓止损，风险可控 |

### 2. 梯度档位(维持保证金率，Ladder)

```
ConfigLadder: [ level, minPositionValue, maxPositionValue, minMarginRate ]
```

- 按**仓位价值区间**分段，仓位越大、档位越高、**维持保证金率越高**(币安/OKX 同款设计)。
- 例：0~5万USDT 档维持 0.4%；5万~10万档 0.5%…仓位越大，越靠近爆仓线就越早触发减仓。
- 目的：**仓位越大，每波动 1% 的绝对亏损越大，平台风险越大，所以要求更高的保证金缓冲**。
- 杠杆配置 `ConfigLever`:杠杆越高 → 可开最大持仓越小。

---

## 📌 二、爆仓触发判定(最核心的公式)

### 1. 判定阈值

```
健康: currentRate >  minMarginRate + closeTakerFee   → 不爆仓
爆仓: currentRate <= minMarginRate + closeTakerFee   → 触发强平流程

其中:
  minMarginRate = 该仓位实际价值命中的档位维持保证金率
  closeTakerFee = 平仓 taker 手续费(强平本身要付手续费, 一并计入缓冲)
```

> 为什么要 **+ closeTakerFee**？因为强平单是吃单(taker)，要预留出手续费，否则按维持线强平连手续费都不够付，会直接穿仓。

### 2. 保证金率计算

```
逐仓: rate = (holdAmount + 未实现盈亏 + 已实现盈亏) / 仓位价值 / 保证金汇率
全仓: rate = (总账户权益 − 其他合约维持保证金) / (全部实时仓位价值) / 保证金汇率
      总账户权益   = 全仓可用余额 + Σ未实现 + Σ已实现
      其他维持保证金 = Σ(其他同币种全仓仓位价值 × 其档位minMarginRate)
```

仓位价值(标记价)：正向 `volume×标记价×面值×汇率`；反向 `volume×面值/标记价×汇率`。

### 3. 预警线(提前提醒)

```
预警线 = minMarginRate × (1 + riskAlarm) + closeTakerFee   // riskAlarm 默认 0.3
```

达到预警线 → 发短信/邮件/推送提醒用户加保证金(`SendRiskTipUtils.sendAlarmTip`)。

---

## 📌 三、扫描机制(谁来发现该爆仓的仓位)

**不依赖每笔成交回调，靠轮询线程扫全量用户**：

```
LiquidationThread × 35 (爆仓扫描)
  while(true):
    随机睡 0~300ms(错峰)
    → 取 PositionScanRecord(type=0) 游标
    → 拉一批用户(每批50, 按uid>游标)
    → Redis 分布式锁防并发(MANAGER_TASK_KEY+uidSum)
    → 过滤机器人白名单
    → 取该用户所有持仓, 按杠杆降序
    → 逐仓构造 CrossMode / IsolatedMode 服务 → doLiquidation()
    → 推进游标; 批不足则重置游标

PositionThread × 5 (强平单下单扫描)
  → 取 PositionScanRecord(type=1) 游标
  → 拉待平仓记录 LiquidationPositionRecord(status≠完成)
  → 多线程下单 placeOrders()
```

> 为什么轮询而不是逐笔？每笔成交都查一次保证金率对全仓判定太贵(全仓要聚合整个账户)，轮询 + 游标 + 分布式锁是"简单可靠"的选择；代价是强平有秒级延迟(可用 MQ 逐笔触发 + 轮询兜底双通道优化)。

---

## 📌 四、强平执行流程(doLiquidation 主流程)

全仓/逐仓各一个实现(`CrossModeLiquidationService` / `IsolatedModeLiquidationService`)，步骤一致：

```
① calculateRate() 算保证金率, 取标记价/价值/档位
② 健康 → 若之前冻结过则解冻, 返回
③ lockPosition() 冻结仓位        → freezeLock = 1(爆仓冻结)
   全仓: 冻结同币种所有全仓仓位; 逐仓: 只锁单仓
④ cancelOrders() 撤掉该用户所有未完成委托(防边撤边下)
   若还有挂单 → 本轮返回, 下一轮再处理
⑤ 撤单后重算保证金率, 若恢复健康 → 解冻返回
⑥ 全仓特有: selfTradePosition() 同合约多空仓内部对冲
   清算量=min(多仓,空仓), 清算盈亏=清算量×(空仓成本−多仓成本), 生成系统平仓单
⑦ liquidationLadderDeal1() 阶梯减仓循环(见下)
⑧ freePosition() 解冻
```

### 冻结与解冻

`lockPosition/freePosition` 批量 update `co_position.freeze_lock`。`PositionLockStatus`: 0正常 / 1爆仓冻结 / 2交割冻结 / 3减仓冻结。

### 系统接管仓位(关键设计)

减仓时不是直接卖给对手方，而是**先记到平台自己账上**：

```
forUpdatePosition()  (对仓位行 SELECT ... FOR UPDATE 加锁)
  ① realizedProfitLoss = 未实现盈亏(接管价 vs 持仓均价)
  ② 插入【系统接管仓位】: uid = 501 (LIQUIDATION_USER)
     avgPrice = openPrice = 接管价, 方向/杠杆/合约与用户仓位一致
  ③ 生成 LiquidationPositionRecord(待市价平仓的任务)
  ④ 更新用户仓位: volume -= 减仓量, closeVolume += , realizedAmount += 盈亏
```

**为什么要有系统接管仓位？** 让"强平瞬间的价格"和"实际平掉的价格"分离——强平按接管价记账(用户亏到接管价就出局)，平台后续用自己的接管仓位慢慢市价平掉，中间的滑点/价差由平台吸收(这就是爆仓手续费和风险准备金的来源)。

---

## 📌 五、阶梯减仓(部分强平 vs 全仓强平)

**不是一次全平，是逐级降档减仓**，尽量"少伤用户"：

```
positionUp() 计算本次减仓量:
  当前档位 level == 1(最低档) → 减仓量 = 全部仓位, isEnd = true(清仓)
  level > 1:
    超出价值   = 实际仓位价值 − 档位minPositionValue
    减仓张数   = 超出价值换算成张数
    千一减仓   = 档位minPositionValue × 0.1%(LIGHTEN_UP_PERCENTAGE), 且最小10张
    本次减仓量 = 超出部分 + 千一/最小10张

liquidationLadderDeal1() 死循环:
  减仓 → 重取仓位 → 重算保证金率
  恢复健康 → 退出
  否则 → 继续降档减仓, 直到仓位归零
```

全仓额外有**协助减仓**(`assistLightenUp`)：当前仓已到 level 1 还不够，就先去减同币种**其他更高档位**的仓位抬升整体保证金率；若其他仓也都是 level 1 → `liquidationAllUp` 全部平仓。

> 设计意图：**只减"多余的风险敞口"，保留用户的仓位**。用户权益跌破维持线越深，减的越多；跌回健康线就停，避免一刀切清仓。这就是"阶梯强平"。

---

## 📌 六、接管价(破产价)与强平单

### 1. 接管价(positionUp)——权益为 0 时的价格

```
正向: takeOverPrice = 标记价 − (账户权益/仓位价值量) / 保证金汇率
反向: takeOverPrice = 1 / (1/标记价 + 账户权益/保证金汇率/仓位价值量)
账户权益: 全仓按比例分摊总权益, 逐仓取保证金+盈亏
```

> 接管价 = "价格跌到这个位置，用户权益正好归零"的价格。**按接管价把仓位转给系统**，用户的亏损被锁定在接管价，之后的下跌由平台承担。

### 2. 强平单怎么下

```
① 记账单(直接落已成交): OrderService.saveOrder
   open=CLOSE, side=仓位反方向, type=LIMIT, status=FILLED, price=接管价,
   volume=减仓量, source=OrderSource.LIQUIDATION(6)
   —— 这单不真去撮合, 只是"记账", 把仓位转移给系统接管仓

② 真实平仓单(平系统接管仓位): PlaceOneOrderService
   先下 IOC 限价单(价=接管价, 向上取整到价格精度)
   失败 → 改下市价单
   部分成交 → 再补一张市价单
   uid=501(系统), source=6
   —— 系统把接管的仓位拿到市场上去真平, 承受滑点
```

> 注意 `type=6` 是**仅页面展示**，数据库实际存 IOC(3)/市价(2)/限价(1)，靠 `source=6` 识别爆仓单。爆仓单**免手续费**(uid=501 时四种费率置 0)。

---

## 📌 七、穿仓分摊 + 风险准备金(亏损兜底)

爆仓后可能出现**穿仓**：接管价都平不掉，系统仓位亏损(用户保证金已亏完，平台还得倒贴)。怎么兜？

```
定期结算 computePosition():
  ① 系统接管仓位(uid=501)的亏损 → 累加为穿仓金额 failAmount
  ② 普通用户若 权益+保证金<0(资不抵债) → 已实现盈亏截断为 -holdAmount, 剩余计入 failAmount
  ③ 若 failAmount < 0(真的穿仓):
     shareRate = 风险准备金余额>0 ? 1−lossSubsidy : 1
       (有风险准备金时, 准备金额度补贴 lossSubsidy 比例, 其余由用户分摊)
     分摊池 = 盈利用户按盈利倒序, 累计到盈利总额的 90%(highRate)
     每户分摊 = |failAmount| × shareRate × (该户盈利/分摊池)
  ④ 资金流水:
     系统仓位亏损 → 系统用户保证金 → 系统平仓中转
     系统仓位盈利 → 划入风险准备金(SETTLEMENT_RISK_IN, "爆仓盈利转入风险准备金")
     用户分摊     → 盈利用户 → 系统用户(SETTLEMENT_LOSS_SHARE)
     风险准备金补贴 → 风险准备金 → 用户(SETTLEMENT_RISK_OUT)
  ⑤ 分摊记录表 SettlementRiskRecord: 1准备金收益/2准备金支出/3用户分摊
```

> 没有 ADL(自动减仓/对手方分摊)。穿仓由 **风险准备金优先承担 + 盈利用户按比例分摊** 解决。这是和币安 ADL 不同的实现，面试主动讲清这个取舍 = 加分。

---

## 📌 八、和主流交易所(BitMEX/币安)的设计对照

| 设计点 | 本系统 | 币安/BitMEX |
|--------|--------|------------|
| 爆仓触发 | 轮询扫描线程(35个) | 逐笔 + 定时双重 |
| 强平方式 | 阶梯降档减仓 + 系统接管 | 阶梯减仓(Insurance Fund) |
| 穿仓兜底 | 风险准备金 + 盈利用户分摊 | **ADL 自动减仓**(对手方强平) + 保险基金 |
| 维持保证金 | 梯度档位 | 梯度档位(同款思路) |
| 强平价 | 接管价(破产价) | 破产价(bankruptcy price) |
| 标记价 | 指数价+资金费基差+EMA | 指数价 + 资金费基差(同款) |

---

## 📌 九、面试高频追问(结合源码答)

**Q1：爆仓线怎么定的？为什么还要加平仓手续费？**
> 爆仓线 = 维持保证金率(minMarginRate) + 平仓 taker 费。加手续费是因为强平单是吃单要付费，若不预留，到维持线强平连手续费都不够 → 必然穿仓。

**Q2：全仓和逐仓的爆仓判定差在哪？**
> 全仓把整个账户同币种所有仓位当整体算保证金率(总权益−其他仓维持保证金)/(总仓位价值)；逐仓只看单仓。所以全仓一个仓位爆仓，会牵连同币种其他全仓仓位一起处理。

**Q3：为什么不一次全平，要阶梯减仓？**
> 只减超出维持线档位的风险敞口，减到健康线就停，尽量保住用户仓位；同时也平滑强平单对盘口的冲击，避免巨量市价单砸穿盘口造成更大滑点。

**Q4：接管价是什么？系统接管仓位干嘛用？**
> 接管价 = 用户权益归零的价格。强平瞬间按接管价记账把仓位转给系统(uid=501)，之后的下跌滑点由平台扛。系统再把自己的接管仓位拿去市价平掉，中间的盈利进风险准备金。

**Q5：穿仓了钱谁来赔？**
> 两层：① 风险准备金优先承担(亏损补贴比例 lossSubsidy 可配)；② 剩余按盈利用户盈利比例分摊。系统仓位盈利会滚入风险准备金。

**Q6：为什么用标记价算保证金率，不用最新价？**
> 最新价易被大单操纵，恶意砸一笔就能把别人爆仓。标记价 = 多交易所指数价 + 资金费基差 + 盘口 EMA 基差，与最新价取中位数，单所操纵拉不动。

**Q7：怎么防止用户在强平前压价/撤单？**
> ① 强平前先撤掉该用户所有委托，撤完重算保证金率；② 下单校验时就禁止"平仓限价单越过强平价"(checkReducePrice)，从源头堵住"挂低价等着被吃"的漏洞。

**Q8：系统怎么知道自己该强平哪些仓？**
> 35 个轮询线程按用户扫描(带游标 + 分布式锁)，逐仓算保证金率。批处理每批 50 人，错峰随机睡 0~300ms，按杠杆降序优先处理高杠杆。

---

## ✅ 使用检查清单

- [ ] 能画出 全仓/逐仓 保证金率公式和爆仓线(含 closeTakerFee 的原因)
- [ ] 能讲清强平八步流程(冻结→撤单→重算→对冲→阶梯减仓→系统接管→下单→解冻)
- [ ] 能解释接管价/破产价、系统接管仓位(uid=501)的设计意图
- [ ] 能讲清阶梯减仓 vs 一刀切的取舍
- [ ] 能讲清穿仓兜底：风险准备金 + 盈利用户分摊(和 ADL 的对比)
- [ ] 能回答"为什么用标记价"和"防操纵"的两个点
