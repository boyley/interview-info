# 合约系统核心技术与实战 — 从零到一的体系总览

> 系统：`chainup-futures` 加密货币合约(期货)交易系统 | Java 8 + Spring Cloud 微服务
> 用途：面试前建立"从零到一做过合约系统"的完整认知：架构 → 数据模型 → 核心流程 → 关键公式 → 演进路线。
> 配套：《合约爆仓与保证金机制》《合约与现货撮合对比》《合约系统面试回答模板》。

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| 合约 vs 现货撮合的本质区别 | 撮合对比文档 | ⭐⭐⭐⭐⭐ |
| 保证金体系：全仓/逐仓、可用/冻结/仓位 | 本文件 §五 | ⭐⭐⭐⭐⭐ |
| 爆仓触发判定 + 强平流程 | 爆仓文档 | ⭐⭐⭐⭐⭐ |
| 梯度档位(维持保证金率)设计 | 爆仓文档 | ⭐⭐⭐⭐⭐ |
| 资金费率(Funding Rate)结算 | 本文件 §六 | ⭐⭐⭐⭐⭐ |
| 标记价(Mark Price)计算与防操纵 | 本文件 §六 | ⭐⭐⭐⭐ |
| 穿仓分摊 + 风险准备金 | 爆仓文档 | ⭐⭐⭐⭐ |
| 条件单/止损止盈触发 | 本文件 §七 | ⭐⭐⭐⭐ |
| 账户模型与资金划转 | 本文件 §五 | ⭐⭐⭐⭐ |
| 指数价多交易所合成 | 本文件 §六 | ⭐⭐⭐ |
| 全局 ID(雪花)为什么必须 | 本文件 §三 | ⭐⭐⭐ |

---

## 📌 二、系统架构与技术栈

### 1. 技术栈

| 层 | 技术 |
|----|------|
| 语言/框架 | Java 8 + Spring Boot 2.1.5 |
| 微服务 | Spring Cloud Alibaba：Nacos(注册/配置) + OpenFeign + Spring Cloud Gateway |
| 存储 | MySQL + MyBatis-Plus + **Sharding-JDBC**(主从读写分离 + **按合约动态分表**) |
| 消息 | **RocketMQ 4.6**(核心解耦通道) |
| 缓存 | Redis Cluster + Redisson(分布式锁) |
| 调度 | XXL-JOB(统计/对账类) |
| 部署 | Docker 容器化 |

### 2. 微服务划分(12 个)

| 服务 | 端口 | 职责 |
|------|------|------|
| **futures-gateway-service** | 8081 | Spring Cloud Gateway：鉴权/限流/路由 |
| **futures-api**(web/app/open/admin) | 8901/8902/20001/8080 | 4 个 API 聚合层(BFF)，Feign 调 service |
| **futures-base-service** | 10003 | 用户、合约配置、币种/币对、商户、KV 配置、账户类型 |
| **futures-order-service** | 10004 | **订单/持仓/成交落库、下单校验、保证金冻结、撮合投递、划转** |
| **futures-triggerorder-service** | 10010 | 条件单触发引擎(消费成交回报) |
| **futures-liquidation-service** | 10007 | **爆仓/强平扫描引擎** |
| **futures-settlement-service** | 10008 | **定期盯市结算 + 资金费率收付 + 穿仓分摊** |
| **futures-index-service** | 10005 | 指数价/标记价/资金费率计算(对接 6 大交易所行情) |
| **futures-job-service** | 10009 | XXL-JOB：对账、资产统计、取消过期条件单 |
| **futures-id** | — | 百度 uid-generator 改造的**雪花 ID** |
| **futures-common** | — | 公共实体 / billing 账务 / redis / utils |
| **protocol-buffers** | — | PlaceOrder / MatchTrade 的 proto 定义(撮合消息格式) |

### 3. 服务依赖关系图

```
                        ┌────────── futures-api (web/app/open/admin) ──────────┐
                        └───────────────────────┬──────────────────────────────┘
                                                │ Feign
                                                ▼
                        futures-gateway-service (鉴权/限流/路由) ◀── 外部流量
                                                │
   ┌──────────────┬──────────────────┬──────────┴──────┬───────────────────┬─────────────┐
   ▼              ▼                  ▼                 ▼                   ▼             ▼
futures-base   futures-order    futures-trigger   futures-          futures-        futures-index
 -service       -service         -order-service    liquidation       -settlement     -service
(配置/用户/      (订单/持仓/        (条件单触发)      -service           -service        (指数/标记价)
 账户/合约)      成交/划转)                          (爆仓引擎)         (结算+资金费)
   │              │                 │                 │                   │
   └──────┬───────┴───────┬─────────┴─────────────────┴───────────────────┘
          ▼               ▼
   futures-job-service (XXL-JOB)  ·  MySQL(分表)  ·  Redis  ·  RocketMQ  ·  Nacos
```

### 4. RocketMQ 拓扑(系统的"血管")

| Topic | 生产者 | 消费者 | 作用 |
|-------|--------|--------|------|
| `FUTURES_ORDER_{SYMBOL}` | futures-order | **撮合引擎(外部服务)** | 下单/撤单投递，每合约一个 topic，**单队列保证同合约串行** |
| `FUTURES_MATCHING_TRADE_{SYMBOL}` | 撮合引擎 | **futures-triggerorder** | 成交回报，驱动条件单触发 |
| `FUTURES_MATCHING_RESULTS_{SYMBOL}` | futures-order | 仓库内无消费者 | 撮合结果初始化/机器人 |

> 🔑 **架构关键点**：真正的**订单簿撮合引擎不在本仓库**(独立外部服务，消费 MQ + 提供 HTTP 撤单/查询)。本仓库负责"下单校验 + 保证金 + 落库 + 投递 MQ"，成交回报经 MQ 回来驱动条件单。这是**业务服务与撮合引擎解耦**的典型设计——撮合是单线程无锁的确定性核心，业务是可水平扩展的无状态服务。

---

## 📌 三、核心数据模型

### 1. 合约(ContractConfig)

| 字段 | 含义 |
|------|------|
| contractType | E 永续 / H 混合 / S 模拟 |
| contractSide | **0 反向(币本位) / 1 正向(USDT 本位)** |
| multiplier / multiplierCoin | **合约面值**(张的单位，如 1 张 = 1 USDT) |
| marginCoin / marginRate | 保证金币种 / 保证金汇率 |
| ladderConfigId / leverConfigId | 关联**梯度档位** / **杠杆配置**(都是 JSON) |
| capitalStartTime / capitalFrequency / capitalRate | 资金费首收时间 / 频率(小时) / 费率 |
| capitalDepthMoney | 资金费率计算用的**加权盘口深度额** |
| capitalIntervalMin/Max、capitalPremiumMin/Max | 资金费率上下限、溢价上下限 |
| settlementFrequency | 盯市结算周期(分钟) |
| lossSubsidy / riskAlarm | 穿仓补贴比例 / 爆仓预警比例 |
| status | 0 停 / 1 启 |

**梯度档位 ConfigLadder**：`level, minPositionValue, maxPositionValue, minMarginRate` —— 按**仓位价值区间**给不同的**维持保证金率**，档位越高维持保证金率越高。**杠杆档位 ConfigLever**：`level, minLever, maxLever, maxHoldAmount` —— 杠杆越高可开最大持仓越小。

### 2. 订单(CoOrder)

| 字段 | 含义 |
|------|------|
| positionType | 1 全仓 / 2 逐仓 |
| open | open 开仓 / close 平仓 |
| side | buy / sell |
| type | 1 LIMIT / 2 MARKET / 3 IOC / 4 FOK / 5 POST_ONLY / **6 爆仓(仅展示)** |
| leverageLevel / price / volume | 杠杆 / 下单价 / 数量(市价开仓=金额) |
| 4 个费率字段 | open/close × taker/maker 手续费(随 MQ 带给撮合) |
| source | 订单来源(**6=LIQUIDATION 强平** / 7=LIQUIDATION_MERGE 爆仓合并) |

订单状态：`INIT(0)→NEW(1)→FILLED(2)/PART_FILLED(3)→CANCELED(4)`，另有 `PENDING_CANCEL(5)/EXPIRED(6)`。

### 3. 仓位(CoPosition)

`positionType`(全仓/逐仓)、`side`、`volume`、`avgPrice`(持仓均价)、`leverageLevel`、`holdAmount`(持仓保证金)、`pendingCloseVolume`(已挂平仓量)、`realizedAmount`(已实现盈亏)、`capitalFee`(资金费)、`shareAmount`(分摊)、`freezeLock`(**0正常/1爆仓冻结/2交割冻结/3减仓冻结**)、`status`。

### 4. 关键枚举

`OrderKind`(OPEN/CLOSE)、`OrderSide`(BUY/SELL)、`PositionType`(TOTAL 全仓/PART 逐仓)、`ContractModel`(SINGLE 净持仓)、`TriggerType`(1止损/2止盈/3追涨/4杀跌/5条件单)、`OrderSource`(6爆仓/7爆仓合并)。

---

## 📌 四、账户模型与资金流转

### 1. 账户类型(assetType)

**用户侧**：
- `U_MARGIN_BALANCE`(251) 保证金**可用**
- `U_MARGIN_LOCK`(252) 委托**冻结**
- `U_POSITION_LOCK`(253) 仓位保证金

**系统侧**：`C_TRADE_FEE`(180 手续费)、`C_CLOSE_POSITION`(181 结算中转)、`C_CAPITAL_COST`(182 资金费中转)、`C_RISK_BALANCE`(183 **风险准备金**)、`C_TRANSFER_MARGIN_IN/OUT`(184/185 划转中转)、`C_TRANSFER_DEPOSIT`(186 充值)、`C_BALANCE`(188 平账)。

### 2. 保证金生命周期(核心！)

```
下单(限价)        下单(市价开)
  冻结额=仓位价值/杠杆      冻结额=金额×marginRate/杠杆
        │                     │
        ▼                     ▼
   U_MARGIN_BALANCE(可用) ──billingTransfer──▶ U_MARGIN_LOCK(委托冻结)
        │                                            │
        │             成交后                        │
        │◀───────────────────────────────────────────┤
        │                                            ▼
   U_MARGIN_BALANCE ◀──平仓解冻── U_POSITION_LOCK(仓位保证金)
```

- 扣/冻/解冻都走 `BillingServiceImpl.billingTransfer()`(**加锁事务**)或 `fastBillingTransfer()`(**乐观锁**)。
- 下单冻结场景 `CREATE_OPEN_ORDER`：**限价单**按 `volume×price×multiplier×marginRate / leverage` 冻结；**市价开仓单**按 `volume×marginRate / leverage` 冻结(volume=金额)。
- 合约账户与现货账户通过 `FuturesTransferService` 划转(入金 exToCo / 出金 coToEx)，出金前做**平账检查**。

### 3. 资金划转流水场景(TransactionScene)

`create_order / cancel_order / open_position / close_position / open|close_fee_taker|maker / CAPITAL_COST_IN/OUT / SETTLEMENT_CLOSE_BUY/SELL / SETTLEMENT_LOSS_SHARE / SETTLEMENT_RISK_IN/OUT / FUTURES_TRANSFER_IN/OUT`。

---

## 📌 五、核心业务流程(从零到一的主线)

### 1. 一次下单的完整链路

```
用户下单
  → gateway(鉴权/限流) → open-api(OrderController)
  → order-service: RPC createOrder
  → CoOrderServiceImpl.place → create:
      ① ValidateServiceImpl.makeOrders(前置校验: 杠杆区间/数量精度/持仓上限/可用保证金/强平价)
      ② makeTransactions(构造 U_MARGIN_BALANCE→U_MARGIN_LOCK 冻结流水)
      ③ doCreate(落库 co_order + billingTransfer 冻结 + 更新 pending_close_volume)
      ④ condition(建条件单, 如有)
      ⑤ sendMessage(封装 protobuf PlaceOrder → RocketMQ FUTURES_ORDER_{symbol})
  → 撮合引擎(外部)消费, 单队列串行撮合
  → 撮合成交 → 回报 FUTURES_MATCHING_TRADE_{symbol}
  → triggerorder 消费 → 驱动条件单; 更新仓位/成交(撮合引擎写共享库)
```

**下单前置校验(合约特有，和现货最大的不同)**：
- 杠杆区间、数量/金额精度、最小/最大下单
- **持仓总量上限** + 全仓可用 = 余额 − 全仓占用
- 挂单总价值 ≤ 当前杠杆允许最大价值
- **平仓限价单不得越过强平价格**(平多价低于强平价、平空价高于强平价则拒单)——防用户"压价平仓让系统兜底"

### 2. 开平仓 × 买卖 → 仓位

四种组合：`OPEN+BUY`(开多)、`OPEN+SELL`(开空)、`CLOSE+SELL`(平多)、`CLOSE+BUY`(平空)。仓位按 `uid+contractId+side` 区分多/空两条。平仓单找**反方向**持仓校验 `volume - pendingCloseVolume`(可平量)，防超卖。

### 3. 结算与爆仓(见专项文档)

- **资金费**：按合约 `capitalFrequency`(小时)周期收，多付空收
- **盯市结算**：按 `settlementFrequency`(分钟)用标记价结算已实现盈亏
- **爆仓**：轮询线程扫描 → 保证金率低于维持线 → 冻结 → 撤单 → 阶梯减仓 → 系统接管

---

## 📌 六、关键公式全集(面试背熟)

### 1. 仓位价值 / 保证金

```
正向: positionBalance = volume × price × multiplier × marginRate
反向: positionBalance = volume × (1/price) × multiplier × marginRate
冻结保证金(限价开) = positionBalance / leverage
```

### 2. 未实现盈亏

```
正向多仓: (标记价 − avgPrice) × volume × multiplier × marginRate
正向空仓: (avgPrice − 标记价) × volume × multiplier × marginRate
反向:     用 1/标记价 与 1/avgPrice 相减, 再 × marginRate
```

### 3. 保证金率(爆仓判定基础)

```
逐仓: (holdAmount + 未实现 + 已实现) / 仓位价值
全仓: (总权益 − 其他合约维持保证金) / (全部实时仓位价值) / 保证金汇率
总权益 = 全仓可用余额 + Σ未实现 + Σ已实现
```

### 4. 资金费率(类 BitMEX)

```
合理价格    = 指数价 × (1 + 上期费率 × 距下次收费分钟 / (频率×60))
溢价指数    = (max(0,深度买价−合理价) − max(0,合理价−深度卖价)) / 指数价 + 基差率
平均溢价    = 最近60条溢价指数的简单平均
资金费率    = clamp(平均溢价 + clamp(基础费率 − 平均溢价, 溢价下限, 溢价上限), 费率下限, 费率上限)
资金费率    = × 频率 / 8   (归一化到 8 小时)
单仓资金费   = ±仓位价值 × 资金费率   (多付空收)
```

### 5. 指数价与标记价

```
指数价 = Σ(交易所权重 × 该所中位数价) / Σ权重
  每所取 buy1/sell1/last 中位数, 错价比例过滤, 最大偏移比例拉回

标记价 = 中位数(最新价中位数, 指数价×(1+资金费率基差), 指数价+EMA价差)
EMA价差 spread = 2/3 × 当前基差 + 1/3 × 上次基差
```

> **为什么用标记价？** 防止操纵最新成交价来恶意爆仓/吃资金费。标记价 = 多交易所指数价 + 资金费/盘口基差，与最新价取中位数，单所操纵拉不动。

### 6. 爆仓相关(详见爆仓文档)

```
爆仓线 = 当前档位维持保证金率(minMarginRate) + 平仓taker费
预警线 = minMarginRate × (1 + riskAlarm) + closeTakerFee    (riskAlarm 默认 0.3)
接管价(破产价, 权益=0时):
  正向: 标记价 − 权益/(仓位×面值×marginRate)
  反向: 1/(1/标记价 + 权益/保证金汇率/仓位)
```

---

## 📌 七、条件单(止损止盈)模块

- **触发源**：`TriggerOrderRun` 每合约一个消费线程，订阅 `FUTURES_MATCHING_TRADE_{SYMBOL}`，用**最新成交价**判断触发。
- **触发条件**：追涨 最新价≥触发价；杀跌 ≤；止损 持仓BUY 价≥触发价 / 持仓SELL 价≤触发价；止盈反之。**止损有防插针过滤**：`|最新价−标记价|/标记价 > diffRate` 则跳过(视为插针)。
- **OTO 止盈止损**：主单成交时自动挂两张反向平仓触发单。
- **触发后**：校验可平量 → 调 `orderRpcClient.createOrder` 转成真实订单 → 状态 INIT→COMPLETED。
- 状态机：`INIT→COMPLETED`，过期→EXPIRED，资金不足/仓位锁定→CANCEL，异常→FAIL。过期兜底有 XXL-JOB `CancelTriggerJob`。

---

## 📌 八、从零到一的演进路线(面试叙事)

> 面试被问"这个合约系统怎么从零做起来的"，按这条主线讲，体现"不是背的，是真懂"：

1. **第一步：复用现货体系**。合约本质 = 现货撮合 + 杠杆 + 方向做空。先想清楚**和现货的差异**：保证金、开平仓、双向持仓、强平。→ 新增 `CoOrder/CoPosition` 模型，订单加 `open`(开/平) + `positionType`(全/逐) + `leverage`。
2. **第二步：账户与保证金体系**。现货是"余额买卖"，合约要"可用/冻结/仓位"三态。→ 设计 `U_MARGIN_BALANCE/LOCK/POSITION_LOCK` 三类账户 + billing 流水，下单冻结、成交转仓位、平仓解冻。
3. **第三步：下单链路与撮合解耦**。撮合必须是**确定性单线程无锁**核心，业务要可水平扩展 → 用 RocketMQ **每合约一个 topic/单队列**做异步投递，撮合引擎独立部署，成交回报回传。
4. **第四步：风控(最值钱的环节)**。→ 设计梯度档位维持保证金 → 轮询扫描线程算保证金率 → 触发冻结 → 阶梯减仓 → 系统接管仓位(uid=501) → 接管仓位市价平掉。
5. **第五步：结算与资金费**。→ 定期盯市结算(标记价) → 资金费率(指数价+溢价) → 穿仓分摊(风险准备金 + 盈利用户分摊)。
6. **第六步：价格体系**。→ 多交易所指数价 → 标记价(防操纵) → 用标记价做爆仓和结算。
7. **第七步：体验层**。→ 条件单/止损止盈(消费成交回报触发) → 全局雪花 ID → 对账/统计 XXL-JOB。

---

## 📌 九、现状与差距(面试主动说的加分项)

诚实讲差距比吹嘘更可信，面试官会眼前一亮：

| 现状/未实现 | 说明 | 如果要上生产怎么补 |
|------------|------|------------------|
| 撮合引擎不在本仓库 | 仓库只有订单侧，撮合是外部服务 | 讲清订单簿撮合设计即可(见撮合对比文档) |
| **指数/标记价计算被注释停用** | `CalculationIndex/Tag/Spreads` Job 主体注释掉 | 上生产必须启用，讲清公式链即可 |
| **交割(到期)结算未实现** | 只有永续流程，deliveryKind 枚举在但无流程 | 按合约到期价结算 + 强平交割单 |
| **ADL 自动减仓未实现** | 穿仓靠"风险准备金 + 定期结算分摊盈利用户" | 可引入对手方自动减仓替代分摊 |
| 主节点选举未启用 | `LeaderElection` 代码在但 Bean 注释 | 多实例条件单引擎需 master 选举防重复触发 |
| 爆仓靠轮询(30+线程) | 不是每笔成交即时检查 | 可加 MQ 逐笔触发 + 轮询兜底双通道 |
