# 合约系统面试回答模板 — 考官问 · 你答(话术集)

> 用法：每题先记**回答结构**，再把**话术**用自己的话讲出来，别死背。
> 配套：《合约系统核心技术与实战》(体系)、《合约爆仓与保证金机制》(爆仓)、《合约与现货撮合对比》(撮合)。
> 面试目标：让面试官相信你**从零到一做过一个合约系统**——重点是讲"为什么这样设计"和"踩过的坑"，不是背概念。

---

## 🎙️ 一、万能开场与收尾

**被问"介绍一下你的合约项目"时的万能开场：**
> "我参与开发的是一个加密货币合约(期货)交易系统，Java 8 + Spring Cloud 微服务，拆了 12 个服务。核心是几个独立引擎：**订单服务负责下单校验和保证金冻结**，通过 RocketMQ 把订单投给**撮合引擎**(每合约一个 topic 单队列，保证串行)，撮合成交回报回来驱动**条件单**；另有独立的**爆仓引擎**(轮询扫描保证金率)、**结算引擎**(资金费+盯市+穿仓分摊)、**指数引擎**(多交易所合成标记价)。"

**答任何技术题后的万能收尾(挑 1 句)：**
- "合约系统真正的复杂度**不在撮合，在撮合两侧**——保证金、爆仓、结算、资金费。"
- "撮合必须确定性可重放，所以我用 **MQ 单队列保证同合约串行**，而不是多线程加锁。"
- "爆仓机制本质是**把'用户亏穿'这件事从平台身上剥离开**——接管价、系统接管仓、风险准备金都是为此服务的。"
- "我这是教学级实现，上生产我会补 **ADL 自动减仓、逐笔触发爆仓、交割结算**。"
- "合约的钱是带杠杆的，所以**每一笔钱都要可追溯、可对账**，账户流水是系统的命根子。"

---

## 🎙️ 二、从零到一：你是怎么把这个合约系统做起来的？(必考)

**回答结构：先复用现货 → 加保证金 → 撮合解耦 → 风控 → 结算 → 价格体系 → 体验**

**话术：**
> "如果从零做，我不会一步到位，而是按风险从小到大加：
>
> **① 先想清楚合约和现货的差别**——就四点：能开空、要保证金、有开平仓方向、会爆仓。所以订单模型在现货基础上加了 `open`(开/平)、`positionType`(全仓/逐仓)、`leverage`(杠杆)，仓位用 `CoPosition` 单独建模。
>
> **② 设计账户和保证金体系**——现货是余额买卖，合约需要 '可用/冻结/仓位' 三态：下单从 `U_MARGIN_BALANCE` 冻结到 `U_MARGIN_LOCK`，成交转 `U_POSITION_LOCK`，平仓解冻。所有变动都走 `billingTransfer` 流水，**保证每一笔钱可追溯**。
>
> **③ 撮合和业务解耦**——撮合是单线程确定性核心，业务要能水平扩展。所以我用 RocketMQ **每合约一个 topic、单队列**，订单服务只管校验+落库+投递，撮合引擎独立消费、独立部署，成交回报再经 MQ 回来。
>
> **④ 做风控(最值钱的一步)**——设计梯度档位维持保证金率，35 个轮询线程扫全量用户算保证金率，低于维持线就冻结→撤单→阶梯减仓→按接管价转给系统接管仓→系统仓位市价平掉。穿仓用风险准备金 + 盈利用户分摊兜底。
>
> **⑤ 加结算和资金费**——定期盯市结算、资金费多付空收。
>
> **⑥ 加价格体系**——多交易所指数价合成标记价，**爆仓和结算全用标记价，防操纵**。
>
> **⑦ 最后加体验层**——条件单/止损止盈、全局雪花 ID、对账统计任务。
>
> 一句话：**从能跑，到能稳，到能赚，到能扛风险**，每一步都是在前一步基础上加一个新维度。"

---

## 🎙️ 三、讲讲这个合约系统的整体架构？

**回答结构：服务划分 → 数据流主线 → MQ 拓扑 → 每层职责**

**话术：**
> "12 个微服务，核心就这几块：
>
> **入口层**：gateway(鉴权限流) + 4 个 API 聚合层(web/app/open/admin)。
> **业务层**：base(合约/用户/配置)、order(订单/持仓/划转)、triggerorder(条件单)、liquidation(爆仓)、settlement(结算)、index(指数/标记价)、job(对账统计)。
> **基础设施**：MySQL(Sharding-JDBC 主从+按合约分表)、Redis、RocketMQ、Nacos。
>
> 数据流主线一句话：**下单→校验→冻结保证金→MQ 投撮合→成交回报→驱动条件单；同时爆仓引擎轮询算保证金率，结算引擎定期盯市和收资金费**。
>
> 通信方式：同步用 Feign，异步用 RocketMQ，锁用 Redisson。订单/成交/持仓表按合约 `simpleSymbol` 动态分表，一个合约一条数据线。"

---

## 🎙️ 四、合约和现货撮合有什么区别？(必考)

**回答结构：撮合本身一样 → 撮合前多了校验/保证金 → 撮合后多了事件**

**话术：**
> "**撮合引擎本身没区别**——都是订单簿、价格优先时间优先、单线程串行。区别全在撮合两侧：
>
> **撮合前**：现货只验余额，合约要过一整层风控——杠杆区间、持仓上限、**可用保证金**、以及最关键的**平仓限价单不能越过强平价**(防止用户把风险甩给平台)。下单要冻结保证金(限价按价值/杠杆、市价按金额)。
>
> **撮合后**：现货交易完就结束了，合约后面还有一串事件——**盯市结算、资金费、爆仓强平、穿仓分摊**。
>
> 订单模型也不同：合约订单有开/平、全/逐、杠杆、爆仓单标记；仓位有方向有均价有保证金，多空分开记账。"
>
> 收尾金句："所以我的体会是，**合约系统的复杂度 80% 在撮合之外**，这也是为什么它比现货难做。"

---

## 🎙️ 五、爆仓是怎么设计的？(必考)

**回答结构：保证金率判定 → 扫描 → 强平流程 → 接管 → 兜底**

**话术：**
> "爆仓 = 用户权益不足以维持仓位时的**风险隔离机制**。设计分五层：
>
> **① 判定**：保证金率 = (权益)/(仓位价值)。跌破**维持保证金率 + 平仓手续费**就触发——加手续费是因为强平单是吃单要付钱，不预留会直接穿仓。维持保证金率用**梯度档位**，仓位越大档位越高，缓冲越厚。
>
> **② 发现**：35 个轮询线程按用户扫(带游标+分布式锁)，逐仓算保证金率，高杠杆优先。
>
> **③ 执行**：冻结仓位 → 撤掉所有委托 → 重算(恢复健康就解冻)→ **阶梯减仓**——只减超出维持线的部分，减到健康线就停，不是一刀切全平。
>
> **④ 接管**：减仓不直接卖，而是**按接管价(破产价)记账转给系统接管仓**(uid=501)，用户亏到接管价就出局，之后的下跌滑点平台扛。系统再把接管仓位拿市价平掉，盈利滚入风险准备金。
>
> **⑤ 兜底**：穿仓时风险准备金先补(lossSubsidy 可配)，剩余按**盈利用户盈利比例分摊**。"
>
> 收尾金句："爆仓的本质，是把'用户亏穿'从平台身上**用价格和记账剥离**出去——接管价定盈亏，系统仓扛滑点，准备金兜风险。"

---

## 🎙️ 六、资金费率怎么设计的？为什么要有？

**回答结构：为什么 → 怎么算 → 怎么收**

**话术：**
> "永续合约没有交割，多空不平衡时会单边倒挂，资金费率就是**平衡多空的机制**：让多空互相付钱，价差大就鼓励反向开仓。
>
> 算法是类 BitMEX：先算**合理价格**(指数价×(1+上期费率×时间占比))，再用盘口深度算**溢价指数**，取最近 60 个溢价指数平均，加上钳制上下限得到费率，最后归一化到 8 小时。
>
> 收取：按合约配置的小时频率(如 8h)定时结算，**多付空收**：资金费 = 仓位价值 × 费率，多仓为负(付)、空仓为正(收)。收付走账户流水，余额不足由风险准备金垫付。"
>
> 一句话："**现货靠交割回归，永续靠资金费回归**——资金费就是永续合约用来'锚定现货'的机制。"

---

## 🎙️ 七、标记价是什么？为什么爆仓结算都用它？

**回答结构：怎么算 → 为什么防操纵 → 用在哪些环节**

**话术：**
> "标记价 = **多交易所指数价 + 资金费基差 + 盘口 EMA 基差**，再和最新成交价取中位数。
>
> 为什么不用最新价？最新价**太容易被操纵**——恶意砸一笔大单把最新价拉下来，就能把别人的仓位爆掉，或者影响资金费结算。标记价来自多个交易所合成 + 与最新价取中位数，**单一交易所拉不动**。
>
> 用在三个最敏感的环节：**爆仓判定(算保证金率)、盯市结算、资金费结算**。用户看到的强平线、风险提示都用标记价。"
>
> 一句话："**最新价是面子，标记价是里子**——展示用最新价，风控和结算全用标记价。"

---

## 🎙️ 八、全仓和逐仓有什么区别？你分别怎么处理爆仓？

**回答结构：定义 → 保证金率公式差异 → 爆仓范围差异**

**话术：**
> "**逐仓**：每个仓位独立的保证金，亏完这个仓位的保证金为止，风险隔离。保证金率 = 单仓(保证金+盈亏)/单仓价值。
> **全仓**：整个账户同币种所有仓位共担保证金，资金利用率高但**牵一发动全身**。保证金率 = (总权益−其他仓维持保证金)/总仓位价值。
>
> 爆仓处理差别就在范围：逐仓只锁当前仓，全仓要**冻结同币种所有全仓仓位一起处理**，还多一步——同合约的多空仓位先**内部对冲**(selfTradePosition)，能抵消的先抵消，再减剩余的净敞口。"
>
> 一句话："**逐仓是'一仓一死'，全仓是'一荣俱荣、一损俱损'**。"

---

## 🎙️ 九、穿仓了怎么办？和币安的 ADL 有什么区别？

**回答结构：穿仓怎么产生 → 风险准备金 → 分摊 → 和 ADL 对比**

**话术：**
> "穿仓 = 用户保证金亏完还不够，平台倒贴。我的实现是**两层兜底**：
>
> ① **风险准备金**：爆仓盈利滚入准备金账户，穿仓优先用准备金补贴(lossSubsidy 可配比例)。
> ② **盈利用户分摊**：不够的部分，按盈利用户的盈利比例分摊。分摊池上限是盈利总额的 90%，留 10% 给平台吸收，避免极端情况把盈利方也拉爆。
>
> 和币安 **ADL(自动减仓)** 的区别：币安是把穿仓单**强制与盈利方反向单撮合**，按利润排序先减利润最高的；我是**定期结算 + 事后分摊**，不打断撮合，但分摊有延迟。取舍上，ADL 实时但冲击盘口，分摊简单但可能引起盈利方不满。"
>
> 一句话："**ADL 是'找赢家接盘'，分摊是'找赢家买单'**——一个在撮合前，一个在结算后。"

---

## 🎙️ 十、条件单/止损止盈是怎么实现的？

**回答结构：触发源 → 触发判断 → 转真实单 → 防插针**

**话术：**
> "条件单不参与撮合，是个**触发器**。触发源是**撮合成交回报**——triggerorder 服务每个合约一个消费者线程，订阅 `FUTURES_MATCHING_TRADE_{symbol}`。
>
> 收到成交消息后，用**最新成交价**判断该合约所有待触发条件单：追涨是价≥触发价，杀跌是≤，止损止盈按仓位方向相反判断。命中就调订单服务把条件单**转成真实订单**提交撮合。
>
> 一个细节：**止损有防插针过滤**——如果 `|最新价−标记价|/标记价` 超过阈值，就认为是插针跳过，防止一次异常波动把用户的止损打掉。
>
> 主节点要防重复：多实例部署时条件单引擎需要**主节点选举**(用 Redisson 分布式锁)，避免多台机器重复触发同一张单。"

---

## 🎙️ 十一、你觉得这个系统哪里还能优化？重写会怎么改？

**回答结构：现状 → 差距 → 演进(主动讲，最加分)**

**话术：**
> "我会诚实说几个我知道的差距和改法：
>
> **① 爆仓是轮询扫描**，有秒级延迟 → 改成 **MQ 逐笔触发 + 轮询兜底** 双通道，成交即检查、轮询防漏。
> **② 穿仓靠分摊**，可能引起盈利用户不满 → 引入 **ADL 自动减仓**，实时、冲击可控。
> **③ 撮合引擎不在我代码里**，我对订单簿只做了架构对接 → 我会把**撮合引擎设计**讲清楚：单线程无锁、价格时间优先、每合约隔离。
> **④ 指数/标记价计算有部分注释停用** → 上生产必须启用，并且加**异常行情熔断**(某交易所价偏离过大直接剔除)。
> **⑤ 没有交割(到期)结算** → 按合约到期价做最后结算 + 交割强平。
> **⑥ 结算/对账可以加**：资金流水与持仓逐笔勾稽、日终对账，风控阈值动态化。
>
> 一句话：**保留'确定性撮合 + 事件驱动 + 两层兜底'的骨架，补齐实时性、交割和 ADL**，就接近生产级了。"

---

## ✅ 使用检查清单

- [ ] 能 2 分钟讲完"从零到一"七步演进
- [ ] 爆仓五层设计(判定→发现→执行→接管→兜底)能完整背出
- [ ] 合约 vs 现货能一句话点透("撮合一样，两侧不同")
- [ ] 能讲清资金费、标记价、全仓/逐仓、穿仓分摊四个必考
- [ ] 每个回答都带一句金句收尾
- [ ] 敢于主动讲"现状差距 + 重写怎么改"——这是加分项不是减分项
