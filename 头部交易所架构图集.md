# 头部交易所架构图集 — 面试讲架构有图可画

> 来源：据各平台公开技术分享/官方文档整理的**示意架构图**（非官方精确内部架构，尤其币安/OKX/火币细节未全公开，面试引用时说明"据公开资料整理"）。
> 用途：面试时能画出 2~3 张架构图，比纯背文字加分得多。配合《撮合引擎核心技术与实战》第十章。

---

## 📌 一、LMAX（三层 Disruptor，必画）

```
    客户端下单
      │  TCP / FIX
      ▼
  网络IO线程们（多线程收单）
      │  事件
      ▼
 ① Input Disruptor（无锁环形缓冲，打序号）
      │
      ▼
  Business Logic Processor（单线程 · 单写者）
      │    拥有全部可变状态：订单簿 / 账户 / 风控
      │    严格按序号串行处理业务事件
      │
      ├────► 成交/状态事件
      │        │
      ▼        ▼
  响应回客户端   ③ Output Disruptor ──► 网络IO线程发响应
```

**要点**：所有状态只活在一个单线程里；IO 线程想改状态只能投事件进 Disruptor 排队。**单线程 600 万订单/s** 的出处。

---

## 📌 二、NASDAQ INET（确定性优先于并行）

```
  交易终端 / 经纪商
      │
      ▼
  接入层 Gateways（多）
      │  订单
      ▼
  Sequencer ──► 严格序列化事件日志（全局序号）
      │            同一序号输入流
      ├──────────────────────────┐
      ▼                          ▼
  撮合引擎（单线程）          热备引擎（重放同一输入流）
  价格-时间优先订单簿          状态 bit-identical
      │
      ├──► 成交 → 行情分发（UDP 组播 + TCP 重传补拉）
      └──► 回执 → 网关 → 终端
```

**要点**：trade-off 是"确定性 > 并行"——单 symbol 不可横向扩展，但重放输入日志即热备。**100万消息/s、<40μs**，记录日峰值 11.3 亿消息。

---

## 📌 三、币安 Binance（分片 + 微服务）

```
  客户端
   │  REST 1200次/min(IP) · WebSocket 单连接200个symbol
   ▼
  API 网关层（多区域接入）
   │
   ▼
  订单服务（微服务 · Go/Java · K8s 部署）
   │  Kafka 分发（按 symbol 路由到单一分区，保序）
   ▼
  撮合引擎（内存撮合 + 订单簿分片）
   │    ├─ 按交易对分片，每片独立撮合引擎
   │    ├─ 成交 → 行情服务（K线/深度/推送）
   │    └─ 成交 → 钱包/账本服务（清算）
   │
   ▼
  风控 / 钱包（冷热分离） / 数据持久化 / 监控
```

**要点**：微服务解耦 + 订单簿分片。**单节点 140万 交易/s、撮合微秒级**；订单确认全链路 ~12ms。

---

## 📌 四、OKX 欧易（全内存 + 硬件加速）

```
  客户端
   │  REST 1000次/min · WS 500 channel · FIX 机构接入
   ▼
  接入层（多区域；亚洲节点 ~18ms，极速模式 ~14ms）
   │
   ▼
  撮合服务（订单簿完全驻留内存）
   │    ├─ 订单簿：跳表 SkipList（16核写吞吐比红黑树+40%）
   │    ├─ 内存布局：缓存行对齐（访问效率+300%）
   │    ├─ 分级存储：热区(NUMA感知) + 冷区(压缩缓冲区)
   │    ├─ 增量匹配算法：O(n) → O(log n)/O(1)
   │    ├─ 持久内存 DCPMM：热区备份（WAL 快速恢复）
   │    ├─ RoCEv2 RDMA：网络→应用 <1μs
   │    └─ 快照：重启 ~2s 恢复全部状态
   │
   ▼
  行情 / 清算 / 钱包（解耦异步）
```

**要点**：跳表+缓存行+热冷区+RDMA 四件套，全为压微秒级延迟。**单核 50万 单/s、P99<50μs、往返 4.2μs**。

---

## 📌 五、火币 Huobi（公开资料较少）

```
  客户端
   │
   ▼
  接入层
   │
   ▼
  撮合引擎（Clojure / JVM：函数式 + STM 软件事务内存）
   │    ├─ 成交 → 行情推送
   │    └─ 成交 → 清算/账务
   │
   ▼
  安全防护：冷钱包(95%离线) + HSM + 3-of-5 多重签名
```

**要点**：公开撮合细节少，可讲两点——**Clojure 函数式 + STM** 的并发模型区别于 Go/Java；**安全体系**（冷钱包/HSM/多重签名）。

---

## 📌 六、CME Globex（多算法 + 隐含单）

```
  经纪商 / 做市商
   │  FIX / 专属线路
   ▼
  接入点 Gateways
   │
   ▼
  Globex 撮合核心
   │    ├─ 多撮合算法按品种配置：FIFO / Pro Rata / TOP / LMM
   │    ├─ 订单簿（价格-时间优先）
   │    └─ 隐含单引擎（期货+期权组合推导合成单）
   │
   ▼
  行情分发（实时 + 事后交易报告）
```

**要点**：不是单算法通吃，**算法可配**（欧元美元用 Pro Rata）；隐含单撮合。全球最大期货撮合。

---

## 📌 七、Bizzan（你的代码，全景图）

```
  前端
   │  /exchange/order/add
   ▼
  exchange-api (6003) ──Kafka: exchange-order────► 撮合服务 exchange (6005)
   │                                                   │ CoinTrader 每交易对一个实例
   │◄─Kafka: exchange-order-completed / cancel-success──┤ 订单簿 TreeMap+队列
   │                                                   ├─ Kafka: exchange-trade ──────┐
   │                                                   └─ Kafka: exchange-trade-plate ─┤
   │                                                        [撮合引擎]                 ▼
   │                                                                        market 行情 (6004)
   │                                                                    ExchangeTradeConsumer
   │                                                                        │ CoinProcessor
   │                                                                        ├─► Mongo：K线/成交
   │                                                                        └─► 推送：WS/Netty/SocketIO
   ▼
  用户收到成交 / 订单状态                前端行情/K线/盘口
```

**要点**：教学级全景——Kafka 串起 下单网关→撮合→行情 三服务；撮合无 WAL/快照/主备（差距即演进方向）。

---

## 📌 八、对比总览表

| 平台 | 核心卖点 | 撮合并发模型 | 订单簿 | 延迟/吞吐标杆 |
|------|---------|-------------|--------|--------------|
| **LMAX** | 三层 Disruptor | 单线程 BLP | 内存 | 600万 单/s |
| **NASDAQ** | 确定性>并行 | 单线程+热备重放 | 内存 PTP | 100万 消息/s、<40μs |
| **币安** | 微服务+分片 | 按 symbol 分片 | 内存 | 单节点 140万 交易/s |
| **OKX** | 全内存+硬件加速 | 单核串行+分片 | 跳表+缓存行 | 单核 50万/s、P99<50μs |
| **火币** | Clojure+STM | STM 软件事务内存 | 内存 | 公开数据少 |
| **CME** | 多算法可配 | 多引擎 | 内存+隐含单 | 全球最大期货 |
| **Bizzan** | Kafka 解耦 | 每交易对实例 | TreeMap+队列 | 教学级 |

---

## 📌 九、共性与差异（面试加分区）

**所有生产级撮合引擎的共性：**
1. 订单簿 **全内存**、撮合 **单写者/单线程**（确定性可重放）
2. **事件溯源 + WAL + 快照** 保数据与恢复
3. **定序** 是地基：公平 + 可重放 + 主备复制
4. **清算/行情解耦**，异步批次化
5. 追求**尾延迟 P99/P999** 而非平均

**差异点（体现理解深度）：**
- 并发模型：Java 单线程+Disruptor（LMAX）vs Clojure STM（火币）vs Go 分片
- 数据结构：TreeMap vs 跳表 vs flat array —— 本质是**缓存局部性与无锁友好性**的取舍
- 硬件加速：OKX 走 RDMA/持久内存，极致方案走 FPGA
- 一致性：主备 bit-identical（重放）vs 集群共识选主

---

## ✅ 检查清单

- [ ] 能默画 LMAX 三层 Disruptor 图
- [ ] 能默画 NASDAQ（定序+热备重放）图
- [ ] 能画出币安/OKX 中至少一张分片或内存架构图
- [ ] 能画出自己 Bizzan 的 Kafka 全景图
- [ ] 能讲 5 点共性与 3 点差异
