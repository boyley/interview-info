# 微服务架构设计与实践 — 面试备战手册

> 来源：极客时间《从 0 开始学微服务》（胡忠想/微博）
> 胡忠想，微博技术专家，亲历微博后端架构的每一次重大升级
> 涵盖 36 讲：入门微服务 → 落地实践 → 进阶容器化 → 展望 Service Mesh

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| **微服务 vs 单体应用**（优缺点/拆分时机） | 01+02 | ⭐⭐⭐⭐⭐ |
| **服务注册与发现**（注册中心/自注册/第三方注册） | 05+12+13 | ⭐⭐⭐⭐⭐ |
| **RPC 远程调用**（协议/序列化/Netty） | 06+14 | ⭐⭐⭐⭐⭐ |
| **服务治理**（熔断/限流/降级/超时重试） | 09+20+21 | ⭐⭐⭐⭐⭐ |
| **服务监控与追踪**（Metrics/Trace/Jaeger/Zipkin） | 07+08+15+16 | ⭐⭐⭐⭐ |
| **负载均衡算法**（随机/轮询/最少连接/自适应） | 18 | ⭐⭐⭐⭐ |
| **容器化 Docker**（Namespace/Cgroups/镜像分层） | 25 | ⭐⭐⭐⭐ |
| **Kubernetes 容器调度**（Pod/Service/Deployment） | 26+27 | ⭐⭐⭐⭐ |
| **DevOps/CI/CD** | 29 | ⭐⭐⭐ |
| **服务配置中心**（配置管理/动态推送） | 22 | ⭐⭐⭐ |
| **服务路由**（灰度发布/蓝绿部署） | 19 | ⭐⭐⭐ |
| **多机房部署**（异地多活/数据同步） | 31 | ⭐⭐⭐ |
| **Service Mesh**（Istio/Sidecar/数据平面+控制平面） | 33+34 | ⭐⭐⭐⭐ |
| **容量规划**（压测/分层/动态扩容） | 30 | ⭐⭐⭐ |

---

## 📌 二、面试回答标准模板（30秒版）

> **面试官："说说微服务架构？"**
>
> **回答**：
>
> "微服务是把单体应用拆分成多个独立部署的小服务，每个服务围绕特定业务功能构建，可以独立开发、测试、部署和扩展。
>
> 核心组件包括：**服务描述**（接口定义）、**注册中心**（服务发现）、**RPC 框架**（远程调用）、**监控系统**（Metrics）、**追踪系统**（Trace）、**治理平台**（熔断限流降级）。
>
> 引入微服务后最大的变化是服务调用从**本地方法调用变成了远程网络调用**，所以必须解决网络带来的不确定性——超时、重试、熔断、限流、负载均衡这些都是微服务的标配。
>
> 现在容器化（Docker+K8s）和 Service Mesh 是微服务的主流方向，前者解决了环境一致性和弹性伸缩的问题，后者把服务治理能力从应用代码抽离到基础设施层。"

---

## 📌 三、微服务核心概念

### 1. 什么是微服务？

| | 单体应用 | 微服务 |
|--|---------|--------|
| **架构** | 一个进程，所有功能在一起 | 多个独立进程 |
| **部署** | 一个 WAR 包 | 每个服务独立部署 |
| **扩展** | 只能整体扩展 | 按需单独扩展 |
| **技术栈** | 统一技术栈 | 每个服务可不同 |
| **团队** | 大团队共同维护 | 小团队各自负责 |
| **故障** | 一个 Bug 整个系统崩溃 | 故障局限在单个服务 |

### 2. 何时需要微服务？

不是所有项目都适合微服务。**什么时候该拆分？**
- 代码臃肿，编译部署慢
- 团队规模大，协作困难
- 不同模块的扩展需求不同
- 技术栈需要异构（部分服务用 Go，部分用 Java）

**什么时候不该拆？**
- 业务早期，快速验证阶段
- 团队规模小（<10人）
- 业务逻辑简单

### 3. 面试回答

> **Q：什么时候应该引入微服务？**
>
> 不是所有项目都需要微服务。早期项目用单体快速迭代，当团队规模变大、代码编译变慢、不同模块扩展需求差异大时，才考虑拆分。我的原则是"**拆的越晚越好**"，但一旦拆就要做好服务治理的配套。

---

## 📌 四、微服务核心组件

```
                         ┌─────────────────┐
                         │   监控系统        │
                         │  (Metrics/告警)   │
                         └────────┬────────┘
                                  │
┌──────────┐    ┌──────────┐    ┌─▼────────┐    ┌──────────┐
│ 服务提供者 │───▶│ 注册中心  │◀───│ 服务消费者  │    │ 配置中心  │
│ (RPC Server)│   │ (Registry)│   │ (RPC Client)│    │ (Config)  │
└──────┬───┘    └──────────┘    └─────┬─────┘    └────┬─────┘
       │                              │               │
       └──────────┐         ┌─────────┘               │
                  ▼         ▼                         │
            ┌──────────────────────┐                  │
            │     RPC 框架          │                  │
            │  (Dubbo/gRPC/Thrift) │                  │
            └──────────────────────┘                  │
                  │         │                         │
          ┌───────▼─────────▼───────┐                 │
          │    服务治理平台           │◄────────────────┘
          │  熔断/限流/降级/负载均衡  │
          └─────────────────────────┘
```

### 1. 服务描述

**RESTful API**（Spring Cloud）vs **接口描述语言 IDL**（Dubbo/gRPC/Thrift）

| 方式 | 优点 | 缺点 | 适用 |
|------|------|------|-----|
| RESTful | 跨语言，简单 | 性能稍差，无强类型 | 对外 API |
| IDL (protobuf/thrift) | 强类型，性能高 | 需要代码生成 | 内部 RPC |

### 2. 服务注册与发现

**三种角色**：服务提供者、服务消费者、注册中心

```
服务提供者启动 → 注册到注册中心 → 定期发送心跳
服务消费者启动 → 从注册中心获取服务列表 → 缓存到本地
服务消费者 → 负载均衡 → 调用具体提供者
注册中心 → 检测心跳 → 剔除异常节点
```

**注册中心对比**：

| 组件 | CAP | 特点 |
|------|-----|------|
| **ZooKeeper** | CP | 强一致性，写性能瓶颈，不适合大规模注册 |
| **Eureka** | AP | 可用性优先，最终一致性，适合注册发现 |
| **Consul** | CP | 自带健康检查，KV 存储 |
| **Nacos** | CP+AP 可切换 | 阿里出品，功能全面 |

**自注册 vs 第三方注册**：
- **自注册**：服务自己向注册中心注册（Dubbo 方式）
- **第三方注册**：由注册代理（如 Kubernetes 的 Pod 注册）负责注册

### 3. RPC 远程调用

```
客户端 ──▶ 动态代理 ──▶ 序列化 ──▶ 网络传输 ──▶ 反序列化 ──▶ 服务端
                                              (Netty/NIO)
```

**主流 RPC 框架**：

| 框架 | 序列化 | 通信 | 特点 |
|------|--------|------|------|
| **Dubbo** | Hessian | Netty | 国内广泛使用，阿里 |
| **gRPC** | Protobuf | HTTP/2 | Google，跨语言，流式 |
| **Thrift** | 二进制 | Socket | Facebook，性能高 |
| **Spring Cloud** | JSON | HTTP | 与 Spring 生态集成好 |

**RPC 核心要解决的问题**：
1. **通信协议**：HTTP vs TCP，短连接 vs 长连接
2. **序列化**：性能 + 跨语言（Protobuf/Thrift/Hessian/JSON）
3. **IO 模型**：BIO（阻塞）vs NIO（非阻塞，Netty 为代表）

### 4. 面试回答

> **Q：服务注册发现的原理？**
>
> 服务提供者启动后向注册中心注册自己的 IP 和端口，并定期发送心跳。服务消费者启动时从注册中心拉取服务列表并缓存本地。当消费者要调用某个服务时，从本地缓存的服务列表中通过负载均衡选出一个节点进行调用。注册中心会检测心跳，如果提供者长时间没有心跳，就从列表剔除。
>
> 注册中心选型：Dubbo 用 ZooKeeper（CP），Spring Cloud 用 Eureka（AP）。前者保证一致性，后者保证可用性。我个人倾向服务注册发现场景用 AP（宁可拿到不准确的列表也不能拿不到列表）。

---

## 📌 五、服务治理

### 1. 为什么需要治理？

单体应用的调用是**本地的、可靠的方法调用**；微服务的调用是**远程的、不可靠的网络调用**。网络会超时、会断开、会变慢，所以需要治理。

### 2. 六大治理手段

| 手段 | 做什么 | 面试要点 |
|------|--------|---------|
| **① 节点管理** | 检测服务存活，自动摘除异常节点 | 主动心跳检测 / 被动摘除 |
| **② 负载均衡** | 请求分发到多个实例 | 随机/轮询/最少连接/自适应 |
| **③ 服务路由** | 控制请求流向（灰度/蓝绿） | 条件路由/脚本路由 |
| **④ 熔断** | 保护调用方不被故障服务拖死 | 断路器三种状态（Closed/Open/Half-Open） |
| **⑤ 限流** | 保护自身不被流量冲垮 | 令牌桶/漏桶/Guava RateLimiter |
| **⑥ 降级** | 牺牲非核心功能保核心 | 超时降级/失败降级/限流降级 |

### 3. 熔断器模式

```
Closed（关闭）── 失败达到阈值 ──▶ Open（断开）
   ▲                              │
   │                              │ 超时后
   │                   ┌──────────────────┐
   │                   │  Half-Open (半开) │
   │                   └────────┬─────────┘
   │                成功恢复 ◀───┘
   └──────────────────────────────────── 继续失败 → Open
```

**Hystrix**（Netflix）vs **Resilience4j**：Hystrix 已停更，推荐 Resilience4j。

### 4. 面试回答

> **Q：微服务故障怎么处理？**
>
> 服务端故障用**熔断**（快速失败，防止级联雪崩）和**限流**（保护自身不被冲垮）。客户端调用失败用**重试**（最多3次，注意幂等）和**超时控制**（设置合理的超时时间，防止长时间阻塞）。
>
> 熔断器三板斧：**Closed→Open→Half-Open**。Closed 正常调用，失败达到阈值后 Open（直接快速失败），一段时间后变成 Half-Open（放少量请求试探），如果成功恢复 Closed，如果继续失败回到 Open。

---

## 📌 六、服务监控与追踪

### 1. 监控的四个维度（MARTIN 法则）

| 维度 | 监控内容 | 工具 |
|------|---------|------|
| **Metrics** | QPS、TP99、错误率 | Prometheus + Grafana |
| **Access** | 请求日志、调用链 | ELK |
| **Resource** | CPU、内存、网络 | Zabbix/Nagios |
| **Business** | 订单量、用户量 | 业务大盘 |

### 2. 分布式追踪（Trace）

```
Trace 1:
  用户请求 → 服务A → 服务B → 服务C
            ↓          ↓
          服务D      服务E
```

**核心概念**（Dapper/Zipkin/Jaeger）：
- **TraceID**：一次请求的唯一标识，贯穿所有服务
- **SpanID**：每次 RPC 调用的标识
- **ParentSpanID**：父调用的 Span，串联调用链
- **Annotation**：记录事件时间戳（Client Send / Server Receive / Server Send / Client Receive）

### 3. 面试回答

> **Q：微服务怎么追踪一次请求的完整调用链？**
>
> 基于 Google Dapper 论文的 Trace 系统。请求入口生成全局唯一的 **TraceID** 并透传到所有下游服务。每次 RPC 调用生成一个 **Span**，记录客户端发送、服务端接收、服务端发送、客户端接收四个时间戳。通过这些数据可以计算出每个环节的耗时，快速定位性能瓶颈。主流工具：**Zipkin**（Twitter）、**Jaeger**（Uber）。

---

## 📌 七、容器化与 Kubernetes

### 1. Docker 核心技术

```
Docker 的本质 = Linux 进程隔离技术

Namespace → 隔离视图（进程/网络/文件系统）
Cgroups   → 隔离资源（CPU/内存/磁盘IO）
```

**Docker 与虚拟机的区别**：

```
虚拟机：            Docker 容器：
┌────────────────┐  ┌────────────────┐
│ App A │ App B  │  │ App A │ App B  │
├────────────────┤  ├────────────────┤
│ Guest OS       │  │ Bin/Lib│Bin/Lib│
├────────────────┤  ├────────────────┤
│ Hypervisor     │  │ Host OS        │
├────────────────┤  ├────────────────┤
│ Host OS        │  │ Hardware       │
├────────────────┤  └────────────────┘
│ Hardware       │
└────────────────┘
```

**Docker 解决了什么问题**？
1. **环境一致性问题**：开发环境、测试环境、线上环境差异大
2. **部署初始化繁琐**：每个服务部署到新机器都要装 JDK/配置等

### 2. Kubernetes 核心概念

| 概念 | 作用 |
|------|------|
| **Pod** | 最小部署单元，一个 Pod 包含一个或多个容器 |
| **Service** | 提供稳定的网络入口，负载均衡到一组 Pod |
| **Deployment** | 声明式更新 Pod，支持滚动升级/回滚 |
| **ConfigMap** | 配置管理 |
| **Namespace** | 资源隔离 |

### 3. 面试回答

> **Q：Docker 和传统虚拟机有什么区别？**
>
> Docker 不是虚拟化操作系统，而是**进程级隔离**。Docker 容器本质上是宿主机上的进程，通过 **Namespace** 隔离视图（每个容器看自己的文件系统、网络），通过 **Cgroups** 限制资源使用。虚拟机需要完整的客户操作系统，启动慢、资源占用大；Docker 直接共享宿主机内核，启动秒级、资源开销小。

---

## 📌 八、DevOps 与 CI/CD

### 1. 微服务 + DevOps + 容器 的关系

```
微服务（架构拆分解耦） → 需要容器化（环境一致性）
   ↓                       ↓
容器化 → 微服务灵活扩缩容 → 需要自动化运维
   ↓                       ↓
自动化运维 → DevOps（开发+测试+部署全流程自动化）
```

### 2. CI/CD 流程

```
代码提交 → 自动构建 → 自动测试 → 自动部署
 (git push)  (mvn package)  (单元测试/集成)   (上线)
```

**DevOps 的本质**：**谁开发谁运维**。服务开发者负责全生命周期（开发→测试→部署→运维），流程完全自动化。

---

## 📌 九、Service Mesh（下一代微服务架构）

### 1. 传统微服务的问题

```
传统微服务：治理逻辑写在应用代码里
┌────────────────────┐
│  应用服务           │
│  ├─ 业务逻辑        │
│  ├─ 熔断器(Hystrix) │  ← 每个服务都要引入 SDK
│  ├─ 限流(限流器)    │
│  └─ 服务发现        │
└────────────────────┘
```

**问题**：服务治理 SDK 侵入业务代码，升级 SDK 需要大量修改。

### 2. Service Mesh 架构

```
Service Mesh：治理逻辑交给 Sidecar，业务代码零侵入

┌───────────────┐         ┌───────────────┐
│  服务A(业务逻辑)│         │  服务B(业务逻辑)│
└──────┬────────┘         └──────┬────────┘
       │                         │
┌──────▼────────┐         ┌──────▼────────┐
│ Sidecar Proxy  │◀────────▶│ Sidecar Proxy  │
│ (Envoy/Istio)  │  通信    │ (Envoy/Istio)  │
└────────────────┘         └────────────────┘
       ↑                         ↑
       │        控制平面          │
       └─────────┬───────────────┘
                 │
         ┌───────▼────────┐
         │ Pilot/Mixer    │
         │ Citadel        │
         └────────────────┘
```

**两大平面**：
- **数据平面**（Data Plane）：Sidecar 代理（Envoy），拦截所有出入流量
- **控制平面**（Control Plane）：管理 Sidecar 配置（Pilot/ Mixer/ Citadel）

### 3. Istio 核心功能

| 功能 | 说明 |
|------|------|
| **流量管理** | 路由规则、灰度发布、故障注入 |
| **安全** | 服务间 mTLS 加密 |
| **可观测性** | 自动 Trace、Metrics、日志 |
| **策略执行** | 限流、访问控制 |

### 4. 面试回答

> **Q：什么是 Service Mesh？为什么需要它？**
>
> 传统微服务把熔断、限流、服务发现等治理能力以 SDK 方式嵌入应用代码，导致：
> - SDK 升级困难（每个服务都要改）
> - 多语言支持差（每种语言要维护一套 SDK）
>
> Service Mesh 把这些治理能力抽离成独立的 **Sidecar 代理**，和业务容器部署在一起，拦截所有网络流量。业务代码零侵入，只需要关心业务逻辑。代表产品 **Istio**（数据平面用 Envoy，控制平面统一管理）。

---

## 📌 十、微服务落地实践关键点

### 1. 服务划分原则

| 原则 | 说明 |
|------|------|
| **单一职责** | 一个服务只做一件事 |
| **闭包原则** | 变更尽量在一个服务内完成 |
| **横向拆分** | 按业务功能拆分（订单/用户/商品） |
| **纵向拆分** | 按调用链路拆分（业务/基础能力） |

### 2. 容量规划

```
压测 → 找出单机 QPS 上限 → 预估总流量 → 计算机器数
↓
验证 → 预留 buffer (30-50%)
```

**分层规划**：接入层、业务层、数据层、缓存层每层的容量规划不同。

### 3. 多机房部署

**三个核心问题**：
1. **数据同步**：跨机房数据实时同步
2. **流量调度**：DNS/负载均衡切换流量
3. **服务自动切换**：机房故障自动转移流量

**常见方案**：同城双活、异地多活、两地三中心

### 4. 混合云部署

**为什么要混合云**：大促时弹性扩容到公有云，平时用自建机房节省成本。

**关键点**：私有云和公有云之间网络互通 + 统一的服务注册发现。

---

## 📌 十一、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **为什么用微服务？** | 独立部署/独立扩展/独立开发/故障隔离 |
| **什么时候拆微服务？** | 代码臃肿编译慢、团队大协作难、扩展需求不同时 |
| **注册中心原理？** | 提供者注册→心跳保活；消费者拉取+本地缓存 |
| **RPC 怎么实现？** | 动态代理 + 序列化 + Netty NIO 通信 + 反序列化 |
| **注册中心怎么选？** | CP(ZK/Consul) vs AP(Eureka)，注册发现推荐 AP |
| **熔断是怎么工作的？** | Closed→Open→Half-Open，三段式状态机 |
| **服务追踪怎么做？** | TraceID 贯穿全链路，Span 记录每次调用的四个时间戳 |
| **Docker 解决了什么？** | 环境一致性 + 部署初始化自动化 |
| **Docker vs VM？** | 共享宿主机内核 vs 完整客户 OS，秒级启动 vs 分钟级 |
| **Service Mesh 是什么？** | 治理能力抽离到 Sidecar，业务零侵入 |
| **灰度发布怎么做？** | 服务路由 + 标签路由 + 逐步放量 |
| **多机房部署三个问题？** | 数据同步/流量调度/服务自动切换 |

---

> 📝 **关键认知**：微服务不是银弹，本质是把**本地调用变成了远程调用**，解决了扩展性问题，但也引入了网络不可靠的新问题。所以面试时一定要体现对"网络带来不确定性"的理解——超时、重试、幂等、熔断、限流这些治理手段全是为此而生。
