# 撮合引擎一页纸速览 — 面试前 5 分钟过一遍

> 用法：面试前快速扫一遍；全部能默写就过关。配合《撮合引擎核心技术与实战》看细节。

---

## 📌 一句话

撮合引擎 = 输入订单事件、输出成交的**确定性状态机**，铁律是**价格优先、时间优先**。

---

## 🏗️ 架构总览（默画）

```
  用户 / 机器人
     │  订单
     ▼
  接入网关 Gateway（多线程收单、校验、限频）
     │
     ▼ 定序 Sequencer ── 全局递增序号（顺序是灵魂）
   ┌────────────── 每个交易对一套 ──────────────┐
   │ Disruptor 无锁环形缓冲（LMAX）             │
   │     ▼                                      │
   │ 撮合核心 Matcher（单写者 · 单线程 · 无锁）   │
   │     └─ 订单簿：TreeMap<价档, 链表<订单>>     │
   │        买盘降序 / 卖盘升序，PTP 撮合         │
   └────────────────────────────────────────────┘
     │  成交事件（带序号）
     ├─► 行情模块：K线/盘口/深度 → 推送（最终一致）
     ├─► 清算模块：资金/仓位（异步批次落库，强一致）
     └─► WAL / 快照：事件溯源，崩溃重放恢复
```

---

## 🧬 三代演进（必背）

| 代 | 撮合 | 清算 | 吞吐 | 教训 |
|---|---|---|---|---|
| 1 | 数据库 | 数据库 | ~100/s | 磁盘慢、锁重 |
| 2 | 内存 | 数据库 | ~1000/s | **清算才是瓶颈** |
| 3 | 内存 | 内存 | ~10万/s | 状态易失，需恢复机制 |

---

## 📊 订单簿与撮合

- **结构**：`TreeMap<价格档位, 双向链表<订单>>`；最优价访问 O(1)；同价队列按时间排序
- **成交条件**：买价 ≥ 卖价；**先挂方价格为成交价**（吃单方获价格改善）
- **限价单**：吃穿对手盘 → 剩余挂盘口；**市价单**：直接吃对手盘 → 剩余按 IOC 撤销
- **替代结构**：跳表（OKX，无锁友好）/ Flat Array（缓存友好，P999 13× 提升）

---

## 🚀 高并发三板斧

1. **单写者**：每交易对一个线程，无锁 + 确定性 + 可重放
2. **定序**：全局序号，重放 = 复现，热备 bit-identical
3. **分片**：按交易对横向扩展，吞吐线性增长

辅助：批次落库（500笔/5ms 双触发）、Redis Lua 预扣资金、对象池零分配、缓存行对齐。

---

## 💾 持久化与可用性

- **事件溯源**：存事件序列，不存终态；内存状态 = 事件重放投影
- **恢复三板**：WAL（先写后算）→ 快照（定期）→ 主备（同输入流秒切）
- **一致性**：资金/仓位**强一致**，行情/推送**最终一致**；双写失败 → 以日志为准 + 对账补偿

---

## 🔢 性能数字（背 5 个）

| 数字 | 出处 |
|---|---|
| 单线程 600万 单/s | LMAX Disruptor |
| 单节点 140万 交易/s | 币安（公开资料） |
| 单核 50万 单/s、P99<50μs、恢复<2s | OKX（公开资料） |
| Array+链表 vs Tree：P999 45μs→3.4μs（**13×**） | TradeX |
| 缓存行对齐：内存访问 **+300%** | OKX |
| 16核写吞吐：跳表比红黑树 **+40%** | OKX |

---

## 💬 面试金句（收尾用）

- **确定性 > 并行**
- **尾延迟 > 平均延迟**
- **瓶颈在清算，不在撮合**
- **顺序交给事件流，不交给锁**
- **单交易对无法水平扩展，但重放输入日志即可热备**

---

## ✅ 检查清单（出门前自测）

- [ ] 能默画架构图（网关→定序→撮合→行情/清算/持久化）
- [ ] 能讲三代演进和"清算瓶颈"
- [ ] 能讲清 TreeMap+链表 的增删查复杂度
- [ ] 能背出 5 个性能数字
- [ ] 能答"内存丢了怎么办"（WAL+快照+主备）
- [ ] 能结合自己 CoinTrader 代码讲 1 个亮点 + 1 个可优化点
