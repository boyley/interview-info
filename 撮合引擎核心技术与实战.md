# 撮合引擎核心技术与实战 — 面试备战手册

> 来源：CME/NASDAQ 官方撮合文档、LMAX Disruptor、币安/OKX/火币公开技术分享、廖雪峰《高性能交易系统设计原理》、GitHub 开源撮合引擎（match-trade / cnfinder/matchengine / crypto-zero/apex-engine / TradeX / Simple-HFT-Engine）、Devexperts《Matching Engines for Dummies》、交易所后端面经
> 适用：交易所/量化/金融后端岗位，重点覆盖"如何设计一个撮合引擎"系统设计题

---

## 📌 一、面试高频考点速查

| 核心考点 | 重要度 |
|---------|--------|
| **价格优先、时间优先（PTP/FIFO）** | ⭐⭐⭐⭐⭐ |
| **三代架构演进（DB撮合→内存撮合→全内存撮合+清算）** | ⭐⭐⭐⭐⭐ |
| **订单簿数据结构（TreeMap+链表 / 跳表 / Flat Array）** | ⭐⭐⭐⭐⭐ |
| **单写者（Single Writer）+ Disruptor 无锁** | ⭐⭐⭐⭐⭐ |
| **事件溯源 + 快照恢复 + WAL** | ⭐⭐⭐⭐⭐ |
| **订单类型（限价/市价/IOC/FOK/冰山）** | ⭐⭐⭐⭐ |
| **撮合算法家族（FIFO/Pro Rata/TOP/LMM）** | ⭐⭐⭐⭐ |
| **分片与横向扩展（按交易对/用户分片）** | ⭐⭐⭐⭐ |
| **双写一致性与分布式事务（强一致 vs 最终一致）** | ⭐⭐⭐⭐ |
| **延迟优化（缓存行对齐/对象池/整数刻度/CPU亲和）** | ⭐⭐⭐⭐ |
| **撤单与改单的并发处理** | ⭐⭐⭐ |
| **头部交易所架构对比（NASDAQ/CME/LMAX/币安/OKX）** | ⭐⭐⭐ |

---

## 📌 二、面试回答标准模板

> **面试官："如何设计一个高性能撮合引擎？"**
>
> **回答（按数据→算法→并发→持久化→可用性五层）**：

"撮合引擎本质是一个**确定性状态机**：输入订单事件、输出成交结果，核心铁律是**价格优先、时间优先**。我从五层来设计：

**① 数据结构层**：订单簿用两个有序结构——买单降序、卖单升序。经典实现是 `TreeMap<价格档位, 双向链表<订单>>`，O(1) 取最优价、O(1) 档位内增删；追求极致性能可换跳表或价格索引的 flat array。

**② 算法层**：新单与对手盘最优价撮合，吃完一档吃下一档；限价单未成交部分挂回盘口，市价单直接吃对手盘；支持 FIFO / Pro Rata 等分配算法。

**③ 并发层**：**每个交易对一个单写者线程**串行处理，天然无锁、确定性可重放；用 Disruptor 无锁环形缓冲承载订单流；多交易对横向分片线性扩展。

**④ 持久化层**：**事件溯源**——每笔订单先落顺序日志（WAL）再撮合，内存状态可由事件重放重建；定期打快照加速崩溃恢复。

**⑤ 高可用层**：主备双引擎消费**同一序列化输入流**，主挂备切、输出 bit-identical；撮合与清算解耦，清算异步批次化落库，资金强一致、行情最终一致。

整个系统追求的是**低尾延迟（P99/P999）和确定性**，而不是平均吞吐。LMAX 用单线程做到 600 万 TPS、OKX 单核 50 万笔/秒，说明这条路是行得通的。"

---

## 📌 三、撮合引擎是什么

### 1. 定义与地位

撮合引擎是交易所/券商**最核心的模块**，本质是买卖订单的"匹配器"。它不负责资金、不负责行情，只做一件事：**按公平规则把买盘和卖盘撮在一起产生成交**。交易系统的延迟、吞吐、公平性都由它决定。

- 输入：用户订单（委托 Order）
- 输出：成交明细（Trade）、订单簿变更（深度/盘口，即行情）

### 2. 撮合基本规则（对照表）

| 规则 | 说明 |
|------|------|
| **成交条件** | 买价 ≥ 卖价即成交（买一 ≥ 卖一） |
| **成交价确定** | 先挂单方（价格制定者）的价格为成交价，吃单方获得价格改善 |
| **价格优先** | 最高买价 / 最低卖价先成交 |
| **时间优先** | 同价位，先到先得（FIFO） |
| **市价单** | 无价格，直接以对手最优价成交，优先于限价单 |
| **部分成交** | 深度不足时吃单方部分成交，剩余依订单类型挂盘或撤销 |

### 3. 订单簿长什么样

```
          ┌─────────────── 卖盘 Ask（升序，最低价最优先）───────────────┐
          │  价格   │  数量   │                                        │
          │  1.002  │  120   │   ← 卖一（最优卖价）                    │
          │  1.003  │   80   │                                        │
          └───────────────────────────────────────────────────────────┘
                         ▲ 买价 ≥ 卖价 = 成交区
          ┌─────────────── 买盘 Bid（降序，最高价最优先）───────────────┐
          │  价格   │  数量   │                                        │
          │  1.001  │  200   │   ← 买一（最优买价）                    │
          │  1.000  │  500   │                                        │
          └───────────────────────────────────────────────────────────┘
```

### 4. 一笔订单的生命周期

```
Order 下单
  │
  ▼
① 定序 Sequence   ← 打全局递增序号，保证顺序
  │
  ▼
② 校验 Validation  ← 风控/资金预扣（Redis 预扣淘汰无效单）
  │
  ▼
③ 撮合 Match      ← 按价格优先、时间优先与对手盘成交
  │
  ▼
④ 成交 Trade      ──► 行情模块（算K线/盘口/推送）
  │                └─► 清算模块（资金变动、账本）
  ▼
⑤ 未成交部分 → 挂盘口等待 / 按订单类型撤销（IOC/FOK）
```

### 5. 最新价 / 盘口价 / 收盘价从哪来

**先分清三个价：**

| 价格 | 含义 | 来源 |
|------|------|------|
| **买一 / 卖一价** | 盘口最优挂单价 | 订单簿（撮合引擎维护的挂单） |
| **最新价（Last）** | 最近一笔成交的价格 | 撮合引擎 `processMatch` 确定 |
| **收盘价（Close）** | K线窗口内最后一笔成交价 | 行情模块累加 |

**核心规则：成交价 = 先挂单（被动单）方的价格。**
- 限价单 vs 限价单 → 取先挂单方的限价
- 市价单吃限价盘口 → 取对手限价单的价格
- 吃单方因此获得"价格改善"（Buy 限价 1.000 吃到卖一 0.999 → 成交价 0.999）
- 代码：`CoinTrader.processMatch` 中 `if (LIMIT_PRICE) dealPrice = matchOrder.getPrice()`

**最新价流转链路：**

```
CoinTrader.processMatch 确定 dealPrice
  → ExchangeTrade.price
  → Kafka topic "exchange-trade"
  → Market 模块 DefaultCoinProcessor.handleThumb()
       coinThumb.setClose(exchangeTrade.getPrice())   ← 最新价 = 最后一笔成交价
  → 推送 / 展示 / K线 close
```

**面试金句**：最新价不是"算出来的平均价"，而是"最后一笔成交的价格"；它由撮合引擎在成交瞬间确定并随事件流投影到行情，行情模块只消费事件、不重算。K 线收盘价 = 该窗口最后一笔成交价，开盘价 = 第一笔——最新价、收盘价本质是"最后一笔"在不同粒度上的别名。

---

## 📌 四、三代架构演进（必背）

廖雪峰《高性能交易系统设计原理》梳理的三代演进是面试经典题：

| 代际 | 模型 | 撮合位置 | 清算位置 | 吞吐量 | 瓶颈 |
|------|------|---------|---------|--------|------|
| **第一代** | 数据库撮合 | MySQL 每次查表排序 | 数据库事务 | ~100 单/s | 磁盘 I/O、表锁 |
| **第二代** | 内存撮合 | 内存订单簿 | 仍走数据库事务 | 撮合可达 100万/s，**整体仅 ~1000/s** | 清算系统 |
| **第三代** | 全内存撮合+清算 | 内存 | 内存 | ~10万/s | 内存易失，需恢复 |

**核心结论（面试金句）**：
- 单靠撮合快没用——**整体瓶颈在清算**。撮合 100 万/s 但清算落库 1000/s，整体还是 1000/s。必须撮合、清算**全内存化**。
- 全内存方案用**单线程无锁模型**（LMAX Disruptor、Redis 单线程都验证了这条路）。
- 代价是**状态易失** → 必须设计可靠的状态恢复机制（WAL / 快照 / 重放）。

---

## 📌 五、订单簿数据结构（重点）

### 1. 经典实现：TreeMap + 双向链表

```
TreeMap<BigDecimal, 链表<Order>> bids   // key=价格档位, value=同价订单队列(时间序)
TreeMap<BigDecimal, 链表<Order>> asks
```

- 撮合：`asks.firstKey()` 取最低卖价、`bids.lastKey()` 取最高买价 → **O(1)**
- 档位插入/删除 → O(log n)；档位内订单按时间排队，链表尾插、头出
- 撤单：找到订单所属档位，链表摘除

> **代码对照**：Bizzan 的 `CoinTrader` 正是这个结构——`TreeMap<BigDecimal, MergeOrder> buyTradePlate/sellTradePlate`，MergeOrder 内部再用队列按时间排队（`exchange/src/.../Trader/CoinTrader.java`）。

### 2. 各数据结构对比（面试对照表）

| 结构 | 最优价访问 | 插入 | 缓存友好 | 无锁并发 | 代表 |
|------|-----------|------|---------|---------|------|
| **红黑树 / TreeMap** | O(1) | O(log n) | 差（指针跳跃） | 难 | bunex、Bizzan |
| **跳表 SkipList** | O(1)/O(log n) | O(log n) | 中 | ✅ 容易 | OKX、GoMatchingKernel |
| **Flat Array（价格索引）** | O(1) | O(1) | ✅✅ 极好 | ✅ | TradeX、Simple-HFT-Engine |
| **优先队列（BBO）** | O(1) | O(log n) | 差 | 需惰性清理 | 简易实现 |

**金句**：撮合场景下算法复杂度常被**缓存局部性**碾压——TradeX 把 Tree+Deque 换成 Array+侵入式链表后，P999 延迟从 45μs 降到 3.4μs（**13 倍**），靠的是内存连续、CPU 预取命中。

### 3. 性能细节

- **缓存行对齐（64 字节）**：热点字段打包进同一缓存行，避免伪共享（False Sharing）。OKX 称内存访问效率提升 **300%**
- **整数刻度**：价格用整数 tick 而非 double/float，避免浮点误差在百万次比较中累积
- **预分配对象池**：启动时 Slab Pool 预分配，热路径**零动态分配**（free-list O(1) 复用）
- **侵入式链表**：订单节点自带 prev/next 指针，撤单 O(1) 摘链，无需 O(n) 扫描

---

## 📌 六、撮合算法详解

### 1. 价格-时间优先（PTP / FIFO）

- 唯一标准：**价格 + 时间**
- 同价位先到先得；改价、加量会**失去时间优先权**（先撤后挂）
- CME 规则细节：直接单优先于隐含单；下单后更改数量/价格/账户号 → 失去优先级

### 2. 撮合算法家族（CME 官方，对照表）

| 算法 | 分配规则 | 适用场景 |
|------|---------|---------|
| **FIFO（PTP）** | 仅按价格→时间 | 全球主流，80%+ 市场 |
| **Pro Rata** | 按各单占价位总挂单量的**比例**分配成交，取整后余数按 FIFO | 流动性极高品种（CME 欧元美元） |
| **Threshold Pro Rata** | 最优价第一单优先（TOP），剩余按 Pro Rata + FIFO | 防止大单被拆碎 |
| **TOP Order** | 最优报价的首单优先成交（每边最多 1 个 TOP） | 激励提供最佳流动性 |
| **FIFO + LMM** | 做市商（LMM）先按配置比例分，剩余 FIFO | 做市商合约 |
| **TimeProRata** | 同价按比例分，但时间越早分得越多 | 折中方案 |

### 3. 限价单 vs 市价单撮合流程

**限价买单 `buy 100 @ 1.000`：**
```
① 卖一 ≤ 1.000？ → 是，以卖一价成交（吃单方获得价格改善）
② 吃穿卖一 → 继续吃卖二、卖三……
③ 全部成交 → 结束；有剩余 → 挂进买盘排队等待
```

**市价买单：**
```
① 跳过价格比较，直接吃卖一
② 吃到卖盘深度不足 → 剩余按订单类型处理（IOC = 剩余立即撤销）
```

### 4. 订单类型（扩展性设计考察点）

| 类型 | 全称 | 行为 |
|------|------|------|
| **限价单 Limit** | — | 指定价格，未成交部分挂盘口 |
| **市价单 Market** | — | 最优价立即成交 |
| **FOK** | Fill or Kill | 全部成交，否则全部撤销 |
| **IOC** | Immediate or Cancel | 立即成交的部分保留，剩余撤销 |
| **AON** | All or None | 全量成交，否则不成交 |
| **冰山单 Iceberg** | — | 只展示部分数量，隐藏总量，保护大单 |
| **止损单 Stop** | — | 触价后转为市价单或限价单 |

---

## 📌 七、高并发架构设计

### 1. 单写者模型（Single Writer）— 架构灵魂

- **每个交易对一个线程/事件循环**，串行处理该交易对的所有订单
- 天然**无锁、确定性、可重放** → 故障恢复后状态逐位一致
- 横向扩展：**按交易对分片**，每片独立引擎（BTC-USD 一片、ETH-USD 一片），吞吐随分片线性增长

```
订单网关(多线程收单)
   │
   ▼ 定序(Sequencer)
Disruptor / 队列  ← 全局序号, 保证顺序
   │
   ▼
撮合核心(单线程, 无锁) ──► 成交 ──► 行情模块 / 清算模块
   │
   └── 每个交易对一套完整流水线，互不干扰
```

### 2. 定序（Sequencing）是灵魂

- 所有订单先进入**单一顺序日志**打全局序列号
- 撮合严格按序列号顺序处理 → **重放日志 = 复现历史 = bit-identical 输出**
- **NASDAQ INET 实证**：每秒处理 100 万消息、亚 40 微秒延迟、记录日峰值 11.3 亿消息/秒。核心 trade-off 就是**"确定性优先于并行"**——单个 symbol 无法横向扩展，但可以靠重放输入日志做热备。

### 3. LMAX Disruptor（必考）

- **无锁环形缓冲区**：生产者多写、消费者单读，靠序号 + CAS 而非锁，零 GC（预分配环形数组），缓存友好（缓存行填充防伪共享）
- 号称**单线程 600 万订单/秒**
- 三层架构：Input Disruptor → Business Logic Processor（单线程）→ Output Disruptor
- **对比 LinkedBlockingQueue**：

| 维度 | Disruptor | LinkedBlockingQueue |
|------|-----------|---------------------|
| 同步 | 无锁（序号/CAS） | 加锁（ReentrantLock） |
| 分配 | 预分配数组，零 GC | 动态分配节点 |
| 延迟 | 确定性低延迟 | 不可控 |
| 场景 | 单写/多写，撮合核心 | 通用队列 |

### 3.1 Disruptor 在流水线里的位置（Kafka vs Disruptor）

**先分清两层角色：**

| 维度 | Kafka | Disruptor |
|------|-------|-----------|
| 作用域 | 跨服务 / 跨进程 | **进程内线程之间** |
| 持久化 | ✅ 落盘可重放 | ❌ 纯内存，进程挂即失 |
| 职责 | 分发 + 持久化 + 跨服务顺序 | 进程内高速无锁保序交付 |
| 延迟 | 毫秒级 | 微秒级 |

**正确数据流：**

```
Kafka 消费者线程拉取一批 → Disruptor（多生产者→单消费者）
                                → 撮合单线程按序号逐个取出 → 应用进订单簿 → 撮合
```

**为什么中间放 Disruptor：** 无锁低延迟、严格保序（序号=顺序）、零 GC（预分配）、背压（环满生产者等待）、批量消费、缓存友好。把"收数据"和"撮合"解耦，撮合线程永远只做纯计算。

**不放大行不行？** 行——Kafka 消费者线程直接兼任撮合线程（Bizzan 的简化做法），代价是 Kafka 拉取阻塞/批处理耦合进撮合热路径，延迟波动大。

**架构替代（加分）：** 超低延迟方案可绕过 Kafka——订单从网关**直推 Disruptor**（进环前先写 WAL 保持久化），Kafka 只用于对外分发成交（行情/清算）。LMAX 即此方案。Kafka→Disruptor 是稳妥方案，网关直推是极致方案。

### 4. 批次处理（Batching）

- 成交/资产流水**批次落库**：数量阈值（如 500 笔）+ 时间阈值（如 5~10ms）**双触发**
- 摊薄 SQL 解析、WAL 刷盘、索引更新的固定成本
- 撮合引擎热路径只算不改库，异步线程负责落盘

### 5. 热点账户与资金预扣（Shift Left）

- **Redis Lua 脚本事前资金预扣**：淘汰无效订单，避免热点余额更新打爆 DB 行锁
- 分片倾斜：`userId % 128` 扎堆 → 观测热点 + 热点账户单独队列/本地合并处理
- 分布式 ID：Snowflake（时间戳+机器ID+序列号），注意时钟回拨

---

## 📌 八、持久化与高可用

### 1. 事件溯源（Event Sourcing）

- 不存最终态，存**事件序列**；内存状态是事件重放的投影
- 优点：可审计、可精确重放、可恢复
- **面试追问点**：分清"真事件源"还是"事件日志 + 最终态"；RocksDB 里存的是事件还是快照；快照生成策略；baseLog 与 RocksDB 的边界

### 2. 恢复机制（对照表）

| 机制 | 说明 |
|------|------|
| **WAL 顺序日志** | 每笔订单先顺序写日志再处理；OKX 重启 **2 秒**恢复全部状态 |
| **快照 + 重放** | 定期把内存订单簿序列化为快照；崩溃后加载最近快照 + 重放快照之后的事件 |
| **主备复制** | 主备消费**同一序列化输入流**，热备实时同步；主挂备切，输出 bit-identical |

### 3. 双写一致性（高频追问）

- 场景：日志/DB 已落、异步同步失败怎么办 → **以日志为准 + 补偿/对账**
- 原则：**资金/仓位强一致，行情/推送最终一致**
- 分布式事务：**TCC**（Try/Confirm/Cancel，适合自动借币还币的预占）vs **Saga**（补偿）
- 幂等：订单 ID 唯一键去重、消费位点持久化，成交以"订单+序号"唯一约束

---

## 📌 九、性能优化全景（背数字）

| 优化项 | 效果 |
|--------|------|
| 内存撮合 | 单节点 100万+/s |
| Disruptor | 单线程 600万 单/s |
| 缓存行对齐 | OKX 内存访问效率 +300% |
| Array+侵入式链表 vs Tree | P999 45μs → 3.4μs（13×） |
| 跳表 vs 红黑树 | 16核写吞吐 +40%（OKX） |
| 批次落库 | 摊薄固定成本，热路径零 DB |
| CPU 亲和性 | 减少缓存失效/线程迁移 |
| 内核旁路 DPDK / RDMA | 网络→应用 <1μs（OKX RoCEv2） |
| FPGA 硬件撮合 | 订单解析 10μs → 2.3ns，单节点 240万 TPS |
| 整数刻度（非浮点） | 消除百万次比较的浮点误差 |

---

## 📌 十、头部交易所/平台架构对比（加分区）

| 平台 | 语言/框架 | 核心设计 | 性能标杆 |
|------|-----------|---------|---------|
| **NASDAQ INET** | — | Disruptor 模式、严格序列化事件日志、单线程撮合、UDP 组播行情、热备重放 | 100万 消息/s、<40μs；峰值 11.3亿 消息/日 |
| **CME Globex** | — | 多撮合算法按品种配置（FIFO/Pro Rata/TOP/LMM） | 全球最大期货撮合 |
| **LMAX** | Java | 单线程 BLP + Disruptor | 600万 订单/s |
| **币安 Binance** | Go/Java + K8s | 自研内存撮合 + 订单簿分片，微服务 | 单节点 140万 交易/s，订单确认 ~12ms |
| **OKX 欧易** | — | 跳表订单簿、缓存行对齐、热/冷区分级存储、RDMA、WAL | 单核 50万 单/s、P99<50μs、恢复<2s |
| **火币 Huobi** | Clojure | JVM 函数式 + STM（软件事务内存）并发模型 | — |
| **Bizzan（你的代码）** | Java | TreeMap 盘口 + 每交易对一实例 + Kafka 解耦 | 学习/教学级 |

> 注意：部分 OKX/币安数值来自第三方技术解读，可能有营销成分，面试引用时说明"公开资料显示/约"。

---

## 📌 十一、结合代码实战：Bizzan CoinTrader 拆解（讲自己的项目）

把通用知识与前面分析的 `exchange` 模块对应起来，面试讲"我做过"最有说服力：

| 通用知识点 | Bizzan 代码对应（`exchange/src/.../Trader/CoinTrader.java`） |
|-----------|------------------------------------------------------------|
| 订单簿 TreeMap + 队列 | `TreeMap<BigDecimal, MergeOrder> buyTradePlate / sellTradePlate`，MergeOrder 内部队列按时间排队 |
| 限价对限价撮合 | `matchLimitPriceWithLPList` 逐档吃盘口 |
| 市价单撮合 | `matchMarketPriceWithLPList` |
| 部分成交拆单 | `processMatch` 计算可成交量，产出 `ExchangeTrade` |
| 每交易对单实例（分片雏形） | `CoinTraderFactory.getTrader(symbol)` 按交易对建独立撮合器 |
| 定序 / 解耦 | Kafka `exchange-order` 进 → 撮合 → Kafka `exchange-trade` / `exchange-trade-plate` 出，行情模块消费 |
| 撤单独立通道 | `exchange-order-cancel` 单独 topic 消费，不阻塞撮合主线 |
| 撮合暂停保护 | `isTradingHalt() / getReady()` 未就绪直接退单（`exchange-order-cancel-success`） |

**能讲出的亮点 / 槽点：**
- ✅ 每交易对单实例 = 单写者分片思想的雏形
- ✅ 撤单走独立队列，不阻塞撮合主线
- ⚠️ 用 `synchronized` 细粒度锁，可换无锁队列/Disruptor 提升
- ⚠️ 内存盘口无 WAL/快照恢复，重启丢状态 → 正是三代演进里"内存易失"要解决的问题
- ⚠️ `tradeCompleted` 同步落库（资金/清算耦合在撮合路径里），可异步批次化
- ⚠️ 行情/清算共用同一 Kafka 消费线程池，容量规划需谨慎

---

## 📌 十二、高频面试题 Q&A

**Q1：价格优先、时间优先怎么实现？**
买盘降序、卖盘升序两个有序表；撮合从对手盘最优价开始取；同价队列按到达时间排队（链表尾插）。改单要失去时间优先：先撤后挂。

**Q2：为什么用单线程撮合？不会慢吗？**
单个交易对的撮合是严格串行逻辑，多线程只会引入锁和不确定性。单线程 + 无锁环形队列消除上下文切换与锁竞争，LMAX 证明单线程可达 600 万 TPS。吞吐靠**多交易对分片**横向扩展，而不是给单交易对加线程。

**Q3：内存撮合丢了怎么办？**
三层防护：① WAL 顺序日志，先写后算；② 定期快照，崩溃后"快照 + 重放"；③ 主备热备消费同一输入流，秒级切换。事件溯源保证确定性重放、输出逐位一致。

**Q4：Disruptor 为什么比 BlockingQueue 快？**
无锁（序号 + CAS 而非锁）、预分配环形数组零 GC、缓存行填充防伪共享、专为单读者/多写者优化。

**Q5：撮合和行情、清算怎么解耦？**
Kafka/MQ 异步：撮合只负责"匹配 + 发事件"（成交、盘口）；行情模块消费成交算 K 线推送；清算模块异步批次落库。顺序性靠"单交易对单分区"保证。

**Q6：怎么防止订单重复处理？**
事件幂等：订单 ID 唯一键去重、消费位点持久化、成交用"订单+序号"唯一约束。

**Q7：撤单和撮合怎么避免竞争？**
单写者模型下，撤单和下单进**同一事件流按序处理**，天然无竞争。多线程方案需把同一交易对路由到同一线程或加锁。

**Q8：改单为什么难？**
改单 = 撤旧单 + 挂新单，但已撮合部分不可逆。需冻结/释放差额资金，处理"订单已撮合、深度变更、风控锁定"等失败回滚，用户可见状态要正确回退。

**Q9：怎么压测撮合引擎？**
用真实订单流回放 + 构造极端行情（单边大单、瞬间洪峰）；看 P50/P99/P999 延迟、TPS、撤单延迟；逐环节找瓶颈（定序→撮合→清算→落库）。

**Q10：行情和撮合怎么保证一致？**
行情是撮合事件的"投影"——撮合每产出一个成交就发一个带序列号的事件，行情模块按序列号消费更新 K 线/盘口。允许最终一致，资金账务才要求强一致。

---

## 📌 十三、系统设计题模板：设计百万级 TPS 撮合引擎

**① 需求澄清**：交易对数量、订单类型、延迟目标（如 P99 < 10ms）、持久化要求、容灾级别。
**② 数据流**：Gateway（多线程收单）→ Sequencer（全局序号）→ Matcher（单线程/交易对）→ Output（成交/行情/清算）。
**③ 订单簿**：TreeMap + 双向链表（或跳表）；整数刻度；缓存行对齐；对象池。
**④ 并发**：单写者 + Disruptor；多交易对分片；热点交易对单独引擎。
**⑤ 持久化**：WAL + 快照 + 重放；事件溯源。
**⑥ 高可用**：主备复制、秒级切换；清算异步批次。
**⑦ 扩展**：交易对分片线性扩容；行情 UDP 组播 + TCP 重传补拉。
**⑧ 收尾金句**：强调"**确定性 > 并行**"、"**尾延迟 > 平均延迟**"、"**瓶颈在清算不在撮合**"。

---

## 📌 十四、参考资料

- 廖雪峰《高性能交易系统设计原理》：三代架构演进
- CME Group 撮合算法官方文档（FIFO / Pro Rata / TOP / LMM / TimeProRata）
- NASDAQ INET 架构（LMAX Disruptor 模式、UDP 组播行情）
- LMAX Disruptor 官方资料
- Binance Academy：撮合引擎词条
- OKX 欧易撮合引擎架构深度解析（跳表/缓存行/热冷区/RDMA/WAL）
- Devexperts《Matching Engines for Dummies》
- GitHub 开源引擎：match-trade / cnfinder/matchengine / crypto-zero/apex-engine / TradeX / Simple-HFT-Engine / bunex
- handbook-academy/engineering-handbook：20-stock-exchange 系统设计案例
- 交易所后端面经（撮合引擎数据结构、事件溯源、双写一致性、改单）

---

## ✅ 检查清单

- [ ] 能画出三代架构演进图并讲清"瓶颈在清算不在撮合"
- [ ] 能讲清 PTP 规则 + 订单簿数据结构（TreeMap+链表）增删查
- [ ] 能用数据/算法/并发/持久化/可用性五层讲高并发设计
- [ ] 能背出至少 5 个性能数字（600万/140万/50万/13×/300%…）
- [ ] 能回答事件溯源与快照恢复、双写一致性、改单难点
- [ ] 能结合自己的 `CoinTrader` 代码讲项目亮点与可优化点
- [ ] 能独立完成"设计百万级 TPS 撮合引擎"系统设计题
