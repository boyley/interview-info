# 撮合引擎模拟面试 50 题 — 题库 + 参考答案

> 分类：基础概念 → 订单簿数据结构 → 撮合算法 → 高并发架构 → 持久化与一致性 → 系统设计 → 项目实战 → 开放追问
> 建议：先自己答一遍再看参考答案；标 ⭐ 的为重点。

---

## 📌 一、基础概念（10 题）

**1. ⭐ 什么是撮合引擎？它在交易系统里处于什么位置？**
撮合引擎是交易所最核心的模块，本质是"买卖订单匹配器"：输入订单事件，按"价格优先、时间优先"匹配产生成交。它处于订单入口（网关）之后、清算与行情之前——撮合负责"算账出结果"，清算负责"资金变动"，行情负责"对外展示"。

**2. ⭐ 价格优先、时间优先（PTP）具体规则是什么？**
① 买价 ≥ 卖价即成交，从最优对手价开始；② 价格优先：最高买价/最低卖价先成交；③ 时间优先：同价位先到先得；④ 市价单无价格，直接吃对手最优价；⑤ 成交价 = 先挂单方的价格（价格制定者）。

**3. 为什么成交价用先挂单方价格，而不是双方取中间？**
保证公平与简单：先挂单方的价格是"市场当前接受的价格"，吃单方想要立即成交就接受该价，同时获得价格改善（Buy 限价 1.000 吃到卖一 0.999 时按 0.999 成交）。取中间价会让挂单方吃亏且破坏盘口激励。

**4. ⭐ 限价单、市价单、FOK、IOC、冰山单有什么区别？**
限价单：指定价，未成交挂盘口；市价单：最优价立即成交，剩余撤销；FOK：全量成交否则全撤；IOC：立即成交的部分保留、剩余撤销；冰山单：隐藏总量只展示部分，保护大单。

**5. 什么叫部分成交？怎么处理剩余部分？**
订单数量大于对手盘深度时只成交部分。剩余部分：限价单挂回盘口继续等待；市价单按 IOC 立即撤销；FOK 则整单作废。

**6. 什么是"吃单（Taker）"和"挂单（Maker）"？**
吃单：主动与现有盘口成交（提供流动性需求）；挂单：挂入盘口等待（提供流动性）。交易所通常对挂单收更低甚至零费率以激励流动性，对吃单收较高费率。

**7. 撮合、清算、结算的区别？**
撮合：匹配订单产生成交（确定成交价、量）；清算（Clearing）：逐笔核算双方资金、仓位的变动；结算（Settlement）：最终交割资产/资金到账。

**8. 什么是滑点（Slippage）？撮合怎么影响它？**
实际成交价与期望价（最优盘口）的偏差。盘口深度不足、大单吃穿多档时滑点增大。撮合速度越快、订单簿越深，滑点越小。

**9. 行情数据（盘口/深度/K线）从哪来？**
行情是撮合事件的"投影"：撮合每产生成交/盘口变更就发事件，行情模块消费后增量更新盘口快照、累加 K 线、广播推送。盘口按价档聚合成深度，K 线按时间窗口聚合 OHLC。

**10. 撮合引擎需要哪些输入？产出哪些输出？**
输入：下单、撤单、改单事件（含校验结果、资金预扣结果）；输出：成交（Trade）、订单状态变更、盘口/深度快照、行情推送。

---

## 📌 二、订单簿数据结构（8 题）

**11. ⭐ 经典订单簿用什么数据结构？各操作复杂度？**
`TreeMap<价格档位, 双向链表<订单>>`：最优价 O(1)（`firstKey/lastKey`），档位插入/删除 O(log n)，档内订单按时间链表排队、头出尾插，撤单 O(1) 摘链。买盘降序、卖盘升序。

**12. ⭐ 为什么用 TreeMap 而不是 HashMap？**
需要按键（价格）有序——撮合必须从最优价开始逐档吃穿，HashMap 无法按序取最值。也可以换跳表/有序数组，但必须"有序"。

**13. 跳表 vs 红黑树，撮合场景怎么选？**
两者查询都是 O(log n)。跳表：实现简单、局部修改加锁即可无锁化并发、写吞吐更高（OKX 实测 16 核写吞吐高约 40%）；红黑树：内存更省、Java TreeMap 开箱即用。追求极致并发选跳表。

**14. 什么是缓存行对齐（Cache Line Alignment）？为什么重要？**
CPU 按 64 字节缓存行读内存，多线程读写同一缓存行的不同字段会互相失效（伪共享 False Sharing）。把热点字段打包进同一缓存行并填充对齐，可避免伪共享。OKX 称内存访问效率提升 300%。

**15. 为什么用整数刻度（tick）表示价格而不是 double？**
浮点在百万次比较中误差累积，且撮合要精确比较价格大小、计算成交额。用整数最小价格单位（tick）表达，消除浮点误差，也便于按刻度做数组索引。

**16. ⭐ 热路径为什么不能有动态分配？**
动态分配（new/malloc）会触发 GC/堆碎片，延迟不可控。预分配对象池（Slab Pool + free-list）让分配/释放 O(1) 且零 GC，把订单节点控制在单条缓存行内。

**17. 什么是侵入式链表？撤单为什么 O(1)？**
订单节点自带 prev/next 指针（而非容器持有）。撤单时直接改前后节点的指针完成摘链，无需遍历容器查找，O(1)。

**18. 深度（Depth）和盘口（Plate）怎么算？**
把同一价格档的挂单量聚合成一条（价+总量），取前 N 档（如 24/50 档）即深度。由订单簿实时聚合，每次成交/挂单/撤单都增量更新。

---

## 📌 三、撮合算法（8 题）

**19. ⭐ 限价买单进来，完整撮合流程？**
① 判断卖一 ≤ 买单价格？是则成交（以卖一价，吃单方获价格改善）；② 吃完卖一 → 继续吃卖二、卖三……直到买单剩余为 0 或对手盘无更低价；③ 全部成交则结束；④ 有剩余则挂入买盘对应档位排队。

**20. 市价单进来，完整撮合流程？**
跳过价格比较直接吃卖一→卖二……；吃到卖盘深度不足时，剩余数量按订单类型处理：IOC 立即撤销、FOK 整单作废（实际市价单多数带 IOC 语义）。

**21. ⭐ FIFO 和 Pro Rata 的区别？**
FIFO（PTP）：同价位按时间先后，先到先得——简单、公平、鼓励主动报价；Pro Rata：同价位按各单占比分配成交——大户多分、小单也能成交，适合流动性极深的合约（CME 欧元美元）。大盘股/主流通证多用 FIFO。

**22. 什么是 TOP Order / Threshold Pro Rata？**
TOP：每边最多 1 个"最优报价首单"，优先成交，激励提供最佳报价；Threshold Pro Rata：先给大单（超阈值）优先，剩余按 Pro Rata + FIFO，防止大单被拆碎。

**23. 撮合怎么处理"价格交叉"（买价 ≥ 卖价）？**
持续撮合直到买一 < 卖一（无交叉）或吃单方成交完毕。注意先挂方优先：盘口先挂的订单有价格/时间优先权。

**24. 改单（Modify）为什么比撤单+重新下单难？**
改单 = 撤旧单 + 挂新单，但①已撮合部分不可逆，只改剩余部分；②差额资金要两阶段冻结/释放；③要处理失败回滚（订单已撮合、深度变更、风控锁定、分片迁移中）；④用户可感知的状态要正确回退。改单还涉及时间优先权丧失。

**25. 什么是隐含单（Implied Order）？**
跨合约/跨市场推导出的合成订单（如 期货+期权 组合单），撮合算法（CME 如 FIFO+LMM）中直接单优先于隐含单。

**26. 撮合顺序怎么保证公平？怎么防插队？**
全局定序：订单在进入撮合前打上全局递增序号，撮合严格按序号处理；同价同时间的先后由序号决定。任何"改价重挂"都失去时间优先权。

---

## 📌 四、高并发架构（9 题）

**27. ⭐ 为什么用单写者/单线程撮合？不慢吗？**
单交易对的撮合是严格串行逻辑，多线程只会引入锁、不确定性、不可重放。单线程 + 无锁环形缓冲避免上下文切换与锁竞争，LMAX 证明单线程可达 600 万 TPS。吞吐靠"多交易对分片"横向扩展。

**28. ⭐ 怎么做横向扩展？**
按交易对分片：BTC 系一片、ETH 系一片，每片独立撮合引擎/线程。吞吐随分片线性增长。订单入口按 symbol 路由到对应分片；跨分片无依赖（不同交易对订单簿独立）。

**29. ⭐ 定序（Sequencing）为什么是灵魂？**
定序把并发订单流序列化成一个确定顺序：①撮合按序处理保证公平；②重放日志 = 复现历史，bit-identical；③主备热备消费同一输入流可秒切；④审计可复盘。NASDAQ INET 就是用"确定性优先于并行"做到了百万级消息/秒。

**30. LMAX Disruptor 为什么快？和 BlockingQueue 比？**
Disruptor：无锁（序号 + CAS）、预分配环形数组零 GC、缓存行填充防伪共享、单消费者场景无锁无竞争；BlockingQueue：ReentrantLock 加锁、动态分配节点、延迟不可控。Disruptor 号称单线程 600 万订单/秒。

**31. 单写者下，多交易对怎么利用多核？**
每个交易对一个线程，多交易对之间天然并行——相当于"数据分片 + 并行流水线"。启动时为每个交易对建独立的事件循环/线程，路由表 symbol→线程。

**32. 撮合和清算怎么解耦？为什么必须解耦？**
撮合只"匹配+发事件"（成交、盘口），清算异步消费事件批次落库。解耦后：①撮合热路径零数据库；②清算可批量摊薄固定成本；③撮合不受 DB 慢拖累。顺序性靠单交易对单一分区保证。

**33. 批次落库（Batching）怎么做？**
成交/资产流水进内存队列，数量阈值（如 500 笔）或时间阈值（如 5~10ms）任一满足即批量写库/写日志。摊薄 SQL 解析、WAL 刷盘、索引更新的固定成本，把随机 IO 变顺序 IO。

**34. 热点账户/热点交易对怎么处理？**
资金预扣前置：Redis Lua 脚本先预扣/校验，淘汰无效订单，避免热点余额 DB 行锁；热点账户单独队列串行化处理；分片倾斜（userId%128 扎堆）需观测 + 再平衡。

**35. 撤单为什么单独走队列？**
撤单/改单与撮合主线并发，若混用同一处理序列会互相阻塞（大单撮合阻塞撤单）。独立队列 + 按交易对路由，撤单不被撮合阻塞，撮合也不被撤单拖慢。

---

## 📌 五、持久化与一致性（7 题）

**36. ⭐ 什么是事件溯源（Event Sourcing）？**
不存最终状态，存导致状态变化的事件序列；内存订单簿 = 事件重放的投影。优点：可审计、可精确重放、可恢复；代价：重放耗时、需快照辅助。面试注意区分"真事件源"与"事件日志 + 最终态"。

**37. ⭐ 内存撮合，宕机了怎么办？**
三层恢复：①**WAL**：每笔订单先顺序写日志再撮合，先写后算；②**快照**：定期把订单簿序列化，崩溃后加载最近快照 + 重放快照后事件；③**主备复制**：备机消费同一序列化输入流，主挂秒切。OKX 公开称重启 2 秒恢复全部状态。

**38. 快照和 WAL 各解决什么问题？**
WAL 解决"逐笔恢复"：日志全则有确定性，但全量重放慢；快照解决"快速恢复"：加载快照 + 只重放快照后的增量事件。两者配合 = 恢复时间可控（由快照频率决定），一致性由日志保证。

**39. ⭐ 双写一致性（日志/DB/缓存）怎么保证？**
原则：资金/仓位强一致，行情/推送最终一致。常见手段：以 WAL/事件日志为唯一真相源，DB 是异步投影；同步失败用补偿/对账（定期比对日志与 DB）；消费端幂等（订单 ID 去重、位点持久化）；分布式事务 TCC/Saga 只用于资金类强一致操作。

**40. 主备切换怎么保证不丢单、不重单？**
主备消费同一序列化输入流（同一 Kafka 分区），备机实时应用；主挂后备机从断点继续，产出 bit-identical。客户端侧用订单 ID + 序号幂等去重；位点持久化防止重复消费。

**41. 分布式事务：TCC vs Saga？**
TCC：Try（预占资源）→ Confirm（提交）→ Cancel（回滚），适合自动借/还币这类可预占场景；Saga：每步执行 + 失败时逆序补偿，适合长流程。撮合场景尽量用"事件 + 幂等 + 对账"替代强一致事务。

**42. 如何防止订单重复处理（消息重复投递）？**
消费幂等：唯一订单 ID 作为去重键（DB 唯一索引/Redis SETNX）；消费位点持久化 + 至少一次语义下靠业务幂等兜底；成交记录用"订单号+序号"唯一约束。

---

## 📌 六、系统设计题（6 题）

**43. ⭐ 设计一个百万级 TPS 撮合引擎（完整答题框架）**
①需求澄清：交易对数、订单类型、延迟目标（P99<10ms）、持久化/容灾级别；②数据流：网关→定序→撮合→行情/清算/持久化；③订单簿：TreeMap+链表/跳表、整数刻度、缓存行对齐、对象池；④并发：单写者 + Disruptor、交易对分片；⑤持久化：WAL + 快照 + 重放；⑥高可用：主备 bit-identical、清算异步批次；⑦收尾：确定性>并行、尾延迟>平均、瓶颈在清算。

**44. 压测撮合引擎，怎么设计？**
真实订单流回放 + 极端行情构造（单边洪峰、大单吃穿、瞬间撤单风暴）；指标：TPS、P50/P99/P999 延迟、撤单延迟、成交公平性（序号无乱序）；逐环节定位瓶颈（定序→撮合→清算→落库→推送）。

**45. 如何降低撮合延迟？从哪些层面入手？**
数据层面：内存驻留、整数刻度、对象池零分配、缓存行对齐；并发层面：单写者、Disruptor、批次化、CPU 亲和；网络层面：长连接、UDP 组播行情、RDMA/DPDK 内核旁路；硬件层面：FPGA 硬撮合、持久内存。

**46. 行情推送怎么做才不拖累撮合？**
行情是撮合事件的异步投影：撮合发事件（带序号）→ 行情模块独立线程消费 → 增量更新盘口 + 批量推 WebSocket/组播；慢消费者用快照 + 断点续推，绝不让行情反压撮合。

**47. 多活/异地容灾怎么考虑？**
撮合是无状态可重放的核心：主备消费同输入流跨可用区部署；日志/快照异地冗余；切主时客户端透明（网关层重连）。难点是网络延迟与时钟一致性，通常做同城双活 + 异地灾备。

**48. 对接 FIX/期货等协议注意什么？**
协议层做标准化订单模型映射；顺序号/心跳/重传机制（FIX 的 SeqNum + Resend）；机构单大单拆分（冰山）；延迟敏感用专用通道；风控前置校验。

---

## 📌 七、项目实战（结合你的 CoinTrader 代码，6 题）

**49. 你项目里的撮合引擎怎么实现的？亮点是什么？**
（参照《撮合引擎核心技术与实战》第十一章）答法：基于 Bizzan 开源交易所改造，`CoinTrader` 每交易对一个实例（`CoinTraderFactory` 管理），订单经 Kafka `exchange-order` 进、串行调 `trade()` 撮合，产出 `ExchangeTrade` 经 Kafka `exchange-trade` 发行情模块。亮点：①每交易对单实例=单写者分片雏形；②撤单走独立 topic 不阻塞撮合；③订单簿 TreeMap+内部队列天然实现价格/时间优先。

**50. 你这个撮合引擎有哪些可优化点？如果重写你会怎么改？**
答法（既展示自知之明又体现深度）：①内存盘口无 WAL/快照，重启丢状态 → 加事件溯源 + 快照恢复；②用 synchronized 细粒度锁 → 换单写者 + Disruptor 无锁；③清算 `tradeCompleted` 同步落库耦合在撮合路径 → 异步批次化；④行情/清算共用消费线程池 → 独立分片；⑤价格用 BigDecimal → 换整数 tick 提升性能。

**开放追问（加分）**：如果让你用 Redis 做撮合你觉得可行吗？→ 单线程天然定序，适合小规模，但缺事务化撮合、恢复与容量受内存约束，生产多用自研/JVM/Rust 内存撮合。

---

## ✅ 自测检查清单

- [ ] 基础概念 10 题能脱口而出
- [ ] 能画出订单簿结构和各操作复杂度
- [ ] 能讲清 FIFO / Pro Rata / TOP 区别
- [ ] 能默写"单写者 + 定序 + 分片"三板斧
- [ ] 能答"宕机了怎么办"（WAL+快照+主备）
- [ ] 能在 10 分钟内答完"设计百万级 TPS 撮合引擎"
- [ ] 能结合自己项目讲亮点 + 可优化点
