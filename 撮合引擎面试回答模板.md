# 撮合引擎面试回答模板 — 考官问 · 你答（话术集）

> 用法：每题先记**回答结构**（3~4 步骨架），再把**话术**用自己的话复述出来，别死背。
> 配套：《撮合引擎核心技术与实战》给知识点、《模拟面试50题》给考点、《一页纸速览》给速记。

---

## 🎙️ 一、万能开场与收尾

**被问"介绍一下你的撮合项目"时的万能开场：**
> "我参与维护的是一套基于 Bizzan 开源改造的加密货币交易所撮合引擎。整体是 Spring Cloud 微服务，撮合独立成 `exchange` 服务；核心是 `CoinTrader`——**每个交易对一个撮合器实例**，订单经 Kafka 进入、串行撮合、产出成交再经 Kafka 分发给行情与清算。"

**答任何技术题后的万能收尾（挑 1 句）：**
- "这个方案里，**确定性 > 并行**，因为撮合必须可重放。"
- "整体追求的是**尾延迟（P99/P999）**而不是平均吞吐。"
- "这里真正的瓶颈**在清算、不在撮合**，所以我会把清算异步化。"
- "我们当前是教学级实现，上生产我会补**事件溯源 + 快照恢复 + 主备同流**。"

---

## 🎙️ 二、系统设计题：如何设计一个高性能撮合引擎？（最常考）

**回答结构：需求澄清 → 数据流 → 订单簿 → 并发 → 持久化/高可用 → 收尾金句**

**话术：**
> "撮合引擎本质是输入订单事件、输出成交结果的**确定性状态机**，铁律是价格优先、时间优先。我按五层设计：
>
> **① 数据流**：网关多线程收单 → 定序打全局序号 → 撮合单线程 → 成交事件分发给行情与清算。
> **② 订单簿**：`TreeMap<价格档位, 双向链表<订单>>`，最优价 O(1)，同价队列按时间排队；价格用整数刻度，对象池预分配，缓存行对齐。
> **③ 并发**：每个交易对**一个单写者线程**串行撮合，天然无锁；用 **Disruptor** 无锁环形缓冲承载订单流；多交易对按 symbol 分片横向扩展。
> **④ 持久化**：**事件溯源**——每笔订单先写 WAL 再撮合，定期快照，崩溃后"快照+重放"恢复。
> **⑤ 高可用**：主备消费**同一序列化输入流**，输出 bit-identical，主挂备切不丢单不重单；清算异步批次落库，资金强一致、行情最终一致。
>
> 一句话总结：**用确定性换可重放，用单线程换无锁，用分片换吞吐，用事件溯源换安全**。LMAX 单线程 600 万 TPS 证明这条路是通的。"

---

## 🎙️ 三、撮合引擎怎么保证高可用？

**回答结构：三个目标 → 定序是地基 → 三层手段**

**话术：**
> "高可用就三个目标：**数据不丢、状态可重建、主备秒切**。
>
> 地基是**定序**——每个事件打上全局递增序号、先写日志再执行。有了确定顺序，状态就可重放；可重放，就可重建。
>
> 具体三层：**① 主备复制**——备机消费和主一样的序列化输入流，状态逐位一致，主挂秒切；**② WAL 先写后算**——事件先落日志再进撮合，崩溃重放不丢；**③ 快照+增量重放**——定期存订单簿快照，恢复时加载快照+重放增量，恢复时间由快照频率控制。再加消费端**幂等去重**防重复。
>
> 权衡点：同步刷 WAL 会拖慢撮合，所以用组提交/批次刷盘摊薄，或用持久内存（像 OKX 的 DCPMM）做热区备份。"

---

## 🎙️ 四、怎么定序？怎么保证公平？

**回答结构：定序作用 → 三种方式 → 按交易对定序即可**

**话术：**
> "定序是撮合公平性和可重放性的来源：**顺序决定了成交结果**。同价订单按序号先到先得，这是时间优先的实现。
>
> 三种实现：**① Kafka 单分区**——交易对路由到固定分区，offset 天然是序号；**② Disruptor Sequencer**——LMAX 里由专用组件打单调序号；**③ 全局发号器**——Redis INCR / Snowflake。
>
> 关键洞察是：**只需按交易对定序，不必全局**。不同交易对的订单簿互不影响，各自独立定序、并行撮合——这正是分片能横向扩展的原因。"

---

## 🎙️ 五、怎么知道哪些订单成交了？

**回答结构：撮合是真相源 → 撮合瞬间事件化 → 订单状态机 → 分发**

**话术：**
> "成交不是事后统计的，而是**撮合瞬间就被确定并事件化**的。撮合引擎是唯一真相源。
>
> 在 `processMatch` 里决定谁和谁、什么价、多少量成交，产出**三样东西**：成交明细 `ExchangeTrade`（买卖单 ID、价格、数量、时间）；订单状态更新（`tradedAmount` 累加，达到总量就变 COMPLETED）；完成订单通知。
>
> 订单状态机：已提交 → 未成交挂盘 → 部分成交 → 全部成交，或撤销。部分成交后剩余继续挂盘，撤单只撤未成交部分。
>
> 这些事件经 Kafka 分发：`exchange-trade` 给行情算 K 线，`exchange-order-completed` 给清算和用户通知。所以**下游全部从事件流读，不需要自己猜**。"

---

## 🎙️ 六、最新价 / 盘口价从哪来？

**回答结构：三个价分清 → 成交价规则 → 流转链路**

**话术：**
> "先分清三个价：**盘口价**是订单簿里最优挂单价；**最新价**是最后一笔成交的价格；**收盘价**是 K 线窗口内最后一笔成交价。
>
> 核心规则：**成交价 = 先挂单（被动单）方的价格**。限价对限价取先挂方限价，市价单吃盘口取对手限价单价格，吃单方因此获得价格改善。
>
> 流转链路：撮合 `processMatch` 确定成交价 → 构造 `ExchangeTrade` → Kafka `exchange-trade` → 行情模块 `handleThumb` 里 `setClose(trade.price)` → 推送展示。所以**最新价不是算出来的平均价，是最后一笔成交的价**，行情模块只是投影它、不重算，天然与撮合一致。"

---

## 🎙️ 七、为什么用单线程撮合？Disruptor 起什么作用？

**回答结构：单线程理由 → 分片扩容 → Disruptor 角色 → 两层分工**

**话术：**
> "单交易对的撮合是严格串行逻辑，多线程只会引入锁和不确定性。**单线程无锁**，确定性可重放，LMAX 证明单线程能到 600 万 TPS。吞吐靠**多交易对分片**横向扩展，不是给单交易对加线程。
>
> Disruptor 的角色是**进程内无锁事件管道**：把多个 IO/消费者线程的事件，以微秒级、无锁、严格保序的方式喂给撮合单线程。它和 Kafka 分工不同——**Kafka 跨服务分发+持久化，Disruptor 进程内交付**，不互相替代。
>
> 数据流：Kafka 消费者线程拉取 → Disruptor → 撮合单线程按序号取 → 应用进订单簿。
>
> 好处：无锁低延迟、保序、零 GC（预分配）、背压（环满生产者等待）、批量消费。极致的方案是订单网关**绕过 Kafka 直推 Disruptor**，Kafka 只做对外分发成交。"

---

## 🎙️ 八、内存撮合，宕机了怎么办？

**回答结构：三层恢复 → 各自作用 → 权衡**

**话术：**
> "三层防护：**WAL 先写后算**保证数据不丢——每笔事件先落顺序日志再撮合；**快照**解决恢复速度——定期把订单簿序列化，崩溃后加载快照+重放增量；**主备同流消费**解决接管——备机状态与主逐位一致，主挂秒切。加上消费端订单 ID 幂等防重复。
>
> WAL 与快照分工：WAL 管'逐笔确定性'，快照管'恢复速度'，配合起来恢复时间由快照频率控制。代价是同步刷 WAL 影响吞吐，所以业界用组提交、批次刷盘，或用持久内存做热区备份（OKX 公开称重启 2 秒恢复）。"

---

## 🎙️ 九、订单簿用什么数据结构？

**回答结构：经典实现 → 复杂度 → 演进方向**

**话术：**
> "经典是 `TreeMap<价格档位, 双向链表<订单>>`：买盘降序、卖盘升序，最优价 O(1)，档位增删 O(log n)，同价队列时间序，撤单 O(1) 摘链。
>
> 演进方向：**跳表**——无锁并发友好，OKX 实测写吞吐比红黑树高约 40%；**价格索引的 flat array**——缓存局部性极好，TradeX 换用后 P999 延迟从 45μs 降到 3.4μs。
>
> 关键是**复杂度常被缓存局部性碾压**，所以热路径追求内存连续、对象池零分配、缓存行对齐、价格用整数刻度。"

---

## 🎙️ 十、撮合和行情、清算怎么解耦？

**回答结构：职责分离 → 事件流 → 一致性分层**

**话术：**
> "撮合只做'匹配+发事件'：产出成交、盘口变更，经 Kafka 发出。行情模块消费成交算 K 线推送，清算模块消费成交异步批次落库。顺序性靠**单交易对单一分区**保证。
>
> 解耦的好处：撮合热路径零数据库、清算可批量摊薄固定成本、撮合不被 DB 拖慢。
>
> 一致性分层：**资金/仓位强一致，行情/推送最终一致**。撮合发事件带序号，下游按序号消费，乱了可重放对齐。双写失败以 WAL 为准+对账补偿。"

---

## 🎙️ 十一、你这个撮合引擎有什么可优化点？重写会怎么改？

**回答结构：现状 → 亮点 → 差距 → 演进**

**话术：**
> "我的项目是教学级实现，亮点是**每交易对单实例**（单写者分片雏形）和**撤单独立 topic**（不阻塞撮合主线），成交也做到了事件化分发。
>
> 但差距明显：**① 没有 WAL/快照**——撮合内存盘口重启靠 Kafka 重放+DB 订单重建，我会加事件溯源+快照恢复；**② 用 synchronized 细粒度锁**——换 Disruptor 无锁；**③ 清算 tradeCompleted 同步落库耦合在撮合路径**——异步批次化；**④ 价格用 BigDecimal**——换整数 tick。
>
> 一句话：**保留事件化解耦的思路，补齐确定性持久化和无锁化**，就接近生产级了。"

---

## ✅ 使用检查清单

- [ ] 系统设计题能 2 分钟完整答出五层结构
- [ ] 高可用、定序、成交追踪三题能按"结构→话术"复述
- [ ] 能区分最新价/盘口价/收盘价并讲清成交价规则
- [ ] 能讲清 Kafka 与 Disruptor 的两层分工
- [ ] 能结合自己代码讲现状+差距+演进
- [ ] 每次回答收尾都用一句金句（确定性>并行 / 瓶颈在清算）
