# 撮合引擎高可用与定序详解 — 数据不丢 · 状态可重建 · 主备秒切

> 回答三个追问：① 高可用怎么实现 ② 引擎怎么定序 ③ 怎么知道哪些撮合成交了
> 配套阅读：《撮合引擎核心技术与实战》第八节（持久化与高可用）、第十一节（CoinTrader 代码实战）

---

## 📌 一、一句话主线

> 撮合引擎之所以能高可用，是因为它把每个事件**打上顺序号 → 先写日志 → 再执行**。顺序号让状态"可重放"，重放让状态"可重建"，重建让宕机"不可怕"。而"哪些成交了"不是事后统计的，是**撮合瞬间就确定并事件化**的。

---

## 📌 二、高可用 = 三个目标

| 目标 | 含义 | 手段 |
|------|------|------|
| **数据不丢** | 成交/订单/资金流水不能丢 | WAL 先写后算 + Kafka acks + 幂等 |
| **状态可重建** | 内存订单簿崩了能恢复 | 事件溯源 + 快照 + 重放 |
| **无缝接管** | 主挂备切，不重复不遗漏 | 主备同输入流 + bit-identical |

**金句**：高可用的地基是"确定性"——只有状态可重放，宕机才不可怕。

---

## 📌 三、定序（Sequencing）：一切的地基

### 1. 为什么定序是地基

撮合是严格串行状态机，**订单顺序决定成交结果**。给事件一个确定顺序就能获得：

| 能力 | 说明 |
|------|------|
| **公平性** | 同价订单按序号先后成交（先到先得） |
| **可重放** | 从序号 N 重放 = 精确复现 N 之后的状态 |
| **可复制** | 主备消费同一序号流 → 状态逐位一致 |

### 2. 三种定序方式（对照表）

| 方式 | 做法 | 顺序粒度 | 代表 |
|------|------|---------|------|
| **Kafka 单分区** | 每个交易对路由到固定分区，offset 天然是序号 | 按交易对 | Bizzan 等 MQ 方案 |
| **Disruptor Sequencer** | 专用组件打单调序号，先入日志再入环形缓冲 | 按事件流 | LMAX / NASDAQ INET |
| **全局发号器** | Redis INCR / Snowflake 独立发号 | 全局 | 分布式架构 |

### 3. 关键洞察：按交易对定序即可，不必全局

- 不同交易对的订单簿互不影响 → 各自独立定序、并行撮合
- 这是"分片"的理论基础：每个分片（交易对）维护自己的序号流
- 全局序号只在跨交易对聚合（如用户总资产、对账）时才需要

### 4. 定序时序链路

```
订单 → 按symbol路由 → 打序号 → 写WAL(带序号) → 撮合按序执行 → 成交事件(带序号) → 下游
                                       └─ 先写后算：先持久化再执行，保证执行前必然可恢复
```

---

## 📌 四、怎么知道哪些撮合成交了

### 1. 撮合引擎是唯一真相源（Source of Truth）

成交不是"事后统计"，而是**撮合的瞬间被确定并事件化**：`processMatch` 决定"谁和谁、什么价、多少量成交"，然后立即产出事件。下游（清算、行情、用户）全部从事件流里读，不自己猜。

### 2. 一次撮合产出什么（数据模型）

| 产物 | 字段 | 去向 |
|------|------|------|
| **成交明细 ExchangeTrade** | buyOrderId / sellOrderId / symbol / price / amount / turnover / time | Kafka `exchange-trade` → 行情算 K 线 + 落库 |
| **订单状态更新** | tradedAmount / turnover / status | 实时维护 + DB 持久化 |
| **完成订单通知** | ExchangeOrder | Kafka `exchange-order-completed` → 通知用户 + 清算 |

### 3. 订单状态机

```
已提交 → 未成交(挂盘口) → 部分成交 → 全部成交(COMPLETED)
              ↘ 撤销 → 已撤销
```

- 撮合循环里实时累加：`tradedAmount += 本次成交量`，`tradedAmount >= amount` 即完成
- 部分成交后剩余继续挂盘口；撤单只撤未成交部分
- "谁成交了、还剩多少"全部由订单上的 `tradedAmount / turnover / status` 三个字段追踪

### 4. 成交分发数据流

```
撮合引擎
  ├─► topic: exchange-trade           每笔成交明细    → 行情模块(K线/推送) + 行情库
  ├─► topic: exchange-order-completed 完成订单        → 通知用户 + 清算
  └─► 本地 WAL                        全部事件         → 恢复用
```

---

## 📌 五、高可用落地：主备复制 + WAL + 快照

### 1. 主备复制（bit-identical）——无缝接管

```
          ┌──── 同一序列化输入流(Kafka 同一分区) ────┐
          ▼                                          ▼
    主撮合引擎(单写者)                            备撮合引擎(热备)
    产出: 成交/行情事件                            同步应用同一序号流
    │
主宕机 ──► 备接管(状态已逐位一致), 路由切换, 不丢单不重单
```

- 备机消费**同一个分区/同一序号流**、处理逻辑相同 → 状态必然相同
- NASDAQ INET 即此模型：热备重放输入日志，输出 bit-identical
- 注意：双主同时撮合是危险的，必须"单主对外撮合"（或共识选主）

### 2. WAL：先写后算

- 每个事件**先顺序写日志，再进撮合**；崩溃后从日志重放，已确认事件永不丢
- 性能折衷：同步刷盘慢 → 组提交（group commit）/ 批次刷盘摊薄；OKX 用持久内存（DCPMM）做热区备份两头兼顾

### 3. 快照 + 增量重放：快速恢复

```
恢复 = 加载最近快照 + 只重放快照之后的增量事件
恢复时间 = 快照加载时间 + (最近快照→崩溃点)事件重放时间，由快照频率控制
```

- WAL 解决"逐笔确定性"（不丢），快照解决"恢复速度"（不快则从头重放）
- 两者配合 = 恢复时间可控 + 一致性由日志保证

### 4. 幂等防重

- Kafka at-least-once → 消费端用**订单 ID 去重**（唯一键/Redis SETNX）
- 消费位点持久化，重复消费靠业务幂等兜底
- 成交记录用"订单号 + 序号"唯一约束

---

## 📌 六、故障场景走查（面试加分区）

| 场景 | 处理 |
|------|------|
| **主节点宕机** | 备机已有相同状态 → 接管；零丢失（撮合前已 WAL、备机已同步） |
| **双节点宕机（断电）** | 加载快照 + 重放 WAL；已确认事件不丢 |
| **消息重复投递** | 订单 ID 幂等去重 |
| **部分写入失败** | 以 WAL/日志为准 + 对账补偿 |
| **网络分区** | 备机继续消费但不接管；恢复一致后再切回 |

---

## 📌 七、结合你的 Bizzan 代码：现状与差距

| 环节 | Bizzan 现状（`exchange` 模块） | 评价 |
|------|------------------------------|------|
| **定序** | 依赖 Kafka 单分区有序消费 `exchange-order` | ✅ 够用 |
| **成交追踪** | `processMatch` 产 ExchangeTrade → topic `exchange-trade` + 落库 | ✅ 有事件化 |
| **订单状态** | 实时改 tradedAmount，DB 持久化订单 | ✅ 有持久化 |
| **WAL / 快照** | 撮合内存订单簿无 WAL/快照 | ⚠️ 重启靠 Kafka 重放 + DB 订单重建 |
| **主备** | 单实例撮合 | ⚠️ 撮合节点单点 |

**面试讲法**："我的项目用 Kafka 保证了事件的顺序消费与分发，成交是撮合瞬间事件化、经 topic 分发到行情与清算；但撮合节点本身没有 WAL/快照和主备，属于教学级实现。如果上生产，我会加**事件溯源 + 快照恢复 + 主备同流消费**。"

---

## ✅ 检查清单

- [ ] 能讲清定序的三种方式 + "按交易对定序即可，不必全局"
- [ ] 能画主备同流消费的 bit-identical 图
- [ ] 能讲清 WAL（先写后算、数据不丢）与 快照（增量重放、快速恢复）的分工
- [ ] 能讲清成交怎么被追踪：processMatch → ExchangeTrade → tradedAmount/status 状态机 → topic 分发
- [ ] 能结合自己代码讲现状（Kafka 定序 + 事件化成交）与生产级差距（无 WAL/主备）
