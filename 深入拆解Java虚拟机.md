# 深入拆解 Java 虚拟机 — 面试备战手册

> 来源：极客时间《深入拆解 Java 虚拟机》（郑雨迪/Oracle Labs）
> 涵盖 36 讲（4 大模块）：基本原理 → 高效编译 → 代码优化 → 黑科技
> 面向面试，力求简洁，一句话能讲清的绝不多说

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| **JVM 运行时数据区**（堆/栈/方法区/程序计数器） | 01 | ⭐⭐⭐⭐⭐ |
| **类加载机制**（双亲委派/加载-链接-初始化） | 03 | ⭐⭐⭐⭐⭐ |
| **垃圾回收算法**（标记-复制/标记-清除/标记-整理） | 11+12 | ⭐⭐⭐⭐⭐ |
| **垃圾回收器**（CMS/G1/ZGC 对比） | 11+12 | ⭐⭐⭐⭐⭐ |
| **Java 内存模型**（happens-before/volatile/内存屏障） | 13 | ⭐⭐⭐⭐⭐ |
| **synchronized 实现**（偏向锁→轻量锁→重量锁） | 14 | ⭐⭐⭐⭐⭐ |
| **方法调用**（虚分派/方法表/静态绑定/动态绑定） | 04+05 | ⭐⭐⭐⭐ |
| **对象内存布局**（mark word/klass pointer/对齐填充） | 10 | ⭐⭐⭐⭐ |
| **即时编译 JIT**（分层编译/热点检测/OSR） | 16+17 | ⭐⭐⭐⭐ |
| **逃逸分析**（栈上分配/锁消除/标量替换） | 23 | ⭐⭐⭐⭐ |
| **方法内联**（内联阈值/虚方法内联/类型去虚化） | 20+21 | ⭐⭐⭐⭐ |
| **反射实现**（Method.invoke/动态变长参数） | 07 | ⭐⭐⭐⭐ |
| **异常处理**（Exception table / finally 字节码） | 06 | ⭐⭐⭐ |
| **泛型与语法糖**（类型擦除/桥接方法/自动装箱） | 15 | ⭐⭐⭐ |
| **Java 字节码基础**（aload/iload/invokevirtual/invokespecial） | 19 | ⭐⭐⭐ |
| **监控诊断工具**（jstat/jmap/jstack/JProfiler） | 30+31 | ⭐⭐⭐ |
| **JMH 基准测试**（@Benchmark/BenchmarkMode） | 28+29 | ⭐⭐⭐ |

---

## 📌 二、Java 代码运行原理

### 1. 从源码到机器码

```
Java 源码 (.java)
    │
    ▼  javac 编译
Java 字节码 (.class)  ←── 平台无关，一次编写到处运行
    │
    ▼  JVM 加载 + 解释执行 / JIT 编译
机器码  ←── 平台相关
```

**关键点**：
- Java 刚启动时是**解释执行**（字节码 → 逐行翻译成机器码）
- **热点代码**才会被 JIT 编译为本地机器码（内存中），下次直接执行机器码
- **JIT 编译**比解释执行快几个数量级，但编译本身也有开销

### 2. 面试回答

> **Q：Java 代码是怎么运行的？**
>
> Java 源码经过 javac 编译成字节码（.class 文件）。JVM 加载 class 文件后，**解释执行**字节码。当某些代码被频繁执行成为**热点代码**后，JIT 即时编译器把这些字节码编译成本地机器码，下次直接运行机器码。这样兼顾了启动速度（解释执行）和长期运行性能（JIT 编译）。

---

## 📌 三、运行时数据区（内存结构）

```
┌─────────────────────────────────────────────────┐
│               线程私有区域                         │
│  ┌──────────────────┐  ┌──────────────────────┐  │
│  │  程序计数器 (PC)  │  │   Java 虚拟机栈       │  │
│  │  ─ 当前线程执行到  │  │  ─ 栈帧：局部变量表   │  │
│  │  的字节码行号      │  │   操作数栈、动态链接  │  │
│  │  唯一没有 OOM 的   │  │   方法出口           │  │
│  └──────────────────┘  └──────────────────────┘  │
│  ┌──────────────────────────────────────────┐     │
│  │         本地方法栈 (native 方法)           │     │
│  └──────────────────────────────────────────┘     │
├─────────────────────────────────────────────────┤
│               线程共享区域                         │
│  ┌────────────────────────────────────────────┐  │
│  │          堆 (Heap)                          │  │
│  │  ─ 对象实例、数组都在这里                    │  │
│  │  ─ 垃圾回收管理的主要区域                     │  │
│  │  ─ 可细分为新生代( Eden / From / To) + 老年代 │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │    方法区 (Metaspace / 永久代)               │  │
│  │  ─ 类信息、常量、静态变量                    │  │
│  │  ─ JDK8+ 用 Metaspace（本地内存）取代永久代   │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────┐  ┌──────────────────────┐    │
│  │  运行时常量池    │  │  直接内存 (DirectBuffer)│   │
│  └────────────────┘  └──────────────────────┘    │
└─────────────────────────────────────────────────┘
```

### 面试回答

> **Q：JVM 运行时数据区有哪些？哪些是线程私有？**
>
> 线程私有的：**程序计数器**（无 OOM）、**JVM 栈**（栈帧：局部变量表+操作数栈+动态链接+方法出口）、**本地方法栈**。线程共享的：**堆**（对象实例）、**方法区**（类信息/常量/静态变量，JDK8+ 元空间 Metaspace）、**运行时常量池**、**直接内存**。

---

## 📌 四、类加载机制

### 1. 类加载的三大阶段

```
加载 → 链接（验证 → 准备 → 解析） → 初始化
```

| 阶段 | 做的事 |
|------|--------|
| **加载** | 通过全限定名获取类的二进制字节流，在堆中生成 Class 对象 |
| **验证** | 校验字节流是否符合 Class 文件格式规范 |
| **准备** | 为静态变量分配内存并设置零值（如 int=0，不是代码赋的值） |
| **解析** | 将常量池中的符号引用替换为直接引用 |
| **初始化** | 执行 `<clinit>` 方法，为静态变量赋正确的初始值 |

### 2. 双亲委派模型

```
┌───────────────────────┐
│  Bootstrap ClassLoader │  ← C++ 实现，加载 rt.jar / java.lang.*
└──────────┬────────────┘
           │ 委托
┌──────────▼────────────┐
│  Extension ClassLoader │  ← 加载 jre/lib/ext/
└──────────┬────────────┘
           │ 委托
┌──────────▼────────────┐
│  Application CL        │  ← 加载 classpath 下的类
└──────────┬────────────┘
           │ 委托
┌──────────▼────────────┐
│  自定义 ClassLoader    │  ← 用户自定义
└───────────────────────┘
```

**工作原理**：一个类加载器收到加载请求，先委派给父加载器去加载，每一层都向上委派，直到 Bootstrap，如果父加载器加载不了才自己加载。

**为什么用双亲委派**：防止核心 API 被篡改（比如有人写了个 `java.lang.String` 恶意类，双亲委派确保核心类库优先加载）。

### 3. 面试回答

> **Q：类加载的双亲委派机制是什么？**
>
> 当一个类加载器收到加载请求，它不会自己先加载，而是委派给父类加载器，一直委派到 Bootstrap ClassLoader。如果父加载器加载不了，才由自己加载。这样保证了核心类库（如 `java.lang.String`）不会被用户自定义的同名类替换，保证了 Java 的安全机制。
>
> 打破双亲委派的典型例子是 **Tomcat**，它需要为每个 Web 应用隔离类库（不同应用可能依赖不同版本的 Spring），于是优先用自己的 WebAppClassLoader 加载。

---

## 📌 五、对象内存布局

### 1. 对象在堆中的组成

```
┌────────────────────────────────────────────┐
│           对象头 (Header)                    │
│  ├── Mark Word (标记字段，8字节)              │
│  │   ├─ 锁状态(2bit)                         │
│  │   ├─ GC分代年龄(4bit)                     │
│  │   ├─ identity hashcode                    │
│  │   └─ 偏向锁相关(线程ID + epoch)            │
│  └── Klass Pointer (类型指针，4/8字节)         │
├────────────────────────────────────────────┤
│          实例数据 (Instance Data)             │
│  各种字段（按类型对齐）                         │
├────────────────────────────────────────────┤
│          对齐填充 (Padding)                   │
│  保证对象大小是 8 字节的倍数                    │
└────────────────────────────────────────────┘
```

### 2. Mark Word 的锁状态标记

| 锁状态 | 标志位 | Mark Word 存储内容 |
|--------|--------|-------------------|
| 无锁 | 01 | 对象的 hashCode、分代年龄 |
| 偏向锁 | 01 | 线程 ID、epoch、分代年龄 |
| 轻量级锁 | 00 | 指向栈中锁记录的指针 |
| 重量级锁 | 10 | 指向互斥量（重量锁）的指针 |
| GC 标记 | 11 | 空（不保存任何信息） |

---

## 📌 六、方法调用（高频面试点）

### 1. 方法调用的字节码指令

| 指令 | 作用 | 示例 |
|------|------|------|
| `invokestatic` | 调用静态方法 | `Math.max()` |
| `invokespecial` | 调用构造器/私有/父类方法 | `super()`, `private` 方法 |
| `invokevirtual` | 调用实例方法（虚分派） | `obj.toString()` |
| `invokeinterface` | 调用接口方法 | `list.get()` |
| `invokedynamic` | 动态方法调用（Lambda） | `()->{}` |

### 2. 静态绑定 vs 动态绑定

| 绑定类型 | 时机 | 使用的指令 | 特点 |
|---------|------|-----------|------|
| **静态绑定** | 编译期确定 | `invokestatic`, `invokespecial` | 目标方法明确，无需运行时判断 |
| **动态绑定** | 运行时确定 | `invokevirtual`, `invokeinterface` | 根据实际类型确定调用哪个方法 |

### 3. 虚方法表（VTable）

为了提高动态分派的效率，JVM 在类的方法区建立**虚方法表**，表中存着每个虚方法的实际入口地址。调用时直接从虚方法表中找，不需要遍历父类层级。

```
class Animal {
    void eat() { ... }
    void sleep() { ... }
}

class Dog extends Animal {
    void eat() { ... }  // 重写
    void bark() { ... }
}

Dog 的虚方法表：
┌─────────┬──────────────────────┐
│ Object  │ hashCode() → Object  │
│         │ equals()  → Object   │
│         │ toString() → Object  │
├─────────┼──────────────────────┤
│ Animal  │ eat()     → ⭐Dog    │  ← 指向 Dog 的 eat
│         │ sleep()   → Animal   │
├─────────┼──────────────────────┤
│ Dog     │ bark()    → Dog      │
└─────────┴──────────────────────┘
```

### 4. 面试回答

> **Q：Java 方法调用时静态绑定和动态绑定的区别？**
>
> 静态方法 `invokestatic`、构造器/私有方法 `invokespecial` 在编译期就能确定目标方法，属于**静态绑定**。普通实例方法 `invokevirtual` 需要根据对象的实际类型运行时确定，属于**动态绑定**。JVM 通过**虚方法表**来加速动态绑定，表中存了每个虚方法的实际入口地址，调用时 O(1) 查表，不需要遍历继承链。

---

## 📌 七、synchronized 实现（高频面试）

### 1. 锁的三种状态

从低到高：**偏向锁 → 轻量级锁 → 重量级锁**（单向膨胀，不可逆）

```
判断锁的状态 → 看对象头 Mark Word 的最后两位：
00 = 轻量级锁    01 = 无锁/偏向锁    10 = 重量级锁
```

| 锁 | 原理 | 开销 | 适用场景 |
|----|------|------|---------|
| **偏向锁** | CAS 记录线程 ID，之后该线程加锁直接返回 | 极低 | 只有一线程持有锁 |
| **轻量级锁** | CAS 替换 Mark Word 为锁记录指针 | 中等 | 多线程**交替**获取锁 |
| **重量级锁** | 阻塞+自旋，依靠 OS 互斥量 | 高 | 多线程**同时**竞争锁 |

### 2. 膨胀过程

```
无锁 (01) → 偏向锁 (01) → 轻量级锁 (00) → 重量级锁 (10)
                                    ↑ 有竞争则膨胀
```

**锁消除**：JIT 逃逸分析发现锁对象只被当前线程访问，就把加锁代码去掉。
**锁粗化**：连续对同一个对象加锁解锁，合并成一次。

### 3. 面试回答

> **Q：synchronized 的底层实现原理？**
>
> synchronized 底层通过**对象头 Mark Word** 来实现。JVM 对 synchronized 做了三级优化：**偏向锁**（只有一线程访问，存线程 ID，不加锁直接返回）→ **轻量级锁**（多线程交替访问，用 CAS 替换 Mark Word）→ **重量级锁**（多线程同时竞争，阻塞+唤醒，依赖操作系统互斥量）。锁只能升级不能降级。

---

## 📌 八、Java 内存模型（JMM）

### 1. 什么是 JMM

**定义**：Java 内存模型规范了多线程下**什么情况下一个线程的写对另一个线程可见**。

### 2. happens-before 规则（重点）

| 规则 | 说明 |
|------|------|
| **程序次序规则** | 同一线程内，前面操作 happens-before 后面操作 |
| **锁规则** | 解锁操作 happens-before 后续对同一锁的加锁 |
| **volatile 规则** | volatile 写 happens-before 后续对该字段的读 |
| **线程启动规则** | Thread.start() happens-before 该线程的第一个操作 |
| **线程终止规则** | 线程的最后操作 happens-before 其他线程检测到它的终止 |
| **中断规则** | 中断操作 happens-before 被中断线程检测到中断事件 |
| **传递性** | A happens-before B, B happens-before C → A happens-before C |
| **final 规则** | 构造器结束 happens-before finalize 开始 |

#### 面试回答

> **Q：happens-before 是什么？**
>
> happens-before 是 JMM 用来**判断一个线程的写对另一个线程的读是否可见**的规则集合。如果 A happens-before B，那么 A 的执行结果对 B 是可见的，且 A 不会被重排序到 B 之后。
>
> **为什么需要这个规则？** 看个例子：
> ```java
> // 线程A                   线程B
> x = 1;                     if (flag) {
> flag = true;                   System.out.println(x);  // 能读到 1 吗？
>                          }
> ```
> 如果 `flag` 不是 volatile，线程B 可能读到 x=0。原因是 CPU 缓存导致线程B 没看到 x=1 的写入，或者 JIT 把 `x=1` 重排序到了 `flag=true` 之后。
>
> 加上 `volatile` 后，利用了 **volatile 规则+传递性**：`x=1` hb `flag=true(volatile写)` hb `flag=true(volatile读)` hb `println(x)`。这样 JIT 不能重排序，CPU 缓存也会在 volatile 写后强制刷新到主内存，线程B 一定能读到 x=1。
>
> **面试记住三条就够了**：`volatile 规则`（写 hb 后续读）、`锁规则`（解锁 hb 后续加锁）、`传递性`。

### 3. volatile 的实现原理

```
volatile 写操作：
  ↓
强制刷新 CPU 写缓存（把值同步到主内存）
使其他 CPU 缓存的该变量缓存行失效
  ↓
volatile 读操作：
从主内存重新读取
```

**底层**：HotSpot 在 volatile 写后插入 `lock addl $0x0, (%rsp)` 指令（相当于内存屏障），强制刷新写缓存。

**volatile 的特点**：
- **可见性**：读一定能看到最新的写
- **不保证原子性**：`count++` 不是原子操作
- **禁止指令重排序**：JIT 不能把 volatile 分配到寄存器

### 4. 内存屏障

**内存屏障（Memory Barrier / Fence）是什么？**：它是 JVM 在生成机器码时插入的一道“顺序检查线”。屏障限制编译器和 CPU 把屏障两侧的内存读写重排序，并让一个线程在屏障前的写入能按 Java 内存模型的规则被其他线程看见。

它**不是锁**，不会阻塞其他线程；也**不让复合操作自动具备原子性**。它解决的是“可见性 + 有序性”。例如线程 A 先写 `data=42`，再把 `volatile ready=true` 发布出去；线程 B 读到 `ready=true` 后，必须也能读到 `data=42`，不能看到 `ready=true` 却仍读到旧的 `data`。

| 屏障类型 | 作用 |
|---------|------|
| **读读屏障** (LoadLoad) | 屏障前的读操作先于屏障后的读 |
| **写写屏障** (StoreStore) | 屏障前的写操作先于屏障后的写 |
| **读写屏障** (LoadStore) | 屏障前的读先于屏障后的写 |
| **写读屏障** (StoreLoad) | 屏障前的写先于屏障后的读；通常是四种屏障中开销最重的一种 |

**与 `volatile` 的关系**：JMM 要求 volatile 写具有“发布”效果、volatile 读具有“获取”效果。可把它理解为：

```text
线程 A：普通写 data=42  → StoreStore → volatile 写 ready=true
线程 B：volatile 读 ready=true → LoadLoad / LoadStore → 普通读 data
```

因此，A 对 `ready` 的 volatile 写，**happens-before** B 后续读到该值的 volatile 读；B 便能看到 A 在发布前对 `data` 的写入。具体会生成哪些 CPU 指令取决于硬件架构和 JDK 实现：x86 内存模型较强，部分屏障可能只需编译器约束，而较强的 StoreLoad 屏障通常需要额外的机器指令。

### 5. 面试回答

> **Q：volatile 能保证原子性吗？它和 synchronized 什么区别？**
>
> volatile 保证**可见性**和**禁止重排序**，但**不保证原子性**。比如 `count++` 包含读-改-写三步，volatile 只能保证每一步的可见性，不能保证这三步整体原子。而 synchronized 既能保证可见性又能保证原子性。volatile 适合一写多读的场景。

---

## 📌 九、垃圾回收（GC）

### 1. 判断对象是否存活

| 算法 | 做法 | 缺点 |
|------|------|------|
| **引用计数法** | 每个对象记录被引用次数 | 循环引用无法回收 |
| **可达性分析**（JVM 使用） | 从 GC Roots 出发，不可达的对象可回收 | 无循环引用问题 |

**GC Roots** 包括：虚拟机栈引用、静态属性引用、常量引用、JNI 引用。

### 2. 三种基本回收算法

| 算法 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **标记-清除** | 标记存活对象，清除未标记的 | 实现简单 | 产生内存碎片 |
| **标记-复制** | 将存活对象复制到另一块空间，原空间整体清除 | 无碎片，分配高效 | 可用内存减半 |
| **标记-整理** | 存活对象向一端移动，清除边界外的 | 无碎片，内存利用率高 | 移动对象需要 STW |

### 3. 分代收集

```
新生代 (Young)                   老年代 (Old)
┌──────┬──────┬──────┐          ┌──────────┐
│ Eden │ From │ To   │          │          │
│ 新对象 │ Survivor│Survivor│    │ 长期存活  │
│      │      │      │          │ 大对象    │
└──────┴──────┴──────┘          └──────────┘
  Minor GC（复制算法）            Major GC（标记-整理）
```

**分配策略**：
- 新对象分配在 Eden
- Minor GC 后存活对象复制到 Survivor（From 和 To 交换）
- 年龄达到阈值（默认 15）进入老年代
- 大对象直接进入老年代

### 4. 常见 GC 对比

| 回收器 | 区域 | 算法 | 特点 | 适用 |
|--------|------|------|------|------|
| **Serial** | 新生代 | 复制 | 单线程、STW | 客户端、小内存 |
| **ParNew** | 新生代 | 复制 | Serial 多线程版 | 配合 CMS |
| **Parallel Scavenge** | 新生代 | 复制 | 关注吞吐量 | 服务端、批处理 |
| **CMS** | 老年代 | 标记-清除 | 并发、低延迟 | 重视响应时间 |
| **G1** | 全堆 | 标记-整理 | 分区、可预测停顿 | **JDK9+ 默认** |
| **ZGC** | 全堆 | 标记-复制 | 超低延迟(<10ms) | 超大堆、JDK11+ |

**CMS 流程**（4 个阶段）：
```
初始标记(STW) → 并发标记 → 重新标记(STW) → 并发清除
```

**CMS 的四个阶段**：
```
初始标记(STW) → 并发标记 → 重新标记(STW) → 并发清除
```

**CMS 的问题**：①标记-清除算法产生内存碎片 ②并发阶段占用 CPU，与业务线程竞争 ③并发标记失败时会退化为 Serial Old，导致长时间 STW。JDK9 已标记为 Deprecated。

### 5. G1（Garbage-First）详解

G1 在 JDK7 引入、JDK9 成为默认 GC，是**全堆分区化**的垃圾回收器。

**核心数据结构**：
```
堆被划分为多个 Region（默认 2048 个，大小 1~32MB）
每个 Region 独立扮演 Eden、Survivor 或 Old 的角色

┌────┬────┬────┬────┬────┬────┐
│ E  │ E  │ S  │ O  │ O  │ H  │   E=Eden, S=Survivor
├────┼────┼────┼────┼────┼────┤   O=Old, H=Humongous(大对象)
│ O  │ E  │ E  │ S  │ O  │ H  │
└────┴────┴────┴────┴────┴────┘
```

**关键技术细节**：

| 技术 | 说明 |
|------|------|
| **Region 分区** | 堆不再固定分代，每个 Region 动态决定角色（E/S/O） |
| **RSet（Remembered Set）** | 每个 Region 记录"有哪些其他 Region 引用本 Region 的对象"，避免全堆扫描 |
| **SATB（Snapshot-At-The-Beginning）** | 并发标记阶段，记录 GC 开始时的堆快照，通过写屏障记录并发期间的变化 |
| **Humongous 对象** | 超过 Region 大小 50% 的对象直接分配到连续 Humongous Region |
| **可预测停顿** | GC 选择回收收益最大的若干 Region（Garbage-First 名字的由来），通过 `-XX:MaxGCPauseMillis=200` 控制停顿时间 |

**Young GC 流程**：
```
① 选出一批 Young Region
② 将存活对象复制到空闲 Region（或晋升到 Old Region）
③ 清空原 Region
```

**Mixed GC 流程**（老年代到达阈值 `-XX:InitiatingHeapOccupancyPercent=45` 触发）：
```
初始标记(STW) → 并发标记 → 最终标记(STW) → 筛选回收(STW，选收益最高的 Region 回收)
```

**G1 相比 CMS 的优势**：
- 无内存碎片（复制/整理算法）
- 可预测停顿时间
- 大堆（几十GB+）下表现稳定
- 自动管理分代

**G1 的局限性**：停顿时间目标设得太小（如 <50ms）时，可能来不及回收足够的 Region，导致 Full GC。

### 6. ZGC（JDK 11+）详解

ZGC 是 JDK 11 引入的超低延迟垃圾回收器，目标是 **STW 不超过 10ms**，无论堆多大（可管理 16TB+ 堆）。

### 1. Region（Page）分区

ZGC 的 Region 叫 Page，分三种大小：

```
ZGC 堆
┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐
│ S  │ S  │ S  │ S  │ M  │ S  │ S  │ L  │ L  │ S  │ ...
└────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘
 2MB  2MB  2MB  2MB  32MB 2MB  2MB N×2MB N×2MB 2MB

S = Small (2MB)   ← <256KB 的小对象
M = Medium (32MB) ← 256KB~4MB 的中对象
L = Large (N×2MB) ← ≥4MB 大对象，独占一个 Region，不移动
```

### 2. 三视图映射（ZGC 最核心的设计）

ZGC 通过 `mmap` 将 **同一块物理内存** 映射到 **三个不同的虚拟地址空间**：

```
虚拟地址空间：

 0x0000000000000000 ─────────────────────────
                     │  Java 堆（4TB）      │
 0x0000040000000000 ─────────────────────────
                     │  Marked0 视图        │  ← 同一物理内存
 0x0000080000000000 ─────────────────────────  ← mmap 映射三次
                     │  Marked1 视图        │
 0x00000C0000000000 ─────────────────────────
                     │  （预留）            │
 0x0000100000000000 ─────────────────────────
                     │  Remapped 视图       │
 0x0000140000000000 ─────────────────────────

同一时间只有一个视图是"好"的（Good），GC 切换视图时，业务线程无感知。
```

### 3. 染色指针（64 位指针布局）

ZGC 把 GC 标记状态**直接编码在指针的高位**，访问对象时不需要锁对象头：

```
 63             47 46  45 44 43 42              0
┌────────────────┼──┼──┼──┼──┼──────────────────┐
│  固定为0(17位)  │ 0│  │  │  │  对象偏移地址(42位)│
└────────────────┴──┴──┴──┴──┴──────────────────┘
                    │  │  │  └── 42位：寻址 4TB
                    │  │  └──── 43位：Marked1
                    │  └─────── 44位：Marked0
                    └────────── 45位：Remapped

四种指针颜色：
  marked0 (第44位=1) → 标记视图0
  marked1 (第43位=1) → 标记视图1
  remapped(第45位=1) → 重映射视图（正常读写时用）
  finalizable       → 终结器引用
```

### 4. GC 过程的视图切换

```
时间 →
GC阶段    初始标记(STW)   并发标记           并发重定位       并发重映射  
            ↓               ↓                  ↓              ↓
有效视图  Remapped→Marked0  Marked0→Marked1    Marked1→Remapped
业务线程   用marked0读      用marked1读         恢复remapped
读指针时   自动自愈         修正指针            指向新地址
```

**读屏障自愈**：业务线程第一次访问已移动的对象时，读屏障检测到指针颜色不对，自动查转发表找到新地址，修正指针。下次访问直接到新地址，不需要再查。

### 5. ZGC vs G1 核心差异

| 对比项 | G1 | ZGC |
|--------|----|-----|
| **STW 时间** | 几十~几百毫秒 | **<10ms** |
| **最大堆** | ~几百 GB | **16TB+** |
| **标记方式** | SATB + RSet | 染色指针 + 读屏障（无需记忆集） |
| **对象移动** | Young GC 复制，Old 整理 | 并发重定位（业务线程不暂停） |
| **读屏障开销** | 无 | 每次引用读取有少量开销 |
| **吞吐量** | 较高 | 略低于 G1 |
| **JDK 版本** | JDK7+（JDK9 默认） | JDK11+（JDK15 生产可用） |


### 7. 面试回答

> **Q：CMS、G1、ZGC 有什么区别？**
>
> **CMS** 是最早的并发回收器，关注低延迟，但标记-清除产生碎片，并发失败时会退化为 Full GC。**G1** 把堆分成 Region，优先回收垃圾最多的区域，可预测停顿，JDK9 起默认。**ZGC** 使用染色指针和读屏障技术，所有阶段几乎完全并发，STW 控制在 10ms 以内，最大支持 16TB 堆。
>
> 选型建议：堆 < 4GB 用 Parallel；4~100GB 对响应时间有要求的用 G1；上百 GB 堆且对延迟极其敏感的用 ZGC。CMS 已被 Deprecated，不要在新项目中使用。

---

## 📌 十、即时编译（JIT）

### 1. 分层编译（C1 + C2）

| 编译层 | 名称 | 编译速度 | 代码质量 | 开启条件 |
|--------|------|---------|---------|---------|
| **0 层** | 解释执行 | 无编译 | 最低 | 默认 |
| **1 层** | C1 简单编译 | 快 | 中 | 热点方法、非循环 |
| **2 层** | C1 受限 profiling | 快 | 中 | 少量 profiling |
| **3 层** | C1 完全 profiling | 中 | 中 | 完整 profiling |
| **4 层** | C2 编译 | 慢 | **最高** | 热点方法/循环 OSR |

**热点检测**：基于计数器的混合模式——方法调用计数器 + 回边计数器（循环）。

### 2. OSR（栈上替换）

当循环体成为热点时，JIT 可以在循环执行过程中把解释执行切换到编译后的代码，不需要等循环结束。

### 3. 面试回答

> **Q：什么是 JIT 即时编译？为什么要分层编译？**
>
> JIT 把热点代码编译成本地机器码，大幅提升性能。分层编译（C1+C2）是为了平衡启动速度和峰值性能：C1 编译快但优化少，适合快速让程序运行起来；C2 编译慢但代码质量极高，适合长期运行的热点。先 C1 再 C2 是最佳路径。

---

## 📌 十一、逃逸分析（Escape Analysis）

### 1. 什么是逃逸分析

判断对象的作用域是否超出了方法范围。

```
public void foo() {
    User u = new User();  // u 没有逃逸出方法
    u.setName("xx");
}

public User bar() {
    User u = new User();  // u 作为返回值逃逸了
    return u;
}
```

### 2. 基于逃逸分析的三种优化

| 优化 | 条件 | 效果 |
|------|------|------|
| **栈上分配** | 对象不逃逸 | 对象直接在栈上分配，随方法结束自动回收，减少 GC 压力 |
| **同步消除** | 锁对象不逃逸 | 去掉 synchronized 代码 |
| **标量替换** | 对象不逃逸且可以被拆散 | 把对象拆成多个局部变量，无需创建对象实例 |

### 3. 面试回答

> **Q：什么是逃逸分析？它能做什么优化？**
>
> 逃逸分析判断一个对象是否逃出方法的作用域。基于此 JIT 可以做三种优化：**栈上分配**（对象在栈上分配，方法结束自动销毁，减少 GC）、**同步消除**（去掉不必要的加锁）、**标量替换**（把对象拆成多个基础变量，省去对象创建开销）。

---

## 📌 十二、方法内联

### 1. 什么是方法内联

把被调用方法的代码"复制"到调用方方法中，消除调用开销。

```
// 内联前
int add(int a, int b) { return a + b; }
int result = add(1, 2);

// 内联后
int result = 1 + 2;
```

**内联阈值**（默认）：
- `-XX:CompileThreshold=10000` — 热点方法触发编译
- `-XX:MaxInlineSize=325` — 方法字节码小于 325 才内联
- `-XX:FreqInlineSize=250` — 热方法不超过 2500 字节

### 2. 虚方法的内联难题

```java
interface Animal { void eat(); }
class Dog implements Animal { public void eat() { ... } }

void feed(Animal a) { a.eat(); }  // 不知道 a 是 Dog 还是 Cat
```

**解决方案**：**类型去虚化（Type Profiling）** — JIT 会统计运行时实际类型，如果 99% 都是 Dog，就乐观假设是 Dog 并内联，同时留一个"去优化陷阱"，万一遇到 Cat 再退回解释执行。

---

## 📌 十三、反射的实现原理

### 1. Method.invoke 的调用链路

```
Method.invoke(target, args)
  → 检查方法是否可访问 (setAccessible)
  → 如果是可变长参数，需要封装数组
  → NativeMethodAccessorImpl.invoke (第一次用 native)
    或
    GeneratedMethodAccessorX.invoke (调用15次后生成字节码实现)
```

**性能优化**：JVM 使用**通货膨胀机制**——前 15 次反射用 native 实现，之后生成字节码直接调用，避免 native 调用的开销。

### 2. 面试回答

> **Q：反射慢在哪里？Java 怎么优化的？**
>
> 反射慢因为每次调用都要**检查方法可见性**、**自动装箱**参数、**处理可变长参数**。JVM 用**通货膨胀机制**优化：前 15 次反射使用 native 方式，15 次阈值后动态生成字节码直接调用目标方法，省去 native 调用开销。

---

## 📌 十四、Java 语法糖底层实现

| 语法糖 | 编译器做了什么 | 面试要点 |
|--------|--------------|---------|
| **泛型** | 类型擦除（擦除为 Object 或上界） | 运行时无泛型；生成桥接方法保持重写语义 |
| **自动装箱/拆箱** | 调用 `Integer.valueOf()` / `intValue()` | 注意缓存范围 -128~127 |
| **foreach** | 编译为迭代器调用或数组遍历 | 循环中删除元素会抛 ConcurrentModificationException |
| **String +** | 编译为 StringBuilder.append() | 循环中拼接建议自己用 StringBuilder |
| **try-with-resources** | 编译为 try-finally，自动 close | 每个资源都关闭，忽略先发生的异常 |
| **Lambda** | invokedynamic + 生成函数式接口实例 | invokedynamic 第一次调用时才确定目标方法 |

---

## 📌 十五、异常处理机制

### 1. Exception table

```
try {
    mayThrow();
} catch (IOException e) {
    handle();
}
```

编译成字节码时生成**异常表**：

```
Exception table:
  from  to  target  type
  0     8    11     Class java/io/IOException
  0     8    22     Class java/lang/Exception
```

- **from-to**：监控的字节码范围
- **target**：异常处理器入口
- **type**：捕获的异常类型（null 表示 finally）

### 2. finally 的实现

finally 块的字节码会被**复制多份**，分别插入到正常路径和每个异常处理器的末尾，确保无论什么情况都会执行。

### 3. 面试回答

> **Q：finally 中的 return 会覆盖 try 中的 return 吗？**
>
> 会。如果 finally 中也有 return，会覆盖 try 中的返回值。所以阿里巴巴规范建议**不要在 finally 中使用 return**，否则会造成意想不到的结果。

---

## 📌 十六、字节码基础（面试常见）

### 常见字节码指令速记

| 类型 | 指令 | 含义 |
|------|------|------|
| 加载 | `aload_0` | 将局部变量表第 0 个引用型变量推入栈顶 |
| 加载 | `iload_1` | 将第 1 个 int 型变量推入栈顶 |
| 存储 | `astore_1` | 将栈顶引用型数值存入第 1 个局部变量 |
| 常量 | `iconst_0` | 将 int 类型常量 0 推入栈顶 |
| 对象 | `new` | 创建类实例 |
| 方法 | `invokevirtual` | 调用实例方法（虚分派） |
| 方法 | `invokespecial` | 调用构造器/私有/父类方法 |
| 方法 | `invokestatic` | 调用静态方法 |
| 方法 | `invokeinterface` | 调用接口方法 |
| 返回 | `ireturn` / `areturn` | 返回 int / 引用类型 |

---

## 📌 十七、监控与诊断工具

| 工具 | 用途 | 常用命令 |
|------|------|---------|
| **jps** | 查看 Java 进程 | `jps -l` |
| **jstat** | 查看 GC 统计 | `jstat -gcutil pid 1000` |
| **jmap** | 堆转储 | `jmap -dump:file=heap.hprof pid` |
| **jstack** | 查看线程栈 | `jstack pid`（找死锁） |
| **jinfo** | 查看 JVM 参数 | `jinfo -flags pid` |
| **JConsole** | GUI 监控 | 连接 JVM 进程 |
| **JProfiler/YourKit** | CPU/内存分析 | 找出最耗 CPU 的函数 |

### 面试回答

> **Q：线上 CPU 飙升 100% 怎么排查？**
>
> 三步走：①`top -H` 找出 CPU 最高的线程 PID；②`printf "%x\n" pid` 转十六进制；③`jstack pid | grep -A 30 十六进制ID` 定位到具体代码行。如果是 GC 导致的飙升，再结合 `jstat -gcutil` 看 GC 频率。

---

## 📌 十八、黑科技概览（面试加分项）

| 技术 | 一句话说明 |
|------|-----------|
| **GraalVM** | 用 Java 写的 JIT 编译器，替代 C2，支持多语言（JS/Python/Ruby 跑在 JVM 上） |
| **Truffle** | 语言实现框架，写一个解释器就能自动获得 JIT 编译能力 |
| **SubstrateVM** | AOT 编译框架，把 Java 应用编译成原生可执行文件，启动毫秒级 |

---

## 📌 十九、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **JVM 运行时数据区有哪些？** | 私有：PC/栈/本地方法栈；共享：堆/方法区/常量池/直接内存 |
| **双亲委派是什么？** | 类加载请求先委派给父加载器，父加载不了才自己加载 |
| **怎么打破双亲委派？** | 重写 `loadClass()` 方法（不委派），Tomcat WebAppClassLoader |
| **对象内存布局？** | 对象头(Mark Word+Klass Pointer) + 实例数据 + 对齐填充 |
| **synchronized 怎么实现的？** | 偏向锁→轻量级锁→重量级锁，靠 Mark Word 状态位标识 |
| **volatile 原理？** | 写后强制刷新缓存，读从主内存取；禁止重排序 |
| **happens-before 最关键的是？** | volatile 写 hb 后读；解锁 hb 后加锁 |
| **GC 判断对象存活？** | 从 GC Roots 做可达性分析 |
| **GC 算法有哪些？** | 标记-清除（碎片）、标记-复制（一半空间）、标记-整理（移动对象） |
| **CMS 和 G1 区别？** | CMS 老年代标记-清除；G1 Region 分区，可预测停顿 |
| **JIT 是啥？** | 热点代码编译成本地机器码，提高性能 |
| **逃逸分析有什么用？** | 栈上分配、同步消除、标量替换 |
| **方法内联的好处？** | 消除调用开销，为后续优化创造条件 |
| **反射为什么慢？** | 权限检查+自动装箱+可变参数；15次后生成字节码优化 |
| **线上 CPU 高怎么排查？** | top -H → printf %x → jstack 定位代码行 |
| **Java 内存模型是啥？** | 规范多线程下内存可见性的规则集合 |

---

> 📝 **学习建议**：本课程共 36 讲，以上提炼了**最常考的 16 个方向**。面试时建议重点关注：**类加载机制、GC、JMM、synchronized、JIT、逃逸分析**这六个高频方向。回答时尽量"从底层原理出发"而非背概念——讲清楚"JVM 是怎么实现的"比"它是什么"更能体现深度。
