# 秒杀系统架构设计与实战 — 面试备战手册

> 来源：极客时间
> - 《手把手带你搭建秒杀系统》（佘志东）— 动手实战：Nginx+Lua/Redis+Lua库存/隔离/限流
> - 《秒杀服务篇》PDFs — 核心设计梳理
> - 《如何设计一个秒杀系统》（许令波/淘宝）— 架构原则/动静分离/热点/削峰/减库存/PlanB
>
> 面试回答力求简洁，一句话能讲清的绝不多说

---

## 📌 一、面试高频考点速查

| 核心考点 | 来源 | 重要度 |
|---------|------|-------|
| **秒杀系统的三大挑战 + 设计原则** | 三套课程综合 | ⭐⭐⭐⭐⭐ |
| **"4要1不要"架构原则**（数据少/请求少/路径短/依赖少/无单点） | 许令波 | ⭐⭐⭐⭐⭐ |
| **漏斗式分层架构**（Nginx网关→Web→RPC） | 佘志东 | ⭐⭐⭐⭐⭐ |
| **Nginx高性能原理**（多进程+epoll事件驱动） | 佘志东 | ⭐⭐⭐⭐⭐ |
| **库存防超卖：Redis+Lua原子扣减** | 佘志东 | ⭐⭐⭐⭐⭐ |
| **减库存三种策略**（下单减/付款减/预扣） | 许令波 | ⭐⭐⭐⭐⭐ |
| **限流算法**（令牌桶vs漏桶） | 佘志东 | ⭐⭐⭐⭐ |
| **削峰手段**（验证码/MQ/分层过滤） | 许令波+佘志东 | ⭐⭐⭐⭐ |
| **动静分离**（静态数据缓存+动态数据异步获取） | 许令波 | ⭐⭐⭐⭐ |
| **热点数据处理**（发现+隔离+优化） | 许令波 | ⭐⭐⭐⭐ |
| **事前预约+熔断**（控制参与人数上限） | 佘志东 | ⭐⭐⭐⭐ |
| **系统隔离策略**（业务/系统/数据） | 佘志东+许令波 | ⭐⭐⭐⭐ |
| **Plan B兜底**（降级/限流/拒绝服务） | 许令波 | ⭐⭐⭐ |
| **性能优化**（减少编码/序列化/本地缓存） | 许令波 | ⭐⭐⭐ |
| **Nginx配置优化**（绑核/零拷贝/超时） | 佘志东 | ⭐⭐⭐ |
| **物理机优化**（CPU模式/网卡中断绑核） | 佘志东 | ⭐⭐⭐ |

---

## 📌 二、面试回答标准模板（30秒版）

> **面试官："说说你怎么设计一个秒杀系统？"**
>
> **回答**：
>
> "秒杀系统的核心挑战是 **瞬时高并发**，设计目标是 **稳、准、快**——系统高可用、数据不超卖、响应够快。
>
> 整体思路是 **漏斗式层层过滤**。架构分三层：
> - **Nginx网关层**（OpenResty+Lua）负责限流、黑白名单、业务校验
> - **Web服务层** 做业务聚合、限购校验
> - **RPC支撑层** 处理数据和外部依赖
>
> 关键设计遵循 **"4要1不要"原则**：数据要少、请求要少、路径要短、依赖要少、不要有单点。
>
> 具体技术上：
> - **防超卖**用 Redis+Lua 脚本保证库存扣减的原子性
> - **削峰限流**用 Nginx限流 + 令牌桶 + MQ异步化 + 验证码打散
> - **事前预约**控制参与人数，达到上限自动熔断
> - **动静分离**，静态资源上CDN，动态数据异步获取
> - **系统隔离**，秒杀独立部署，不混用普通交易资源
> - **兜底方案**，降级 + 限流 + 熔断，防止最坏情况"

---

## 📌 三、秒杀系统三大挑战

```
              ╔═══════════════════════╗
              ║  秒杀系统的三大挑战   ║
              ╚═══════════════════════╝
                        │
    ┌───────────────────┼───────────────────┐
    ▼                   ▼                   ▼
┌──────────┐     ┌──────────┐     ┌──────────┐
│ 瞬时高并发 │     │ 热点数据  │     │ 刷子流量  │
│          │     │          │     │          │
│ 秒杀瞬间   │     │ 热门SKU  │     │ 黄牛脚本  │
│ 流量暴增   │     │ 单点瓶颈  │     │ 自动抢购  │
│ 百倍千倍   │     │ 行锁竞争  │     │ 对普通用  │
│ 常规架构   │     │ 缓存和DB  │     │ 户不公平  │
│ 扛不住    │     │ 压力集中  │     │          │
└──────────┘     └──────────┘     └──────────┘
```

### 面试回答

> **Q：秒杀系统面临哪些挑战？**
>
> 三大挑战：一是**瞬时高并发**，秒杀开始瞬间流量暴增百倍千倍，Tomcat 线程池瞬间打满，机器可能挂掉；二是**热点数据**，热门商品 SKU 成为单点瓶颈，数据库行锁竞争激烈；三是**刷子流量**，黄牛脚本抢购，普通用户抢不到。

---

## 📌 四、"稳准快"设计目标 + "4要1不要"原则

### 稳准快

| 关键字 | 含义 | 对应技术 |
|--------|------|---------|
| **稳** | 高可用，流量符合预期要稳，超出预期也不能掉链子 | 限流/熔断/降级/隔离/PlanB |
| **准** | 数据一致，100件商品就卖100件，不超卖不少卖 | Redis+Lua/数据库事务 |
| **快** | 高性能，极致优化请求链路的每个环节 | 动静分离/CDN/本地缓存/减少序列化 |

### "4要1不要"架构原则

| 原则 | 含义 | 举例 |
|------|------|------|
| **① 数据要尽量少** | 传输数据少、依赖数据少 | 简化页面、去掉装修效果 |
| **② 请求数要尽量少** | 减少额外请求(CSS/JS/图片) | 合并JS/CSS文件 |
| **③ 路径要尽量短** | 减少中间节点数 | 合并部署、减少RPC调用 |
| **④ 依赖要尽量少** | 减少强依赖 | 弱依赖可降级(优惠券降级不影响下单) |
| **⑤ 不要有单点** | 消除单点故障 | 服务无状态化、多副本冗余 |

---

## 📌 五、三层漏斗式架构

```
               用户请求（100%）
                    │
                    ▼
┌──────────────────────────────────┐
│   demo-nginx (OpenResty + Lua)    │ ← 拦截~70%无效请求
│  ● 流量筛选：黑白名单/登录态/参数  │
│  ● 限流：IP / user ID 维度限流     │
│  ● 负载均衡：IP hash / user hash   │
│  ● 简单业务校验：活动有效性、库存校验  │
│  ● 异常提示页面                     │
└──────────────┬───────────────────┘
               │ (upstream 分发)
               ▼
┌──────────────────────────────────┐
│   demo-web (SpringMVC)            │ ← 业务聚合层
│  ● 结算页 H5 提供                  │
│  ● 业务聚合接口                    │
│  ● 限购校验 + 关键接口限流          │
│  ● 灰度发布支持                    │
└──────────────┬───────────────────┘
               │ (Dubbo RPC)
               ▼
┌──────────────────────────────────┐
│   demo-support (RPC 服务)          │ ← 数据支撑层
│  ● 活动数据 / 商品数据              │
│  ● 用户数据                        │
│  ● MySQL + Redis                  │
└──────────────────────────────────┘
```

### 面试回答

> **Q：秒杀系统三层架构每一层的职责是什么？**
>
> 第一层 **Nginx 网关**（OpenResty+Lua），处理流量筛选、限流、黑白名单，拦截大部分无效请求。第二层 **Web 服务**，做业务聚合和限购校验。第三层 **RPC 支撑**，提供数据和外部服务。这样形成一个**流量漏斗**：Nginx 拦截~70%的无效请求，Web 层二次过滤，最终只有少量优质请求到达下单环节。

---

## 📌 六、Nginx 高性能原理（面试高频）

### 面试回答

> **Q：为什么 Nginx 能支持高并发？**
>
> Nginx 采用 **多进程 + epoll 事件驱动模型**。多进程中每个 worker 是单线程的，省去了线程切换和锁竞争的开销。epoll 是异步非阻塞的 IO 多路复用——worker 遇到 IO 时不阻塞等待，而是去处理其他请求，等 IO 完成通过回调回来继续处理。相比 select/poll 需要遍历所有连接，epoll 只处理就绪的连接，效率高得多。所以 Nginx 单机可以轻松支持数万并发。

```
传统 Apache（一个连接一个线程）：
连接1 → 线程1(等待IO)   ← 大量线程 = 大量内存 + 上下文切换
连接2 → 线程2(等待IO)

Nginx（epoll事件驱动）：
worker → 处理连接1 → IO等待(不阻塞) → 处理连接2 → IO回调 → 处理连接1后续
        ↑ 单worker可并发处理数千连接，一个线程搞定
```

---

## 📌 七、库存防超卖（最高频面试题）

### 1. 超卖问题的根因

```
库存剩余：2件
请求A(买1件) → 查库存(够) → 扣减1件 → 成功
请求B(买2件) → 查库存(够) → 扣减2件 → 成功
                       结果：库存 = -1 ❌ 超卖！
```

**根因**：查询和扣减不是原子操作 + 并发请求无序

### 2. 三种减库存策略

| 策略 | 时机 | 优点 | 缺点 | 秒杀适用？ |
|------|------|------|------|-----------|
| **下单减库存** | 用户下单即扣 | 控制精确，不超卖 | 恶意下单不付款 | ✅ 秒杀推荐（抢到=赚到，不付款少） |
| **付款减库存** | 用户付款才扣 | 防止恶意下单 | 下单成功但付不了款 | ❌ |
| **预扣库存** | 下单预扣，超时释放 | 折中方案 | 实现复杂 | 视场景 |

**秒杀场景推荐下单减库存**，因为抢到就是赚到，下单不付款的情况很少。

### 3. 解决方案对比

| 方案 | 性能 | 推荐度 | 一句话 |
|------|------|--------|--------|
| **数据库行锁** | ❌ 低 | 不推荐 | 高并发下性能极差 |
| **分布式锁(Redis/ZK)** | ⚠️ 中 | 不推荐 | 锁有效期难设置：长则阻塞、短则失效 |
| ✅ **Redis+Lua脚本** | ⭐ 高 | **强烈推荐** | Redis单线程保证顺序，Lua保证原子性 |

### 4. Redis+Lua 实现（核心代码 + 面试背诵版）

```lua
-- 库存扣减 Lua 脚本
local c_s = redis.call('get', KEYS[1])           -- 查库存
if not c_s or tonumber(c_s) < tonumber(KEYS[2]) then
    return 0                                       -- 库存不足
end
redis.call('decrby', KEYS[1], KEYS[2])           -- 扣减
return 1
```

**为什么用 EVALSHA 而不是 EVAL？**
- EVAL 每次都传脚本字符串，浪费网络和编译开销
- SCRIPT LOAD 预加载后直接用 sha1 调用，性能更好

### 5. 面试回答

> **Q：秒杀如何防止库存超卖？**
>
> 超卖的根本原因是**查询和扣减不是原子操作**。我们使用 **Redis+Lua 脚本**——Redis 单线程保证请求顺序执行，Lua 把查库存和扣库存合并成一次 Redis 调用，保证原子性。生产中用 `SCRIPT LOAD` 预加载脚本得到 sha1，后续用 `EVALSHA` 调用，避免每次传脚本的网络开销。
>
> 此外，业务上再加一层**限购系统**：先校验活动库存和个人购买上限（同一手机号只能买1件等），通过后才扣真实库存，进一步保护库存系统。

---

## 📌 八、动静分离

### 1. 什么是动静分离？

将用户请求的数据划分为"动态数据"和"静态数据"：

| | 静态数据 | 动态数据 |
|--|---------|---------|
| **特点** | 不随URL/用户/时间/地域变化 | 含个性化数据 |
| **例子** | 商品标题、描述、图片 | 是否登录、购物车数量、推荐 |
| **缓存位置** | 用户浏览器 / CDN / 服务端Cache | 不缓存或短时间缓存 |

### 2. 静态数据缓存到哪里？

```
浏览器缓存 (最快，但不可控)
    ↓
CDN 缓存 (全国节点，建议用二级Cache，命中率高)
    ↓
Web服务器层 (Nginx/Varnish，推荐不做Java层缓存)
    ↓
统一Cache层 (Redis/Tair)
```

**核心技巧**：静态化改造是直接**缓存 HTTP 连接**，而不是只缓存数据。Web代理根据 URL 直接取出 HTTP 响应头和体返回，不用重新组装 HTTP 协议。

### 3. 怎么做动静分离改造？

| 步骤 | 做法 |
|------|------|
| **URL唯一化** | `http://item.xxx.com/item.htm?id=xxx` 作为缓存 key |
| **分离浏览者因素** | 登录身份等用动态请求获取 |
| **分离时间因素** | 服务端时间用动态请求（防止客户端篡改） |
| **异步化地域因素** | 地域信息异步加载 |
| **去掉Cookie** | 缓存的静态数据中不包含 Cookie |

### 3. 动态内容合并方案

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **ESI** | 在Web代理层合并动态内容到静态页面 | 用户体验好 | 服务端性能有影响 |
| **CSI** | 前端 JS 异步请求获取动态内容 | 服务端性能佳 | 页面可能延时加载 |

### 4. 面试回答

> **Q：什么是动静分离？为什么它能提升性能？**
>
> 动静分离就是把页面的"静态数据"（商品标题、图片等不变内容）和"动态数据"（是否登录、库存等个性化内容）分开处理。静态数据可以**直接缓存 HTTP 连接**到靠近用户的地方，比如 CDN 或浏览器，这样用户请求时直接返回，不经过后端服务；动态数据用异步请求获取。这样可以大幅减少后端服务的请求压力，释放带宽资源。

---

## 📌 九、热点数据处理（80/20原则）

### 1. 热点分类

| 类型 | 说明 | 发现方式 |
|------|------|---------|
| **静态热点** | 可提前预测的热点 | 运营报名打标 / 大数据分析TOP N |
| **动态热点** | 运行中临时产生的热点 | 系统实时发现（Nginx统计/中间件日志） |

### 2. 实时热点发现系统

```
用户请求 → 中间件(Nginx/Cache/RPC)收集热点key → Agent采集日志
    → 聚合分析集群 → 热点订阅分发 → 下游系统做保护(缓存/限流)
```

**要求**：接近实时（3秒内），异步采集不影响主流程。

### 3. 热点处理的三种思路

| 思路 | 做法 | 面试关键词 |
|------|------|-----------|
| **优化** | 缓存热点数据（队列+LRU，短暂缓存数秒） | "临时缓存" |
| **限制** | 一致性Hash分桶 + 处理队列，防止热点霸占资源 | "Hash分桶" |
| **隔离** | 独立集群部署热点数据（单独Cache/DB） | "独立集群" |

### 4. 隔离的三种层次

| 隔离层次 | 做法 | 目的 |
|---------|------|------|
| **业务隔离** | 秒杀商品特殊标识，单独活动报名 | 不影响普通商品 |
| **系统隔离** | 独立域名/独立部署集群 | 物理隔离故障 |
| **数据隔离** | 单独 Cache 集群 / 单独 MySQL 库 | 热点不冲击普通数据 |

### 5. 面试回答

> **Q：如何处理秒杀的热点数据？**
>
> 热点数据分**静态热点**（可提前预测，通过运营报名或大数据分析发现）和**动态热点**（实时产生，通过中间件日志采集+聚合分析在3秒内发现）。处理思路是三个词：**优化、限制、隔离**。优化就是缓存热点数据；限制就是一致性Hash分桶，不让热点请求霸占全部资源；隔离就是独立集群部署热点数据，不要让1%的热点影响99%的正常请求。

---

## 📌 十、削峰与限流

### 1. 削峰手段对比

| 手段 | 类型 | 原理 | 面试关键词 |
|------|------|------|-----------|
| **验证码** | 无损 | 人手速不同，打散请求到30秒~1分钟 | "打散流量" |
| **消息队列** | 无损 | 同步改异步，消费方按能力处理 | "水库削峰" |
| **分层过滤** | 有损 | CDN→前台→后台→DB逐层拦截 | "漏斗式过滤" |
| **限流** | 有损 | 丢弃超出系统能力的请求 | "有损保护" |

**大厂实践**：天猫京东一般不使用验证码/答题（影响用户体验），倾向**有损逐级限流+分层过滤**。

### 2. 流量漏斗模型

```
100% 进入流量
  │
  ▼
风控+防刷 ──→ 拦截~60%刷子流量
  │
  ▼
限购+预约校验 ──→ 拦截无资格流量
  │
  ▼
限流 (Nginx限流+令牌桶) ──→ 丢弃超出能力的请求
  │
  ▼
~10% 优质流量 → 下单系统
```

### 3. 限流算法

| 算法 | 特点 | 场景 | Nginx实现 |
|------|------|------|----------|
| **漏桶** | 严格平滑，匀速出水 | 需要精确控制速率 | `limit_req` |
| **令牌桶** | 允许一定突发，限制平均速率 | 有波动但需控制总量 | Guava RateLimiter |

**Nginx 限流配置**（面试背这个）：
```nginx
limit_req_zone $binary_remote_addr zone=one:10m rate=1r/s;
limit_req_zone $user_id zone=limit_by_user:10m rate=1r/s;
server {
    location /seckill/ {
        limit_req zone=one burst=2 nodelay;
    }
}
```

**Java 应用层限流**（Guava RateLimiter，基于令牌桶）：
```java
@MyRateLimit  // 自定义注解 + AOP
public void submitOrder(String skuId) { ... }

// AOP 切面
@Around("@annotation(...)")
public Object around(ProceedingJoinPoint pjp) {
    boolean flag = rateLimiter.tryAcquire();
    if (!flag) return "被限流了";
    return pjp.proceed();
}
```

### 4. 面试回答

> **Q：秒杀系统怎么做削峰和限流？**
>
> 削峰的本质是把瞬时大流量变得平滑，让我们有限的硬件能处理。主要手段：**验证码**让用户手速不同从而打散流量；**消息队列**把同步下单改成异步削峰；**分层过滤**在CDN→前台→后台→DB各层拦截无效请求。限流用**令牌桶算法**（允许一定突发）或**漏桶算法**（严格平滑），在 Nginx 和 Java 应用层各设一层限流。最终形成一个**流量漏斗**，有效请求层层递减。

---

## 📌 十一、预约+熔断（事前流量管控）

### 核心思路
- 预约期先开放预约，记录用户关系
- 达到人数上限自动熔断，关闭预约入口
- 秒杀期只有预约用户有资格参与

### 面试回答

> **Q：如何控制秒杀的参与人数？**
>
> 用**预约+自动熔断**机制。活动分预约期和秒杀期，必须先预约才能参与。预约系统设置人数上限，达到后自动关闭预约入口。这样能精准控制参与规模，避免流量失控。

---

## 📌 十二、减库存核心逻辑（许令波·淘宝视角）

### 三种减库存方式

| 方式 | 扣减时机 | 秒杀推荐？ | 原因 |
|------|---------|-----------|------|
| 下单减库存 | 用户下单时 | ✅ 推荐 | 秒杀抢到=赚到，不付款少 |
| 付款减库存 | 用户付款时 | ❌ | 可能导致下单成功但付不了款 |
| 预扣库存 | 下单预扣+超时释放 | ⚠️ 折中 | 买机票/电影票场景 |

### 数据库层的极致优化

秒杀减库存最终还是要落库，但高并发下 MySQL 行锁竞争激烈：

| 优化手段 | 做法 | 效果 |
|---------|------|------|
| **应用层排队** | 按商品ID Hash到固定队列，串行处理 | 减少并发行锁竞争 |
| **数据库层排队** | MySQL InnoDB patch，单行并发排队 | 全局排队，阿里MySQL团队已开源 |
| **热点数据隔离** | 热商品单独放一个数据库 | 不影响其他商品 |
| **SQL优化** | `UPDATE item SET inventory = CASE WHEN inventory>=? THEN inventory-? ELSE inventory END` | 一次SQL完成判断+扣减 |

### 面试回答

> **Q：减库存有哪几种方式？秒杀用什么方案？**
>
> 有三种：**下单减库存**（用户下单就扣）、**付款减库存**（用户付款才扣）、**预扣库存**（下单预扣+超时释放）。秒杀推荐用**下单减库存**，因为抢到就是赚到，下单不付款的情况很少，逻辑最简单性能最好。另外 MySQL 层的优化可以用**应用层排队**（按商品ID Hash到固定队列）或**数据库层排队**（阿里MySQL团队InnoDB patch，已开源）。

---

## 📌 十三、Plan B兜底方案

### 1. 高可用建设全生命周期

| 阶段 | 要点 |
|------|------|
| **架构阶段** | 可扩展性、容错性、消除单点 |
| **编码阶段** | 超时退出、异常捕获、默认处理结果 |
| **测试阶段** | 覆盖度、异常流程 |
| **发布阶段** | 回滚机制 |
| **运行阶段** | 监控准确、报警详细 |
| **故障发生时** | 及时止损、快速恢复 |

### 2. 三件套兜底

| 手段 | 力度 | 做法 |
|------|------|------|
| **降级** | 轻 | 牺牲非核心功能（如关闭优惠券展示），保核心流程 |
| **限流** | 中 | 压测确定阈值，QPS到8000就限流（系统最高1w） |
| **拒绝服务** | 重 | CPU 90% 或 load>2*核数时，直接 503，快速失败 |

### 3. 面试回答

> **Q：秒杀系统如何做兜底？**
>
> 一个成熟系统必须要有 Plan B。三件套：**降级**（优惠券挂了先降级，保证下单流程正常）、**限流**（压测知道系统上限QPS 1w，设8000为限流阈值）、**拒绝服务**（CPU飙到90%直接503快速失败）。这三个层层递进，保系统不崩溃。

---

## 📌 十四、性能优化要点

### 1. 真正影响性能的因素

```
总QPS = (1000ms / 响应时间) × 线程数
```

- **真正关键的是CPU执行时间**，不是线程等待时间
- 减少CPU执行时间一半，QPS提升一倍
- 线程数不是越多越好（线程切换成本）

### 2. Java 优化四板斧

| 优化 | 做法 | 效果 |
|------|------|------|
| **减少编码** | `resp.getOutputStream()` 代替 `getWriter()`，静态数据预转字节 | 性能提升~30% |
| **减少序列化** | 合并部署应用，避免RPC | 减少序列化/反序列化开销 |
| **Java极致优化** | 直接用 Servlet，少用 MVC 框架；输出用 JSON 不用模板引擎 | 每请求省1ms |
| **并发读优化** | 本地缓存(LocalCache)热点数据 | 彻底解决读瓶颈 |

### 3. 面试回答

> **Q：秒杀系统如何做性能优化？**
>
> 核心公式：`总QPS = (1000ms / 响应时间) × 线程数`。真正关键的是**减少CPU执行时间**。四个手段：**减少编码**（用OutputStream直接输出，不用Writer）；**减少序列化**（合并部署减少RPC调用）；**Java极致优化**（Servlet代替MVC框架）；**并发读优化**（本地缓存热点数据，避免每次都查远端缓存）。

---

## 📌 十五、Nginx + 物理机配置优化

### 核心配置

```nginx
worker_processes 2;              # =CPU核数
worker_cpu_affinity 01 10;       # 绑核
worker_rlimit_nofile 65535;     # 文件描述符上限

events {
    worker_connections 65535;    # 单进程最大连接数
}

http {
    sendfile on;                 # 零拷贝
    tcp_nopush on;               # 合并小包发送
    keepalive_timeout 0;         # 客户端短连接

    # 后端用长连接
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_connect_timeout 500ms;
    proxy_read_timeout 3000ms;

    gzip on;
    gzip_min_length 1k;
}
```

### 物理机优化

| 优化项 | 做法 |
|--------|------|
| **CPU模式** | 大促 Performance，日常 PowerSave |
| **网卡中断绑核** | 单独一核处理网卡中断，避免单核飙高 |
| **Nginx绑核** | worker与CPU核一一绑定，提高缓存命中率 |

---

## 📌 十六、全流程秒杀示意图

```
                  ┌───── 时间线 ─────┐
预约期：           │                   │
 用户→预约商品      │   事前：预约+熔断   │
 达上限→熔断关闭    │                   │
                  ├─────────────────────┤
秒杀开始：         │   事中：漏斗式过滤   │
                  │                   │
① 用户点击"抢购"   │  Nginx层：         │
    ↓              │  黑白名单/限流/校验  │
② demo-nginx     ├─────────────────────┤
    ↓              │  Web层：           │
③ demo-web       │  限购校验/扣Redis库存 │
  Redis+Lua扣库存  │  推MQ              │
  推MQ            ├─────────────────────┤
    ↓              │  支撑层：           │
④ demo-support   │  MQ消费/生成订单/写DB │
  消费MQ/生成订单  │                   │
    ↓              │  事后：兜底PlanB    │
⑤ 前端轮询结果    │  降级/限流/503      │
                  └─────────────────────┘
```

---

## 📌 十七、面试必问核心问题速查（精简版）

| 面试题 | 一句话回答（面试直接说） |
|--------|------------------------|
| **秒杀系统的挑战？** | 瞬时高并发、热点数据、刷子流量 |
| **"稳准快"是什么？** | 稳=高可用，准=不超卖，快=高性能 |
| **"4要1不要"？** | 数据少/请求少/路径短/依赖少/无单点 |
| **Nginx为什么快？** | 多进程+epoll事件驱动，异步非阻塞，单机万级并发 |
| **怎么防超卖？** | Redis+Lua，查库存和扣库存合在一次调用，原子性 |
| **减库存有几种方式？** | 下单减(秒杀推荐)、付款减、预扣 |
| **削峰有哪些手段？** | 验证码打散、MQ异步、分层过滤 |
| **限流算法有哪些？** | 漏桶(严格平滑)、令牌桶(允许突发) |
| **怎么发现热点？** | 静态热点=运营报名，动态热点=日志采集+实时分析 |
| **热点怎么隔离？** | 业务隔离、系统隔离(独立集群)、数据隔离(独立DB) |
| **动静分离怎么做？** | 静态数据缓存到CDN/浏览器，动态数据异步请求 |
| **为什么秒杀要独立部署？** | 防止1%的热点流量影响99%的正常交易 |
| **Plan B怎么做？** | 降级→限流→拒绝服务，三件套层层兜底 |
| **Java性能怎么优化？** | 减少编码、减少序列化、本地缓存 |
| **预约有什么用？** | 控制参与人数上限，达到自动熔断 |

---

> 📝 **最终面试建议**：秒杀系统面试一定要体现**漏斗式架构思维**——从用户请求到下单，层层过滤。不用背太多细节，能说清以下四点即可合格：
> 1. **整体架构**怎么分层（Nginx→Web→RPC）
> 2. **库存防超卖**怎么解决（Redis+Lua）
> 3. **高并发怎么扛**（削峰限流+动静分离+热点隔离）
> 4. **怎么兜底**（降级限流+PlanB）
