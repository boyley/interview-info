# 红包系统设计 — 面试备战手册

> 综合运用：秒杀系统的 Redis+Lua 原子扣减、高并发的削峰限流、分布式事务的最终一致性、幂等设计
> 面试高频题："如何设计一个抢红包系统？"

---

## 📌 一、需求分析

### 1. 核心场景

```
发红包：群主/用户发一个总金额固定、个数固定的红包
抢红包：多人同时抢，先到先得，抢完即止
拆红包：抢到的人看到金额（金额是拆开时才确定的）
查红包：查看红包详情（谁抢了多少、还剩多少）
```

### 2. 核心挑战

| 挑战 | 说明 |
|------|------|
| **金额拆分** | 每个红包金额随机，且**所有子红包之和 = 总金额**，不能多不能少 |
| **高并发** | 一个红包被千万人抢，Redis/DB 扛压力 |
| **超抢** | 总共 10 个红包，不能被第 11 个人抢到 |
| **重复抢** | 同一个人不能抢两次 |
| **金额精度** | 金额涉及钱，不能用浮点数（用"分"存储） |
| **最终一致性** | Redis 抢完，最终要落库，不能丢 |

---

## 📌 二、红包金额拆分算法

### 核心公式：二倍均值法

**思想**：每次拆的金额 = 剩余金额 / 剩余个数的 2 倍范围内随机。

```
第 i 个红包的金额 = random(0.01, 剩余金额 / 剩余个数 × 2)

原理：剩余 n 个红包，剩余金额 M
     每个红包的期望 = M / n
     如果每次随机 (0, 2M/n)，则所有组合的期望之和 = M
     保证了"所有人拿到金额的数学期望相同" → 公平
```

**示例**：发 100 元，10 个红包

```
剩余 100元/10个 → 第1个随机 (0, 20)     → 假设 8 元
剩余  92元/9个  → 第2个随机 (0, 20.4)   → 假设 15 元
剩余  77元/8个  → 第3个随机 (0, 19.2)   → 假设 10 元
...
最后 1 个 = 剩余全部金额（保证总和 = 100）
```

### 二倍均值法的优点

```
✅ 公平：每人期望金额相同
✅ 简单：O(n) 时间完成拆分
✅ 有惊喜：金额有波动（有人多有人少）
```

### 其他方案对比

| 算法 | 原理 | 缺点 |
|------|------|------|
| **二倍均值法** | 每次随机(0, 2×平均) | 推荐，简单公平 |
| **线性切割法** | 在[0,总金额]随机 n-1 个切割点，切成的段即金额 | 可能出现0元或极不均 |
| **固定金额** | 每个红包固定金额 | 无惊喜，不叫红包了 |
| **手气模式+平均兜底** | 先算出最大手气，其余均分 | 实现复杂 |

**面试关键点**：说出"二倍均值法" + 公式 `random(0, 2×剩余平均)` + 公平性论证，就算过关。

---

## 📌 三、整体架构

```
发红包流程：
用户发红包 → 网关 → 红包服务（拆分金额）
              → Redis 预存红包
              → DB 记录红包主单
              → 返回红包 ID

抢红包流程：
用户抢 → 网关 → 红包服务（校验）
       → Redis 原子抢（lpop）
       → 命中 → MQ 异步落库
       → 返回金额 / 未命中返回"手慢了"
```

### 三层结构

```
┌──────────────────────────────────────────┐
│  接入层：限流（每个用户限1次）、鉴权       │
└──────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────┐
│  红包服务：金额拆分、抢红包、查详情         │
│  Redis（抢红包主战场） + MQ（异步落库）     │
└──────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────┐
│  存储层：MySQL（红包主单/明细/流水）        │
└──────────────────────────────────────────┘
```

---

## 📌 四、Redis 数据结构设计

### 发红包时预拆分并存入 Redis

```
发红包时：把拆分好的所有子红包金额存到 Redis List

KEY: red_packet:{packetId}         → TYPE: List
VALUE: [ 8, 15, 10, 12, 9, ... ]   → 每个元素是一个子红包金额（单位：分）

另存一个 Set 记录已抢用户（幂等去重）

KEY: red_packet:{packetId}:users   → TYPE: Set
VALUE: { userId1, userId2, ... }
```

### 为什么用 List 而不是别的？

| 数据结构 | 特点 | 是否适合 |
|---------|------|---------|
| **List** | `lpop` 原子弹出头部 | ✅ 完美匹配"先到先得" |
| **ZSet** | 按分数排序 | ❌ 不需要排序 |
| **Hash** | 字段-值 | ❌ 需要弹出消耗 |
| **String+incr** | 计数器 | ❌ 只适合计数 |

---

## 📌 五、抢红包核心流程

### 核心：Redis + Lua 原子操作

抢红包必须保证**原子性**——"判断用户没抢过 + 弹出金额 + 标记用户"三步不能分开，否则会超抢/重复抢。

```lua
-- 抢红包 Lua 脚本
-- KEYS[1] = red_packet:{packetId}（金额List）
-- KEYS[2] = red_packet:{packetId}:users（已抢用户Set）
-- ARGV[1] = userId

-- ① 幂等检查：该用户是否已抢过
if redis.call('sismember', KEYS[2], ARGV[1]) == 1 then
    return -1   -- 重复抢，返回 -1
end

-- ② 原子弹出最后一个子红包金额
local amount = redis.call('rpop', KEYS[1])
if not amount then
    return -2   -- 红包被抢完了，返回 -2
end

-- ③ 标记该用户已抢
redis.call('sadd', KEYS[2], ARGV[1])

-- ④ 返回抢到的金额
return amount
```

**为什么用 Lua**：和秒杀库存扣减同理——Redis 单线程保证顺序，Lua 保证这四步是原子的。不会出现"两个人同时弹出同一个金额"。

### 完整抢红包流程

```
用户点击抢红包
    ↓
① 接入层限流（同一用户限频）
    ↓
② 调用红包服务 → 执行 Lua 脚本
    ↓
③ 返回值判断：
    -1（重复抢） → 返回"您已抢过"
    -2（抢完了） → 返回"手慢了，红包被抢光"
    正数（金额） → 继续
    ↓
④ 抢到 → 发送 MQ 消息（packetId + userId + amount）
    ↓
⑤ 立即返回用户金额（体验：秒开）
    ↓
⑥ MQ 消费者异步落库（DB 写明细 + 更新红包主单）
```

---

## 📌 六、削峰与异步落库

### 为什么需要 MQ？

```
直接落库的问题：
  千万人同时抢 → 瞬间 DB 写入量爆炸 → DB 崩溃

用 MQ 削峰：
  抢红包在 Redis 完成（QPS 极高）
  只有抢到的人才发 MQ（量很小，一个红包最多 n 个）
  消费者按自己的速度落库，不被打爆
```

### MQ 消息内容

```
Topic: red_packet_result
Message: {
  packetId: "xxx",
  userId: 123,
  amount: 800,        // 分
  timestamp: ...
}
```

### 幂等消费（防止重复落库）

MQ 可能重复投递，消费者必须幂等：

```
方案：user_packet 表加唯一索引 (packet_id, user_id)
  → 重复消息插入时主键冲突 → 直接忽略
```

---

## 📌 七、数据库设计

### 三张表

```sql
-- 红包主单表（一个红包一条）
CREATE TABLE red_packet (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    packet_id     VARCHAR(32) NOT NULL UNIQUE,   -- 红包ID（雪花算法生成）
    user_id       BIGINT NOT NULL,               -- 发红包的人
    total_amount  INT NOT NULL,                  -- 总金额（单位：分）
    total_count   INT NOT NULL,                  -- 总个数
    remain_amount INT NOT NULL,                  -- 剩余金额（兜底）
    remain_count  INT NOT NULL,                  -- 剩余个数（兜底）
    status        TINYINT NOT NULL,              -- 0未发完 1已抢完 2已过期
    create_time   DATETIME NOT NULL
);

-- 红包明细表（每人抢到一条）
CREATE TABLE red_packet_detail (
    id           BIGINT PRIMARY KEY AUTO_INCREMENT,
    packet_id    VARCHAR(32) NOT NULL,
    user_id      BIGINT NOT NULL,
    amount       INT NOT NULL,                   -- 抢到的金额（分）
    create_time  DATETIME NOT NULL,
    UNIQUE KEY uk_packet_user (packet_id, user_id)  -- ← 幂等关键
);

-- 用户红包流水表（每个用户收/发红包记录）
CREATE TABLE user_packet_record (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id     BIGINT NOT NULL,
    packet_id   VARCHAR(32) NOT NULL,
    type        TINYINT NOT NULL,                -- 0发出 1收到
    amount      INT NOT NULL,
    create_time DATETIME NOT NULL,
    KEY idx_user (user_id, create_time)
);
```

### 金额为什么用 INT（分）不用 DECIMAL？

```
用"分"存储：100元 = 10000 分 → INT 类型
优点：
  ① 避免浮点数精度问题（0.1+0.2 != 0.3）
  ② 运算快、省空间
  ③ 展示时前端转元
```

### Redis 与 DB 的最终一致性

```
正常流程：Redis 抢 → MQ → DB 落库

兜底：Redis 的 List 里可能还有金额，但 DB 没同步完
  → 定时任务扫描 Redis，把剩余未落库的金额补写 DB
  → 或者：Redis 数据全丢了，以 DB 的 remain_amount 为准补发（不常用）

关键原则：Redis 是"抢"的第一道关卡，DB 是"钱"的最终依据
```

---

## 📌 八、边界情况与解决方案

| 边界情况 | 解决方案 |
|---------|---------|
| **红包过期未抢完** | 定时任务：状态置为已过期，剩余金额退回发红包用户 |
| **重复抢** | Lua 里 `sismember` 幂等检查 |
| **Redis 宕机** | 降级：直接用 DB 抢（`SELECT ... FOR UPDATE` 行锁），性能差但能用 |
| **金额拆分总和不为总金额** | 最后一个是剩余全部金额，保证总和精确 |
| **用户退款/删红包** | 状态机 + 对账任务 |
| **红包金额为0** | 二倍均值法下限设为 0.01 元（1分） |

---

## 📌 九、面试回答模板（完整版）

> **面试官：如何设计一个抢红包系统？**
>
> 我从三个核心问题来讲：**拆金额、抢红包、保一致**。
>
> **① 金额拆分**：用**二倍均值法**。每次拆的金额在 `(0, 剩余金额/剩余个数×2)` 内随机，这样每个红包的数学期望相等，既公平又有惊喜。金额全部用"分"（INT）存储，避免浮点精度问题。拆分在发红包时一次性完成，存进 Redis。
>
> **② 抢红包**：Redis 是主战场。发红包时把所有子红包金额 push 到 List `red_packet:{id}`，另用一个 Set 记录已抢用户。抢的时候用 **Lua 脚本**原子执行三步——检查用户是否抢过（幂等）、rpop 弹出金额、sadd 标记用户。和秒杀扣库存同理，Redis 单线程 + Lua 保证不会超抢、不会重复抢。
>
> **③ 削峰与一致性**：抢到后立即返回金额（秒开体验），通过 **MQ 异步落库**。只有抢到的人才发 MQ，量很小，消费者按自己速度写 DB。明细表加 `(packet_id, user_id)` 唯一索引保证幂等。Redis 是"抢"的第一道关卡，DB 是"钱"的最终依据，用定时任务兜底保证最终一致。
>
> 最后还有边界处理：红包过期未抢完定时退回、Redis 宕机降级到 DB 行锁。

---

## 📌 十、面试加分点

| 加分点 | 说明 |
|--------|------|
| **二倍均值法的公平性论证** | 说出"每个红包期望 = 总金额/个数"，证明不是运气差就是被安排的 |
| **Redis List 选型理由** | `lpop/rpop` 天然契合"先到先得"，比 ZSet/Hash 都合适 |
| **Lua 原子性** | 和秒杀库存扣减同一思路，体现知识迁移能力 |
| **分/元转换** | 金额用 INT 分存储，避免浮点误差——体现工程经验 |
| **削峰** | 抢红包在 Redis（百万QPS），落库走 MQ（小流量）——体现高并发思维 |
| **幂等** | 唯一索引 + 用户 Set 双重幂等——体现可靠性思维 |
