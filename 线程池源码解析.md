# 线程池源码解析与面试要点 — ThreadPoolExecutor 全解

> 学习路径：**先懂"7 个参数 + 状态机"，再走源码主线"execute → addWorker → runWorker → getTask"，最后落到"拒绝策略 / 生命周期 / 高频追问"**。
> 源码版本：JDK 8 `java.util.concurrent.ThreadPoolExecutor`。
> 一句话主线：**任务提交走 execute；线程按需创建走 addWorker；线程干活不退出走 runWorker 循环 + getTask；线程多了超时回收、满了拒绝。**

---

## 📌 一、面试高频考点速查

| 核心考点 | 出现在源码哪里 | 重要度 |
|---------|--------------|-------|
| 7 个核心参数各自的语义 | 构造函数 | ⭐⭐⭐⭐⭐ |
| execute() 的"core→队列→max→拒绝"四级流程 | execute() | ⭐⭐⭐⭐⭐ |
| ctl 位打包：runState(高3位)+workerCount(低29位) | ctl 字段 | ⭐⭐⭐⭐⭐ |
| 线程复用原理（runWorker 的 while 循环） | runWorker() | ⭐⭐⭐⭐⭐ |
| 线程为什么不退出 / 非核心线程怎么回收 | getTask() | ⭐⭐⭐⭐⭐ |
| 四种拒绝策略 | 内部类 | ⭐⭐⭐⭐ |
| 线程池状态机（5 状态迁移） | runStateOf/ctl | ⭐⭐⭐⭐ |
| shutdown vs shutdownNow | 生命周期方法 | ⭐⭐⭐⭐ |
| 为什么 Worker 继承 AQS（不可重入锁） | Worker 内部类 | ⭐⭐⭐⭐ |
| 任务抛异常后线程池怎么自愈 | processWorkerExit() | ⭐⭐⭐⭐ |
| 核心线程数怎么定（CPU/IO 密集） | 经验公式 | ⭐⭐⭐⭐ |

---

## 📌 二、一图总览：线程池骨架

```
                 ┌──────────────────────────────┐
  任务提交 execute │       ThreadPoolExecutor      │
    ───────────▶ │                              │
                 │  ① 不够核心数  → 新建核心线程      │
                 │  ② 核心满了    → 丢进阻塞队列      │
                 │  ③ 队列满了    → 新建非核心线程    │
                 │  ④ 线程到 max  → 拒绝策略         │
                 │                              │
                 │  Worker(线程) → runWorker 循环   │
                 │    ├─ getTask() 从队列取任务     │
                 │    └─ task.run() 执行          │
                 └──────────────────────────────┘
```

---

## 📌 三、核心参数（7 个）

```java
new ThreadPoolExecutor(
    corePoolSize,      // ① 核心线程数（常驻，默认不回收）
    maximumPoolSize,   // ② 最大线程数（核心 + 非核心上限）
    keepAliveTime,     // ③ 非核心线程空闲存活时间
    TimeUnit unit,     // ④ 时间单位
    workQueue,         // ⑤ 任务队列（阻塞队列）
    threadFactory,     // ⑥ 线程工厂（命名/守护线程等）
    handler            // ⑦ 拒绝策略
);
```

| 参数 | 语义 | 易错点 |
|------|------|--------|
| corePoolSize | 常驻线程数，**队列空也存活** | 不是"一开始就建这么多"，是**按需创建**，到任务时才 addWorker |
| maximumPoolSize | 线程数上限 | **只在队列满时才会创建超过核心数的线程** |
| keepAliveTime | 非核心线程空闲存活时间 | 默认**核心线程不回收**，除非 `allowCoreThreadTimeOut(true)` |
| workQueue | 待执行任务队列 | 建议**有界队列**，无界队列 max 形同虚设 |
| threadFactory | 建线程的工厂 | 建议自定义命名，方便排查问题 |
| handler | 队列满+线程满时的策略 | 默认 AbortPolicy（抛异常） |

---

## 📌 四、状态机：ctl 位打包 + 5 个状态

### 1. ctl 一个 int 存两样东西

```java
private final AtomicInteger ctl = new AtomicInteger(ctlOf(RUNNING, 0));
private static final int COUNT_BITS = Integer.SIZE - 3;       // 29
private static final int CAPACITY   = (1 << COUNT_BITS) - 1;  // 2^29-1

private static final int RUNNING    = -1 << COUNT_BITS;  // 高3位 111
private static final int SHUTDOWN   =  0 << COUNT_BITS;  // 高3位 000
private static final int STOP       =  1 << COUNT_BITS;  // 高3位 001
private static final int TIDYING    =  2 << COUNT_BITS;  // 高3位 010
private static final int TERMINATED =  3 << COUNT_BITS;  // 高3位 011
```

**高 3 位 = 线程池状态 runState，低 29 位 = 工作线程数 workerCount。**
为什么位打包？因为要**一次 CAS 原子地同时更新"状态 + 线程数"**，避免两个变量间的并发不一致。数值上 RUNNING < SHUTDOWN < STOP < TIDYING < TERMINATED，所以源码里大量用 `rs >= SHUTDOWN` 判断"池是否进入关闭序列"。

### 2. 状态迁移

```
RUNNING ──shutdown()──▶ SHUTDOWN ──队列空&池空──▶ TIDYING ──terminated()──▶ TERMINATED
   │                        │
   └────shutdownNow()───────┴──▶ STOP ──池空──▶ TIDYING ──▶ TERMINATED
```

| 状态 | 含义 | 还能接收新任务? | 还执行队列任务? |
|------|------|:---:|:---:|
| RUNNING | 正常运行 | ✅ | ✅ |
| SHUTDOWN | 关停中 | ❌ | ✅ 执行完剩余任务 |
| STOP | 立即停 | ❌ | ❌ 并中断所有线程 |
| TIDYING | 收尾 | — | — 钩子 terminated() |
| TERMINATED | 结束 | — | — |

---

## 📌 五、execute()：任务的入口（核心中的核心）

```java
public void execute(Runnable command) {
    int c = ctl.get();
    // ① 线程数 < 核心数 → 建核心线程执行
    if (workerCountOf(c) < corePoolSize) {
        if (addWorker(command, true)) return;   // core=true：建的是核心线程
        c = ctl.get();
    }
    // ② 池在运行且队列能放 → 丢进队列
    if (isRunning(c) && workQueue.offer(command)) {
        int recheck = ctl.get();                 // ⚠️ 二次检查
        if (!isRunning(recheck) && remove(command))
            reject(command);                     // 池刚停了 → 从队列移除并拒绝
        else if (workerCountOf(recheck) == 0)
            addWorker(null, false);              // 池活着但没线程了 → 补一个
    }
    // ③ 队列满了（或池停了）→ 建非核心线程（到 max）
    else if (!addWorker(command, false))
        reject(command);                         // ④ 线程数也满了 → 拒绝
}
```

**记忆主线（4 级演进）：核心没满 → 建线程；核心满了 → 进队列；队列满了 → 建非核心线程；线程也满了 → 拒绝。**

面试必答的**二次检查**：任务入队后要 `recheck` 一次——
- 万一刚入队线程池就 shutdown 了，把任务**从队列移除并拒绝**（任务不能执行，也不该留在队里）；
- 万一池还活着但 workerCount 已经是 0（比如线程全被回收了），**补建一个线程**去消费队列（`addWorker(null, false)` 的 firstTask 传 null，表示这个线程专门去队列里取任务）。

---

## 📌 六、addWorker()：建线程（重点：两层 CAS + 失败回滚）

```java
private boolean addWorker(Runnable firstTask, boolean core) {
    retry:
    for (;;) {                                  // 外层：状态变了重来
        int rs = runStateOf(ctl.get());
        // 关闭状态下不允许再新建任务线程（SHUTDOWN 且无任务可做时也拒绝）
        if (rs >= SHUTDOWN && !(rs == SHUTDOWN && firstTask == null && !workQueue.isEmpty()))
            return false;
        for (;;) {                              // 内层：CAS 增加 workerCount
            int wc = workerCountOf(ctl.get());
            if (wc >= CAPACITY || wc >= (core ? corePoolSize : maximumPoolSize))
                return false;                   // 超上限，返回 false 走拒绝
            if (compareAndIncrementWorkerCount(c))   // CAS 线程数 +1 成功 → 跳出
                break retry;
            // CAS 失败：说明被别的线程改了，重新读
            if (runStateOf(ctl.get()) != rs) continue retry;
        }
    }
    // ---- 下面真正创建 Worker 并启动 ----
    Worker w = new Worker(firstTask);
    Thread t = w.thread;
    mainLock.lock();                            // 加全局锁操作 workers 集合
    try {
        // 再查一次状态：正常 或 SHUTDOWN但无任务可执行 才允许加
        if (runStateOf(ctl.get()) < SHUTDOWN ||
            (runStateOf(ctl.get()) == SHUTDOWN && firstTask == null)) {
            if (t.isAlive()) throw new IllegalThreadStateException();
            workers.add(w);                     // 加入工作线程集合
            workerAdded = true;
        }
    } finally { mainLock.unlock(); }
    if (workerAdded) {
        t.start();                              // 真正启动线程
        workerStarted = true;
    }
    if (!workerStarted) addWorkerFailed(w);     // 失败回滚：workerCount-1 并移除
    return workerStarted;
}
```

**三个面试点：**
1. **两层循环 + CAS** 保证并发下 workerCount 只增不减、不会超上限；
2. 建线程要**先 CAS 加计数、后真正 start**，顺序不能反（否则可能线程建了但计数没对上）；
3. 启动失败走 `addWorkerFailed` **回滚计数和集合**，不留脏状态。

---

## 📌 七、Worker + runWorker()：线程复用怎么实现（必考）

### 1. Worker 是"一个线程 + 一个任务"的包装，且继承 AQS

```java
private final class Worker extends AbstractQueuedSynchronizer implements Runnable {
    final Thread thread;      // 真正的执行线程
    Runnable firstTask;       // 第一个任务（可能为 null，null = 专门去队列取）
    // 构造时 state 置 -1：启动前禁止中断
    Worker(Runnable firstTask) {
        setState(-1);
        this.firstTask = firstTask;
        this.thread = getThreadFactory().newThread(this);
    }
    public void run() { runWorker(this); }
    // —— 实现一把【不可重入】锁 ——
    protected boolean tryAcquire(int unused) {
        if (compareAndSetState(0, 1)) {
            setExclusiveOwnerThread(Thread.currentThread());
            return true;
        }
        return false;                       // state==1 时再次 acquire 会失败 = 不可重入
    }
    protected boolean tryRelease(int unused) { setState(0); return true; }
    public void lock()      { acquire(1); }
    public boolean tryLock(){ return tryAcquire(1); }
    public void unlock()    { release(1); }
}
```

**为什么 Worker 继承 AQS？** 为了做**中断控制**：
- shutdown 时要"打断空闲线程"（`interruptIdleWorkers`），通过 **`worker.tryLock()`** 判断该线程是不是空闲——**正在执行任务的线程持有锁，tryLock 失败 → 不打断**；
- 所以锁必须是**不可重入**的，否则执行任务的线程自己 tryLock 也能成功（重入），会**误打断正在跑的任务**。
- 构造时 `setState(-1)`：线程启动前禁止被中断。

### 2. runWorker：线程复用的真相 = 一个 while 循环

```java
final void runWorker(Worker w) {
    Thread wt = Thread.currentThread();
    Runnable task = w.firstTask;    // 先执行第一个任务
    w.firstTask = null;
    w.unlock();                     // state -1 → 0，允许中断了
    boolean completedAbruptly = true;
    try {
        // ⭐ 循环取任务：firstTask 跑完，就从队列 getTask() 继续取，取到就一直跑
        while (task != null || (task = getTask()) != null) {
            w.lock();               // 执行任务期间持锁（防止被误中断）
            // 池在 STOP 态则确保中断；否则清掉中断标志
            if ((runStateAtLeast(ctl.get(), STOP) ||
                 (Thread.interrupted() && runStateAtLeast(ctl.get(), STOP))) &&
                !wt.isInterrupted())
                wt.interrupt();
            try {
                beforeExecute(wt, task);   // 钩子1
                task.run();                // ⭐ 真正执行任务（调用的是 Runnable.run）
                afterExecute(task, null);  // 钩子2
            } finally {
                task = null;
                w.completedTasks++;
                w.unlock();
            }
        }
        completedAbruptly = false;
    } finally {
        processWorkerExit(w, completedAbruptly);  // 线程退出清理 + 补偿
    }
}
```

**一句话：线程复用 = 一个 Worker 线程反复执行 "getTask() 取任务 → task.run() 执行 → 再取下一个"，循环不退出，线程就不会销毁。** 创建线程的开销只在第一次 addWorker 时发生。

---

## 📌 八、getTask()：线程为什么不退出？非核心线程怎么回收？

```java
private Runnable getTask() {
    boolean timedOut = false;
    for (;;) {
        int c = ctl.get();
        // ① 池在 STOP 或 SHUTDOWN 且队列空 → 没任务可做了，退出
        if (runStateAtLeast(c, SHUTDOWN)
                && (runStateAtLeast(c, STOP) || workQueue.isEmpty())) {
            decrementWorkerCount();
            return null;
        }
        int wc = workerCountOf(c);
        // ② timed = 线程数超过核心数，或核心线程允许超时
        boolean timed = allowCoreThreadTimeOut || wc > corePoolSize;
        // ③ 超时回收：线程数超 max，或（timed 且上次超时）且（不止一个线程或队列已空）
        if ((wc > maximumPoolSize || (timed && timedOut))
                && (wc > 1 || workQueue.isEmpty())) {
            if (compareAndDecrementWorkerCount(c))
                return null;                 // 返回 null → runWorker 循环结束 → 线程退出
            continue;
        }
        try {
            // ④ 核心线程（timed=false）→ 阻塞 take()：队列空就一直睡
            //    非核心线程（timed=true）→ 定时 poll(keepAliveTime)：超时拿不到 → timedOut=true
            Runnable r = timed
                    ? workQueue.poll(keepAliveTime, TimeUnit.NANOSECONDS)
                    : workQueue.take();
            if (r != null) return r;
            timedOut = true;
        } catch (InterruptedException retry) {
            timedOut = false;
        }
    }
}
```

| 问题 | 源码答案 |
|------|---------|
| **核心线程为什么常驻？** | 队列空时走 `workQueue.take()` **阻塞等待**，不退出 |
| **非核心线程怎么回收？** | 走 `poll(keepAliveTime)` 定时取，超时拿不到任务 → 返回 null → runWorker 循环结束 → 线程终止 |
| **核心线程也能回收吗？** | 可以：`allowCoreThreadTimeOut(true)` 后 timed 恒为 true，核心线程同样超时回收 |

---

## 📌 九、四种拒绝策略（触发时机：队列满 + 线程数到 max + 新任务进来）

| 策略 | 行为 | 适用场景 |
|------|------|---------|
| **AbortPolicy**（默认） | 直接抛 `RejectedExecutionException` | 必须保证不丢任务 |
| **CallerRunsPolicy** | 由调用 execute 的线程自己执行该任务 | 用调用方的执行，天然形成背压，不让提交太快 |
| **DiscardPolicy** | 静默丢弃，不抛异常 | 允许丢任务的场景 |
| **DiscardOldestPolicy** | 丢弃队头最老的任务，再重试提交新的 | 追求新任务优先 |

面试点：默认是 **Abort**，生产中常用 **CallerRuns**（削峰）+ 自定义队列 + 监控打点。

---

## 📌 十、生命周期：shutdown / shutdownNow / awaitTermination

| 方法 | 做了什么 | 正在执行的任务 | 队列中未执行的任务 |
|------|---------|:---:|:---:|
| `shutdown()` | 置 SHUTDOWN，`interruptIdleWorkers()` 打断**空闲**线程 | 继续执行完 | 继续执行完 |
| `shutdownNow()` | 置 STOP，`interruptWorkers()` 打断**所有**线程 | 尝试中断 | **返回 List，不再执行** |
| `awaitTermination(t, u)` | 阻塞等待池进入 TERMINATED | — | 配合 shutdown 用 |

注意 shutdownNow 的**中断不保证生效**：任务若不响应中断（如死循环且不查标志位），线程仍会跑完；返回的列表是"理论上还没执行的任务"。

---

## 📌 十一、高频面试追问（结合源码答）

**Q1：一个任务进来，线程数是怎么演进的？**
> 见 execute() 四步：`线程数 < core → 建核心线程` → `否则入队` → `队列满 → 建非核心线程(到 max)` → `都满 → 拒绝`。关键：**maximumPoolSize 只在队列满后才生效**。

**Q2：execute 和 submit 有什么区别？**
> submit 内部把 Runnable/Callable 包成 `FutureTask`（一个 RunnableFuture），走同一个 execute，只是**多了个 Future 返回值和异常捕获**。所以 **submit 的任务抛异常不会让线程退出**（异常被 FutureTask 捕获，`future.get()` 时才抛）；execute 的任务抛异常，线程会终止但**线程池会自愈**（见 Q4）。

**Q3：任务抛异常，线程池会怎样？**
> runWorker 里 `task.run()` 抛运行时异常 → 跳出 while 循环 → `processWorkerExit`：移除该 Worker、`decrementWorkerCount`，然后**如果池还在运行，`addWorker(null, false)` 补建一个新线程**。所以**池不会因一个任务异常而线程数越来越少**——这就是自愈机制。

**Q4：线程池怎么知道哪个线程空闲、可以打断？**
> 靠 Worker 的**不可重入 AQS 锁**：执行任务的线程在 `w.lock()` / `w.unlock()` 之间。`interruptIdleWorkers` 对每个 worker 调 `tryLock()`，**tryLock 成功 = 没在执行任务 = 空闲可打断**；失败 = 在跑任务，跳过。

**Q5：核心线程数怎么定？**
> CPU 密集：`CPU核数 + 1`（防止缺页等偶发停顿）；IO 密集：`CPU核数 × (1 + 等待时间/计算时间)`（如 2 倍）；更可靠是**压测**。经验值只是起点，生产要按 P99 延迟和吞吐调。

**Q6：为什么建议用有界队列？**
> 无界队列（如 LinkedBlockingQueue 默认）时，**队列永远不会满，maximumPoolSize 和拒绝策略永远不生效**，任务无限堆积 → 内存 OOM。有界队列（如 ArrayBlockingQueue）+ CallerRuns 是生产常用组合。

**Q7：线程池的线程是怎么被创建出来的？**
> addWorker → `Worker(firstTask)` → 构造里 `getThreadFactory().newThread(this)` 创建线程（Worker 本身是 Runnable）→ `t.start()` 启动 → 线程进入 `Worker.run()` → `runWorker(this)`。

---

## 📌 十二、面试回答模板（考官问 · 你答）

**被问"讲讲线程池"时的回答骨架：4 步**
> ① **一句话定义**："线程池是复用线程、管理线程生命周期和执行策略的框架，核心类 ThreadPoolExecutor。"
> ② **7 个参数**背出来，重点强调三个：corePoolSize（常驻）、maximumPoolSize（**队列满才生效**）、workQueue（**建议有界**）。
> ③ **execute 四步流程**：核心没满建线程 → 满了进队列 → 队列满建非核心 → 都满拒绝。
> ④ **复用与回收**：runWorker 的 while 循环反复 getTask+run（复用）；非核心线程 poll 超时回收、核心线程 take 常驻。

**被问"线程池怎么复用线程的"时的回答：**
> "Worker 线程在 `runWorker` 里是一个 **while 循环**：先执行构造传入的 firstTask，然后反复调用 `getTask()` 从阻塞队列取任务、`task.run()` 执行。**只要队列里有任务，循环就不结束，线程就不退出**；核心线程在队列空时走 `take()` 阻塞等待（常驻），非核心线程走 `poll(keepAliveTime)` 超时拿不到就返回 null 退出回收。"

**收尾金句（挑 1 句）：**
- "线程池的核心不是'快'，而是**把'线程生命周期 + 排队 + 拒绝'这套策略集中管理**。"
- "maximumPoolSize 只在**队列满之后**才生效，很多人背了参数却不理解它的生效时机。"
- "生产上我会用**有界队列 + CallerRuns + 线程命名工厂 + 监控队列深度**，而不是默认配置。"
- "拒绝策略不是兜底方案，是**背压信号**——队里排太多，就该提醒上层限流了。"

---

## ✅ 使用检查清单

- [ ] 能 1 分钟背出 7 参数并讲清"maximumPoolSize 队列满才生效"
- [ ] 能画出 execute() 的 4 级演进 + 二次检查
- [ ] 能讲清 ctl 位打包（高3位状态/低29位线程数）为什么这么设计
- [ ] 能说清 Worker 继承 AQS 的原因（不可重入锁 + tryLock 判空闲）
- [ ] 能讲透 runWorker 循环 = 线程复用；getTask take/poll = 常驻/回收
- [ ] 能答任务抛异常后的自愈（processWorkerExit → addWorker 补偿）
- [ ] 能区分 shutdown / shutdownNow / awaitTermination
- [ ] 每个回答都有金句收尾（有界队列 / 背压信号 / 生效时机）
