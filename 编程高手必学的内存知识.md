# 编程高手必学的内存知识 — 面试备战手册

> 来源：极客时间《编程高手必学的内存知识》（海纳，前华为 JDK 团队负责人）
> 涵盖：软件篇（虚拟内存/栈切换/动态链接/内存虚拟化）→ 硬件篇（CPU Cache）→ 自动内存管理（GC）
> 核心价值：从一条 C 变量的旅程串起编译、链接、加载、OS、CPU 全链路

---

## 📌 一、面试高频考点速查

| 核心考点 | 重要度 |
|---------|-------|
| **虚拟内存原理**（为什么可用内存远超物理内存/分页/页表） | ⭐⭐⭐⭐⭐ |
| **CPU Cache 层级**（L1/L2/L3/缓存行/局部性） | ⭐⭐⭐⭐⭐ |
| **伪共享（False Sharing）** | ⭐⭐⭐⭐⭐ |
| **缓存映射**（直接相连/全相连/组组相连/命中率） | ⭐⭐⭐⭐ |
| **栈切换**（进程/协程/用户态内核态切换的本质） | ⭐⭐⭐⭐ |
| **写时复制（Copy-on-Write）** | ⭐⭐⭐⭐ |
| **动态链接**（PLT/GOT/延迟绑定） | ⭐⭐⭐ |
| **ZGC 染色指针 + 读屏障 + 三视图** | ⭐⭐⭐⭐⭐ |
| **G1 Region/RSet/SATB** | ⭐⭐⭐⭐⭐ |
| **Scavenge 复制回收**（From/To + 转发指针） | ⭐⭐⭐⭐ |
| **内存虚拟化**（影子页表/EPT） | ⭐⭐⭐ |

---

## 📌 二、软件篇：虚拟内存

### 1. 为什么可用内存远超物理内存？（课程第01讲）

**核心：局部性原理 + 虚拟内存映射**

```
局部性原理：
  时间局部性 — 刚访问过的位置很可能再次访问
  空间局部性 — 附近的位置很可能被访问

结论：无论进程占多大内存，任何时刻真正需要的物理内存很少
```

**虚拟内存映射（按页进行）**：
- malloc 拿到的虚拟内存是**未映射**的，首次读写时才分配物理页
- 连续的虚拟页，物理上可以不连续（由页表维护映射）
- 32位 → 4G 虚拟空间；64位用低48位 → 256T

**页表结构**（32位 CPU 寻址 4 步）：
```
虚拟地址分成 10位页目录 + 10位页表 + 12位页内偏移

① 从 CR3 寄存器取页目录基址
② 页目录基址 + 高10位×4 → PDE（页目录项）
③ PDE → 页表基址 + 中10位×4 → PTE（页表项）
④ PTE 存物理页地址 + 低12位偏移 → 最终物理地址

64位（48位虚拟地址）用 4 级页表 → 5 步查找
```

**虚拟内存的两大性质**：
1. 每个进程有独立页表 → 地址空间隔离，解决地址冲突
2. PTE 有属性位（读写权限、present位）→ 提供内存安全

**虚拟内存机制的应用**：
- `fork()` 用写保护实现 **写时复制**
- JVM 改页面读权限实现 **安全点（safepoint）**

---

### 2. 栈切换：进程、协程、用户态/内核态切换的本质（课程第05讲）

**核心洞察：所有切换都是"切换 rsp 栈指针"**

| 执行单元 | 切换方式 | 特点 |
|---------|---------|------|
| **协程** | 用户态协作式切换，无系统调用 | 轻量，只有协程主动 yield 才切换 |
| **进程** | fork 后父子共享页表，写时才复制 | 写时复制（COW） |
| **用户态↔内核态** | 中断时切换栈到内核栈 | 特权级切换 |

**协程实现**（关键）：
```
协程切换不改变指令流（IP 是独立的）
只需切换 rsp → ret 就跳到保存的入口地址
movq %%rsp, old_co->stack_pointer
movq co->stack_pointer, %%rsp
```

**用户态/内核态切换**：
- 内核有自己的**内核栈**（特权级）
- 中断时 CPU 从 TSS 加载内核栈的 ss/rsp → 切换栈
- 返回时用 `iret`（不是 ret）恢复用户栈
- 中断服务程序把用户所有寄存器保存在内核栈

---

### 3. 动态链接：PLT/GOT 与延迟绑定（课程第08讲）

**动态链接的两个开销**：
1. 每次访问全局符号都走 GOT 间接寻址（慢）
2. 启动时要重定位所有 .so 的 GOT（启动慢）

**延迟绑定（Lazy Binding）**：首次访问才解析符号，没用到的不解析

**PLT 三层跳转**（为了让 `_dl_runtime_resolve` 能拿到两个参数：库ID + GOT槽位）：
```
第一次调用：
  B@plt: jmpq *GOT[3]      → 未解析，GOT[3] 指向 B@plt+0x6
         push $0x0          → 压入符号ID
         jmp .plt[0]        → 进 PLT[0]
  PLT[0]: pushq GOT[1]      → 压入库ID
          jmpq *GOT[2]      → _dl_runtime_resolve 解析并回填 GOT[3]

之后调用：
  B@plt: jmpq *GOT[3]       → 已解析，直接跳转，其余短路
```

---

### 4. 内存虚拟化：影子页表与 EPT（课程第12讲）

**Guest 三种模式的内存翻译**：

| 模式 | 翻译过程 | 实现 |
|------|---------|------|
| **实模式 Guest** | 段地址→GPA→HVA→HPA | V8086 模式 + VMM 查表 |
| **保护模式 Guest** | GVA→GPA（页表）+ GPA→HPA | **影子页表**（软件，慢） |
| **硬件支持（Intel EPT）** | GVA→GPA（Guest页表）+ EPT 完成 GPA→HPA | **硬件二级页表**（快） |

**影子页表**：VMM 合成一个 GVA→HPA 的页表，"影子"了 Guest 的页表，MMU 查它直接得到物理地址。缺点是纯软件，频繁 VM Exit，慢。

**EPT（Extended Page Table）**：硬件有两套地址翻译器，MMU 先按 Guest 页表翻译 GVA→GPA，再按 EPT 翻译 GPA→HPA。Guest 页错误不再需要 Guest↔Host 切换，对 Guest 透明。

---

## 📌 三、硬件篇：CPU Cache

### 1. CPU Cache 层级（课程第14讲）

**为什么需要缓存**：处理器速度每年 +55%，内存只 +7%，靠缓存弥补差距。

```
延迟对比（数量级）：
寄存器    < 1ns    （1个周期）
L1缓存     1-2ns   （约3周期）
L2/L3     10-20ns  （几十周期）
主存      50-100ns （几百周期）
写好的程序缓存命中率可达 70-90%
```

**物理构成**：缓存=SRAM（6晶体管，可集成片上）；内存=DRAM（1晶体管+电容，更便宜）。

**多核缓存组织**：现代处理器用混合式——每个核独立 L1/L2，共享 L3。

**缓存行的组织**：最小单位=缓存行（cache line）
```
容量 = 组数 × 路数 × 行大小

三种映射：
  直接相连（1路）— 冲突频繁
  全相连（1组）— 冲突少但查找慢
  组组相连（折中，现代常用）— 如 4路/8路

寻址分解：32位地址，64B行，4路，8K缓存 → 32组
  组索引 = (地址>>6) & 0x1F
  低6位 = 行内偏移
  高21位 = tag，与每路的tag比较
```

**缓存行字段**：V（有效位）、M（脏位）、tag。

**替换策略**：LRU（最近最少使用），用位矩阵实现。

**缓存缺失三类**：
| 类型 | 含义 |
|------|------|
| **强制缺失** | 首次访问（冷启动） |
| **冲突缺失** | 组相连路数有限，同一组被不同块争用 |
| **容量缺失** | 访问范围超过缓存总容量 |

### 2. 局部性与性能（实验数据）

**行优先 vs 列优先**（二维数组遍历）：
```
行优先：1.245s（相邻元素同一缓存行）
列优先：2.527s（每次访问跨行）
差距：2倍
```

**伪共享（False Sharing）—— 面试高频**：
```
两个线程同时修改同一缓存行上的不同变量（struct {a; b}）
线程1 改 a → 使缓存行失效
线程2 改 b → 也要重新加载 → 互相拖累

struct { long long a; long long b; }  ← 0.79s
填充 padding 让 a、b 相距≥64B        ← 0.347s（快1倍+）

Java 的 @Contended 注解、Disruptor 的缓存行填充都是为防伪共享
```

> **伪共享的本质**：不在于读还是写，而在于**多个线程写同一缓存行上的不同数据**。

---

## 📌 四、自动内存管理（GC）

### 1. Scavenge：基于 copy 的回收算法（课程第20讲）

**基本原理（Minsky，1963）**：
```
堆分成两半：From 空间（分配区）+ To 空间（幸存区）
对象分配在 From → 满了 → 把存活对象拷到 To → 清空 From → 交换

特点：
  分配快（bump pointer 顶针分配，无碎片）
  回收成本取决于存活对象比例（适合短命对象）
  空间利用率差（任一时间一半空闲）
  必须 STW（移动对象时业务线程要暂停）
```

**转发指针（Forwarding Pointer）**解决共享对象被重复拷贝：
```
对象被拷贝后，旧对象头部的转发指针指向新地址
再次被引用时：copy() 先查转发指针，已拷贝则直接返回新地址
```

**DFS vs BFS**：
- DFS：关联对象在 To 空间相邻 → 空间局部性好，缓存命中高（JDK8+ 用）
- BFS：把队列藏在 To 空间省空间，但局部性差（JDK6 前用）

**Hotspot 的 Scavenge**：新生代 Eden:S0:S1 = 8:1:1，S0/S1 是幸存区。

### 2. G1 GC（课程第22讲）

```
堆分成等大小的 Region（Eden/Survivor/Old/Humongous）
Young GC：Eden 存活对象 → Survivor，年龄够了 → Old
Mixed GC：Young + 部分 Old（选垃圾比例最高的）
```

**三个核心技术**：

| 技术 | 作用 |
|------|------|
| **SATB（起始快照）** | 并发标记时，引用被删除也标灰（防止漏标），本质是"删除屏障" |
| **RSet（记忆集）** | 记录"谁引用我"（point-in），只需记录 非CSet老→年轻 和 非CSet老→CSet老 |
| **TAMS + BitMap** | 标记开始后的新对象（TAMS以上）默认存活，用位图记录存活对象 |

**CSet 选择**：垃圾比例高的 Region 优先（Garbage First 名字由来）+ `MaxGCPauseMillis`（默认200ms）限制数量。停顿目标太小 → 来不及回收 → Full GC。

### 3. ZGC / Pauseless GC（课程第23讲）

**目标**：最大停顿 ≤10ms，且**不随堆大小增长**（靠并发重定位）。

**三大技术**：

| 技术 | 作用 |
|------|------|
| **染色指针** | 64位指针用低42位寻址（4TB），42-45位存标记状态 → 地址视图 |
| **读屏障** | 访问对象时检查指针颜色，发现是旧地址 → 查转发表修正（自愈） |
| **并发重定位** | 移动对象时业务线程不停（读屏障自愈），这是停顿小的关键 |

**多视图映射**：同一物理内存映射到三个虚拟地址（Marked0/Marked1/Remapped），切换视图标记 GC 阶段。

**三阶段循环**：
```
① Mark（标记）：从 Remapped 视图开始，存活对象染为 Marked0
② Relocate（重定位）：移动存活对象到新页，记入转发表，视图切回 Remapped
③ Remap（重映射）：修正所有旧指针 → 与下一轮的 Mark 合并执行
   → 所以 Marked0/Marked1 交替使用，区分两轮重叠的循环
```

---

## 📌 五、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **为什么可用内存能超过物理内存？** | 虚拟内存按页映射 + 局部性原理，进程只需很少物理页常驻 |
| **页表怎么寻址？** | CR3→页目录→页表→物理页，32位三级/64位四级 |
| **进程和协程切换区别？** | 都是切换 rsp 栈指针；协程用户态无系统调用，进程要内核参与 |
| **写时复制是什么？** | fork 时父子共享页表，某方写入才复制物理页 |
| **动态链接为什么慢？** | 全局符号走 GOT 间接寻址 + 启动时重定位；用延迟绑定缓解 |
| **CPU 缓存为什么快？** | SRAM 离 CPU 近，利用局部性原理，命中率70-90% |
| **伪共享是什么？** | 多线程写同一缓存行不同变量互相拖累；padding 或 @Contended 解决 |
| **缓存缺失有哪三类？** | 强制（首次）/冲突（路数有限）/容量（超容量） |
| **复制回收算法特点？** | 无碎片、分配快、回收依赖存活比例、空间利用率差、要STW |
| **ZGC 为什么停顿小？** | 染色指针+读屏障+并发重定位，移动对象时业务线程不停 |

---

> 📝 **课程特色**：这门课讲的是**系统级的内存原理**——虚拟内存、缓存、GC 的底层实现。面试时最有区分度的是：伪共享、页表寻址、栈切换本质、ZGC 三视图。能讲清"一条 C 变量从编译到 CPU 的全旅程"，就体现了扎实的计算机基础功。
