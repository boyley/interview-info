# 高并发系统设计 40 问 — 面试备战手册

> 来源：极客时间《高并发系统设计 40 问》
> 涵盖 40 余讲：基础方法 → 数据库 → 缓存 → 消息队列 → 分布式服务 → 维护 → 实战

---

## 📌 一、面试高频考点速查

| 核心考点 | 对应章节 | 重要度 |
|---------|---------|-------|
| **高并发系统设计方法**（Scale-up/Scale-out/无状态化/哨兵模式） | 基础篇 | ⭐⭐⭐⭐⭐ |
| **读写分离**（主从延迟/一致性解决方案） | 数据库篇 | ⭐⭐⭐⭐⭐ |
| **分库分表**（垂直拆分/水平拆分/分片算法） | 数据库篇 | ⭐⭐⭐⭐⭐ |
| **缓存穿透/击穿/雪崩**（定义+解决方案） | 缓存篇 | ⭐⭐⭐⭐⭐ |
| **缓存与数据库一致性**（Cache Aside/双写一致性） | 缓存篇 | ⭐⭐⭐⭐ |
| **CDN 加速**（静态资源加速/动态加速） | 缓存篇 | ⭐⭐⭐⭐ |
| **消息队列解耦+削峰**（异步处理/流量整形） | 消息队列篇 | ⭐⭐⭐⭐⭐ |
| **服务注册与发现**（ZooKeeper/Consul/Nacos） | 分布式服务篇 | ⭐⭐⭐⭐ |
| **熔断/限流/降级**（Hystrix/Sentinel） | 分布式服务篇 | ⭐⭐⭐⭐⭐ |
| **全链路压测**（压测标／影子库／流量隔离） | 维护篇 | ⭐⭐⭐⭐ |
| **CAP 与一致性模型** | 基础篇 | ⭐⭐⭐⭐⭐ |
| **秒杀系统设计** | 实战篇 | ⭐⭐⭐⭐⭐ |

---

## 📌 二、面试回答标准模板

> **面试官："如何设计一个高并发系统？"**
>
> **回答**：
>
> "设计高并发系统遵循**分而治之**和**缓存**两大原则。核心思路是从以下六个维度逐层优化：
>
> **① 数据库层**：读写分离扛读压力，分库分表扛写压力，用连接池管理数据库连接。
> **② 缓存层**：Redis 扛热点数据，CDN 扛静态资源，注意防范缓存穿透/击穿/雪崩。
> **③ 消息队列**：解耦上下游，削峰填谷，保护下游系统不被突发流量冲垮。
> **④ 服务层**：无状态化设计方便水平扩展，服务注册发现实现动态扩缩容，熔断限流降级保护系统稳定。
> **⑤ 监控层**：全链路压测提前暴露问题，Metrics+Tracing+Logging 三支柱监控系统健康。
> **⑥ 治理层**：灰度发布、配置中心、过载保护，让系统在面对突发流量时能柔性降级。"

---

## 📌 三、高并发设计方法论

### 1. 系统设计核心原则

| 原则 | 做法 | 目的 |
|------|------|------|
| **Scale-up（垂直扩展）** | 升级单机硬件（CPU/内存/SSD） | 短期快速提升能力，但有上限 |
| **Scale-out（水平扩展）** | 增加机器数量 | 理论基础，无限扩展，但需无状态设计 |
| **无状态化** | Session 不绑定机器，用 Redis 集中存储 | 任意机器处理任意请求，方便水平扩展 |
| **哨兵模式** | 关键组件主从切换（Redis Sentinel/DB HA） | 保证高可用 |
| **读写分离** | 主库写从库读 | 分散读压力 |

### 2. CAP 与 BASE

| 原则 | 含义 |
|------|------|
| **CAP** | 一致性、可用性、分区容忍性，三者不可兼得 |
| **BASE** | 基本可用、软状态、最终一致性，高并发系统的实践指导 |

---

## 📌 四、数据库篇

### 1. 读写分离

```
主库（写）→ 同步 → 从库1（读）
                 → 从库2（读）
                 → 从库3（读）
```

**主从延迟问题**：刚写入主库的数据无法立即从从库读到。

**解决方案**：
- **强制读主**：对一致性要求高的操作强制走主库
- **延迟读取**：写入后等几百毫秒再读取
- **缓存标记**：写入后标记缓存，读请求先查缓存

### 2. 分库分表

| 拆分方式 | 做法 | 优点 | 缺点 |
|---------|------|------|------|
| **垂直拆分** | 按业务拆库（订单/用户/商品） | 解耦清晰 | 跨库 Join 困难 |
| **水平拆分** | 按 ID 哈希拆表（user_00~user_99） | 突破单表数据量 | 跨分片查询/聚合困难 |

**分片算法**：取模 / 范围 / 一致性哈希

**分页/排序/跨分片查询**：在应用层或中间件层合并结果。

### 3. 数据库连接池

合理配置连接池大小。不是越大越好——连接过多会导致上下文切换和数据库压力上升。

### 4. 分布式 ID 生成器（发号器，课程第10讲）

| 方案 | 原理 | 缺点 |
|------|------|------|
| **UUID** | 32 位十六进制 | 非递增（B+tree 页分裂）、无业务含义 |
| **Snowflake** | 1位预留+41位时间戳+10位机器ID+12位序列号 | 依赖机器时钟，时钟回拨有问题 |
| **Redis incr** | 原子自增 | 依赖 Redis，需考虑高可用 |

**Snowflake 优化**：部署为独立服务或嵌入应用。时钟回拨时拒绝生成 ID 直到追上。低 QPS 时序列号最后一位固定为 1 导致哈希不均匀——可随机起始序列号。

### 5. 发号器：保证分库分表后ID的全局唯一性

**为什么不用UUID作为数据库主键：**
- 不单调递增（对B+tree插入性能不友好）
- 无业务含义
- 长度大（32位十六进制）
- 随机插入导致页分裂和随机I/O

**Snowflake算法（Twitter）：**
- 64位：1位(保留) + 41位(时间戳,约69年) + 10位(机器ID) + 12位(序列号,4096/ms)
- 可自定义：增加机房位、业务位等
- 部署方式：嵌入应用服务器（需ZK分配机器ID）或独立部署服务
- 问题与解决：
  - 时钟回拨：回拨时拒绝生成ID
  - 低QPS时序列号末位固定为1导致哈希不均匀：用秒级时间戳或随机序列号起始值

---

## 📌 五、缓存篇

### 1. 缓存三大问题

| 问题 | 定义 | 解决方案 |
|------|------|---------|
| **缓存穿透** | 查询一个不存在的数据（缓存不命中，DB 也没有） | **布隆过滤器**（bitmap+多个哈希函数，O(1)判断是否存在，有误判但无漏判） + 缓存空值（短过期时间） |
| **缓存击穿** | 某个热点 key 过期瞬间，大量请求打到 DB | 互斥锁（只让一个线程去 DB 加载）+ 永不过期策略 |
| **缓存雪崩** | 大量 key 同时过期，大量请求打到 DB | 过期时间加随机偏移量 + 多级缓存 + 互斥锁 |

**布隆过滤器**：
- 位数组 + 多个哈希函数，元素映射到位图中的多个位置
- 空间效率高，**有误判（可能认为不存在的元素存在）但无漏判（不会错过存在的元素）**
- 不支持删除（可用计数型位图替代）
- 适合用户 ID 校验等海量数据场景

### 2. 缓存更新策略

| 策略 | 做法 | 一致性保证 |
|------|------|-----------|
| **Cache Aside** | 读时先查缓存，不命中再查 DB 并回写缓存；写时先更新 DB 再删除缓存 | 最终一致 |
| **先删缓存再更新 DB** | 有并发问题（删除缓存后、写 DB 前，有其他线程读了旧值写回缓存） | 不一致 |
| **先更新 DB 再删缓存** | 较安全（即 Cache Aside），推荐方式 | 最终一致 |
| **双写 + 延时双删** | 先删缓存→写 DB→延迟 N 毫秒→再删缓存 | 数据一致性高 |

---

## 📌 六、消息队列篇

### 1. 消息队列的三大作用

```
同步调用：         消息队列异步：
┌─────┐    ┌─────┐  ┌─────┐    ┌─────┐  ┌─────┐
│ 上游 │───▶│ 下游 │  │ 上游 │───▶│  MQ  │──▶│ 下游 │
└─────┘    └─────┘  └─────┘    └─────┘  └─────┘
  阻塞等待                  立即返回
```

| 作用 | 说明 |
|------|------|
| **解耦** | 上下游不直接依赖，下游故障不影响上游 |
| **削峰填谷** | 瞬时流量先到 MQ，下游按能力消费 |
| **异步** | 非核心操作异步处理，提升响应速度 |

### 2. RocketMQ vs Kafka 选型

| 场景 | 推荐 | 原因 |
|------|------|------|
| **金融交易、事务消息** | RocketMQ | 支持事务消息、死信队列 |
| **日志收集、大数据** | Kafka | 高吞吐、分区顺序、配合 Flink 好 |
| **需要顺序消息** | RocketMQ 队列内有序 | RocketMQ 队列模型天然有序 |

### 3. 消息可靠性保证

| 阶段 | 措施 |
|------|------|
| **生产端** | 同步发送 + 重试 + 回调确认 |
| **Broker** | 主从同步 + 刷盘策略 |
| **消费端** | 手动 ACK + 幂等消费（消息去重） |

---

## 📌 七、分布式服务篇

### 1. 服务注册与发现

服务启动时注册 IP:PORT 到注册中心，消费者定期拉取或订阅变更。

**主流注册中心**：ZooKeeper（CP）、Eureka（AP）、Consul（CP）、Nacos（CP+AP）

### 2. 熔断/限流/降级

| 手段 | 作用 | 工具 |
|------|------|------|
| **熔断** | 保护调用方不被故障服务拖死 | Hystrix / Resilience4j / Sentinel |
| **限流** | 保护自身不被流量冲垮 | Guava RateLimiter / Sentinel |
| **降级** | 牺牲非核心保核心 | 配置中心开关 |

### 3. 负载均衡

**常用算法**：轮询、加权轮询、最少连接、一致性哈希

### 4. API 网关（课程第27讲）

**功能**：统一入口、协议转换（HTTP→RPC）、认证鉴权、限流、黑白名单、请求追踪

**选型**：Kong（Nginx+Lua，性能最高）、Zuul（Spring Cloud，Java生态）

### 5. 多机房部署（课程第28讲）

**三种模式**：
| 模式 | 延迟 | 复杂度 |
|------|------|--------|
| **同城双活** | 1-3ms | 低 |
| **异地多活** | <50ms | 高 |
| **跨洲部署** | 100-200ms | 极高 |

**关键原则**：尽量避免跨机房写操作，数据和用户按机房路由。

### 6. Service Mesh（课程第29讲）

| 层面 | 组件 | 功能 |
|------|------|------|
| **数据平面** | Sidecar（Envoy） | 拦截流量，实现熔断/限流/负载均衡 |
| **控制平面** | Istio Pilot/Mixer | 管理策略下发 |

**解决的问题**：多语言服务无法共享治理SDK，Sidecar 独立部署零侵入注入治理能力。

### 4. 分布式锁

**Redis 实现**：`SET key value NX PX 30000`，注意主从切换锁丢失的风险。

---

## 📌 八、实战篇 — 经典系统设计（课程第37-40讲）

### 1. 计数系统设计（50万QPS）

**演进**：MySQL → MySQL+分片 → MySQL+Redis → Redis-only
**优化**：Key 改用 Long（8B）替代 String（28+B），冷热数据分离（SSD存冷数据）

### 2. 未读数系统设计

- **一对一未读**（评论/@）：用通用计数系统
- **系统通知**（广播）：记录每个用户的"最后已读通知ID"，未读 = 总通知数 − 最后已读ID
- **Feed 未读**：存每个关注对象的发布数快照，未读 = sum(当前数 − 快照数)，16台机器扛50万QPS

### 3. 信息流设计 — 推模式 vs 拉模式

| 模式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **推** | 发布时写入所有粉丝收件箱 | 读时快 | 大V发帖需写千万次，延迟高 |
| **拉** | 粉丝读时聚合关注列表 | 写时快 | 读时需查上千人，聚合开销大 |

**混合方案**：大V发帖只推送给活跃粉丝，不活跃粉丝上线后拉取。

## 📌 九、维护篇

| 主题 | 要点 |
|------|------|
| **全链路压测** | 压测流量打标、影子库隔离、监控系统负载 |
| **监控体系** | Metrics（Prometheus）+ Tracing（Jaeger/Zipkin）+ Logging（ELK） |
| **容量规划** | 压测找到单机上限 → 预估总流量 → 预留 30-50% buffer |
| **灰度发布** | 先 1% → 10% → 50% → 100%，发现问题立即回滚 |

---

## 📌 九、面试核心问题速查表

| 面试题 | 一句话回答 |
|--------|-----------|
| **高并发系统设计思路？** | 分而治之（服务拆分+数据分片） + 缓存（多级缓存扛热点） + 异步（MQ削峰） + 治理（熔断限流降级） |
| **缓存穿透/击穿/雪崩？** | 穿透=查不存在（布隆过滤器）；击穿=热点key过期（互斥锁）；雪崩=大量key同时过期（过期时间随机化） |
| **缓存与数据库一致性问题？** | 推荐 Cache Aside（先更新DB再删缓存），最终一致性可接受 |
| **读写分离的主从延迟怎么解决？** | 强一致性读走主库、或写后延迟读、或先读缓存判断 |
| **为什么要用消息队列？** | 解耦（上下游不直接依赖）、削峰（缓冲瞬时流量）、异步（提升响应速度） |
| **如何保证消息可靠性？** | 生产者同步发送+重试，Broker 主从+持久化，消费者手动 ACK+幂等消费 |
| **熔断和限流什么区别？** | 熔断保护调用方（下游故障时快速失败），限流保护被调方（自身不被打垮） |
| **分库分表怎么做？** | 垂直拆分（按业务拆库）+ 水平拆分（按ID哈希拆表），注意跨分片查询代价 |
| **全链路压测怎么做？** | 请求打标 → 影子库隔离数据 → 监控系统负载 → 逐步加压到预估流量的 1.5 倍 |
